export const AUTH_RECOVERY_EXEMPT = new Set(["src/pages/api/legal/consent.ts#POST", "src/pages/api/auth/resend-confirmation.ts#POST"]);
export const batch1aTraces = {
  "src/pages/api/admin/forum/circles.ts#GET": { traceStatus: "complete", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "postgres-read", "response"], evidence: [{ sourceFile: "src/pages/api/admin/forum/circles.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Loads runtime env then awaits requireModerator before listing circles, posts, comments, and cover URLs." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Parses bearer credentials, verifies auth.getUser, and derives the acting user from the verified session." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "role-check", conciseFinding: "Reads profiles.role through the RLS client and rejects non moderator/admin roles before returning." }, { sourceFile: "src/pages/api/admin/forum/circles.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "Only select/order queries and cover URL resolution occur; no insert, update, delete, email, or external moderation call is present." }] },
  "src/pages/api/admin/forum/circles.ts#POST": { traceStatus: "complete", consentInsertionPoint: "POST after requireModerator and before checkDuplicates", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "schema-validation", "duplicate-check", "external-service-call", "postgres-write", "response"], evidence: [{ sourceFile: "src/pages/api/admin/forum/circles.ts", symbol: "POST", evidenceType: "handler", conciseFinding: "Authenticates with requireModerator before parsing and validating the create payload." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "verified-user", conciseFinding: "The acting user is authData.user.id, not owner_id from the request payload." }, { sourceFile: "src/pages/api/admin/forum/circles.ts", symbol: "checkDuplicates", evidenceType: "duplicate-guard", conciseFinding: "Checks case-insensitive circle-name duplicates before moderation and insert." }, { sourceFile: "src/pages/api/admin/forum/circles.ts", symbol: "insertCircleWithSlug", evidenceType: "database-write", conciseFinding: "Performs the first normal Postgres write after moderation and duplicate checks." }, { sourceFile: "src/pages/api/admin/forum/circles.ts", symbol: "moderateCircleCoverImage", evidenceType: "external-service", conciseFinding: "May call image moderation before insert; rejected image cleanup uses the authenticated RLS client." }] },
  "src/pages/api/admin/forum/circles.ts#PATCH": { traceStatus: "complete", consentInsertionPoint: "PATCH after requireModerator and before request.json", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "schema-validation", "duplicate-check", "external-service-call", "postgres-write", "response"], evidence: [{ sourceFile: "src/pages/api/admin/forum/circles.ts", symbol: "PATCH", evidenceType: "handler", conciseFinding: "Authenticates moderator before reading the target id and validating requested update fields." }, { sourceFile: "src/pages/api/admin/forum/circles.ts", symbol: "PATCH", evidenceType: "validation", conciseFinding: "Requires an id, rejects invalid name, description, type, image path, status, and empty update payloads." }, { sourceFile: "src/pages/api/admin/forum/circles.ts", symbol: "PATCH", evidenceType: "database-write", conciseFinding: "Updates circles by target id only after validation and optional text/image moderation." }, { sourceFile: "src/pages/api/admin/forum/circles.ts", symbol: "PATCH", evidenceType: "absence-confirmed", conciseFinding: "No service-role client, R2 write, email send, or acting-user value from the payload is used." }] },
};
export const AUTHENTICATED_READ_ONLY_PREFIXES = ["src/pages/api/admin/", "src/pages/api/legal/consent.ts", "src/pages/api/users/me/"];
export const historicalCommentCreationCircleAuthorizationFindings = [{
  id: "COMMENT_CREATION_CIRCLE_ANCESTOR_AUTHORIZATION",
  status: "historical-checkpoint",
  severity: "historical-release-blocking",
  sourceFile: "src/pages/api/forum/comments.ts",
  method: "POST",
  symbol: "POST",
  conciseFinding: "The historical POST post-to-circle authorization mismatch has source remediation, but it remains release-blocking until the authored forward RLS migration is executed and POST receives a separate re-audit.",
  historicalRouteEvidence: { sourceFile: "src/pages/api/forum/comments.ts", symbol: "POST", evidenceType: "historical-resource-authorization-gap", conciseFinding: "Before remediation, POST verified the bearer, safety state, UUID-shaped post_id, and the post's status/moderation_status, but selected no posts.circle_id and performed no circle authorization before comments.insert." },
  routeEvidence: { sourceFile: "src/pages/api/forum/comments.ts", symbol: "resolveAccessibleCommentCreationTarget", evidenceType: "runtime-resource-authorization", conciseFinding: "POST now resolves the server-derived post circle_id, exact active canonical-visible circle, and optional published same-post parent before moderation, rate persistence, or comments.insert." },
  historicalRlsEvidence: { sourceFile: "supabase/migrations/20260616_community_moderation_mvp.sql", symbol: "comments_insert_self", evidenceType: "historical-rls-policy-gap", conciseFinding: "The historical INSERT policy required only author_id = auth.uid() and comment status, with no parent-post or parent-circle predicate." },
  rlsEvidence: { sourceFile: "supabase/migrations/20260713_comment_creation_circle_authorization.sql", symbol: "comments_insert_self", evidenceType: "forward-rls-remediation", conciseFinding: "The authored forward INSERT policy requires author_id = auth.uid(), valid comment moderation state, and can_create_comment_target(post_id, parent_id), which proves published post, active canonical-visible circle, and same-post published parent ancestry." },
  firstIrreversibleEffect: { sourceFile: "src/lib/server/rate-limit.ts", symbol: "enforceUserRateLimit", evidenceType: "rate-attempt-write", conciseFinding: "After profile and full post-circle-parent authorization plus moderation, the helper inserts a forum_upload_attempts row for comment_create when under the limit. The first domain mutation remains the later comments.insert." },
  requiresRuntimeRemediation: true,
  requiresForwardMigrationRemediation: true,
  singleLayerFixInsufficient: true,
  consentDoesNotRemediateAuthorization: true,
  runtimeRemediationStatus: "implemented-source-awaiting-post-reaudit",
  forwardMigration: "supabase/migrations/20260713_comment_creation_circle_authorization.sql",
  migrationExecutionStatus: "authored-not-executed",
  postReauditRequired: true,
  requiredRemediation: "Runtime now resolves the post, active canonical-visible circle, and same-post published parent before moderation, rate persistence, or comments.insert. A forward RLS migration mirrors the ancestry predicate for INSERT, remains unexecuted, and requires a separate POST re-audit; legal-consent checks cannot establish resource accessibility.",
  evidence: [
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "POST", evidenceType: "authentication-and-actor-binding", conciseFinding: "POST requires a bearer, creates a bearer-bound anon-key client, verifies auth.getUser(token), and always inserts author_id: authData.user.id. Request payload has post_id, body, and optional parent_id only, so it cannot replace the actor or author." },
    { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "safety-authorization", conciseFinding: "POST passes only the verified user id and comment_create action to the helper, which reads that actor's safety state and denies banned, suspended, or unavailable states before request parsing, rate persistence, or comment insertion." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "resolveAccessibleCommentCreationTarget", evidenceType: "post-and-parent-validation", conciseFinding: "The resolver validates a server-read published post with moderation_status published, derives circle_id, and constrains an optional published parent with both parent id and resolved post id. It rejects missing, cross-post, deleted, pending, or moderation-hidden parents." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "resolveAccessibleCommentCreationTarget", evidenceType: "circle-authorization-remediation", conciseFinding: "The resolver reads the exact parent circle and rejects a missing, mismatched, non-active, deleted, or canonical-hidden circle before moderation, rate persistence, or comment insertion. No private membership schema exists to authorize." },
      { sourceFile: "supabase/migrations/20260713_comment_creation_circle_authorization.sql", symbol: "can_create_comment_target", evidenceType: "rls-remediation", conciseFinding: "The authored security-definer predicate joins post to circle, requires published post moderation state and active canonical visibility, and validates an optional same-post published parent. Missing rows fail the exists predicate closed." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "POST", evidenceType: "safe-mutation-boundary", conciseFinding: "Both top-level comments and replies reach comments.insert only after the shared resolver succeeds. The insert uses authData.user.id and resolver-returned post and parent ids, so request payload cannot substitute actor, circle, post ownership, visibility, membership, role, or status." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "POST", evidenceType: "consent-boundary", conciseFinding: "A legal-consent guard can verify policy acceptance but cannot prove the selected post belongs to an accessible circle. The source repair is implemented, but migration execution and a separate POST re-audit remain release prerequisites." }
  ]
}];
export const historicalCommentReadCircleVisibilityFindings = [{
  id: "COMMENT_READ_CIRCLE_ANCESTOR_VISIBILITY_AUTHORIZATION",
  status: "resolved-source-awaiting-deployment",
  severity: "release-blocking",
  sourceFile: "src/pages/api/forum/comments.ts",
  method: "GET",
  symbol: "GET",
  conciseFinding: "Public comment GET historically had no post-to-circle authorization. Remediation commit aa2cf01be3a21eed9d3a0de111bc249c0ead046a added matching runtime and forward SELECT-RLS controls; the separate source re-audit is now complete, while migration execution remains a deployment prerequisite.",
  routeEvidence: { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "runtime-ancestor-authorization-remediation", conciseFinding: "GET now validates post_id and calls resolveAccessibleCommentReadTarget, which derives posts.circle_id from the server post read, requires published post moderation state, reads the exact circle, and requires active canonical visibility before GET queries comments, profiles, reactions, or requester-specific liked_by_me." },
  runtimeRemediationEvidence: { sourceFile: "src/pages/api/forum/comments.ts", symbol: "resolveAccessibleCommentReadTarget", evidenceType: "runtime-visibility-helper", conciseFinding: "The helper fails closed for missing or malformed post-circle relationships, unpublished or moderation-hidden posts, missing circles, non-active circles, and canonical-hidden circles before returning the resolver-derived post id to GET." },
  postRlsEvidence: { sourceFile: "supabase/migrations/20260611_forum_permission_lockdown.sql", symbol: "posts_select_published_public", evidenceType: "select-rls-ancestor-authorization-gap", conciseFinding: "The active source policy permits a published post to anon or authenticated callers without joining circles or checking circle active/deleted/canonical-hidden visibility." },
  commentRlsEvidence: { sourceFile: "supabase/migrations/20260611_forum_permission_lockdown.sql", symbol: "comments_select_public_or_staff", evidenceType: "select-rls-ancestor-authorization-gap", conciseFinding: "The active source policy permits a published comment without a comment-to-post-to-circle visibility predicate, so it cannot compensate for the route's missing circle authorization." },
  firstDatabaseOperation: { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "first-database-read", conciseFinding: "After post_id validation and optional bearer verification, GET first selects the target post. It performs no insert, update, delete, RPC, notification, audit, cache, storage, email, telemetry, or external operation." },
  requiresRuntimeRemediation: true,
  requiresForwardMigrationRemediation: true,
  singleLayerFixInsufficient: true,
  consentDoesNotRemediateAuthorization: true,
  runtimeRemediationStatus: "implemented-source-reaudited",
  forwardMigration: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql",
  forwardMigrationStatus: "authored-not-executed",
  postReauditRequired: false,
  requiredRemediation: "Runtime server-derives posts.circle_id and requires the exact active canonical-visible circle before comment reads or enrichment. The authored forward SELECT-RLS migration enforces the same post-to-circle and comment-to-post-to-circle visibility chain; historical migrations remain immutable. The GET source re-audit is complete. Execute no migration in this task; deployment must apply the forward migration. Legal-consent enforcement cannot establish public resource visibility.",
  evidence: [
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "public-request-and-validation", conciseFinding: "GET validates only the post_id query parameter as a UUID. Without a bearer it constructs createAnonClient; an invalid bearer falls back to the same anonymous read path, while a verified bearer only supplies viewerUserId for own pending comments, liked_by_me, and can_delete." },
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "resolveAccessibleCommentReadTarget", evidenceType: "runtime-visibility-remediation", conciseFinding: "The first database operation now selects id, author_id, circle_id, status, and moderation_status from posts by post_id. The helper rejects malformed or inaccessible post relationships, then reads the exact circle and requires status active plus isPublicVisibleCircle before GET can query comments." },
    { sourceFile: "src/lib/site-navigation.ts", symbol: "isPublicVisibleCircle", evidenceType: "canonical-visibility-contract", conciseFinding: "The canonical helper excludes deleted circles and configured QA-hidden slug/name values. POST, PUT, and GET now additionally require circle status active; GET reaches this helper through resolveAccessibleCommentReadTarget. The schema has no private-circle membership relation." },
    { sourceFile: "supabase/migrations/20260611_forum_permission_lockdown.sql", symbol: "posts_select_published_public", evidenceType: "post-select-rls-gap", conciseFinding: "The final source definition allows status = published to anon and authenticated callers. It has no join or exists predicate over circles, no active-status requirement, and no canonical-hidden-circle exclusion." },
    { sourceFile: "supabase/migrations/20260611_forum_permission_lockdown.sql", symbol: "comments_select_public_or_staff", evidenceType: "comment-select-rls-gap", conciseFinding: "The final source definition allows status = published to anon and authenticated callers. Its only post relation is an authenticated post-author branch; it has no public predicate proving the parent post and circle remain accessible." },
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "reachable-public-data-after-remediation", conciseFinding: "Only after the resolver succeeds does GET read published comments for the server-derived post, including top-level comments and published replies, then select author username, display_name, avatar_url, and role plus comment_reactions to calculate like_count and, for a verified requester, liked_by_me." },
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "comment-filter-boundary", conciseFinding: "The response excludes moderation-hidden published comments because it filters published rows to moderation_status published, and returns deleted comments only as body-empty placeholders when they have a published child. The resolver now stops inaccessible circles, including canonical QA-hidden circles, before either response path is reached." },
    { sourceFile: "supabase/migrations/20260603_forum_comments_interactions.sql", symbol: "comment_reactions_select_public", evidenceType: "historical-reaction-select-gap", conciseFinding: "The currently deployed historical reaction SELECT policy requires only a published comment. The later reaction visibility migration is authored but unexecuted, so it cannot make the GET route's post/circle authorization sufficient." },
    { sourceFile: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql", symbol: "posts_select_published_public,comments_select_public_or_staff,comment_reactions_select_accessible", evidenceType: "forward-select-rls-remediation", conciseFinding: "The authored forward migration requires an active canonical-public circle for public post SELECT, a published comment-post-circle chain for public comment SELECT, and the same full chain for reaction SELECT while preserving authenticated-own and staff branches. Missing ancestors fail closed." },
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "read-only-absence-confirmed", conciseFinding: "GET uses select, in-memory filters/maps, and a JSON response only. It has no view-count, read-marker, comment/reply-count, reaction, notification, audit, analytics, cache-persistence, RPC, email, R2, storage, or other irreversible side effect, so no legal-consent insertion point applies to this read-only method." },
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "resolveAccessibleCommentCreationTarget,resolveAccessibleCommentReactionTarget,resolveAccessibleCommentReadTarget", evidenceType: "cross-method-consistency-remediation", conciseFinding: "POST, PUT, and GET now server-derive the exact post circle and require an active canonical-visible circle before comment creation, reaction access, or comment/profile/reaction read enrichment." },
    { sourceFile: "scripts/test-comment-read-circle-visibility-authorization.mjs", symbol: "comment-read-circle-visibility-authorization", evidenceType: "offline-regression-test", conciseFinding: "Deterministic offline cases permit public top-level/reply reads and authenticated liked_by_me only for active canonical-visible ancestry. Inactive, deleted, hidden, missing, and mismatched circles plus inaccessible posts stop before comment, profile, and reaction reads; hidden, deleted, unpublished, and mismatched comments are excluded before enrichment." },
    { sourceFile: "tests/fixtures/legal-consent-api-methods.mjs", symbol: "COMMENT_READ_CIRCLE_ANCESTOR_VISIBILITY_AUTHORIZATION", evidenceType: "remediation-boundary", conciseFinding: "A route-only repair leaves direct public SELECT paths insufficiently protected, while an RLS-only repair cannot make GET's route-level public visibility contract explicit or consistently control enrichment. The runtime and forward policy are source-aligned and re-audited; migration execution remains required. A consent guard verifies policy acceptance, not post-to-circle accessibility." }
  ]
}];
const resolvedExternalVideoAuthorizationOrderingFindings = [{
  id: "EXTERNAL_VIDEO_UPLOAD_TARGET_AUTHORIZATION_ORDERING",
  status: "resolved",
  severity: "historical-release-blocking",
  remediationCommit: "30ece7ec89e7de477b3b74d94e36ee51709419d5",
  sourceFile: "src/pages/api/forum/external-video-upload.ts",
  method: "POST",
  symbol: "POST",
  conciseFinding: "The separate source re-audit confirms the runtime order validates the selected post and proves server-read post.author_id equals the verified actor before conditional Turnstile validation. This route-local ordering blocker is resolved; no migration was required.",
  authenticationEvidence: { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "getBearerToken,POST", evidenceType: "bearer-authentication", conciseFinding: "POST requires a bearer, constructs an anon-key Supabase client bound to it, verifies auth.getUser(token), and derives the actor only from authData.user.id before payload handling or Turnstile." },
  globalWriteSafetyEvidence: { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "global-write-authorization", conciseFinding: "POST passes only authData.user.id and external_video_upload. The helper denies banned, suspended, or safety-lookup-failure actors, but it does not read or authorize the requested post." },
  firstExternalEffect: { sourceFile: "src/lib/server/turnstile.ts", symbol: "validateTurnstileToken", evidenceType: "outbound-network-effect", conciseFinding: "When called, the helper fetches the fixed https://challenges.cloudflare.com/turnstile/v0/siteverify endpoint with the client-supplied Turnstile token and request IP. The destination is not client-selected, so this is not SSRF, but it is an outbound server-side effect." },
  targetAuthorizationEvidence: { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST#post.lookup,POST#post.authorize", evidenceType: "target-authorization-before-external-effect", conciseFinding: "After local payload validation, POST selects only id and author_id by the UUID-shaped post_id, fails closed for a missing row, and requires post.author_id === authData.user.id before stage turnstile can conditionally call validateTurnstileToken." },
  firstPersistentEffect: { sourceFile: "src/lib/server/rate-limit.ts", symbol: "enforceUploadRateLimit", evidenceType: "later-rate-attempt-write", conciseFinding: "After successful target ownership authorization and local R2 PUT URL signing, the helper can insert forum_upload_attempts. The endpoint itself performs no R2 upload, post_media insert, notification, audit, RPC, email, cache, or domain-record mutation." },
  runtimeRemediationStatus: "implemented-source-reaudited",
  targetAuthorizationBeforeTurnstile: true,
  migrationRequired: false,
  postReauditRequired: false,
  requiredRemediation: "Runtime source performs target post lookup and post.author_id === authData.user.id authorization before conditional validateTurnstileToken. Offline re-audit evidence proves denied targets return before Turnstile, R2 signing, rate-attempt persistence, or any later effect. Consent enforcement cannot establish post ownership or substitute for this ordering.",
  evidence: [
    { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST#post.lookup,POST#post.authorize,validateTurnstileToken", evidenceType: "implemented-order", conciseFinding: "Current order is runtime check; bearer presence; bearer-bound client; auth.getUser; assertUserCanWrite; JSON parse and post_id/file/mime/declared-size validation; post lookup; server-read author comparison; conditional Turnstile; daily rate reads; local R2 signing; rate-attempt insertion; signed-upload response." },
    { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST#post.lookup,POST#post.authorize", evidenceType: "denied-target-effect-boundary", conciseFinding: "Missing target rows return 404 and non-owner rows return 403 before the conditional Turnstile branch. Those paths therefore cannot invoke Turnstile, daily-rate reads, R2 signing, enforceUploadRateLimit, database mutation, notification, audit, email, or other later external effect." },
    { sourceFile: "src/lib/server/turnstile.ts", symbol: "validateTurnstileToken", evidenceType: "fixed-destination-not-ssrf", conciseFinding: "The helper has one hard-coded Cloudflare URL and no caller-provided URL, host, protocol, port, redirect, DNS, download, or video metadata input. No client-selected outbound destination or video fetch exists in this endpoint." },
    { sourceFile: "src/lib/r2-server.ts", symbol: "signR2PutUrl,buildTmpVideoKey", evidenceType: "later-storage-boundary", conciseFinding: "After target authorization the server locally creates a random UUID key under tmp/<verified-user-id>/<authorized-post-id>/ and cryptographically signs a PUT URL; it does not upload to R2. The browser performs the later direct upload outside this endpoint, and no post_media row is inserted here." },
    { sourceFile: "scripts/test-external-video-authorization-ordering.mjs", symbol: "external-video-authorization-ordering", evidenceType: "offline-ordering-regression-test", conciseFinding: "Fully offline dependency fakes prove missing, non-owner, malformed, unauthenticated, and safety-denied paths make zero Turnstile, Cloudflare-fetch, daily-rate, R2-signing, rate-attempt, or direct mutation calls; the valid-owner path orders post lookup and author comparison before Turnstile, while an invalid Turnstile result stops before rate-attempt insertion." },
    { sourceFile: "supabase/migrations/20260611_forum_permission_lockdown.sql", symbol: "posts_select_published_public", evidenceType: "deployed-post-select-rls", conciseFinding: "The deployed-source policy permits only published posts, rows whose author_id equals auth.uid(), or server-derived moderator/admin access. The bearer-bound client therefore cannot select arbitrary non-public cross-user rows; the route independently rejects any selected row whose server-read author_id differs from authData.user.id." },
    { sourceFile: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql", symbol: "posts_select_published_public", evidenceType: "authored-forward-post-select-rls", conciseFinding: "The authored but unexecuted comment-read migration further constrains the public branch to published moderation-visible posts in an accessible public circle while preserving own-author and staff branches. Its unexecuted status is a comment-read deployment prerequisite, not an external-video route blocker." },
    { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST", evidenceType: "consent-boundary", conciseFinding: "A future legal-consent guard belongs after verified auth.getUser(token) and before payload parsing, post lookup, Turnstile, rate reads, signing, or persistence. It cannot prove the selected post belongs to the actor, so the existing server-derived ownership comparison remains required." },
    { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST,formatDbError", evidenceType: "phase4b-hardening", conciseFinding: "The route does not require a Content-Type header or transport body-size limit, has no request idempotency key, and returns formatted database/caught error messages. validateTurnstileToken retains helper error text internally but POST exposes only its TURNSTILE_REQUIRED or TURNSTILE_INVALID code. Rate attempts can duplicate on retries and remain persisted if a later response path fails; these are Phase 4B hardening concerns, not target-authorization bypasses." }
  ]
}];
const historicalPostMediaCrossPostProvenanceBlocker = [{
  id: "POST_MEDIA_TMP_OBJECT_CROSS_POST_PROVENANCE",
  status: "active",
  severity: "release-blocking",
  sourceFile: "src/pages/api/forum/post-media.ts",
  method: "POST",
  symbol: "validateMediaArray,POST,buildTmpVideoKey,post_media_insert_self",
  conciseFinding: "A verified owner can replay an R2 upload authorization issued for owned post A onto different owned post B because the temporary object key and all current route/RLS checks are actor-scoped but not post-scoped.",
  uploadAuthorizationEvidence: { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST#post.lookup,POST#post.authorize,buildTmpVideoKey", evidenceType: "post-scoped-upload-authorization", conciseFinding: "The upload route validates selected post_id, reads id and author_id, requires post.author_id to equal authData.user.id, then signs an R2 PUT URL for buildTmpVideoKey(authData.user.id, fileNameRaw)." },
  objectKeyEvidence: { sourceFile: "src/lib/r2-server.ts", symbol: "buildTmpVideoKey", evidenceType: "actor-scoped-temporary-key", conciseFinding: "buildTmpVideoKey returns only tmp/<verified-user-id>/<random-uuid>-<normalized-file-name>; the selected post id, circle id, upload-session id, nonce, and media record id are absent from the key." },
  routeEvidence: { sourceFile: "src/pages/api/forum/post-media.ts", symbol: "validateMediaArray", evidenceType: "cross-post-replay-acceptance", conciseFinding: "For video storage_path, validateMediaArray accepts either the current user/post prefix or tmp/<verified-user-id>/ prefix. The tmp branch compares no post id, so a key issued while authorizing post A passes when the same actor submits it with owned post B." },
  rlsEvidence: { sourceFile: "supabase/migrations/20260524_forum_phase3_post_media.sql", symbol: "post_media_insert_self,post_media_objects_insert_self", evidenceType: "current-rls-provenance-gap", conciseFinding: "post_media_insert_self requires user_id = auth.uid() and a target post authored by auth.uid(); storage insert requires owner = auth.uid() and a user-id prefix. Neither predicate stores or verifies that a tmp object was authorized for the target post." },
  absenceEvidence: { sourceFile: "src/pages/api/forum/post-media.ts", symbol: "POST", evidenceType: "absence-confirmed", conciseFinding: "No upload session, post-bound object key, nonce, object-to-post provenance row, or server-side object metadata lookup is read before post_media update/insert, so no current source control preserves original-post binding." },
  firstMutation: { sourceFile: "src/pages/api/forum/post-media.ts", symbol: "POST#resetCover", evidenceType: "first-domain-mutation-after-authorizing-target-only", conciseFinding: "After authorizing only the selected target post and validating the actor-scoped tmp prefix, POST updates post_media is_cover false for that target before moderation and the later insert. That mutation cannot establish provenance for the uploaded object." },
  requiresRuntimeRemediation: true,
  requiresForwardMigrationRemediation: true,
  singleLayerFixInsufficient: true,
  consentDoesNotRemediateAuthorization: true,
  requiredRemediation: "A runtime remediation must bind a finalized media object to the authorized post before post_media mutation, and a new forward RLS/schema remediation must independently require that stored provenance for INSERT/UPDATE. A route-only check leaves direct client/RLS paths unbound; an RLS-only predicate has no post-binding fact to enforce until provenance exists. Legal consent cannot establish object-to-post authorization.",
  evidence: [
    { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST#post.lookup,POST#post.authorize", evidenceType: "verified-actor-and-original-post-authorization", conciseFinding: "The signing route verifies auth.getUser(token), derives only authData.user.id, validates post_id, reads the server post, and requires server-read author_id equality before it signs an upload URL." },
    { sourceFile: "src/lib/r2-server.ts", symbol: "buildTmpVideoKey", evidenceType: "temporary-key-shape", conciseFinding: "The generated key is tmp/<actor-id>/<crypto.randomUUID>-<normalized-file-name>, with no source post identifier or capability nonce embedded beyond the random object suffix." },
    { sourceFile: "src/pages/api/forum/post-media.ts", symbol: "validateMediaArray", evidenceType: "same-actor-cross-post-replay", conciseFinding: "A video storage_path matching tmp/<actor-id>/ is accepted independently of the postId argument. The same verified actor can therefore provide an upload path authorized for post A while selecting owned post B." },
    { sourceFile: "supabase/migrations/20260524_forum_phase3_post_media.sql", symbol: "post_media_insert_self,post_media_objects_insert_self", evidenceType: "independent-rls-gap", conciseFinding: "Current insert policies bind the row and storage object to auth.uid(), and bind the row target to an auth.uid()-authored post, but contain no tmp object-to-post provenance predicate. They do not independently prevent A-to-B replay by the same actor." },
    { sourceFile: "src/pages/api/forum/post-media.ts", symbol: "POST", evidenceType: "consent-insufficiency", conciseFinding: "A future consent check can run after auth.getUser(token), but it cannot establish which post authorized an existing tmp object. Runtime provenance and forward RLS/schema enforcement are both required before this method can be re-audited complete." }
  ]
}];
export const releaseBlockingFindings = [];
export const resolvedPostMediaCrossPostProvenanceFindings = [{
  id: "POST_MEDIA_TMP_OBJECT_CROSS_POST_PROVENANCE",
  status: "resolved-source-awaiting-deployment",
  severity: "historical-release-blocking",
  blockerCheckpoint: "7fae49fc9e0d13928bbd9ab196051fbbdacc4c0a",
  remediationCommit: "Fix and trace post-bound media provenance",
  sourceFile: "src/pages/api/forum/post-media.ts",
  method: "POST",
  symbol: "POST,validateMediaArray,buildTmpVideoKey,post_media_insert_self,post_media_update_self_or_staff",
  forwardMigration: "supabase/migrations/20260713_post_bound_media_provenance.sql",
  migrationExecutionStatus: "authored-not-executed",
  runtimeRemediationStatus: "implemented-source-reaudited",
  historicalEvidence: historicalPostMediaCrossPostProvenanceBlocker,
  conciseFinding: "The former same-actor post-A-to-post-B temporary-media replay is resolved in source by canonical tmp/<verified-actor-id>/<authorized-post-id>/<random-object-name> keys, finalization validation that binds actor, selected post, and object path, and matching forward post_media INSERT/UPDATE RLS. The new migration remains unexecuted and is a deployment prerequisite.",
  evidence: [
    { sourceFile: "src/lib/r2-server.ts", symbol: "buildTmpVideoKey,requireCanonicalUuidSegment", evidenceType: "post-bound-key-generation", conciseFinding: "The key builder normalizes and UUID-validates the verified actor and target post before returning exactly tmp/<actor-id>/<post-id>/<server-random-uuid>-<normalized-file-name>; client input cannot choose the actor namespace, target segment, or random identity." },
    { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST#post.lookup,POST#post.authorize,POST#key.build", evidenceType: "upload-authorization-order", conciseFinding: "POST verifies the bearer actor, applies assertUserCanWrite, validates the body and UUID-shaped post id, reads the server post, requires server author equality, then creates the post-bound key before Turnstile, rate processing, R2 signing, and operational attempt persistence." },
    { sourceFile: "src/pages/api/forum/post-media.ts", symbol: "POST,validateMediaArray", evidenceType: "runtime-provenance-finalization", conciseFinding: "POST verifies the actor, applies user-safety authorization, validates the payload and target post ownership, then validates every storage path against the exact actor/post direct or temporary canonical form before resetCover or post_media insert; foreign video URLs and ambiguous separator forms are rejected." },
    { sourceFile: "supabase/migrations/20260713_post_bound_media_provenance.sql", symbol: "is_canonical_post_media_object_key,can_bind_post_media_provenance,post_media_insert_self,post_media_update_self_or_staff", evidenceType: "forward-rls-provenance-enforcement", conciseFinding: "The forward policy requires auth.uid row ownership, an auth.uid-authored target post, and a fully anchored actor/post object path for INSERT and ordinary-user UPDATE. Missing, cross-user, cross-post, percent-encoded, query, fragment, backslash, and prefix-confused provenance fails closed." },
    { sourceFile: "scripts/test-post-media-provenance-authorization.mjs", symbol: "post-media-provenance-authorization", evidenceType: "offline-runtime-and-migration-regression", conciseFinding: "Offline Vite-loaded validation accepts an actor A post A key only for post A and rejects post-A-to-post-B replay, actor substitution, actor-only legacy tmp paths, encoded or duplicate separators, traversal, and foreign URLs before finalization writes; static assertions pin historical migration blobs and the new RLS predicates." },
    { sourceFile: "src/pages/api/forum/post-media.ts", symbol: "POST#resetCover,POST#insert", evidenceType: "first-mutation-and-retry-behavior", conciseFinding: "After authorization and provenance validation, resetCover is the first persistent domain mutation, followed by media insert and optional post moderation update. The route has no transaction or idempotency key, so retry and partial-failure behavior remains Phase 4B hardening rather than an authorization bypass." },
    { sourceFile: "src/pages/api/forum/post-media.ts", symbol: "POST", evidenceType: "consent-boundary", conciseFinding: "A future consent guard belongs immediately after verified auth.getUser(token) and before assertUserCanWrite, target-post processing, provenance validation, resetCover, or insertion. Consent does not establish actor-to-post-to-object provenance and cannot replace the runtime and RLS controls." }
  ]
}];
export const resolvedForumPostsCircleAuthorizationFindings = [{
  id: "FORUM_POSTS_CIRCLE_VISIBILITY_AND_EFFECT_ORDERING",
  status: "resolved-source-awaiting-deployment",
  severity: "historical-release-blocking",
  remediationCommit: "Fix and trace forum posts authorization",
  sourceFile: "src/pages/api/forum/posts.ts",
  methods: ["GET", "POST", "PATCH", "DELETE"],
  symbol: "GET,POST,PATCH,DELETE,resolveWritableCircleBySlug,resolveAccessibleForumPostTarget",
  forwardMigration: "supabase/migrations/20260713_forum_posts_circle_authorization.sql",
  migrationExecutionStatus: "authored-not-executed",
  conciseFinding: "The route now filters public feed rows by active canonical circles, rejects the legacy GET view-write flag, authorizes a writable circle before POST rate persistence or moderation, and resolves a visible post-to-circle target before PATCH or DELETE writes. Matching forward RLS constrains post INSERT/UPDATE/DELETE and the view-count RPC. Migration execution remains a deployment prerequisite.",
  evidence: [
    { sourceFile: "src/pages/api/forum/posts.ts", symbol: "GET,listForumFeed", evidenceType: "read-only-public-visibility", conciseFinding: "GET keeps moderation and ownership checks bearer-bound, constructs an anon client for feed reads, rejects increment_view without invoking the historical RPC, and returns only listForumFeed results after public-circle filtering." },
    { sourceFile: "src/lib/forum-feed.ts", symbol: "resolveCircleId,filterPublicVisibleFeedPosts", evidenceType: "runtime-circle-ancestry-filter", conciseFinding: "Feed circle selection and every loaded post require an active canonical visible circle; deleted and QA-hidden circles are filtered before feed enrichment is returned." },
    { sourceFile: "src/pages/api/forum/posts.ts", symbol: "POST,resolveWritableCircleBySlug", evidenceType: "target-before-effects", conciseFinding: "POST derives the actor from verified auth, checks safety, validates payload and profile, then resolves the active canonical circle before the first persistent rate-attempt insert and any optional moderation-provider call." },
    { sourceFile: "src/pages/api/forum/posts.ts", symbol: "DELETE,PATCH,resolveAccessibleForumPostTarget", evidenceType: "mutation-target-authorization", conciseFinding: "DELETE and PATCH require a verified actor, safety authorization, UUID target, and a published moderation-visible post in an active canonical circle before ownership/staff authorization or any update, media read, cleanup, or storage effect." },
    { sourceFile: "supabase/migrations/20260713_forum_posts_circle_authorization.sql", symbol: "posts_insert_self,posts_update_self_or_staff,posts_delete_self_or_staff,increment_post_view_count", evidenceType: "forward-rls-and-rpc-alignment", conciseFinding: "The forward policies bind ordinary writes to auth.uid and an accessible public circle, preserve server-derived staff branches, and restrict the security-definer view RPC to published, moderation-visible posts in an accessible public circle." },
    { sourceFile: "scripts/test-forum-posts-authorization.mjs", symbol: "forum-posts-authorization", evidenceType: "offline-regression", conciseFinding: "Offline Vite-loaded helpers and static ordering assertions cover accessible and hidden/deleted/QA/missing ancestry, target-before-rate/moderation/update ordering, no hard-delete fallback, unchanged historical migrations, and no real network, Supabase, R2, or auth operation." }
  ]
}];
export const resolvedForumReportAuthorizationFindings = [{
  id: "FORUM_REPORT_TARGET_AND_INITIAL_STATE_AUTHORIZATION",
  status: "resolved-source-awaiting-deployment",
  severity: "historical-release-blocking",
  remediationCommit: "Fix and trace forum report authorization",
  sourceFile: "src/pages/api/forum/reports.ts",
  method: "POST",
  symbol: "POST,resolveReportTargetPreview,can_create_user_report_target,reports_insert_self",
  forwardMigration: "supabase/migrations/20260713_forum_report_target_authorization.sql",
  migrationExecutionStatus: "authored-not-executed",
  conciseFinding: "POST now verifies reporter safety and resolves the exact report target with published post/comment ancestry and active canonical circle visibility before rate reads or reports insertion. The authored forward policy independently binds auth.uid reporter identity, neutral initial report fields, and target type/id ancestry. Migration execution remains a deployment prerequisite.",
  evidence: [
    { sourceFile: "src/pages/api/forum/reports.ts", symbol: "POST,getBearerToken,createUserClient,auth.getUser", evidenceType: "verified-reporter-and-rls-client", conciseFinding: "POST requires a bearer, builds only an anon-key client bound to it, verifies auth.getUser(token), and supplies only authData.user.id as reporterId. Payload fields cannot supply reporter_id, user_id, actor_id, role, status, priority, assignment, or resolution fields." },
    { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "pre-effect-write-safety", conciseFinding: "POST passes only the verified reporter and report_create action to the safety helper before parsing a target, resolving a target, rate reads, report insertion, or report-event insertion." },
    { sourceFile: "src/lib/server/reports.server.ts", symbol: "parseUserReportPayload,resolveReportTargetPreview", evidenceType: "target-type-binding-and-runtime-ancestry", conciseFinding: "The parser allowlists post/comment/circle/user, UUID target ids, reason codes, and 5-1000 character optional detail. The resolver queries only the table selected by target_type and requires the returned exact id; post/comment targets require published moderation-visible post/comment rows under an active canonical-visible circle, circle targets require that same active canonical visibility, and user targets require an existing profile." },
    { sourceFile: "src/pages/api/forum/reports.ts,src/lib/server/reports.server.ts", symbol: "POST,countRecentReportsByUser,createUserReport,findDuplicateUserReport", evidenceType: "ordering-duplicate-and-rate", conciseFinding: "After target availability succeeds, POST counts the verified reporter's trailing-hour reports and denies at 12. createUserReport then checks a 24-hour duplicate predicate over reporter_id, target_type, and target_id before insertion. The checks are read-only; no denied/invalid target writes a rate attempt or report. There is no uniqueness constraint or request key, so concurrent retries can race to duplicate reports, a bounded Phase 4B concern." },
    { sourceFile: "src/lib/server/reports.server.ts", symbol: "createUserReport,insertReportEvent", evidenceType: "domain-writes-and-partial-failure", conciseFinding: "reports.insert is the first persistent and first domain mutation, with server-controlled status open and priority normal. It is followed by report_events.insert(created) using the same verified reporter as actor. The writes are not transactional; event failure is logged and the successful report remains, so event completeness and retry repair remain Phase 4B hardening." },
    { sourceFile: "supabase/migrations/20260627_reports_optimization_mvp.sql", symbol: "report_events_insert_reporter_created,validate_report_target", evidenceType: "existing-rls-and-trigger", conciseFinding: "The existing created-event policy requires event_type created, actor_id auth.uid, and a report whose reporter_id equals auth.uid. The existing trigger only proves target existence, so it did not enforce target visibility or neutral initial report state." },
    { sourceFile: "supabase/migrations/20260713_forum_report_target_authorization.sql", symbol: "can_create_user_report_target,reports_insert_self", evidenceType: "forward-rls-equivalence", conciseFinding: "The authored unexecuted function type-binds post/comment/circle/user target checks and fails closed on missing/inaccessible ancestors. The replacement INSERT policy requires reporter_id auth.uid, status open, priority normal, null assignment/resolution fields, and the predicate; direct ordinary users cannot create actioned, assigned, or cross-ancestry reports after deployment." },
    { sourceFile: "scripts/test-forum-reports-authorization.mjs", symbol: "forum-reports-authorization", evidenceType: "offline-regression", conciseFinding: "The offline Vite-loaded matrix allows all four accessible target kinds and denies malformed, missing, hidden/deleted, canonical-hidden, and mismatched targets before report/event writes. Static checks pin safety-to-target-to-rate-to-duplicate-to-report-to-event ordering and the forward RLS fields without real services." },
    { sourceFile: "src/pages/api/forum/reports.ts,src/lib/server/reports.server.ts", symbol: "POST,createUserReport", evidenceType: "phase4b-hardening", conciseFinding: "POST has no Content-Type or transport body-size guard and returns helper/caught error details. Own-content reporting is allowed; unavailable hidden/deleted/rejected/inaccessible targets return the existing already_handled response without creating a report. No notification, email, R2, cache, RPC, telemetry, service-role client, or external request occurs." }
  ]
}];
export const resolvedForumSearchVisibilityFindings = [{
  id: "FORUM_SEARCH_PUBLIC_VISIBILITY_AND_QUERY_FILTER_SAFETY",
  status: "resolved-source-awaiting-deployment",
  severity: "historical-release-blocking",
  remediationCommit: "Fix and trace forum search visibility",
  sourceFile: "src/pages/api/forum/search.ts",
  method: "GET",
  symbol: "GET,runForumSearch,isActivePublicSearchCircle,resolveSignedPostMedia",
  forwardMigration: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql",
  migrationExecutionStatus: "authored-not-executed",
  conciseFinding: "The public search route now uses an anon-only client, reduces query input to bounded plain text before PostgREST or filters, and rejects every post, circle, profile count, snippet, ranking value, and media URL whose parent circle is missing, inactive, deleted, or canonical-hidden. The existing authored public post SELECT migration remains a deployment prerequisite for database-layer equivalence.",
  evidence: [
    { sourceFile: "src/pages/api/forum/search.ts,src/lib/supabase-server.ts", symbol: "GET,createSSRClient", evidenceType: "anon-client-and-request-boundary", conciseFinding: "GET validates runtime configuration and URL parameters, then constructs only createSSRClient with the anon key. It does not parse or forward a bearer, derive an actor, invoke staff access, or construct a service-role client, so authenticated requests cannot broaden search visibility." },
    { sourceFile: "src/lib/forum-search.ts", symbol: "sanitizeSearchInput,parseForumSearchParams", evidenceType: "query-safety-and-bounds", conciseFinding: "Search text removes all PostgREST grammar characters except Unicode letters, numbers, whitespace, and hyphen; it must remain 2 through 80 characters after normalization. The type is allowlisted to all/posts/circles/users/devices, circle filtering forces posts only, and every result limit is clamped to 1 through 20. There is no offset, cursor, caller-selected table, column, sort expression, or RPC name." },
    { sourceFile: "src/lib/forum-search.ts", symbol: "fetchMatchingAuthorIds,fetchPublishedPosts,isActivePublicSearchCircle", evidenceType: "post-circle-runtime-visibility", conciseFinding: "The first database read is the bounded matching-author profile id read when posts are enabled. Post searches then require published status and moderation status, load id/slug/name/status for the parent circle, and discard missing, inactive, deleted, or canonical-hidden ancestors before excerpt, author, media, count, or relevance computation." },
    { sourceFile: "src/lib/forum-search.ts", symbol: "resolveCircleIdForSearch,circlesPromise,fetchCirclePostCountMap", evidenceType: "circle-and-count-visibility", conciseFinding: "Circle-scoped search and circle results require status active plus canonical visibility without a compatibility fallback. Circle post counts are requested only for already accepted circle ids, so hidden or inactive circles cannot appear in rows, totals, ranking boosts, descriptions, slugs, or count metadata." },
    { sourceFile: "src/lib/forum-search.ts", symbol: "fetchPublicProfiles", evidenceType: "profile-exposure-and-derived-counts", conciseFinding: "Profiles expose only id, username, display name, public avatar proxy, bounded bio excerpt, derived visible-post/circle counts, and created time. A profile is returned only when it has visible public activity; post and circle counts independently discard inaccessible parent circles, preventing hidden activity from leaking through user ranking or counts." },
    { sourceFile: "src/lib/forum-media.ts", symbol: "resolveSignedPostMedia", evidenceType: "media-and-external-effect-boundary", conciseFinding: "Search passes publicProxy, so visible post media becomes only a same-origin media proxy URL and public-proxy resolution skips storage.createSignedUrls. GET performs no R2 upload/delete, signed-storage request, RPC, cache mutation, view write, recent-search persistence, rate attempt, audit, notification, email, telemetry, insert, update, or delete." },
    { sourceFile: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql", symbol: "can_access_public_circle,posts_select_published_public", evidenceType: "forward-rls-alignment", conciseFinding: "The existing authored but unexecuted migration constrains public post SELECT to published moderation-visible rows whose exact parent circle is active and canonical-public-visible. The route runtime now enforces the same condition before response enrichment; applying the migration remains required for database-layer enforcement." },
    { sourceFile: "scripts/test-forum-search-visibility.mjs", symbol: "forum-search-visibility", evidenceType: "offline-regression", conciseFinding: "The offline Vite-loaded matrix permits visible posts/circles/profiles and excludes inactive, canonical-hidden, and missing-circle rows from payload and counts. It pins bounded grammar, fixed endpoint surface, deterministic read stages, zero writes/RPC/storage signing, and zero real network/database/storage operations." }
  ]
}];
export const resolvedCircleCoverVisibilityFindings = [{
  id: "CIRCLE_COVER_PUBLIC_VISIBILITY_AND_OBJECT_PATH_AUTHORIZATION",
  status: "resolved-source-awaiting-deployment",
  severity: "historical-release-blocking",
  remediationCommit: "Fix and trace circle media visibility",
  sourceFile: "src/pages/api/media/circle/[circleId].ts",
  method: "GET",
  symbol: "GET,resolvePublicCircleCoverTarget,isCircleCoverPath,streamStorageObjectViaSignedUrl",
  forwardMigration: "supabase/migrations/20260714_circle_cover_public_visibility_authorization.sql",
  migrationExecutionStatus: "authored-not-executed",
  conciseFinding: "The public circle-cover proxy now validates a UUID path id, server-loads the exact circle, requires active canonical public visibility, and accepts only canonical database-owned cover paths before storage signing. The authored forward migration replaces permissive circle and prefix-only cover SELECT policies with the same active canonical circle and exact object binding; deployment must execute it.",
  evidence: [
    { sourceFile: "src/pages/api/media/circle/[circleId].ts", symbol: "GET", evidenceType: "request-boundary-and-read-order", conciseFinding: "GET accepts only the route circleId. It has no query, pagination, cursor, limit, sort, table, column, bucket, object-key, URL, actor, bearer, staff, or response-field selector. It constructs the anon-only SSR client, resolves the public cover target, and only then calls the fixed-bucket proxy." },
    { sourceFile: "src/lib/circle-cover.ts,src/lib/site-navigation.ts", symbol: "resolvePublicCircleCoverTarget,isCircleId,isCircleCoverPath,isPublicVisibleCircle", evidenceType: "runtime-circle-and-path-authorization", conciseFinding: "The resolver rejects malformed ids before a database call, selects only id/slug/name/status/image_path for the exact server-selected circle, then requires active status, canonical public visibility, exact returned id, and a timestamp-plus-normalized-filename circle-covers path. Missing, deleted, QA-hidden, mismatched, traversal, encoded-separator, query, fragment, backslash, duplicate-separator, and foreign-prefix values fail closed." },
    { sourceFile: "src/pages/api/media/circle/[circleId].ts", symbol: "GET", evidenceType: "media-ancestry-scope", conciseFinding: "This endpoint serves a circle cover stored only in circles.image_path. It does not query posts, post_media, profiles, authors, media ids, dimensions, durations, counts, timestamps, or media metadata, so no post-to-circle or post-media ancestry response path exists in this method." },
    { sourceFile: "src/lib/supabase-server.ts,src/lib/media-proxy.ts", symbol: "createSSRClient,streamStorageObjectViaSignedUrl", evidenceType: "anon-signing-proxy-and-ssrf-boundary", conciseFinding: "The route constructs an anon-key client with no bearer propagation or service role. After authorization, the helper asks the fixed post-media bucket for a ten-minute signed URL, then fetches only that URL returned by storage and streams its body without exposing the signed URL. No client-selected origin, bucket, key, or URL reaches signing or fetch, so this route has no SSRF-capable caller-controlled destination." },
    { sourceFile: "supabase/migrations/20260518_forum_phase1_schema.sql,supabase/migrations/20260605_circle_cover_public_select.sql", symbol: "circles_select_public,circle_cover_objects_select_public", evidenceType: "historical-rls-gap", conciseFinding: "The active historical circle SELECT policy is using true and the cover storage SELECT policy accepts any post-media object under the circle-covers prefix. Neither policy requires active canonical circle visibility nor proves the object is the exact circles.image_path row." },
    { sourceFile: "supabase/migrations/20260714_circle_cover_public_visibility_authorization.sql", symbol: "can_access_public_circle_cover_object,circles_select_public,circle_cover_objects_select_public", evidenceType: "forward-rls-remediation", conciseFinding: "The forward migration uses can_access_public_circle for public circle rows and requires a canonical storage key equal to the image_path of an active canonical-public circle. It preserves authenticated owner and database-derived staff cover reads; missing relationships fail the exists predicate closed." },
    { sourceFile: "src/pages/api/media/circle/[circleId].ts", symbol: "GET", evidenceType: "read-only-and-error-sanitization", conciseFinding: "GET performs only the circle SELECT, storage signed-URL request, trusted storage fetch, and stream response. It performs no insert, update, delete, upsert, view/download/recent marker, audit, analytics, cache, rate, notification, email, RPC, or R2 mutation. Every route denial returns fixed MEDIA_NOT_FOUND and no legal-consent insertion applies to this read-only method." },
    { sourceFile: "scripts/test-circle-cover-media-visibility.mjs", symbol: "circle-cover-media-visibility", evidenceType: "offline-regression", conciseFinding: "Offline fake-client tests allow only active canonical public circles and prove malformed, missing, inactive, deleted, QA-hidden, mismatched, and noncanonical paths perform zero signing, R2, external fetch, write, audit, cache, or rate effect. Static assertions pin immutable historical policies and the authored forward public-circle and exact-object RLS predicates." }
  ]
}];
export const resolvedPostMediaDeliveryVisibilityFindings = [{
  id: "POST_MEDIA_DELIVERY_ANCESTRY_AND_PROVENANCE_AUTHORIZATION",
  status: "resolved-source-awaiting-deployment",
  severity: "historical-release-blocking",
  remediationCommit: "Fix and trace post media delivery authorization",
  sourceFile: "src/pages/api/media/post/[mediaId].ts",
  method: "GET",
  symbol: "GET,resolvePublicPostMediaTarget,isCanonicalPostMediaObjectPath,streamTrustedMediaUrl",
  forwardMigration: "supabase/migrations/20260715_post_media_delivery_visibility_authorization.sql",
  migrationExecutionStatus: "authored-not-executed",
  conciseFinding: "The public post-media proxy now validates the route UUID, resolves one server-owned media row with its exact published moderation-visible post and active canonical-public circle, then validates the actor/post-bound finalized or temporary key before any signing or fetch. The authored forward migration aligns public post_media and storage SELECT with the same ancestry and exact object binding; it remains a deployment prerequisite.",
  evidence: [
    { sourceFile: "src/pages/api/media/post/[mediaId].ts", symbol: "GET,createPostMediaGet", evidenceType: "request-boundary-and-ordering", conciseFinding: "GET accepts only the UUID-shaped route mediaId and an allowlisted thumb variant. It creates the anon-only SSR client without parsing or forwarding a bearer, resolves the full target before any signing/fetch, and returns fixed MEDIA_NOT_FOUND for every denied case." },
    { sourceFile: "src/pages/api/media/post/[mediaId].ts,src/lib/site-navigation.ts", symbol: "resolvePublicPostMediaTarget,isCanonicalPostMediaObjectPath,isPublicVisibleCircle", evidenceType: "runtime-ancestry-and-provenance", conciseFinding: "The resolver requires exact media, post, and circle id relationships; published post and moderation states; active canonical-public circle status; image/video kind; no stored URL; and a strict owner/post-bound final or video-only tmp key. Missing, hidden, deleted, QA-hidden, malformed, cross-user, cross-post, legacy tmp, encoded-separator, traversal, query, fragment, or duplicate-separator paths fail closed." },
    { sourceFile: "src/lib/media-proxy.ts", symbol: "streamStorageObjectViaSignedUrl,streamTrustedMediaUrl", evidenceType: "signing-fetch-stream-boundary", conciseFinding: "After target authorization only, the fixed Supabase post-media bucket creates a short signed URL or the server-configured HTTPS R2 origin receives the canonical tmp key. The helper fetches only those trusted destinations with a fifteen-second abort bound and a 150 MiB trusted content-length cap, forwards only content-type, content-length, etag, and last-modified, and never exposes signed URLs, credentials, Set-Cookie, authorization, or tracing headers." },
    { sourceFile: "supabase/migrations/20260611_forum_permission_lockdown.sql,supabase/migrations/20260524_forum_phase3_post_media.sql", symbol: "post_media_select_public_or_owner,post_media_objects_select_public_or_owner", evidenceType: "historical-rls-gap", conciseFinding: "Historical public media and storage SELECT predicates accepted published posts without moderation-visible circle ancestry or exact canonical object provenance, so route-only checks could not supply database-layer equivalence." },
    { sourceFile: "supabase/migrations/20260715_post_media_delivery_visibility_authorization.sql", symbol: "can_access_public_post_media_object,post_media_select_public_or_owner,post_media_objects_select_public_or_owner", evidenceType: "forward-rls-remediation", conciseFinding: "The authored unexecuted migration requires published moderation-visible posts, can_access_public_circle, canonical media owner/post key binding, and exact storage-path or thumbnail binding for public access. Owner and database-role-derived staff branches remain for authenticated management; the public route never forwards bearer access." },
    { sourceFile: "scripts/test-post-media-delivery-authorization.mjs", symbol: "post-media-delivery-authorization", evidenceType: "offline-regression", conciseFinding: "Offline Vite fakes prove allowed final and post-bound tmp media and denied malformed ids, inaccessible ancestry, provenance replay/traversal, foreign URL, request override, and malformed-bearer broadening attempts all stop before signing, fetch, streaming, writes, audit, cache, or rate persistence. Historical migrations are hash-pinned and the forward migration is statically checked without real services." }
  ]
}];
export const resolvedProfileMediaDeliveryVisibilityFindings = [{
  id: "PROFILE_MEDIA_USER_KIND_OBJECT_DELIVERY_AUTHORIZATION",
  status: "resolved-source-awaiting-deployment",
  severity: "historical-release-blocking",
  remediationCommit: "Fix and trace profile media delivery authorization",
  sourceFile: "src/pages/api/media/profile/[userId]/[kind].ts",
  method: "GET",
  symbol: "GET,resolvePublicProfileMediaTarget,isProfileMediaPathForUser,streamStorageObjectViaSignedUrl",
  forwardMigration: "supabase/migrations/20260716_profile_media_delivery_authorization.sql",
  migrationExecutionStatus: "authored-not-executed",
  conciseFinding: "The public profile-media proxy now validates the route UUID and exact avatar/banner kind, server-loads the exact profile row, and accepts only the corresponding canonical lower-case user-bound storage key before fixed-bucket signing. The authored forward migration replaces prefix-only avatar/banner storage SELECT with exact profile-field binding; it remains a deployment prerequisite.",
  evidence: [
    { sourceFile: "src/pages/api/media/profile/[userId]/[kind].ts", symbol: "GET,createProfileMediaGet,isProfileMediaUserId,isProfileMediaKind", evidenceType: "request-boundary-and-ordering", conciseFinding: "GET accepts only a UUID-shaped route userId and exact avatar or banner kind. It ignores query parameters and Authorization, constructs an anon-only client only after path validation, resolves the server-owned target before signing, and returns fixed MEDIA_NOT_FOUND on every denial." },
    { sourceFile: "src/pages/api/media/profile/[userId]/[kind].ts,src/lib/profile-media.ts", symbol: "resolvePublicProfileMediaTarget,isProfileMediaPathForUser", evidenceType: "profile-kind-and-key-authorization", conciseFinding: "The first database read selects one exact profiles row and requires returned id equality. Avatar reads only avatar_url and banner reads only banner_url; the selected value must be a lower-case canonical profile-avatars or profile-banners key whose UUID segment equals that same profile. Missing rows/media, cross-kind, cross-user, foreign URL, traversal, encoded separator, query, fragment, backslash, duplicate separator, and case-confused keys fail closed." },
    { sourceFile: "supabase/migrations/20260518_forum_phase1_schema.sql,supabase/migrations/20260626_user_safety_states_and_bans.sql", symbol: "profiles_select_public,user_safety_states_select_self", evidenceType: "public-profile-lifecycle-contract", conciseFinding: "The established profile SELECT policy is public while user safety state is not anonymously readable and profiles have no separate hidden, soft-delete, or deactivation field. Therefore suspended and banned accounts retain public profile media while a profile row exists; an auth/profile deletion removes the profile relationship and the route denies. Missing media has no default-object fallback and returns MEDIA_NOT_FOUND." },
    { sourceFile: "src/lib/supabase-server.ts,src/lib/media-proxy.ts", symbol: "createSSRClient,streamStorageObjectViaSignedUrl,streamTrustedMediaUrl", evidenceType: "anon-signing-proxy-and-ssrf-boundary", conciseFinding: "The route uses the anon key without bearer propagation or a service role. After authorization only, it signs the fixed post-media bucket and fetches only the storage-issued URL with a fifteen-second abort and 150 MiB content-length cap. Avatar/banner delivery allowlists JPEG, PNG, WebP, and GIF; only content-type, content-length, etag, and last-modified can pass through, and no signed URL, cookie, authorization, trace header, bucket, host, object key, or client-selected destination is exposed." },
    { sourceFile: "supabase/migrations/20260606_profile_banner_and_storage.sql", symbol: "profile_avatar_objects_select_public,profile_banner_objects_select_public", evidenceType: "historical-rls-gap", conciseFinding: "The historical storage SELECT policies permit any post-media object under profile-avatars or profile-banners prefixes. They do not prove the object belongs to the selected profile or is its current avatar_url/banner_url, so route-only checking could not provide storage-layer equivalence." },
    { sourceFile: "supabase/migrations/20260716_profile_media_delivery_authorization.sql", symbol: "can_access_public_profile_media_object,profile_avatar_objects_select_public,profile_banner_objects_select_public", evidenceType: "forward-rls-remediation", conciseFinding: "The authored unexecuted function requires a canonical lower-case profile key, exact UUID segment equal to profiles.id, and exact current avatar_url or banner_url equality. Both public storage SELECT policies call it; missing profiles and mismatched fields fail the exists predicate closed, and no public mutation authority is granted." },
    { sourceFile: "scripts/test-profile-media-delivery-authorization.mjs", symbol: "profile-media-delivery-authorization", evidenceType: "offline-regression", conciseFinding: "Offline Vite fakes prove canonical avatar/banner delivery and identical public decisions with ordinary or malformed bearer input. Malformed ids/kinds, missing/mismatched profiles/media, cross-kind/cross-user, foreign, traversal, encoded, and request-override cases cause zero signing, fetch, streaming, writes, audit, cache, or rate persistence; the test pins historical migration content and forward policy structure without real services." }
  ]
}];
export const resolvedPublicNewsApiSafetyFindings = [{
  id: "PUBLIC_NEWS_API_QUERY_ERROR_AND_OUTPUT_SAFETY",
  status: "resolved",
  severity: "historical-release-blocking",
  remediationCommit: "Fix and trace news API safety",
  sourceFile: "src/pages/api/news.ts",
  method: "GET",
  symbol: "GET,createPublicNewsGet,parsePublicNewsQuery,normalizePublicNewsArticle",
  conciseFinding: "The public news endpoint now validates its bounded category/page/limit query before constructing the anon client, returns fixed errors, and exposes only normalized published rows. It has no provider fetch, cache, or persistent effect; no migration is required for this route-local hardening.",
  evidence: [
    { sourceFile: "src/pages/api/news.ts", symbol: "GET,createPublicNewsGet,parsePublicNewsQuery,parsePublicNewsPositiveInt", evidenceType: "request-validation-and-ordering", conciseFinding: "GET accepts only an exact news category plus page 1-1000 and limit 1-12. Invalid category, malformed numeric form, control character, zero, or excessive values return INVALID_NEWS_QUERY before anon-client construction or a database read; unknown query keys cannot select an endpoint, provider, host, header, cache namespace, or parser." },
    { sourceFile: "src/lib/supabase-server.ts,supabase/migrations/20260612_hot_news_mvp.sql", symbol: "createSSRClient,news_articles_select_published_public", evidenceType: "public-authentication-and-rls", conciseFinding: "The route builds only an anon-key client with persistent sessions disabled and never reads Authorization or derives a requester. The published-news RLS policy permits only status = published rows, and runtime normalization independently drops nonpublished or invalid-category records before response." },
    { sourceFile: "src/lib/news.ts", symbol: "listPublicNewsFeed,normalizePublicNewsArticle,normalizeNewsUrl", evidenceType: "read-only-data-and-output-sanitization", conciseFinding: "The helper performs bounded published news SELECTs only: featured/latest/hot reads, then the bounded page read. It has no fetch, provider adapter, cache, insert, update, delete, upsert, RPC, filesystem, KV, or storage operation. Public rows remove author_id, bound text fields, strip HTML/control characters, reject nonpublished/invalid-category records, and reject javascript, data, credentialed, localhost, private, link-local, loopback, and IPv6-loopback source or cover URLs." },
    { sourceFile: "src/lib/news-content.ts,src/pages/news/[slug].astro", symbol: "parseNewsContent,parseInlineSegments,CommunityLayout", evidenceType: "frontend-rendering-contract", conciseFinding: "Public news text is parsed into text, heading, list, link, and image blocks, while Astro renders text segments as escaped content. Link parsing accepts only relative or HTTP(S) values and public article normalization removes literal HTML tags and unsafe URLs before this route or page receives a row." },
    { sourceFile: "src/pages/api/news.ts", symbol: "GET", evidenceType: "error-and-effect-classification", conciseFinding: "Missing runtime bindings return NEWS_UNAVAILABLE and helper failures return NEWS_FETCH_FAILED without environment keys, provider details, hostnames, tokens, or raw database errors. The first and only effect is the bounded published-news database read; there is no outbound request, cache write, user state, analytics, audit, rate, notification, email, R2, or consent-required mutation." },
    { sourceFile: "scripts/test-public-news-api-safety.mjs", symbol: "public-news-api-safety", evidenceType: "offline-regression", conciseFinding: "Offline Vite fakes prove valid bounded public reads, malformed-query zero-read denial, bearer-agnostic public behavior, generic error sanitization, URL and HTML/control sanitization, no cache or write effect, and no real network/database/provider operation." }
  ]
}];
export const resolvedPublicNewsDetailApiSafetyFindings = [{
  id: "PUBLIC_NEWS_DETAIL_SLUG_AND_OUTPUT_SAFETY",
  status: "resolved",
  severity: "historical-release-blocking",
  remediationCommit: "Fix and trace news detail API safety",
  sourceFile: "src/pages/api/news/[slug].ts",
  method: "GET",
  conciseFinding: "The public news-detail GET now single-decodes and canonical-validates the route slug before constructing its anon client, returns one generic not-found response for malformed, missing, and inaccessible targets, normalizes both database and fixed fallback rows, bounds related results, and never returns raw helper details.",
  evidence: [
    { sourceFile: "src/pages/api/news/[slug].ts", symbol: "parsePublicNewsSlug,createPublicNewsDetailGet", evidenceType: "runtime-remediation", conciseFinding: "The route rejects empty, oversized, malformed-percent, control, slash, backslash, traversal, double-encoded, filter-grammar, whitespace, and noncanonical values before client construction or lookup, then uses only the decoded validated slug." },
    { sourceFile: "src/lib/news.ts", symbol: "getPublicNewsArticleBySlug,listRelatedPublicNews,normalizePublicNewsArticle", evidenceType: "public-output-remediation", conciseFinding: "Exact published lookup and fixed related category/exclude values pass through the common public normalizer. Missing-table fallback rows are now normalized too, related results are capped at four, and no author id, unsafe URL, control character, literal HTML, invalid status, category, or slug reaches the response." },
    { sourceFile: "scripts/test-public-news-detail-api-safety.mjs", symbol: "public-news-detail-api-safety", evidenceType: "offline-regression", conciseFinding: "Offline Vite fakes prove zero-read invalid-slug denial, identical malformed-bearer public behavior, generic errors, normalized fallback/related output, no provider/cache/write effect, and no real network/database operation." }
  ]
}];
export const resolvedSecurityFindings = [...resolvedExternalVideoAuthorizationOrderingFindings, ...resolvedPostMediaCrossPostProvenanceFindings, ...resolvedForumPostsCircleAuthorizationFindings, ...resolvedForumReportAuthorizationFindings, ...resolvedForumSearchVisibilityFindings, ...resolvedCircleCoverVisibilityFindings, ...resolvedPostMediaDeliveryVisibilityFindings, ...resolvedProfileMediaDeliveryVisibilityFindings, ...resolvedPublicNewsApiSafetyFindings, ...resolvedPublicNewsDetailApiSafetyFindings, {
  id: "RESEND_CONFIRMATION_EXTERNAL_REDIRECT",
  status: "resolved",
  severity: "historical-release-blocking",
  remediationCommit: "eb92faf98dd0dd60c10fb6d770781b3a99c1e604",
  sourceFile: "src/pages/api/auth/resend-confirmation.ts",
  method: "POST",
  symbol: "POST",
  unsafeInput: "/\\\\evil.example",
  conciseFinding: "The re-audit confirms that the formerly unsafe slash-plus-backslash input is reduced to the internal fallback before callback metadata and is sanitized again at every browser navigation sink.",
  evidence: [
    { sourceFile: "src/pages/api/auth/resend-confirmation.ts", symbol: "POST", evidenceType: "untrusted-input", conciseFinding: "Reads payload.next only through getSafeNext before it can reach buildAuthCallbackRedirect; the unauthenticated email is a target account identifier, not an acting-user field." },
    { sourceFile: "src/lib/auth-redirect.ts", symbol: "getSafeNext", evidenceType: "canonical-validation", conciseFinding: "Rejects raw and repeatedly decoded backslashes, controls, boundary whitespace, malformed encodings, protocol-relative authority, and non-relative input; accepted output is resolved and required to remain at the trusted origin." },
    { sourceFile: "src/lib/auth-redirect.ts", symbol: "buildAuthCallbackRedirect", evidenceType: "callback-propagation", conciseFinding: "Validates the supplied application origin, constructs only /auth/callback/, and serializes a second getSafeNext result into its next query value." },
    { sourceFile: "src/components/auth/AuthCallback.tsx", symbol: "AuthCallback", evidenceType: "callback-validation", conciseFinding: "Sanitizes callback next again before passing it to navigation.replace after the confirmation session and consent-status path." },
    { sourceFile: "src/lib/legal-consent-adapters.ts", symbol: "browserNavigationAdapter", evidenceType: "navigation-sink", conciseFinding: "Both browser navigation adapter methods call getSafeNext immediately before window.location.assign or window.location.replace, so the final browser sink receives only an internal canonical destination." },
    { sourceFile: "src/components/legal/LegalConsentGate.tsx", symbol: "LegalConsentGate", evidenceType: "navigation-sink", conciseFinding: "The consent-gate final redirect calls getSafeNext immediately before adapter or window.location.replace navigation." },
    { sourceFile: "src/lib/auth-redirect.ts", symbol: "buildResetPasswordRedirect", evidenceType: "trusted-origin", conciseFinding: "Password recovery accepts only a trusted http or https application origin and creates a fixed /auth/reset-password/ destination without client next input." },
    { sourceFile: "scripts/test-auth-redirect-safety.mjs", symbol: "redirect-safety-matrix", evidenceType: "regression-test", conciseFinding: "Exercises the original slash-plus-backslash input plus authority, encoded, scheme, malformed-percent, control, whitespace, and userinfo variants; every rejected case resolves to an internal fallback and the browser sink receives only that fallback." }
  ]
}, {
  id: "COMMENT_REACTION_PARENT_POST_VISIBILITY_AUTHORIZATION",
  status: "resolved-source-awaiting-deployment",
  severity: "historical-release-blocking",
  remediationCommit: "d57aae680fb81ed4133af73aae955473218d9c09",
  sourceFile: "src/pages/api/forum/comments.ts",
  method: "PUT",
  symbol: "PUT",
  migrationExecutionStatus: "authored-not-executed",
  conciseFinding: "The re-audit confirms that runtime and forward RLS now enforce the same comment, parent-post, and circle visibility chain before a reaction mutation. Applying the authored migration is a deployment prerequisite, not an unresolved source authorization bypass.",
  evidence: [
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "PUT", evidenceType: "authentication-and-actor-binding", conciseFinding: "PUT requires a bearer, constructs an anon-key client bound to that bearer, verifies auth.getUser(token), and uses only authData.user.id for reaction lookup and insert; request fields cannot replace the actor or reaction owner." },
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "resolveAccessibleCommentReactionTarget", evidenceType: "runtime-visibility-chain", conciseFinding: "After UUID validation and before any comment_reactions query, the helper reads the exact comment, its exact parent post, and that post's exact circle; missing, mismatched, unpublished, hidden, deleted, inactive, or QA-hidden rows return before a reaction effect." },
    { sourceFile: "src/lib/site-navigation.ts", symbol: "isPublicVisibleCircle", evidenceType: "canonical-circle-visibility", conciseFinding: "The canonical public-circle helper excludes deleted circles and explicitly hidden QA slugs/names; PUT additionally requires status active. The schema has no private-circle or membership relation, so no membership branch exists to enforce." },
    { sourceFile: "supabase/migrations/20260713_comment_reaction_visibility_authorization.sql", symbol: "can_access_comment_reaction_target", evidenceType: "rls-visibility-chain", conciseFinding: "The forward stable security-definer predicate joins comments to posts to circles and requires published comment/post moderation states plus active visible circle conditions. Missing ancestor rows fail the exists predicate closed." },
    { sourceFile: "supabase/migrations/20260713_comment_reaction_visibility_authorization.sql", symbol: "comment_reactions_insert_self,comment_reactions_update_self,comment_reactions_delete_self", evidenceType: "rls-mutation-ownership", conciseFinding: "INSERT requires user_id = auth.uid and the full predicate; UPDATE applies both ownership and predicate in USING and WITH CHECK; DELETE applies both in USING. The route currently toggles only INSERT or DELETE, while these policies also cover future UPDATE/UPSERT behavior." },
    { sourceFile: "scripts/test-comment-reaction-visibility-authorization.mjs", symbol: "comment-reaction-visibility-authorization", evidenceType: "offline-regression-test", conciseFinding: "Offline cases cover published targets and deny unpublished/hidden/deleted comments, hidden/deleted posts and circles, missing/mismatched ancestors, unauthenticated or client-supplied actor attempts with zero reaction mutation calls; it also verifies forward policy coverage and historical migration hash." },
    { sourceFile: "supabase/migrations/20260713_comment_reaction_visibility_authorization.sql", symbol: "migration-file", evidenceType: "deployment-prerequisite", conciseFinding: "The migration is authored and deliberately not executed in this re-audit. Deployment must apply it before production receives the database-layer enforcement; this is not a remaining source-code authorization bypass." }
  ]
}, {
  id: "COMMENT_CREATION_CIRCLE_ANCESTOR_AUTHORIZATION",
  status: "resolved-source-awaiting-deployment",
  severity: "historical-release-blocking",
  remediationCommit: "485215b2c311a0347258c5618db4db5326f84a58",
  sourceFile: "src/pages/api/forum/comments.ts",
  method: "POST",
  symbol: "POST",
  migrationExecutionStatus: "authored-not-executed",
  conciseFinding: "The re-audit confirms that POST and the authored forward INSERT policy require the same verified actor and post-circle-parent visibility chain. Applying the migration remains a deployment prerequisite, but no active source authorization blocker remains.",
  evidence: [
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "POST", evidenceType: "authentication-and-actor-binding", conciseFinding: "POST requires a bearer, constructs an anon-key client bound to that bearer, verifies auth.getUser(token), and derives author_id only from authData.user.id. Request fields provide only post_id, body, and optional parent_id." },
    { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "safety-authorization", conciseFinding: "POST supplies only the verified user id and comment_create action. The helper reads that actor safety state and denies banned, suspended, or unavailable states before parsing, target resolution, rate persistence, or comment insertion." },
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "resolveAccessibleCommentCreationTarget", evidenceType: "runtime-ancestor-authorization", conciseFinding: "After UUID validation and before moderation, POST reads the exact post including server-derived circle_id, requires published post and moderation state, reads the exact active canonical-visible circle, and requires an optional parent to be published and bound to that exact post. Missing, mismatched, deleted, hidden, pending, or noncanonical ancestors fail closed." },
    { sourceFile: "src/lib/site-navigation.ts", symbol: "isPublicVisibleCircle", evidenceType: "canonical-visibility", conciseFinding: "The shared public-circle helper rejects deleted and configured hidden QA circles; POST separately requires status active. The schema has no private-circle or membership relation, so no member authorization path exists to omit." },
    { sourceFile: "src/lib/moderation/moderate-content.server.ts", symbol: "moderateContent", evidenceType: "post-authorization-moderation", conciseFinding: "Content moderation runs only after the full runtime target chain succeeds. Reject returns before rate persistence and insertion; review changes the inserted pending-review state without replacing the verified actor or target ids." },
    { sourceFile: "src/lib/server/rate-limit.ts", symbol: "enforceUserRateLimit", evidenceType: "first-persistent-effect", conciseFinding: "After target authorization and moderation, the helper counts recent attempts and inserts forum_upload_attempts for comment_create when allowed. Its backend errors fail open, a non-blocking Phase 4B rate-limit finding; comments.insert is the first domain mutation." },
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "POST", evidenceType: "comment-insert-and-effects", conciseFinding: "The RLS-scoped comments.insert uses only resolver-returned post_id and parent_id plus verified author_id. The route has no notification, count/RPC, audit, email, cache, R2, or direct external write; the database comment-created trigger can create notifications only after a successful published comment insert." },
    { sourceFile: "supabase/migrations/20260713_comment_creation_circle_authorization.sql", symbol: "comments_insert_self", evidenceType: "forward-rls-alignment", conciseFinding: "The forward policy replaces comments_insert_self and requires author_id = auth.uid(), valid published or pending-review comment state, and can_create_comment_target(post_id, parent_id). The security-definer predicate requires a published post, active canonical-visible circle, and optional same-post published parent; missing ancestors deny through exists." },
    { sourceFile: "scripts/test-comment-creation-circle-authorization.mjs", symbol: "comment-creation-circle-authorization", evidenceType: "offline-regression-test", conciseFinding: "Deterministic cases allow accessible top-level and same-post reply creation, deny deleted/hidden/inactive/missing/mismatched ancestors, cross-post or nonpublished parents, unauthenticated and malformed input with zero modeled effects, and verify source order plus historical migration hash." },
    { sourceFile: "supabase/migrations/20260713_comment_creation_circle_authorization.sql", symbol: "migration-file", evidenceType: "deployment-prerequisite", conciseFinding: "The forward migration was authored in remediation commit 485215b2c311a0347258c5618db4db5326f84a58 and was not executed during this re-audit. Deployment must apply it before database-layer enforcement is live." }
  ]
}, {
  id: "COMMENT_READ_CIRCLE_ANCESTOR_VISIBILITY_AUTHORIZATION",
  status: "resolved-source-awaiting-deployment",
  severity: "historical-release-blocking",
  remediationCommit: "aa2cf01be3a21eed9d3a0de111bc249c0ead046a",
  sourceFile: "src/pages/api/forum/comments.ts",
  method: "GET",
  symbol: "GET",
  migrationExecutionStatus: "authored-not-executed",
  conciseFinding: "The GET re-audit confirms runtime and authored SELECT-RLS both require the same published post and active canonical-visible circle before public comment, profile, or reaction enrichment. The source blocker is resolved; deployment must still execute the forward migration.",
  evidence: [
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "handler-execution-order", conciseFinding: "GET checks runtime configuration, validates post_id as UUID-shaped, parses an optional bearer, constructs an anon-key RLS client, optionally verifies the bearer and derives viewerUserId, then calls resolveAccessibleCommentReadTarget before any comment, profile, or reaction query." },
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "resolveAccessibleCommentReadTarget", evidenceType: "first-database-read-and-authorization", conciseFinding: "The first database read selects the exact post id, author_id, circle_id, status, and moderation_status. The helper rejects a missing or mismatched row, absent or non-UUID circle_id, non-published post or moderation state, then reads that server-derived exact circle and requires active canonical visibility before returning." },
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "post-authorization-read-order", conciseFinding: "Only after the resolver succeeds does GET select comments for the resolver-derived post, filter public published/moderation-published rows, preserve an authenticated author's own pending-review branch, retain only body-empty deleted structural placeholders with a published child, then select profiles and reactions for the remaining response set." },
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "verified-viewer-and-enrichment", conciseFinding: "A successfully verified bearer supplies viewerUserId before target resolution and is used only for own pending visibility, can_delete, and liked_by_me. The conditional late bearer re-verification exists only if the earlier verification did not establish a viewer; it occurs after the already-authorized reaction read and cannot bypass the resolver or replace an actor from client payload." },
    { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "read-only-absence-confirmed", conciseFinding: "GET performs select queries, in-memory filtering/counting, and a JSON response only. It has no insert, update, delete, upsert, RPC, cache refresh or persistence, audit, notification, email, telemetry mutation, R2/storage, provider call, or other irreversible side effect; legal-consent insertion is not applicable to this read-only method." },
    { sourceFile: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql", symbol: "can_access_public_circle,posts_select_published_public", evidenceType: "forward-post-rls-alignment", conciseFinding: "The authored forward migration replaces the public post SELECT branch with status and moderation publication checks plus can_access_public_circle(circle_id), whose exists predicate requires the exact active canonical-visible circle and fails closed for missing rows." },
    { sourceFile: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql", symbol: "can_access_public_comment_read_target,comments_select_public_or_staff,comment_reactions_select_accessible", evidenceType: "forward-comment-reaction-rls-alignment", conciseFinding: "The authored predicate joins comment to published/moderation-visible post and active canonical-visible circle. Recreated public comment and reaction SELECT policies require that predicate while preserving authenticated-own and staff branches; missing comment, post, or circle rows deny through exists." },
    { sourceFile: "scripts/test-comment-read-circle-visibility-authorization.mjs", symbol: "comment-read-circle-visibility-authorization", evidenceType: "offline-regression-test", conciseFinding: "Deterministic offline tests allow only active canonical public ancestry and verified liked_by_me, while inaccessible post or circle paths stop before comment/profile/reaction reads. Static checks assert the runtime order, forward predicates, policy coverage, and unchanged historical migration source." },
    { sourceFile: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql", symbol: "migration-file", evidenceType: "deployment-prerequisite", conciseFinding: "This forward migration was authored by remediation commit aa2cf01be3a21eed9d3a0de111bc249c0ead046a and was not executed during this source re-audit. Deployment execution remains required before database-layer enforcement is live." }
  ]
}];
export const batch3hTraces = {
  "src/pages/api/auth/resend-confirmation.ts#POST": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "not-applicable: pre-auth account-confirmation resend is an auth/recovery exemption", consentInsertion: { symbol: "POST", precedingStage: "No verified session exists; the request email identifies only the confirmation target account.", followingStage: "Successful confirmation establishes a session before the callback and legal-consent flow.", refactorRequired: false, smallestFutureRefactor: "No legal-consent guard insertion applies to this pre-auth confirmation resend route." }, currentExecutionOrder: ["runtime-validation", "parse-json", "normalize-email", "sanitize-next", "email-validation", "construct-anon-auth-client", "derive-ip-hash", "rate-limit-rpc-first-irreversible-effect", "trusted-callback-url-construction", "auth-provider-resend", "generic-enumeration-safe-response"], evidence: [{ sourceFile: "src/pages/api/auth/resend-confirmation.ts", symbol: "POST", evidenceType: "handler", conciseFinding: "Returns a generic failure for missing runtime env, parses JSON with a null fallback, normalizes email by trim and lowercase, canonicalizes next with getSafeNext, and rejects invalid email before client construction, rate limiting, or provider resend." }, { sourceFile: "src/pages/api/auth/resend-confirmation.ts", symbol: "POST", evidenceType: "authentication-exemption", conciseFinding: "The route intentionally has no bearer/session read because an unconfirmed account must be able to request confirmation; email is only the target account identifier and cannot impersonate an authenticated actor." }, { sourceFile: "src/pages/api/auth/resend-confirmation.ts", symbol: "POST", evidenceType: "validation", conciseFinding: "The endpoint validates only a normalized basic email shape; it has no content-type or request-body-size check, which remains Phase 4B hardening and occurs before every side effect." }, { sourceFile: "src/lib/request-ip.ts", symbol: "getRequestIp", evidenceType: "rate-limit-identity", conciseFinding: "Uses cf-connecting-ip, then the first x-forwarded-for value, then a fixed fallback; POST hashes that value with RATE_LIMIT_SALT before the rate RPC." }, { sourceFile: "src/lib/server/rate-limit.ts", symbol: "consumeVerificationEmailResendLimit", evidenceType: "first-irreversible-effect", conciseFinding: "Calls the verification-email rate-limit RPC with the hashed IP, five attempts, and a 24-hour window before callback construction or provider resend; the RPC records the attempt when available, making it the first persistent effect." }, { sourceFile: "src/lib/server/rate-limit.ts", symbol: "backendUnavailableResult", evidenceType: "phase4b-finding", conciseFinding: "A rate-limit backend error fails open with allowed true; there is no source-proven request idempotency, per-email limit, or explicit RPC serialization, so retry and concurrent behavior remain Phase 4B hardening." }, { sourceFile: "src/pages/api/auth/resend-confirmation.ts", symbol: "POST", evidenceType: "provider-ordering", conciseFinding: "After an allowed rate result, builds the trusted callback URL and calls supabase.auth.resend with signup type, normalized email, and only that callback URL; no service-role client, R2, cache, audit, notification, or direct database operation exists in the route." }, { sourceFile: "src/pages/api/auth/resend-confirmation.ts", symbol: "POST", evidenceType: "error-sanitization", conciseFinding: "Provider throws and normal provider returns both receive the same generic success message, while outer failures return a fixed error code; raw provider errors and Error.message are not exposed, preserving account-enumeration resistance at this response boundary." }, { sourceFile: "src/lib/auth-redirect.ts", symbol: "getSafeNext", evidenceType: "redirect-sanitization", conciseFinding: "The original /\\\\evil.example exploit and protocol-relative, encoded-authority, encoded-backslash, absolute-URL, scheme, malformed-percent, control, whitespace, and userinfo variants resolve to the internal fallback rather than callback metadata or an external URL." }, { sourceFile: "src/pages/api/auth/resend-confirmation.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for every method other than POST." }, { sourceFile: "src/pages/api/auth/resend-confirmation.ts", symbol: "POST", evidenceType: "absence-confirmed", conciseFinding: "No client field can set a verified actor, no authorization-controlled resource exists before confirmation, and no side effect occurs before request validation; legal consent cannot apply before account confirmation and session establishment." }] },
};
export const batch3iTraces = {
  "src/pages/api/forum/circles.ts#GET": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", currentExecutionOrder: ["runtime-validation", "construct-anon-rls-client", "circle-list-read-with-schema-fallback", "public-visibility-filter", "response"], evidence: [{ sourceFile: "src/pages/api/forum/circles.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Reads only runtime env, constructs the anon-key client, calls fetchCirclesWithFallback, then applies isPublicVisibleCircle before returning circles and schema-support metadata." }, { sourceFile: "src/lib/server/circle-management.ts", symbol: "createAnonClient", evidenceType: "client-construction", conciseFinding: "Constructs an anon-key Supabase client without a bearer token, service-role credential, or authenticated acting user." }, { sourceFile: "src/lib/circle-data.ts", symbol: "fetchCirclesWithFallback", evidenceType: "query-validation", conciseFinding: "GET accepts no request query or body; it selects the complete circle list ordered by name ascending with no pagination, limit, filter, or caller-controlled sort, and retries only the select shape when extended columns are absent." }, { sourceFile: "src/lib/site-navigation.ts", symbol: "isPublicVisibleCircle", evidenceType: "visibility", conciseFinding: "Excludes deleted circles plus explicitly hidden QA slugs and names; the schema constrains persisted status to active or deleted, so this route exposes only active public circles after its server-side filter." }, { sourceFile: "supabase/migrations/20260518_forum_phase1_schema.sql", symbol: "circles_select_public", evidenceType: "rls", conciseFinding: "The public select policy permits anon/authenticated circle reads; visibility for this route is therefore enforced by its explicit active/deleted and QA-artifact filter rather than a privileged client." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "The handler and called list helper contain only select/order/map/filter work: no view-count, last-viewed, membership, audit, analytics, cache, notification, RPC, email, R2/storage, moderation-provider, or other external side effect occurs." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "GET", evidenceType: "phase4b-finding", conciseFinding: "Database and caught Error.message values are returned in 500 responses, and the unpaginated public list has no read-rate limit; these are Phase 4B hardening findings." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET, POST, and PATCH." }] },
  "src/pages/api/forum/circles.ts#POST": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "POST after requireForumUser(request, env) and before assertUserCanWrite or request.json()", consentInsertion: { symbol: "POST", precedingStage: "requireForumUser verifies the bearer with auth.getUser, derives auth.user.id, loads the verified profile, and returns the bearer-bound RLS client.", followingStage: "assertUserCanWrite reads the verified actor safety state before JSON parsing, validation, rate accounting, duplicate checks, moderation, or circle insertion.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireForumUser and pass only auth.user.id before the safety-state check." }, currentExecutionOrder: ["runtime-validation", "authenticate", "derive-verified-user", "construct-bearer-rls-client", "profile-read", "safety-state-read", "parse-json", "schema-validation-and-owner-binding", "ip-hash", "rate-limit-count-and-attempt-insert-first-irreversible-effect", "name-duplicate-read", "text-moderation-external-read", "cover-signed-url-and-moderation", "slug-duplicate-read", "circle-insert", "slug-conflict-retry", "response"], evidence: [{ sourceFile: "src/lib/server/circle-management.ts", symbol: "requireForumUser", evidenceType: "authentication", conciseFinding: "Parses only a bearer token, creates an anon-key client bound to that token, verifies auth.getUser(token), and loads the profile by authData.user.id before returning the actor and RLS client." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "authorization", conciseFinding: "Reads the verified actor safety state before request parsing and denies banned, suspended, or unavailable safety-state paths without proceeding to rate, moderation, or circle creation." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "POST", evidenceType: "validation", conciseFinding: "After authentication it parses JSON with a null fallback, bounds name to 2-40, description to 200, image path to 500, and type to topic/device/project; it has no content-type or request-body-size validation." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "POST", evidenceType: "actor-binding", conciseFinding: "Request owner_id must be empty or equal auth.user.id, and insertCircleWithSlug always writes owner_id: auth.user.id; request slug is ignored, so no body value can replace the verified creator." }, { sourceFile: "src/lib/server/rate-limit.ts", symbol: "enforceUserRateLimit", evidenceType: "first-irreversible-effect", conciseFinding: "After validated payload fields, counts recent circle_create attempts for verified userId and inserts an attempt with the salted request-IP hash before duplicate checks, moderation, or circle insertion; that attempt insert is the first persistent effect." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "findDuplicateCircle", evidenceType: "duplicate-guard", conciseFinding: "Performs an exact lowercased name comparison after an ilike select before moderation and insert; the database also has a lower(name) unique index, which converts a create-time race to CIRCLE_NAME_ALREADY_EXISTS." }, { sourceFile: "src/lib/circle-slug.ts", symbol: "buildUniqueCircleSlug", evidenceType: "slug-validation", conciseFinding: "Builds a normalized 64-character-or-less slug, probes existing slugs, and POST retries one database slug-key conflict with a timestamp-derived base; the schema enforces the slug check and uniqueness." }, { sourceFile: "src/lib/moderation/moderate-content.server.ts", symbol: "moderateContent", evidenceType: "moderation", conciseFinding: "Runs local sensitive-lexicon and quality checks and may call configured OpenAI moderation/forum-policy providers before the circle insert; reject or review paths return before circle creation, while a configured local-only degraded allow is logged only." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "moderateCircleCoverImage", evidenceType: "external-read", conciseFinding: "When enabled, creates signed cover URLs through the authenticated storage client, may fetch image data and call the moderation provider before insertion; a rejected supplied managed path is removed from storage before returning, with cleanup errors logged rather than thrown." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "insertCircleWithSlug", evidenceType: "database-write", conciseFinding: "The circle insert is the first route-owned domain write after all authorization, validation, duplicate, slug, and moderation stages; there is no owner/member relationship table write, notification, audit, email, RPC, cache, R2, or service-role operation in this endpoint." }, { sourceFile: "supabase/migrations/20260603_forum_circle_owner_management.sql", symbol: "circles_insert_owner_or_staff", evidenceType: "rls", conciseFinding: "Database RLS requires inserted owner_id to equal auth.uid or the verified session to be moderator/admin, providing a second owner-binding control for the bearer-bound client." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "POST", evidenceType: "partial-failure-and-idempotency", conciseFinding: "There is no transaction or idempotency key: a rate attempt remains after later rejection/failure, storage cleanup can occur on a rejected cover, and concurrent same-name requests rely on the unique index while a persisted slug conflict gets one retry; no separate membership row can partially fail because this source and schema create none." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "POST", evidenceType: "error-handling", conciseFinding: "Validation and moderation responses use route codes, but several database and caught errors are returned as details/Error.message; raw error sanitization, request-size/content-type controls, rate-backend fail-open behavior, and retry idempotency remain Phase 4B hardening." }] },
  "src/pages/api/forum/circles.ts#PATCH": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "PATCH after requireForumUser(request, env) and before assertUserCanWrite or request.json()", consentInsertion: { symbol: "PATCH", precedingStage: "requireForumUser verifies the bearer, derives auth.user.id, reads the verified profile, and constructs the bearer-bound RLS client.", followingStage: "assertUserCanWrite reads the verified actor safety state before body parsing, target lookup, ownership checks, moderation, or update.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireForumUser and pass only auth.user.id before the safety-state check." }, currentExecutionOrder: ["runtime-validation", "authenticate", "derive-verified-user", "construct-bearer-rls-client", "profile-read", "safety-state-read", "parse-json", "id-or-slug-presence-validation", "target-circle-read", "owner-or-staff-authorization", "field-validation", "name-duplicate-read", "text-moderation-external-read", "cover-signed-url-and-moderation", "circle-update-first-domain-write", "response"], evidence: [{ sourceFile: "src/lib/server/circle-management.ts", symbol: "requireForumUser", evidenceType: "authentication", conciseFinding: "Authentication, verified actor derivation, profile read, and bearer-bound RLS client construction complete before PATCH parses JSON or reads the target circle." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "authorization", conciseFinding: "Banned, suspended, and safety-check-failure actors are stopped before payload parsing, target lookup, ownership checks, moderation, or mutation." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "PATCH", evidenceType: "validation", conciseFinding: "Requires JSON plus an id or slug, allows only name/description/image_path changes, bounds name/description/image path, and rejects an empty update; it has no UUID format, content-type, body-size, or rate-limit validation." }, { sourceFile: "src/lib/server/circle-management.ts", symbol: "isCircleManager", evidenceType: "ownership-authorization", conciseFinding: "After reading the requested circle through the verified RLS client, permits only its owner or a profile role of moderator/admin; no request field supplies owner, acting user, or role." }, { sourceFile: "supabase/migrations/20260603_forum_circle_owner_management.sql", symbol: "circles_update_owner_or_staff", evidenceType: "rls", conciseFinding: "RLS independently limits update using and with-check to owner_id = auth.uid or moderator/admin, matching the route's owner-or-staff decision." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "findDuplicateCircle", evidenceType: "duplicate-guard", conciseFinding: "A name change checks lowercased duplicates excluding the current circle before text moderation and update; the unique index still resolves concurrent conflicts to a 409 response." }, { sourceFile: "src/lib/moderation/moderate-content.server.ts", symbol: "moderateContent", evidenceType: "moderation", conciseFinding: "Changed name or description is locally lexicon-checked and may be provider-classified before update; deny/review returns before the circle write, while degraded local allow is only logged." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "moderateCircleCoverImage", evidenceType: "external-read", conciseFinding: "A changed cover may require signed-url/storage reads and provider moderation before update; rejected managed input is removed from storage before the route returns, so storage cleanup is a separate pre-update side effect." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "PATCH", evidenceType: "database-write", conciseFinding: "auth.client.from(circles).update(updates).eq(id) is the first domain write after verified ownership/staff authorization and all requested validation/moderation; no status, type, slug, owner, membership, notification, audit, email, RPC, cache, R2, service-role, or second table write exists." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "PATCH", evidenceType: "partial-failure-and-idempotency", conciseFinding: "The single circle update has no companion membership write or transaction; concurrent duplicate-name requests rely on the unique index, and retries have no request idempotency key. Rejected cover cleanup can succeed independently before the database update is skipped." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "PATCH", evidenceType: "error-handling", conciseFinding: "Authorization and not-found paths use fixed route codes, but query/update and generic catches return raw error.message details; error sanitization and missing content-type/body-size/rate/idempotency controls remain Phase 4B hardening." }, { sourceFile: "src/pages/api/forum/circles.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET, POST, and PATCH." }] },
};
export const batch3jTraces = {
  "src/pages/api/forum/circles/[slug]/comments.ts#GET": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", currentExecutionOrder: ["runtime-validation", "slug-normalization-and-presence-validation", "authenticate", "derive-verified-user", "construct-bearer-rls-client", "profile-read", "managed-circle-read-and-owner-or-staff-authorization", "circle-post-read", "comment-read", "author-profile-read", "response"], evidence: [{ sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Trims and lowercases params.slug, rejects only an empty slug, then requires a managed circle before selecting its posts, their comments ordered created_at descending, and only the resulting comment authors." }, { sourceFile: "src/lib/server/circle-management.ts", symbol: "requireManagedCircleBySlug", evidenceType: "authentication-and-visibility", conciseFinding: "First requires a verified bearer through requireForumUser, then uses its bearer-bound RLS client to read the exact slug and permits only the circle owner or moderator/admin profile role; it does not construct a service-role client or accept a request actor field." }, { sourceFile: "src/lib/server/circle-management.ts", symbol: "requireManagedCircleBySlug", evidenceType: "deleted-circle-handling", conciseFinding: "The managed-circle lookup does not reject status deleted, so an owner or staff member may continue to inspect/manage comments for a deleted circle; this route has no private/member visibility model and no public access path." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "GET", evidenceType: "query-validation", conciseFinding: "GET has no query filters, cursor, limit, pagination, or caller-controlled sort; it reads every RLS-visible post in the managed circle, then every matching comment, ordered newest first." }, { sourceFile: "supabase/migrations/20260603_forum_circle_owner_management.sql", symbol: "comments_select_public_or_staff", evidenceType: "rls", conciseFinding: "The RLS policy permits published comments publicly and permits circle managers through can_manage_post; the route is stricter because requireManagedCircleBySlug already limits this management view to owner or staff." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "Only select, map, and response work occurs: no view count, read marker, comment count, notification, audit, analytics, cache persistence, RPC, email, R2/storage, moderation-provider, or other external effect is called." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "GET", evidenceType: "error-handling", conciseFinding: "Post, comment, profile, and generic caught Error.message values are returned in 500 responses; raw error sanitization, slug-format validation, and unbounded management-list pagination are Phase 4B hardening findings." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET, PATCH, and DELETE." }] },
  "src/pages/api/forum/circles/[slug]/comments.ts#PATCH": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "PATCH after requireManagedCircleBySlug({ request, env, slug }) and before request.json()", consentInsertion: { symbol: "PATCH", precedingStage: "requireManagedCircleBySlug verifies the bearer, derives auth.user.id, reads the verified profile and target circle through the bearer-bound RLS client, and proves owner-or-staff management authorization.", followingStage: "request.json() parses the target comment id and requested status before comment lookup, cross-circle validation, author/staff authorization, or the status update.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireManagedCircleBySlug and pass only auth.user.id before request.json()." }, currentExecutionOrder: ["runtime-validation", "slug-normalization-and-presence-validation", "authenticate", "derive-verified-user", "construct-bearer-rls-client", "profile-and-managed-circle-read", "owner-or-staff-authorization", "parse-json", "comment-id-and-status-validation", "comment-read", "comment-post-circle-membership-read", "author-or-staff-authorization", "already-status-idempotency-check", "single-comment-status-update-first-irreversible-effect", "response"], evidence: [{ sourceFile: "src/lib/server/circle-management.ts", symbol: "requireManagedCircleBySlug", evidenceType: "authentication", conciseFinding: "Bearer authentication and verified actor/profile derivation occur before PATCH parses the body; isCircleManager limits the route entry to the target circle owner or moderator/admin." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "PATCH", evidenceType: "validation", conciseFinding: "Parses JSON with a null fallback, requires a UUID comment id, and allowlists only published or deleted status; it has no content-type, body-size, rate-limit, idempotency-key, or comment-body/parent field." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "PATCH", evidenceType: "resource-authorization", conciseFinding: "Reads the comment, then reads its post constrained by both existingComment.post_id and auth.circle.id before author/staff authorization; a comment from another circle returns 404 and cannot reach update." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "PATCH", evidenceType: "actor-binding", conciseFinding: "For non-staff callers, the route requires existingComment.author_id === auth.user.id and applies a second update filter on author_id; body data cannot provide author, circle, post, parent, or role identity." }, { sourceFile: "supabase/migrations/20260603_forum_circle_owner_management.sql", symbol: "comments_update_self_or_staff", evidenceType: "rls", conciseFinding: "RLS independently allows the comment author, a manager of the comment post's circle, or moderator/admin; the route is narrower for circle owners because it grants cross-author status changes only to staff." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "PATCH", evidenceType: "idempotency", conciseFinding: "A requested status equal to the existing status returns unchanged before update; different repeated toggles have no request idempotency key and can change status again." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "PATCH", evidenceType: "first-irreversible-effect", conciseFinding: "The single comments status/updated_at update is the first persistent effect after all authentication, circle, cross-circle, and author/staff checks; there is no rate-attempt, insert, count update, notification, audit, email, RPC, cache, R2, or external call." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "PATCH", evidenceType: "phase4b-finding", conciseFinding: "The route calls neither assertUserCanWrite nor moderation: a verified author can change an owned deleted comment back to published, including while a safety restriction exists, subject to RLS; safety enforcement and restore-specific moderation policy are Phase 4B hardening concerns." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "PATCH", evidenceType: "error-handling", conciseFinding: "Query/update and generic catch branches expose raw error.message; there is only one domain write, so no multi-write partial-failure or notification/count transaction exists in this method." }] },
  "src/pages/api/forum/circles/[slug]/comments.ts#DELETE": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "DELETE after requireManagedCircleBySlug({ request, env, slug }) and before URL query parsing", consentInsertion: { symbol: "DELETE", precedingStage: "requireManagedCircleBySlug verifies the bearer, derives auth.user.id, reads the verified profile and target circle through the bearer-bound RLS client, and proves owner-or-staff management authorization.", followingStage: "new URL(request.url) reads the id query parameter before comment lookup, cross-circle validation, author/staff authorization, or the soft-delete update.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireManagedCircleBySlug and pass only auth.user.id before URL query parsing." }, currentExecutionOrder: ["runtime-validation", "slug-normalization-and-presence-validation", "authenticate", "derive-verified-user", "construct-bearer-rls-client", "profile-and-managed-circle-read", "owner-or-staff-authorization", "query-id-validation", "comment-read", "comment-post-circle-membership-read", "author-or-staff-authorization", "already-deleted-idempotency-check", "single-comment-soft-delete-first-irreversible-effect", "response"], evidence: [{ sourceFile: "src/lib/server/circle-management.ts", symbol: "requireManagedCircleBySlug", evidenceType: "authentication", conciseFinding: "Verified bearer authentication, actor/profile derivation, bearer-bound RLS construction, and owner-or-staff circle authorization precede DELETE query parsing and every database effect." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "DELETE", evidenceType: "validation", conciseFinding: "Trims and lowercases the route slug, rejects empty slug, reads only URL query id, and requires that id to be UUID-shaped; it has no body, content-type, body-size, rate-limit, or idempotency-key input." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "DELETE", evidenceType: "resource-authorization", conciseFinding: "After loading the comment, verifies its post belongs to auth.circle.id before permitting only the verified author or staff; cross-circle comment ids return 404 before the update." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "DELETE", evidenceType: "actor-binding", conciseFinding: "The actor comes only from auth.user.id; non-staff updates include a second author_id equality predicate, and no query/body field can supply author, circle, post, parent, or role identity." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "DELETE", evidenceType: "idempotency", conciseFinding: "A status already equal to deleted returns already_deleted without a write, making repeat deletes no-op successes; concurrent requests have no key or transaction but the final state remains deleted." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "DELETE", evidenceType: "first-irreversible-effect", conciseFinding: "The single comments status/updated_at soft-delete is the first and only persistent domain effect after authorization; no count, notification, audit, email, RPC, cache, R2/storage, or provider effect is invoked." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "DELETE", evidenceType: "absence-confirmed", conciseFinding: "This source has no comment-create path, so it cannot accept parent_id, create a reply, insert a comment, persist rate attempts, trigger the insert-only comment notification path, or update comment/reply counts; those behaviors belong to the separate forum/comments endpoint." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "DELETE", evidenceType: "phase4b-finding", conciseFinding: "DELETE does not call assertUserCanWrite, but it only removes content; raw database/caught error exposure and missing rate/request-size controls remain Phase 4B hardening with no multi-write partial-failure path." }, { sourceFile: "src/pages/api/forum/circles/[slug]/comments.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET, PATCH, and DELETE." }] },
};
export const batch4bTraces = {
  "src/pages/api/forum/circles/[slug]/manage.ts#GET": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", currentExecutionOrder: ["runtime-validation", "slug-normalization-and-presence-validation", "authenticate", "derive-verified-user", "construct-bearer-rls-client", "profile-and-managed-circle-read", "owner-or-staff-authorization", "circle-post-count-read", "circle-post-id-read", "circle-comment-count-read", "cover-signed-url-read", "response"], evidence: [{ sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Checks runtime environment and a non-empty lowercased slug, then awaits requireManagedCircleBySlug before counting only posts in auth.circle.id, counting their comments, resolving the circle cover URL, and returning the verified viewer." }, { sourceFile: "src/lib/server/circle-management.ts", symbol: "requireManagedCircleBySlug", evidenceType: "authentication-and-authorization", conciseFinding: "Requires a Bearer token, verifies it with auth.getUser, derives auth.user.id, creates an anon-key client bound to that token, reads the verified profile and exact circle slug, then permits only the circle owner or moderator/admin; no request field supplies the actor, owner, role, or circle identity." }, { sourceFile: "src/lib/server/circle-management.ts", symbol: "isCircleManager", evidenceType: "ownership-and-membership", conciseFinding: "Management is owner equality or moderator/admin role only. This route has no member, invite, membership-role, transfer-owner, or cross-circle membership operation, so it cannot create an ownerless circle or escalate membership privileges." }, { sourceFile: "supabase/migrations/20260603_forum_circle_owner_management.sql", symbol: "circles_select_and_update_policies", evidenceType: "rls", conciseFinding: "The bearer-bound client remains subject to circles RLS policies whose owner path compares owner_id to auth.uid and whose staff path checks moderator/admin role; GET uses no privileged or service-role client." }, { sourceFile: "src/lib/circle-cover.ts", symbol: "resolveCircleCoverUrl", evidenceType: "storage-read", conciseFinding: "For a managed circle-covers path, requests a ten-minute post-media signed URL and otherwise returns null; this is not a persistent storage write, cache refresh, audit event, or external moderation call." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "GET has no query filters, pagination, mutation, safety-state write, notification, email, audit, telemetry, RPC, cache persistence, R2 write, or provider call. It also does not reject status deleted, so an authorized owner or staff member can read management data for a deleted circle." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "GET", evidenceType: "error-handling", conciseFinding: "Database and generic caught Error.message values are returned in error details; raw error sanitization, slug-format validation, and unbounded post-id counting are Phase 4B hardening findings." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET, PATCH, and DELETE." }] },
  "src/pages/api/forum/circles/[slug]/manage.ts#PATCH": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "PATCH after requireManagedCircleBySlug({ request, env, slug }) and before assertUserCanWrite/authenticated payload parsing", consentInsertion: { symbol: "PATCH", precedingStage: "requireManagedCircleBySlug verifies the bearer, derives auth.user.id, constructs the bearer-bound RLS client, reads the profile and target circle, and proves owner-or-staff management authorization.", followingStage: "assertUserCanWrite checks the verified actor's safety state before request.json parses the allowlisted update fields, duplicate lookup, moderation, or circle update.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireManagedCircleBySlug and pass only auth.user.id before assertUserCanWrite and request.json()." }, currentExecutionOrder: ["runtime-validation", "slug-normalization-and-presence-validation", "authenticate", "derive-verified-user", "construct-bearer-rls-client", "profile-and-managed-circle-read", "owner-or-staff-authorization", "safety-write-authorization", "parse-json", "allowlisted-field-validation", "name-duplicate-read", "text-moderation", "cover-url-sign-and-asset-moderation", "rejected-cover-storage-cleanup-when-needed", "single-circle-update-first-domain-effect", "cover-signed-url-read", "response"], evidence: [{ sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "PATCH", evidenceType: "authentication-and-authorization", conciseFinding: "Managed-circle authentication, verified actor derivation, owner-or-staff authorization, and assertUserCanWrite all complete before body parsing, duplicate reads, moderation, storage cleanup, or the circles update." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "PATCH", evidenceType: "validation", conciseFinding: "Malformed JSON is rejected; only name, description, image_path, and status are accepted. Name is 2-40 characters, description is at most 200, image path at most 500, status is active or deleted, and an empty update is rejected. The route has no content-type, body-size, rate-limit, or idempotency-key control." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "PATCH", evidenceType: "actor-binding", conciseFinding: "Payload fields cannot replace auth.user.id, auth.profile.role, auth.circle.id, auth.circle.owner_id, slug, membership, or owner identity. The update predicate fixes the target to auth.circle.id after management authorization." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "PATCH", evidenceType: "duplicate-handling", conciseFinding: "A case-insensitive name lookup excludes auth.circle.id before moderation; the lower-name unique constraint error is mapped to 409, but concurrent requests have no request idempotency key or transaction." }, { sourceFile: "src/lib/moderation/moderate-content.server.ts", symbol: "moderateContent", evidenceType: "external-service", conciseFinding: "Text moderation runs only after verified authorization and validation and before the circle update; local checks and configured provider moderation do not persist a database record in this helper chain." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "moderateCircleCoverImage", evidenceType: "external-and-storage-effect", conciseFinding: "A configured cover check may create signed URLs, fetch image bytes, and call the moderation provider after authorization. On rejection, removeStoragePathIfAllowed can remove only the proposed managed circle-covers path before returning; this is a separate pre-domain storage cleanup, not a circle mutation." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "PATCH", evidenceType: "first-irreversible-effect", conciseFinding: "After the authorized and allowed moderation path, circles.update limited to auth.circle.id is the first persistent domain effect. There is one normal database write and no notification, email, audit, cache, telemetry, RPC, or follow-up persistent effect, so no multi-write database transaction is required." }, { sourceFile: "supabase/migrations/20260604_forum_circle_soft_delete_and_management.sql", symbol: "circles_status", evidenceType: "phase4b-finding", conciseFinding: "Because requireManagedCircleBySlug permits status deleted and PATCH accepts status active, an owner or staff member can reactivate a deleted circle through the same RLS-authorized update. The source has no distinct moderation-deleted state or action history; restore policy separation is Phase 4B hardening, not a role or ownership escalation." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "PATCH", evidenceType: "error-handling", conciseFinding: "Status-schema fallback can retry the single update only when status was not requested. Query and generic caught errors expose raw message details; no hidden cache, audit, notification, email, R2, or provider mutation follows the successful update." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET, PATCH, and DELETE." }] },
  "src/pages/api/forum/circles/[slug]/manage.ts#DELETE": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "DELETE after requireManagedCircleBySlug({ request, env, slug }) and before assertUserCanWrite", consentInsertion: { symbol: "DELETE", precedingStage: "requireManagedCircleBySlug verifies the bearer, derives auth.user.id, constructs the bearer-bound RLS client, reads the profile and target circle, and proves owner-or-staff management authorization.", followingStage: "assertUserCanWrite checks the verified actor's safety state before the single soft-delete status update.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireManagedCircleBySlug and pass only auth.user.id before assertUserCanWrite." }, currentExecutionOrder: ["runtime-validation", "slug-normalization-and-presence-validation", "authenticate", "derive-verified-user", "construct-bearer-rls-client", "profile-and-managed-circle-read", "owner-or-staff-authorization", "safety-write-authorization", "single-circle-soft-delete-first-irreversible-effect", "response"], evidence: [{ sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "DELETE", evidenceType: "authentication-and-authorization", conciseFinding: "Managed-circle authorization derives the actor only from verified bearer auth, limits access to the circle owner or moderator/admin, then assertUserCanWrite completes before the status update. No client value can target another circle, replace ownership, grant membership, or alter a role." }, { sourceFile: "supabase/migrations/20260603_forum_circle_owner_management.sql", symbol: "circles_update_owner_or_staff", evidenceType: "rls", conciseFinding: "The route uses the verified bearer's anon-key client and circles.update by auth.circle.id. RLS independently permits the owner or moderator/admin and provides no service-role bypass." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "DELETE", evidenceType: "first-irreversible-effect", conciseFinding: "The one circles update setting status deleted is the first and only persistent domain effect after authentication, ownership/staff authorization, and safety authorization. It is a soft delete; no hard delete, membership removal, post/comment update, notification, email, audit, cache, telemetry, RPC, R2, or external effect occurs." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "DELETE", evidenceType: "idempotency", conciseFinding: "DELETE does not inspect existing status or use an idempotency key, so repeating it can issue another deleted status update and refresh updated_at; no duplicate action row or multi-write partial-failure path exists." }, { sourceFile: "src/lib/server/circle-management.ts", symbol: "requireManagedCircleBySlug", evidenceType: "deleted-circle-handling", conciseFinding: "The helper does not reject a deleted circle, so an authorized owner or staff member can repeat DELETE or subsequently reach PATCH. This remains a Phase 4B lifecycle-policy finding without a client-controlled actor or privilege bypass." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "DELETE", evidenceType: "error-handling", conciseFinding: "Missing status schema and RLS-like errors have dedicated responses, but remaining database and generic caught errors return raw message details; rate, content-size, and request-id hardening remain Phase 4B." }, { sourceFile: "src/pages/api/forum/circles/[slug]/manage.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET, PATCH, and DELETE." }] },
};
export const batch4cTraces = {
  "src/pages/api/forum/circles/[slug]/posts.ts#GET": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", currentExecutionOrder: ["runtime-validation", "slug-normalization-and-presence-validation", "authenticate", "derive-verified-user", "construct-bearer-rls-client", "profile-and-managed-circle-read", "owner-or-staff-circle-authorization", "circle-post-read-sorted-newest", "parallel-author-profile-media-and-report-read", "in-memory-count-and-author-mapping", "response"], evidence: [{ sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Checks runtime environment and a non-empty lowercased slug, requires a managed circle, reads posts constrained to auth.circle.id ordered newest first, then reads only their authors, post_media rows, and post reports before mapping the response." }, { sourceFile: "src/lib/server/circle-management.ts", symbol: "requireManagedCircleBySlug", evidenceType: "authentication-and-circle-authorization", conciseFinding: "Requires Bearer authentication, verifies it through auth.getUser, derives auth.user.id, creates an anon-key client bound to that bearer, reads the verified profile and exact circle, and permits only its owner or moderator/admin. The route constructs no anon public client, service-role client, or request-controlled actor." }, { sourceFile: "src/lib/server/circle-management.ts", symbol: "isCircleManager", evidenceType: "visibility-and-membership", conciseFinding: "This management list is authenticated owner-or-staff only, not a public list. The circles schema has type and soft-delete status but no private visibility or membership relation, and this route has no member, invite, or membership-role input or operation." }, { sourceFile: "supabase/migrations/20260611_forum_permission_lockdown.sql", symbol: "posts_select_published_public", evidenceType: "rls", conciseFinding: "The bearer-bound RLS client can select published posts, the verified actor's own posts, or staff-visible posts; a non-staff circle owner does not gain hidden or deleted posts solely by owning the circle. Report and post-media reads remain independently RLS-filtered." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "GET", evidenceType: "query-validation", conciseFinding: "GET accepts no query filter, cursor, limit, offset, pagination, or caller-selected sort; it always reads all RLS-visible posts in the resolved circle ordered created_at descending, so unbounded management-list pagination is Phase 4B hardening." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "Only select, count, map, and response work occurs. There is no view count, last-viewed marker, audit, analytics, cache persistence, notification, search-index, RPC mutation, R2/storage write or delete, media cleanup, email, moderation provider, or other external effect." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "GET", evidenceType: "error-handling", conciseFinding: "Post, profile, media, report, and generic caught Error.message values are returned directly. Raw dependency details and weak slug-only validation remain Phase 4B hardening findings." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET, PATCH, and DELETE." }] },
  "src/pages/api/forum/circles/[slug]/posts.ts#PATCH": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "PATCH after requireManagedCircleBySlug({ request, env, slug }) and before isModeratorRole/request.json", consentInsertion: { symbol: "PATCH", precedingStage: "requireManagedCircleBySlug verifies the bearer, derives auth.user.id, constructs the bearer-bound RLS client, reads the verified profile and exact circle, and proves owner-or-staff circle management authorization.", followingStage: "isModeratorRole derives staff status from the verified profile, then request.json parses the post id and status before post lookup, post-author authorization, or update.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireManagedCircleBySlug and pass only auth.user.id before deriving isStaff and parsing request.json()." }, currentExecutionOrder: ["runtime-validation", "slug-normalization-and-presence-validation", "authenticate", "derive-verified-user", "construct-bearer-rls-client", "profile-and-managed-circle-read", "owner-or-staff-circle-authorization", "parse-json", "post-id-and-status-validation", "post-read", "post-circle-relationship-check", "author-or-staff-authorization", "unchanged-status-idempotency-check", "single-post-status-update-first-irreversible-effect", "response"], evidence: [{ sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "PATCH", evidenceType: "authentication-and-actor-binding", conciseFinding: "Verified bearer authentication and resolved-circle owner-or-staff authorization precede body parsing. The actor is auth.user.id and staff status comes only from auth.profile.role; payload fields supply only id and status and cannot replace actor, author, circle, owner, moderator, or administrator identity." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "PATCH", evidenceType: "validation", conciseFinding: "Malformed JSON becomes an invalid empty payload; id must match the UUID expression and status is limited to published, hidden, or deleted. There is no content-type, body-size, rate-limit, idempotency-key, post title/body/type/media input, or pagination control." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "PATCH", evidenceType: "resource-authorization", conciseFinding: "Reads the selected post through RLS and rejects it unless existingPost.circle_id equals auth.circle.id. A non-staff caller must also equal existingPost.author_id, and its update adds a second author_id predicate; staff may update any RLS-visible target in that resolved circle." }, { sourceFile: "supabase/migrations/20260611_forum_permission_lockdown.sql", symbol: "posts_update_self_or_staff", evidenceType: "rls", conciseFinding: "RLS independently permits only the post author or moderator/admin. Although route validation accepts hidden, the final RLS with-check allows an ordinary author only pending, published, or deleted status, while staff may set hidden." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "PATCH", evidenceType: "idempotency", conciseFinding: "An existing status equal to the requested status returns unchanged before update. Different repeated status changes have no request idempotency key or transaction, but this method has a single domain write and no downstream write to duplicate." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "PATCH", evidenceType: "first-irreversible-effect", conciseFinding: "After all route authorization and the unchanged check, one RLS-scoped posts.update fixed to post id and auth.circle.id is the first and only persistent effect. The route performs no post creation, rate-attempt insertion, title/body/media validation, media attachment or cleanup, notification, audit, email, search-index, cache, RPC, R2, or external provider action." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "PATCH", evidenceType: "phase4b-finding", conciseFinding: "PATCH does not call assertUserCanWrite or content moderation. A verified author can restore an owned deleted post to published without a safety gate or fresh moderation; the helper also permits management of a deleted circle. These are lifecycle and safety hardening gaps, not a client-controlled actor, private-circle bypass, or cross-circle mutation." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "PATCH", evidenceType: "error-handling", conciseFinding: "Post read, update, and generic caught Error.message values are returned directly; raw error exposure remains Phase 4B hardening. There is no companion write, so multi-write partial-failure ordering does not arise." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET, PATCH, and DELETE." }] },
  "src/pages/api/forum/circles/[slug]/posts.ts#DELETE": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "DELETE after requireManagedCircleBySlug({ request, env, slug }) and before isModeratorRole/URL query parsing", consentInsertion: { symbol: "DELETE", precedingStage: "requireManagedCircleBySlug verifies the bearer, derives auth.user.id, constructs the bearer-bound RLS client, reads the verified profile and exact circle, and proves owner-or-staff circle management authorization.", followingStage: "isModeratorRole derives staff status from the verified profile, then new URL(request.url) reads the id before post lookup, post-author authorization, or soft deletion.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireManagedCircleBySlug and pass only auth.user.id before deriving isStaff and parsing the URL query." }, currentExecutionOrder: ["runtime-validation", "slug-normalization-and-presence-validation", "authenticate", "derive-verified-user", "construct-bearer-rls-client", "profile-and-managed-circle-read", "owner-or-staff-circle-authorization", "query-post-id-validation", "post-read", "post-circle-relationship-check", "author-or-staff-authorization", "already-deleted-idempotency-check", "single-post-soft-delete-first-irreversible-effect", "response"], evidence: [{ sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "DELETE", evidenceType: "authentication-and-actor-binding", conciseFinding: "The route authenticates and resolves the managed circle before reading the id query parameter. Actor id and staff status originate only from verified auth/profile data; the URL controls only the UUID-shaped target id." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "DELETE", evidenceType: "validation-and-resource-authorization", conciseFinding: "Requires UUID-shaped id, reads the post through RLS, rejects a target outside auth.circle.id, then permits only the verified author or staff. Non-staff updates add author_id = auth.user.id, preventing cross-author and cross-circle deletion." }, { sourceFile: "supabase/migrations/20260611_forum_permission_lockdown.sql", symbol: "posts_update_self_or_staff", evidenceType: "rls", conciseFinding: "The bearer-bound RLS policy independently restricts the status update to the author or moderator/admin; no privileged or service-role client is constructed or used." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "DELETE", evidenceType: "idempotency", conciseFinding: "A status already deleted returns already_deleted without a write, making sequential retry a no-op success. Concurrent requests have no idempotency key or lock, but successful paths converge on deleted and create no action record or downstream effect." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "DELETE", evidenceType: "first-irreversible-effect", conciseFinding: "One posts.update setting status deleted and constrained to the resolved circle is the first and only persistent effect after authentication, circle relationship, and author-or-staff authorization. It does not hard-delete the post or delete media, comments, reports, storage objects, or indexes." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "DELETE", evidenceType: "absence-confirmed", conciseFinding: "DELETE has no user-safety check, moderation, rate-attempt persistence, notification, audit, email, cache, RPC, R2/storage, search-index, or external side effect; there is therefore no multi-write transaction or later media/notification failure path." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "DELETE", evidenceType: "phase4b-finding", conciseFinding: "A suspended or banned author is not checked through assertUserCanWrite before deletion, and the managed-circle helper allows deleted circles. Safety enforcement, narrow lifecycle policy, raw error sanitization, and request-size/rate controls remain Phase 4B hardening." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "DELETE", evidenceType: "error-handling", conciseFinding: "Post read, update, and generic caught Error.message values are returned directly; no hidden helper ordering remains after the single update path." }, { sourceFile: "src/pages/api/forum/circles/[slug]/posts.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET, PATCH, and DELETE." }] },
};
export const batch4dTraces = {
  "src/pages/api/forum/comments.ts#PUT": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    remediationCommit: "d57aae680fb81ed4133af73aae955473218d9c09",
    migrationExecutionStatus: "authored-not-executed",
    consentInsertionPoint: "PUT after auth.getUser(token) verification and before request.json()",
    consentInsertion: { symbol: "PUT", precedingStage: "createUserClient binds the supplied bearer and auth.getUser(token) verifies it, deriving authData.user.id.", followingStage: "request.json() parses the untrusted payload before comment_id validation, target reads, reaction lookup, or mutation.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after the authData.user check and pass only authData.user.id before request.json()." },
    currentExecutionOrder: ["runtime-validation", "bearer-presence-validation", "construct-bearer-rls-client", "authenticate", "derive-verified-user", "parse-json", "comment-id-validation", "published-comment-read", "published-parent-post-read", "active-visible-parent-circle-read", "reaction-target-authorization", "existing-own-like-read", "delete-own-reaction-or-insert-verified-actor-like-first-irreversible-effect", "reaction-count-read", "response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "PUT", evidenceType: "handler-and-validation", conciseFinding: "PUT checks runtime env and bearer presence, verifies auth.getUser(token), parses JSON with a null fallback, and accepts only a UUID-shaped payload.comment_id. No payload reaction type, user_id, actor_id, post_id, or circle_id is read." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "createUserClient", evidenceType: "client-construction", conciseFinding: "Constructs an anon-key Supabase client with the verified request bearer in its Authorization header; there is no service-role or other privileged client." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "resolveAccessibleCommentReactionTarget", evidenceType: "resource-authorization", conciseFinding: "Reads comment id/post_id/status/moderation_status, then the exact post id/circle_id/status/moderation_status, then the exact circle id/slug/name/status. Null, mismatched, unpublished, hidden, deleted, inactive, or noncanonical-visible links fail closed before comment_reactions is touched." },
      { sourceFile: "src/lib/site-navigation.ts", symbol: "isPublicVisibleCircle", evidenceType: "visibility-and-membership", conciseFinding: "Canonical public visibility rejects deleted and configured hidden QA circles; PUT also requires active status. No private-circle or membership schema exists, so membership enforcement is not applicable and no private route branch is omitted." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "PUT", evidenceType: "reaction-branches", conciseFinding: "After the full target check, the existing reaction lookup is constrained by comment_id, verified authData.user.id, and fixed reaction_type like. Existing rows take DELETE by returned reaction id; absent rows take INSERT with that verified user id. There is no runtime UPDATE or UPSERT branch." },
      { sourceFile: "supabase/migrations/20260713_comment_reaction_visibility_authorization.sql", symbol: "comment_reactions_select_accessible", evidenceType: "rls-select", conciseFinding: "SELECT uses can_access_comment_reaction_target(comment_id), so reactions are readable only for the same accessible comment/post/circle chain." },
      { sourceFile: "supabase/migrations/20260713_comment_reaction_visibility_authorization.sql", symbol: "comment_reactions_insert_self", evidenceType: "rls-insert", conciseFinding: "INSERT WITH CHECK requires user_id = auth.uid() and can_access_comment_reaction_target(comment_id), which joins the published comment, published post, and active visible circle." },
      { sourceFile: "supabase/migrations/20260713_comment_reaction_visibility_authorization.sql", symbol: "comment_reactions_update_self", evidenceType: "rls-update", conciseFinding: "UPDATE applies user_id = auth.uid() and the full target predicate in both USING and WITH CHECK, covering direct update and UPSERT conflict-update behavior." },
      { sourceFile: "supabase/migrations/20260713_comment_reaction_visibility_authorization.sql", symbol: "comment_reactions_delete_self", evidenceType: "rls-delete", conciseFinding: "DELETE USING requires both reaction ownership and accessible ancestors, so a stale reaction cannot be removed through this policy after its content becomes inaccessible." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "PUT", evidenceType: "first-effect-and-partial-failure", conciseFinding: "The first irreversible effect is either comment_reactions.delete for an existing own like or comment_reactions.insert for an absent like, after all authorization. Reaction count reads occur afterward and their errors are not checked; there is no audit, notification, email, cache, RPC, R2, or external effect." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "PUT", evidenceType: "idempotency-and-hardening", conciseFinding: "This is a toggle without an idempotency key, transaction, content-type/body-size limit, or rate limiter. Sequential retries alternate state; concurrent requests can race with the unique reaction constraint. Several database and caught Error.message values are returned raw. These remain non-blocking Phase 4B findings." },
      { sourceFile: "scripts/test-comment-reaction-visibility-authorization.mjs", symbol: "comment-reaction-visibility-authorization", evidenceType: "regression-test", conciseFinding: "Deterministic offline cases prove denied target chains make zero reaction mutation calls, static source ordering puts target resolution before reaction queries, migration policies cover ancestors for SELECT/INSERT/UPDATE/DELETE, and the historical migration hash is unchanged." },
      { sourceFile: "supabase/migrations/20260713_comment_reaction_visibility_authorization.sql", symbol: "migration-file", evidenceType: "deployment-prerequisite", conciseFinding: "The forward migration is authored but was not executed during this re-audit. Applying it remains a deployment prerequisite rather than a source-level authorization blocker." }
    ]
  }
};
export const batch4eTraces = {
  "src/pages/api/forum/comments.ts#POST": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    remediationCommit: "485215b2c311a0347258c5618db4db5326f84a58",
    forwardMigration: "supabase/migrations/20260713_comment_creation_circle_authorization.sql",
    migrationExecutionStatus: "authored-not-executed",
    consentInsertionPoint: "POST after auth.getUser(token) verification and before assertUserCanWrite",
    consentInsertion: { symbol: "POST", precedingStage: "createUserClient binds the supplied bearer and auth.getUser(token) verifies it, deriving authData.user.id.", followingStage: "assertUserCanWrite reads only that verified actor safety state before payload parsing, profile lookup, target authorization, moderation, rate persistence, or comment insertion.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after the authData.user check and pass only authData.user.id before assertUserCanWrite." },
    currentExecutionOrder: ["runtime-validation", "bearer-presence-validation", "construct-bearer-rls-client", "authenticate", "derive-verified-user", "user-safety-state-read", "parse-json", "post-parent-and-body-validation", "profile-read", "server-derived-post-read", "active-canonical-visible-circle-read", "same-post-published-parent-read", "target-authorization", "content-moderation", "rate-limit-attempt-write-first-persistent-effect", "comment-insert-first-domain-mutation", "database-notification-trigger-after-successful-published-insert", "response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "POST", evidenceType: "handler-and-actor", conciseFinding: "POST checks runtime env and bearer presence, creates a bearer-bound anon-key client, verifies auth.getUser(token), and inserts only authData.user.id. Client author_id, user_id, circle_id, role, status, visibility, and membership fields are never read." },
      { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "safety-authorization", conciseFinding: "The helper reads user_safety_states by verified actor id and fails closed with a 503 decision on lookup error; banned and suspended actors return before every later effect." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "resolveAccessibleCommentCreationTarget", evidenceType: "resource-authorization", conciseFinding: "The helper reads post id/circle_id/status/moderation_status, then exact circle id/slug/name/status, and optional parent id/post_id/status/moderation_status. It rejects malformed, missing, mismatched, unpublished, deleted, moderation-hidden, inactive, or canonical-hidden links before moderation or rate persistence." },
      { sourceFile: "src/lib/site-navigation.ts", symbol: "isPublicVisibleCircle", evidenceType: "visibility-and-membership", conciseFinding: "Canonical public visibility excludes deleted and configured hidden QA circles. The runtime separately requires active status; no private-circle membership table or relation exists in the schema." },
      { sourceFile: "src/lib/moderation/moderate-content.server.ts", symbol: "moderateContent", evidenceType: "moderation-ordering", conciseFinding: "POST calls moderation only after target authorization. A reject returns 403 before rate persistence; review permits only a pending/pending_review comment state." },
      { sourceFile: "src/lib/server/rate-limit.ts", symbol: "enforceUserRateLimit", evidenceType: "first-persistent-effect", conciseFinding: "After authorization and moderation, the helper reads recent attempts and inserts forum_upload_attempts for comment_create. Backend failure returns allowed true, and there is no idempotency key; those are Phase 4B hardening findings." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "POST", evidenceType: "domain-mutation-and-hardening", conciseFinding: "comments.insert is the first domain mutation and uses verified actor plus resolver-derived target ids. Insert failure returns a raw database message; retry behavior has no idempotency key or transaction, and the route has no Content-Type header validation or payload byte-size cap beyond String coercion and a 1-5000 character body check. These remain Phase 4B findings; a later trigger failure can fail the insert atomically while the earlier rate attempt remains persisted." },
      { sourceFile: "supabase/migrations/20260606_forum_notifications_mvp.sql", symbol: "trg_comments_notify_created", evidenceType: "notification-ordering", conciseFinding: "The after-insert trigger invokes notify_comment_created only after a successful comments row exists. Published top-level inserts can notify the post author and published replies can additionally notify the parent author; denied targets cannot reach this trigger." },
      { sourceFile: "supabase/migrations/20260713_comment_creation_circle_authorization.sql", symbol: "comments_insert_self", evidenceType: "rls-alignment", conciseFinding: "The forward policy requires author_id = auth.uid(), published or pending-review state, and can_create_comment_target(post_id, parent_id). Its predicate requires the published post, active canonical-visible circle, and optional same-post published parent; it covers top-level and reply insertion and fails closed on absent ancestors." },
      { sourceFile: "scripts/test-comment-creation-circle-authorization.mjs", symbol: "comment-creation-circle-authorization", evidenceType: "regression-test", conciseFinding: "Offline cases allow accessible top-level and same-post replies and deny deleted/hidden/inactive/missing/mismatched circles, deleted/hidden/unpublished posts, invalid parents, unauthenticated actors, malformed ids, and client actor/circle overrides with zero modeled effects. Static assertions verify runtime order, forward policy terms, and the unchanged historical migration hash." },
      { sourceFile: "supabase/migrations/20260713_comment_creation_circle_authorization.sql", symbol: "migration-file", evidenceType: "deployment-prerequisite", conciseFinding: "The forward migration is authored but was not executed in this re-audit. Deployment application remains required before database-layer enforcement is live; this does not leave a source ordering or authorization mismatch." }
    ]
  }
};
export const batch4fTraces = {
  "src/pages/api/forum/comments.ts#GET": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    remediationCommit: "aa2cf01be3a21eed9d3a0de111bc249c0ead046a",
    forwardMigration: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql",
    migrationExecutionStatus: "authored-not-executed",
    consentInsertionPoint: "not-applicable: GET is read-only",
    consentInsertion: { symbol: "GET", precedingStage: "GET validates and authorizes the read target before selecting response data.", followingStage: "No mutation follows the read pipeline; it maps selected rows into a JSON response.", refactorRequired: false, smallestFutureRefactor: "No legal-consent guard applies because this method performs no consent-required mutation." },
    currentExecutionOrder: ["runtime-validation", "post-id-query-validation", "optional-bearer-token-parsing", "construct-anon-rls-client", "optional-bearer-client-construction-and-verified-viewer-derivation", "server-derived-post-read-first-database-read", "post-publication-and-moderation-validation", "exact-circle-read", "active-canonical-visible-circle-validation", "comment-list-read", "comment-visibility-filtering", "profile-enrichment-read", "reaction-enrichment-read", "conditional-fallback-bearer-reverification-and-profile-role-read", "in-memory-like-and-response-mapping", "response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "handler-and-request-validation", conciseFinding: "GET checks runtime configuration, reads only post_id from the URL, rejects a non-UUID-shaped value, parses an optional bearer, and constructs an anon-key RLS client before any database operation. No body, pagination, sort, cursor, or client-supplied post/circle/actor field is accepted." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "createAnonClient,createUserClient", evidenceType: "client-construction-and-authentication", conciseFinding: "Absent or invalid bearer credentials retain the anon client. A supplied bearer creates a bearer-bound anon-key client and auth.getUser(token) verifies the session; only its verified user id becomes viewerUserId. No service-role or privileged client is constructed." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "resolveAccessibleCommentReadTarget", evidenceType: "resource-authorization", conciseFinding: "The first database operation selects the exact post and server-derived circle_id, requires published post and moderation state, then reads that exact circle and requires active plus isPublicVisibleCircle. Missing, malformed, mismatched, deleted, hidden, unpublished, or noncanonical ancestry returns before comment, profile, and reaction reads." },
      { sourceFile: "src/lib/site-navigation.ts", symbol: "isPublicVisibleCircle", evidenceType: "canonical-visibility", conciseFinding: "The shared helper excludes deleted and configured QA-hidden circles. GET separately requires an active circle status; the current schema has no private-circle membership relation, so there is no membership branch to apply." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "comment-and-enrichment-ordering", conciseFinding: "After target authorization GET selects comments only by resolver-returned post id, filters public published/moderation-published rows, allows only the verified author's own pending-review branch, and emits deleted rows only as body-empty placeholders supporting a published child. It then selects profiles and reactions only for remaining response comment/author ids and computes counts and liked_by_me in memory." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "fallback-auth-boundary", conciseFinding: "If initial optional authentication did not yield viewerUserId, a conditional re-verification after the authorized reaction read may derive a viewer and profile role for response metadata. It neither reads a new target nor authorizes a write, so it cannot bypass the completed post-circle chain or accept a client-controlled identity." },
      { sourceFile: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql", symbol: "can_access_public_circle,posts_select_published_public", evidenceType: "post-rls", conciseFinding: "The authored forward migration replaces the public post SELECT branch with published status and moderation checks plus can_access_public_circle(circle_id), which requires an exact active canonical-visible circle and fails closed through exists." },
      { sourceFile: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql", symbol: "can_access_public_comment_read_target,comments_select_public_or_staff,comment_reactions_select_accessible", evidenceType: "comment-and-reaction-rls", conciseFinding: "The forward predicate joins published/moderation-visible comment, parent post, and active canonical-visible circle. Public comment and reaction SELECT policies require it while authenticated-own and staff branches remain explicit; missing relationships deny through the predicate." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "GET has no write, rate-limit persistence, cache refresh, audit, notification, email, telemetry mutation, R2/storage, external provider, or RPC. There is no duplicate/idempotency or partial-write path, and the first database operation is the post select." },
      { sourceFile: "scripts/test-comment-read-circle-visibility-authorization.mjs", symbol: "comment-read-circle-visibility-authorization", evidenceType: "regression-test", conciseFinding: "Offline tests permit active canonical public ancestry and a verified viewer's liked_by_me, deny inaccessible ancestors before enrichment, verify static runtime and policy ordering, and assert that the historical migration remains unchanged." },
      { sourceFile: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql", symbol: "migration-file", evidenceType: "deployment-prerequisite", conciseFinding: "The migration was authored in remediation commit aa2cf01be3a21eed9d3a0de111bc249c0ead046a and is deliberately unexecuted. Applying it remains a production deployment prerequisite, not an unresolved source trace blocker." }
    ]
  }
};
export const batch5aTraces = {
  "src/pages/api/forum/comments.ts#DELETE": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    consentInsertionPoint: "DELETE after auth.getUser(token) verification and before the verified profile role read",
    consentInsertion: { symbol: "DELETE", precedingStage: "createUserClient binds the supplied bearer and auth.getUser(token) verifies it, deriving authData.user.id.", followingStage: "profiles.select(role) reads the verified actor's role before comment lookup, already-deleted handling, author-or-staff authorization, or the soft-delete update.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after the authData.user check and pass only authData.user.id before the profile role query." },
    currentExecutionOrder: ["runtime-validation", "bearer-presence-validation", "url-query-and-optional-json-parsing", "comment-id-validation", "construct-bearer-rls-client", "authenticate", "derive-verified-user", "verified-profile-role-read", "server-derived-staff-role-check", "comment-read", "already-deleted-idempotency-check", "author-or-staff-authorization", "single-comment-soft-delete-first-irreversible-effect", "response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "DELETE", evidenceType: "handler-and-validation", conciseFinding: "DELETE checks runtime configuration and bearer presence, then reads only URL id or optional JSON comment_id, preferring URL id. The trimmed candidate must be UUID-shaped; no post_id, circle_id, author_id, user_id, role, status, or reaction field is accepted." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "createUserClient", evidenceType: "authentication-and-client-construction", conciseFinding: "After ID validation, DELETE constructs an anon-key Supabase client bound to the bearer and verifies auth.getUser(token). Only authData.user.id becomes the acting user; no service-role or other privileged client is constructed." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "DELETE,isModeratorRole", evidenceType: "server-derived-staff-authorization", conciseFinding: "DELETE reads profiles.role by the verified actor id and isModeratorRole recognizes only moderator or admin. Client data cannot replace author or staff identity; a missing profile returns 403 and a profile lookup error returns before target lookup or mutation." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "DELETE", evidenceType: "target-and-author-authorization", conciseFinding: "The only target read selects id, author_id, status, and post_id by the validated comment id through the bearer-bound RLS client. A non-deleted target is mutable only when its server-read author_id equals authData.user.id or the server-derived role is staff; another ordinary user receives 403 before update." },
      { sourceFile: "supabase/migrations/20260611_forum_permission_lockdown.sql", symbol: "comments_update_self_or_staff", evidenceType: "rls", conciseFinding: "The current source policy independently restricts update USING to author_id = auth.uid() or is_moderator_or_admin(), and ordinary-author WITH CHECK permits only published or deleted status. It contains no post or circle predicate because DELETE accepts no supplied post/circle id and changes only the selected comment row." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "DELETE", evidenceType: "ancestor-and-safety-boundary", conciseFinding: "DELETE does not read the parent post or circle and does not call assertUserCanWrite. The verified author or staff can therefore soft-delete an RLS-visible comment after its post or circle becomes hidden, deleted, or otherwise inaccessible; this is existing cleanup lifecycle behavior, not a cross-post/circle mutation, because no ancestor identifier is accepted or used in the update." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "DELETE", evidenceType: "idempotency", conciseFinding: "A server-read status already equal to deleted returns already_deleted with no write, making sequential retries no-op successes. There is no request idempotency key, lock, or status predicate on the subsequent update, so concurrent requests can both issue equivalent soft-delete updates." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "DELETE", evidenceType: "first-irreversible-effect", conciseFinding: "comments.update({ status: deleted, updated_at }) constrained only by the validated comment id is the first and only persistent effect, after authentication, verified role derivation, target read, already-deleted check, and author-or-staff authorization. It is a soft delete and preserves the row body, replies, reactions, and associated profile/media relationships." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "DELETE", evidenceType: "absence-confirmed", conciseFinding: "The route calls no comment/reply count update, reaction cleanup, notification cleanup or creation, audit, RPC, email, cache, R2/storage, external provider, moderation, or user-safety helper. There is one database write only, so no multi-write transaction or downstream partial-failure chain exists." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "DELETE", evidenceType: "error-handling-and-phase4b", conciseFinding: "Comment/profile/update and caught Error.message values are returned directly. The route lacks content-type, body-size, rate-limit, request-idempotency, ancestor-visibility, and user-safety checks; these are documented Phase 4B lifecycle and hardening findings rather than actor, staff, or cross-resource authorization bypasses." },
      { sourceFile: "src/pages/api/forum/comments.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "ALL returns 405 for methods other than GET, POST, PUT, and DELETE." }
    ]
  }
};
export const batch5bTraces = {
  "src/pages/api/forum/external-video-upload.ts#POST": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    remediationCommit: "30ece7ec89e7de477b3b74d94e36ee51709419d5",
    migrationRequired: false,
    consentInsertionPoint: "POST after auth.getUser(token) verification and before assertUserCanWrite, request.json, post.lookup, or Turnstile",
    consentInsertion: { symbol: "POST", precedingStage: "The bearer-bound anon-key client has completed auth.getUser(token) and derives authData.user.id as the only actor.", followingStage: "assertUserCanWrite checks the verified actor, then POST parses and validates the payload before server-derived target post lookup and ownership authorization.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after the authData.user check, pass only authData.user.id, and leave the existing safety and target-authorization stages in their current order." },
    currentExecutionOrder: ["runtime-environment-validation", "bearer-presence-validation", "construct-bearer-bound-anon-rls-client", "authenticate", "derive-verified-actor", "global-user-safety-authorization", "json-payload-parse", "post-id-file-mime-and-declared-size-validation", "server-derived-target-post-read", "server-derived-post-author-comparison", "conditional-turnstile-validation-first-external-effect", "daily-rate-ip-hash-and-read", "local-r2-put-url-signing", "rate-attempt-read-and-insert-first-persistent-effect", "signed-upload-response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST,getBearerToken,createClient,auth.getUser", evidenceType: "handler-authentication-and-actor-binding", conciseFinding: "POST requires a bearer, constructs only an anon-key Supabase client with that bearer in its Authorization header, verifies auth.getUser(token), and derives the actor exclusively from authData.user.id. Payload fields are limited to post_id, file_name, mime_type, size_bytes, and turnstile_token, so they cannot replace an actor, author, owner, role, staff flag, or post relationship." },
      { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "global-write-safety", conciseFinding: "POST calls the helper with only the verified actor id and external_video_upload action. It reads user_safety_states by that id and returns banned, suspended, or safety-lookup-failure denials before payload parsing or target processing." },
      { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST#payload,POST#post.lookup,POST#post.authorize", evidenceType: "target-resource-authorization", conciseFinding: "After JSON parsing and UUID/file/mime/declared-size validation, post_id is used only to select id and author_id from posts. Missing rows return 404; POST then requires the server-read post.author_id to equal authData.user.id and returns 403 otherwise before any later helper." },
      { sourceFile: "supabase/migrations/20260611_forum_permission_lockdown.sql", symbol: "posts_select_published_public", evidenceType: "bearer-rls-client", conciseFinding: "The deployed-source SELECT policy permits published rows, rows authored by auth.uid(), or server-derived moderator/admin access. It does not grant arbitrary private-post selection; even a selectable public row cannot pass POST's separate server-read equality comparison for another user." },
      { sourceFile: "src/lib/server/turnstile.ts", symbol: "shouldRequireUploadTurnstile,validateTurnstileToken,fetch", evidenceType: "first-external-effect-and-no-ssrf", conciseFinding: "Only after post.authorize can the conditional helper validate Turnstile. validateTurnstileToken uses one fixed Cloudflare siteverify URL, accepts no client URL/host/protocol/port/redirect input, and POST returns only the sanitized TURNSTILE_REQUIRED or TURNSTILE_INVALID code on a failed result before rate persistence." },
      { sourceFile: "src/lib/server/rate-limit.ts", symbol: "hashRateLimitIp,enforceUploadRateLimit", evidenceType: "rate-ordering-and-first-persistent-effect", conciseFinding: "After successful target authorization and, when enabled, Turnstile, POST hashes the request IP, reads daily forum_upload_attempts and post_media totals, signs the upload URL, then enforceUploadRateLimit reads current IP-window attempts and inserts forum_upload_attempts when below the limit. That operational attempt row is the first persistent effect; the route has no domain mutation." },
      { sourceFile: "src/lib/r2-server.ts", symbol: "buildTmpVideoKey,signR2PutUrl,buildR2PublicUrl", evidenceType: "r2-boundary", conciseFinding: "buildTmpVideoKey uses the verified actor id plus crypto.randomUUID and normalized file name. signR2PutUrl constructs an S3 PutObject command and obtains a signed URL; it does not upload an R2 object. POST performs no post_media insert, update, delete, R2 object write, client-selected fetch, notification, count, audit, email, cache, RPC, or telemetry mutation." },
      { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST,enforceUploadRateLimit", evidenceType: "retry-and-partial-failure", conciseFinding: "There is no request idempotency key or transaction. Retries can insert duplicate operational rate-attempt rows, and an attempt row can remain if a later response construction path fails. Rate-limit backend errors fail open through the helper; daily attempt-read errors are treated as zero usage while post_media read errors fail the request. No cleanup is required because the endpoint creates neither an object nor a domain record." },
      { sourceFile: "scripts/test-external-video-authorization-ordering.mjs", symbol: "external-video-authorization-ordering", evidenceType: "fully-offline-ordering-regression", conciseFinding: "Vite-loaded route fakes prove missing, wrong-owner, malformed, unauthenticated, and safety-denied requests make zero Turnstile/Cloudflare-fetch, daily-rate, R2-signing, rate-attempt, or direct mutation calls. The valid-owner path orders lookup and author comparison before Turnstile; invalid Turnstile stops before rate-attempt insertion; disabled Turnstile still authorizes the target first; static assertions cover both deployed-source and authored post SELECT policies." },
      { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST,formatDbError", evidenceType: "phase4b-hardening", conciseFinding: "POST lacks Content-Type and transport body-size enforcement and request idempotency. It formats database and caught errors into responses; Turnstile helper messages are not returned by POST, which exposes only its fixed failure codes. These hardening concerns do not create a client-controlled actor, privileged-client, pre-authorization side effect, or unresolved ordering path." }
    ]
  }
};
export const batch5cTraces = {
  "src/pages/api/forum/media-upload-guard.ts#POST": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    consentInsertionPoint: "POST after auth.getUser(token) verification and before request.json, assertUserCanWrite, Turnstile, or rate persistence",
    consentInsertion: { symbol: "POST", precedingStage: "The bearer-bound anon-key client has completed auth.getUser(token) and derives authData.user.id as the only actor.", followingStage: "POST parses upload_kind, size_bytes, and optional turnstile_token before global write safety, conditional Turnstile, and rate-limit persistence.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after the authData.user check and pass only authData.user.id before parsing the user-level guard payload." },
    currentExecutionOrder: ["runtime-environment-validation", "bearer-presence-validation", "construct-bearer-bound-anon-rls-client", "authenticate", "derive-verified-actor", "json-payload-parse", "upload-kind-enum-validation", "size-coercion-without-route-bound", "global-user-safety-authorization", "conditional-turnstile-validation-first-external-effect", "ip-hash", "rate-window-read-and-attempt-insert-first-persistent-effect", "ok-response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/media-upload-guard.ts", symbol: "POST,getBearerToken,createClient,auth.getUser", evidenceType: "handler-authentication-and-client-construction", conciseFinding: "POST requires a bearer, constructs only an anon-key Supabase client bound to its Authorization header, verifies auth.getUser(token), and derives the actor exclusively from authData.user.id. It constructs no service-role or other privileged client." },
      { sourceFile: "src/pages/api/forum/media-upload-guard.ts", symbol: "POST#payload", evidenceType: "accepted-input-and-scope", conciseFinding: "JSON payload accepts only upload_kind, size_bytes, and optional turnstile_token. upload_kind is restricted to post_media, circle_cover, profile_avatar, or profile_banner; size_bytes is Number-coerced without a route-local positive/maximum bound. No user, author, owner, role, staff, post, circle, media, object-key, URL, host, or target relationship field is accepted." },
      { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "user-level-write-authorization", conciseFinding: "After enum validation, POST passes only the verified actor id and media_upload action. The helper reads that user's safety state and denies banned, suspended, or unavailable actors before Turnstile or persistence." },
      { sourceFile: "src/pages/api/forum/media-upload-guard.ts", symbol: "POST", evidenceType: "guard-capability-boundary", conciseFinding: "This is a verified-user and upload-purpose guard, not a post-bound, circle-bound, media-bound, storage, or signing endpoint. It accepts no target identifier and responds only with ok: true, so it creates no reusable upload authorization that can be replayed for another target, user, or purpose." },
      { sourceFile: "src/lib/server/turnstile.ts", symbol: "shouldRequireUploadTurnstile,validateTurnstileToken,fetch", evidenceType: "first-external-effect-and-no-ssrf", conciseFinding: "When required, Turnstile is the first external effect after verified actor, enum validation, and user-safety authorization. The helper fetches one fixed Cloudflare siteverify URL and accepts no caller-supplied URL, host, protocol, port, redirect, DNS, download, or media metadata destination; no SSRF-capable path exists." },
      { sourceFile: "src/lib/server/rate-limit.ts", symbol: "hashRateLimitIp,enforceUploadRateLimit", evidenceType: "rate-and-persistent-effect-ordering", conciseFinding: "After any enabled Turnstile success, POST hashes the request IP and enforceUploadRateLimit reads the same IP-window post_media_upload/external_video_upload attempts, then inserts forum_upload_attempts when below the limit. The insert is the first persistent operational effect; the quota read is side-effect free." },
      { sourceFile: "supabase/migrations/20260531_forum_phase6_upload_guardrails.sql", symbol: "forum_upload_attempts_insert_authenticated", evidenceType: "upload-attempt-rls", conciseFinding: "The insert policy requires user_id = auth.uid() or null. POST supplies authData.user.id, so this bearer-bound client cannot create an attempt row for another user; no UPDATE or DELETE policy is granted by this migration. Its authenticated SELECT policy uses true, which provides no write authority." },
      { sourceFile: "supabase/migrations/20260524_forum_phase3_post_media.sql", symbol: "post_media_insert_self,post_media_objects_insert_self", evidenceType: "later-media-and-storage-rls", conciseFinding: "POST itself never touches post_media or storage. Later post_media insertion independently requires user_id = auth.uid() and an existing post whose author_id = auth.uid(); direct post-media storage insertion independently requires bucket_id post-media, owner = auth.uid(), and a user-id-prefixed path. Those policies bind later media persistence to the authenticated owner rather than this guard response." },
      { sourceFile: "src/pages/api/forum/media-upload-guard.ts", symbol: "POST", evidenceType: "absence-confirmed", conciseFinding: "The route creates no object key, signed URL, token, R2 client, storage request, post_media row, post/circle/profile mutation, notification, audit, email, cache, RPC, or domain record. No storage cleanup is needed because it creates no object or credential." },
      { sourceFile: "src/lib/server/rate-limit.ts", symbol: "enforceUploadRateLimit", evidenceType: "retry-partial-failure-and-phase4b", conciseFinding: "No idempotency key or transaction exists, so repeated calls can insert duplicate operational attempt rows. A successful attempt insert occurs before the ok response and can remain if a later response path fails; failed count/insert operations fail open through the helper. Missing Content-Type and transport body-size limits, weak route-local MIME/extension validation, Number-coerced size, and raw caught configuration errors remain Phase 4B hardening concerns. No signed credential, key, or object exists to be overly broad, reusable, long-lived, predictable, or to leave storage residue." },
      { sourceFile: "scripts/test-media-upload-guard-authorization.mjs", symbol: "media-upload-guard-authorization", evidenceType: "offline-static-regression", conciseFinding: "Offline source and migration assertions verify actor/payload ordering, the no-target/no-credential boundary, fixed route response, upload-attempt ownership predicate, and later post-media self-and-post-author policy. The test makes no real auth, database, storage, or network request." }
    ]
  }
};
export const batch5dTraces = {
  "src/pages/api/forum/post-media.ts#POST": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    historicalBlockerId: "POST_MEDIA_TMP_OBJECT_CROSS_POST_PROVENANCE",
    blockerCheckpoint: "7fae49fc9e0d13928bbd9ab196051fbbdacc4c0a",
    forwardMigration: "supabase/migrations/20260713_post_bound_media_provenance.sql",
    migrationExecutionStatus: "authored-not-executed",
    runtimeRemediationStatus: "implemented-source-reaudited",
    consentInsertionPoint: "POST after auth.getUser(token) verification and before assertUserCanWrite, target-post processing, provenance validation, resetCover, or insertion",
    consentInsertion: { symbol: "POST", precedingStage: "The bearer-bound anon-key client has completed auth.getUser(token) and derives authData.user.id as the only actor.", followingStage: "POST calls assertUserCanWrite, parses JSON, validates the target post and canonical provenance, resets the existing cover, moderates storage media, inserts post_media, and may update post moderation state.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after the authData.user check and pass only authData.user.id before assertUserCanWrite." },
    currentExecutionOrder: ["runtime-environment-validation", "bearer-presence-validation", "construct-bearer-bound-anon-rls-client", "authenticate", "derive-verified-actor", "global-user-safety-authorization", "json-payload-parse", "post-id-validation", "server-target-post-read", "server-post-author-authorization", "canonical-actor-post-object-provenance-validation", "reset-existing-cover-first-persistent-domain-mutation", "post-bound-storage-moderation-reads-and-fixed-provider-call-when-configured", "post-media-insert", "conditional-post-moderation-update", "response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/post-media.ts", symbol: "POST,getBearerToken,createClient,auth.getUser", evidenceType: "handler-authentication-and-verified-actor", conciseFinding: "POST requires a bearer, constructs only an anon-key client carrying that bearer, verifies auth.getUser(token), and derives the actor only from authData.user.id; payload fields cannot replace user, author, owner, role, or client identity." },
      { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "pre-write-safety-authorization", conciseFinding: "POST passes only the verified actor id and post_media action to the safety helper, which reads that actor's state before payload target work or persistent mutation." },
      { sourceFile: "src/pages/api/forum/post-media.ts", symbol: "POST#post.lookup,POST#post.authorize", evidenceType: "server-derived-target-authorization", conciseFinding: "POST trims and normalizes post_id, reads the selected server post, and requires server-read author_id equality with the verified actor before media provenance validation or resetCover." },
      { sourceFile: "src/pages/api/forum/post-media.ts", symbol: "validateMediaArray", evidenceType: "canonical-runtime-provenance", conciseFinding: "Video finalization accepts only exact direct actor/post paths or tmp/actor/post paths and rejects foreign URLs, actor-only legacy tmp paths, missing post segments, encoded separators, duplicate slashes, query/fragment forms, and backslash traversal before any domain write." },
      { sourceFile: "src/pages/api/forum/external-video-upload.ts", symbol: "POST#key.build", evidenceType: "issuer-binding", conciseFinding: "The upload issuer calls buildTmpVideoKey(authData.user.id, postId, fileNameRaw) only after verified server post ownership and before Turnstile/signing, preserving the exact target post in the generated temporary object key." },
      { sourceFile: "supabase/migrations/20260713_post_bound_media_provenance.sql", symbol: "post_media_insert_self,post_media_update_self_or_staff", evidenceType: "forward-rls-equivalence", conciseFinding: "The authored forward policies independently require auth.uid ownership, an auth.uid-authored target post, and can_bind_post_media_provenance for INSERT and ordinary-user UPDATE; the anchored helper binds actor, target post, and canonical object path." },
      { sourceFile: "src/pages/api/forum/post-media.ts", symbol: "POST#resetCover,POST#insert", evidenceType: "first-irreversible-effect-and-partial-failure", conciseFinding: "resetCover is the first persistent domain mutation only after target and provenance checks; post_media insertion follows. No transaction or idempotency key exists, so retries and a later insert failure can leave the cover reset as Phase 4B hardening work." },
      { sourceFile: "scripts/test-post-media-provenance-authorization.mjs", symbol: "post-media-provenance-authorization", evidenceType: "offline-regression", conciseFinding: "The offline Vite-loaded test proves post A to post B and cross-user replay are denied before finalization writes, pins historical migrations unchanged, and verifies the new forward policy binds actor, post, and object provenance without real network, R2, Supabase, or auth operations." }
    ]
  }
};
export const batch5eTraces = {
  "src/pages/api/forum/posts.ts#GET": {
    traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete",
    consentInsertionPoint: "not-applicable: GET is read-only",
    currentExecutionOrder: ["runtime-environment-validation", "query-parse-and-bounds", "optional-bearer-moderation-or-ownership-read", "increment-view-rejection", "construct-anon-rls-client", "feed-post-read", "runtime-post-circle-visibility-filter", "reaction-comment-bookmark-enrichment-reads", "response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/posts.ts", symbol: "GET,getBearerToken,createUserClient,auth.getUser", evidenceType: "conditional-bearer-authentication", conciseFinding: "The moderation_check and ownership_check branches require and verify a bearer, derive authData.user.id, and perform only role or post-author reads. The normal feed path accepts no actor and constructs a separate anon-key RLS client." },
      { sourceFile: "src/pages/api/forum/posts.ts", symbol: "GET,parseFeedSort", evidenceType: "query-validation-and-ordering", conciseFinding: "GET parses circle, allowlisted sort, and finite page/limit values, clamping page to at least one and limit to 1 through 50. ownership_check passes its supplied id only to an RLS-scoped select and does not mutate it." },
      { sourceFile: "src/pages/api/forum/posts.ts", symbol: "GET#increment_view", evidenceType: "read-only-effect-boundary", conciseFinding: "increment_view=1 returns a fixed 400 response before client construction; GET no longer invokes increment_post_view_count, a counter RPC, notification, cache write, analytics write, email, R2 operation, or external provider." },
      { sourceFile: "src/lib/forum-feed.ts", symbol: "loadCandidatePosts,loadLatestPostsPage,filterPublicVisibleFeedPosts", evidenceType: "public-resource-ancestry", conciseFinding: "Feed reads published and moderation-published posts with circle slug/name/status, then filters every candidate through isPublicVisibleCircle before media, reaction, comment, and bookmark enrichment reads. Missing, inactive, deleted, and canonical QA-hidden circles therefore contribute no public post or requester enrichment." },
      { sourceFile: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql,supabase/migrations/20260713_forum_posts_circle_authorization.sql", symbol: "can_access_public_circle,posts_select_published_public,increment_post_view_count", evidenceType: "forward-rls-and-rpc-deployment-prerequisite", conciseFinding: "The existing authored comment-read migration supplies fail-closed public circle access, and this run's authored forward post migration applies it to post mutation policies and the historical view RPC. Both migrations remain unexecuted deployment prerequisites; runtime filtering already enforces the same public ancestry contract." },
      { sourceFile: "src/pages/api/forum/posts.ts", symbol: "GET", evidenceType: "error-and-side-effect-absence", conciseFinding: "The route returns fixed authentication errors but formats some database/caught messages directly, a Phase 4B sanitization finding. Every completed GET branch is read-only, so no future legal-consent insertion applies." },
      { sourceFile: "scripts/test-forum-posts-authorization.mjs", symbol: "forum-posts-authorization", evidenceType: "offline-regression", conciseFinding: "The focused offline test proves public-circle filtering and increment_view rejection with zero network, database, storage, authentication, RPC, or mutation calls." }
    ]
  },
  "src/pages/api/forum/posts.ts#POST": {
    traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete",
    consentInsertionPoint: "POST after auth.getUser(token) verification and before assertUserCanWrite, payload parsing, profile/circle lookup, rate persistence, moderation, or insertion",
    consentInsertion: { symbol: "POST", precedingStage: "The bearer-bound anon-key client has completed auth.getUser(token) and derives authData.user.id as the sole actor.", followingStage: "POST calls assertUserCanWrite, parses and validates payload, reads profile and writable circle, persists rate accounting, conditionally calls moderation, and inserts the post.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after the authData.user check and pass only authData.user.id before any later route stage." },
    currentExecutionOrder: ["runtime-environment-validation", "bearer-presence-validation", "construct-bearer-bound-anon-rls-client", "authenticate", "derive-verified-actor", "global-user-safety-authorization", "json-payload-parse", "payload-validation", "profile-read", "writable-active-canonical-circle-read", "ip-hash", "rate-window-read-and-attempt-insert-first-persistent-effect", "content-moderation-conditional-first-external-effect", "post-insert-first-domain-mutation", "response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/posts.ts", symbol: "POST,getBearerToken,createUserClient,auth.getUser", evidenceType: "authentication-and-actor-binding", conciseFinding: "POST requires a bearer, verifies it with auth.getUser, and uses only authData.user.id for profile lookup, safety, rate accounting, moderation attribution, and author_id. The payload contains no actor, author, owner, role, staff, circle_id, or moderation identity field." },
      { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "pre-write-safety-authorization", conciseFinding: "POST passes only the verified actor and post_create action to the safety helper before body target processing, rate persistence, moderation, or insertion." },
      { sourceFile: "src/pages/api/forum/posts.ts", symbol: "validatePayload,resolveWritableCircleBySlug", evidenceType: "payload-and-target-authorization", conciseFinding: "POST rejects malformed JSON and invalid title/body/type combinations, confirms the verified profile, then resolves only the requested slug as an active canonical public circle before any effect. Missing, inactive, deleted, or QA-hidden circles return denial and make zero rate, moderation, or post writes." },
      { sourceFile: "src/lib/server/rate-limit.ts", symbol: "hashRateLimitIp,enforceUserRateLimit", evidenceType: "first-persistent-effect", conciseFinding: "After circle authorization, the route hashes request IP and rate checks; an under-limit forum_rate_limit_attempts insert is the first persistent effect. There is no idempotency key or transaction, so retries may retain duplicate operational attempts, a Phase 4B concern." },
      { sourceFile: "src/lib/moderation/moderate-content.server.ts", symbol: "moderateContent", evidenceType: "first-external-effect", conciseFinding: "Only after target authorization and rate processing can POST call local moderation and an optional configured provider; rejected content returns before posts insertion. The route supplies fixed provider metadata rather than a caller-selected URL or destination." },
      { sourceFile: "supabase/migrations/20260713_forum_posts_circle_authorization.sql", symbol: "posts_insert_self", evidenceType: "forward-rls-equivalence", conciseFinding: "The authored unexecuted INSERT policy independently requires auth.uid author equality, allowed initial statuses, and can_access_public_circle(circle_id), so missing or inaccessible circle ancestry fails closed after deployment." },
      { sourceFile: "src/pages/api/forum/posts.ts", symbol: "POST", evidenceType: "partial-failure-and-response", conciseFinding: "posts.insert is the first domain mutation and there is no notification, count, audit, cache, email, R2, RPC, or follow-on write. Insert errors and caught errors can expose formatted messages; body Content-Type/transport-size limits, retry handling, and raw error sanitization remain Phase 4B work." },
      { sourceFile: "scripts/test-forum-posts-authorization.mjs", symbol: "forum-posts-authorization", evidenceType: "offline-ordering-regression", conciseFinding: "Static offline assertions pin circle authorization before rate and moderation and rate before insertion; no test invokes a real provider, Supabase, R2, or network service." }
    ]
  },
  "src/pages/api/forum/posts.ts#PATCH": {
    traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete",
    consentInsertionPoint: "PATCH after auth.getUser(token) verification and before assertUserCanWrite, target lookup, profile role read, or update",
    consentInsertion: { symbol: "PATCH", precedingStage: "The bearer-bound anon-key client has verified auth.getUser(token) and derived authData.user.id.", followingStage: "PATCH calls assertUserCanWrite, validates id/status, verifies post-circle visibility, reads the server profile role, and updates status hidden.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after the authData.user check with only the verified actor id." },
    currentExecutionOrder: ["runtime-environment-validation", "bearer-presence-validation", "construct-bearer-bound-anon-rls-client", "authenticate", "derive-verified-actor", "global-user-safety-authorization", "json-payload-parse", "id-and-hidden-status-validation", "published-post-active-canonical-circle-read", "server-profile-role-read", "moderator-admin-authorization", "post-hidden-update-first-persistent-domain-mutation", "response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/posts.ts", symbol: "PATCH,getBearerToken,createUserClient,auth.getUser", evidenceType: "authentication-and-input-boundary", conciseFinding: "PATCH requires a verified bearer and accepts only id plus the literal hidden status. The actor and staff role are derived from verified auth and server profile data, never request fields." },
      { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "pre-write-safety-authorization", conciseFinding: "PATCH uses the verified actor with post_moderate before parsing target data or reading target state." },
      { sourceFile: "src/pages/api/forum/posts.ts", symbol: "resolveAccessibleForumPostTarget,loadViewerRole,isModeratorRole", evidenceType: "target-and-role-authorization", conciseFinding: "After UUID/literal status validation, PATCH requires a published moderation-visible post in an active canonical circle, then reads only the verified actor's profile role and permits moderator/admin before update. A caller cannot hide a hidden, deleted, pending, cross-circle, or canonical-hidden-ancestor post." },
      { sourceFile: "supabase/migrations/20260713_forum_posts_circle_authorization.sql", symbol: "posts_update_self_or_staff", evidenceType: "forward-rls-equivalence", conciseFinding: "The authored unexecuted UPDATE policy retains server-derived staff access and adds can_access_public_circle to ordinary-user write checks. PATCH itself is stricter: it permits only moderator/admin status hiding after route target visibility authorization." },
      { sourceFile: "src/pages/api/forum/posts.ts", symbol: "PATCH", evidenceType: "effects-idempotency-and-errors", conciseFinding: "The one posts.update is the first and only persistent/domain effect; no rate, moderation provider, notification, audit, cache, email, R2, RPC, or external call occurs. Retry returns 404 after a successful hide because the resolver requires published status; no idempotent-success contract, transaction, Content-Type/body-size control, or raw error sanitization exists, all Phase 4B hardening." },
      { sourceFile: "scripts/test-forum-posts-authorization.mjs", symbol: "forum-posts-authorization", evidenceType: "offline-ordering-regression", conciseFinding: "Offline static checks pin safety before target resolution, target resolution before role lookup, and role authorization before update, with denied paths having zero update calls." }
    ]
  },
  "src/pages/api/forum/posts.ts#DELETE": {
    traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete",
    consentInsertionPoint: "DELETE after auth.getUser(token) verification and before assertUserCanWrite, role read, target lookup, media lookup, update, or cleanup",
    consentInsertion: { symbol: "DELETE", precedingStage: "The bearer-bound anon-key client has verified auth.getUser(token) and derived authData.user.id.", followingStage: "DELETE calls assertUserCanWrite, reads the verified profile role, validates the id, authorizes visible post-circle ancestry, reads media, soft-deletes, then cleans media.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after authData.user verification with only the verified actor id." },
    currentExecutionOrder: ["runtime-environment-validation", "bearer-presence-validation", "construct-bearer-bound-anon-rls-client", "authenticate", "derive-verified-actor", "global-user-safety-authorization", "server-profile-role-read", "query-id-validation", "published-post-active-canonical-circle-read", "author-or-staff-authorization", "media-read", "post-soft-delete-first-persistent-domain-mutation", "managed-r2-and-post-media-cleanup", "response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/posts.ts", symbol: "DELETE,getBearerToken,createUserClient,auth.getUser", evidenceType: "authentication-and-actor-binding", conciseFinding: "DELETE requires a bearer, verifies auth.getUser, and derives the actor only from authData.user.id. URL input supplies only id/post_id and cannot replace author, owner, role, or staff identity." },
      { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "pre-write-safety-authorization", conciseFinding: "DELETE calls the verified-actor post_delete safety check before target processing, media lookup, post mutation, or cleanup." },
      { sourceFile: "src/pages/api/forum/posts.ts", symbol: "UUID_REGEX,resolveAccessibleForumPostTarget,loadViewerRole", evidenceType: "target-owner-and-ancestor-authorization", conciseFinding: "DELETE rejects malformed ids, requires a published moderation-visible target with an active canonical circle, and then compares its server-read author to the verified actor unless server-derived moderator/admin role allows staff handling. Denied targets produce no media read, status update, or R2 cleanup." },
      { sourceFile: "supabase/migrations/20260713_forum_posts_circle_authorization.sql", symbol: "posts_delete_self_or_staff", evidenceType: "forward-rls-equivalence", conciseFinding: "The authored unexecuted DELETE policy requires a published moderation-visible row, can_access_public_circle(circle_id), and either auth.uid author equality or server-derived staff role, matching the route's accessible-target and actor boundary." },
      { sourceFile: "src/pages/api/forum/posts.ts,src/lib/server/media-cleanup.ts", symbol: "DELETE,deletePostMediaObjects", evidenceType: "effect-order-and-partial-failure", conciseFinding: "After authorization, post_media is read, the status=deleted update is the first persistent domain mutation, and managed R2/object-row cleanup follows. Cleanup is non-transactional and may fail after soft deletion; the response reports warnings/errors. The removed hard-delete fallback prevents cleanup bypass, while retries after deletion return 404 rather than a success-idempotent response; both lifecycle/retry behavior is Phase 4B hardening." },
      { sourceFile: "scripts/test-forum-posts-authorization.mjs", symbol: "forum-posts-authorization", evidenceType: "offline-ordering-regression", conciseFinding: "Offline assertions prove safety and accessible target authorization precede media reads and updates, update precedes cleanup, denied paths have zero mutation calls, and no hard-delete fallback remains." }
    ]
  }
};
export const batch5fTraces = {
  "src/pages/api/forum/reports.ts#POST": {
    traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete",
    historicalBlockerId: "FORUM_REPORT_TARGET_AND_INITIAL_STATE_AUTHORIZATION",
    forwardMigration: "supabase/migrations/20260713_forum_report_target_authorization.sql",
    migrationExecutionStatus: "authored-not-executed",
    runtimeRemediationStatus: "implemented-source-reaudited",
    consentInsertionPoint: "POST after auth.getUser(token) verification and before assertUserCanWrite, payload parsing, target lookup, rate reads, report insertion, or event insertion",
    consentInsertion: { symbol: "POST", precedingStage: "The bearer-bound anon-key client has completed auth.getUser(token) and derives authData.user.id as the sole reporter.", followingStage: "POST calls assertUserCanWrite, parses/validates the request, resolves target ancestry, rate checks, duplicate checks, and report/event writes.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after authData.user verification with only the verified reporter id." },
    currentExecutionOrder: ["runtime-environment-validation", "bearer-presence-validation", "construct-bearer-bound-anon-rls-client", "authenticate", "derive-verified-reporter", "global-user-safety-authorization", "json-payload-parse", "target-type-id-reason-validation", "type-bound-target-and-public-ancestry-read", "reporter-hour-rate-read", "duplicate-report-read", "report-insert-first-persistent-and-domain-mutation", "created-report-event-insert", "response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/reports.ts", symbol: "POST,getBearerToken,createUserClient,auth.getUser", evidenceType: "authentication-and-reporter-binding", conciseFinding: "POST requires a bearer, creates only an anon-key client carrying it, verifies auth.getUser(token), and derives reporterId only from authData.user.id. No payload field is read as reporter, user, actor, role, staff, status, priority, assignment, or resolution identity." },
      { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "assertUserCanWrite", evidenceType: "pre-write-safety-authorization", conciseFinding: "POST applies the verified reporter report_create safety decision before JSON parsing, target lookup, report-rate reads, duplicate lookup, report insertion, or event insertion." },
      { sourceFile: "src/lib/server/reports.server.ts", symbol: "parseUserReportPayload,sanitizeReportReasonText", evidenceType: "request-validation", conciseFinding: "Payload must use an allowlisted target_type, UUID target_id, allowlisted reason_code, and optional sanitized reason_text of 5 through 1000 characters. Legacy post_id is normalized server-side to a post/other payload. There is no Content-Type or transport body-size guard, a Phase 4B finding." },
      { sourceFile: "src/lib/server/reports.server.ts", symbol: "resolveReportTargetPreview", evidenceType: "target-type-binding-and-accessibility", conciseFinding: "The target type selects exactly one table and requires returned id equality. Posts and comments require published/moderation-published rows and the exact active canonical-visible parent circle; comments also require their exact parent post. Circles require active canonical visibility; users require an existing profile. Own content remains reportable, while unavailable/deleted/hidden/rejected/inaccessible targets return already_handled without a report write." },
      { sourceFile: "src/lib/server/reports.server.ts", symbol: "countRecentReportsByUser,findDuplicateUserReport", evidenceType: "rate-and-duplicate-behavior", conciseFinding: "After target authorization, the route counts the reporter's prior-hour reports and denies at 12. createUserReport then reads a 24-hour duplicate keyed by reporter_id, target_type, and target_id. These are read-only and invalid targets consume neither report nor event write; concurrent requests can still race without a uniqueness constraint or idempotency key, a Phase 4B concern." },
      { sourceFile: "src/lib/server/reports.server.ts", symbol: "createUserReport,insertReportEvent", evidenceType: "write-order-and-partial-failure", conciseFinding: "reports.insert is the first persistent and first domain mutation and sets reporter_id from verified input, status open, and priority normal. report_events.insert(created) follows with the same reporter actor. The operations are not transactional and event failure is logged, leaving the report persisted; no notification, email, R2, cache, RPC, telemetry, service-role, or external effect occurs." },
      { sourceFile: "supabase/migrations/20260627_reports_optimization_mvp.sql,supabase/migrations/20260713_forum_report_target_authorization.sql", symbol: "report_events_insert_reporter_created,validate_report_target,can_create_user_report_target,reports_insert_self", evidenceType: "rls-and-trigger-alignment", conciseFinding: "Existing RLS binds created event actor/report ownership and the trigger proves target existence. The new authored forward policy additionally binds report auth.uid identity, neutral initial fields, and full type-bound public target ancestry; missing relationships fail closed after deployment." },
      { sourceFile: "scripts/test-forum-reports-authorization.mjs", symbol: "forum-reports-authorization", evidenceType: "offline-regression", conciseFinding: "The focused offline test covers accessible post/comment/circle/user targets, malformed/missing/mismatched/hidden/deleted/canonical-hidden targets, client identity/status override exclusion, historical migration hashes, RLS predicate shape, and exact route/helper effect order without real network, database, auth, or storage operations." },
      { sourceFile: "src/pages/api/forum/reports.ts", symbol: "POST", evidenceType: "error-sanitization-and-consent-boundary", conciseFinding: "Target/rate helper and caught error details can reach responses, so sanitization remains Phase 4B hardening. A future consent guard belongs immediately after verified authentication and cannot replace safety, target type binding, reporter ownership, or RLS ancestry controls." }
    ]
  }
};
export const batch5gTraces = {
  "src/pages/api/forum/search.ts#GET": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    historicalBlockerId: "FORUM_SEARCH_PUBLIC_VISIBILITY_AND_QUERY_FILTER_SAFETY",
    forwardMigration: "supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql",
    migrationExecutionStatus: "authored-not-executed",
    runtimeRemediationStatus: "implemented-source-reaudited",
    consentInsertionPoint: "not-applicable: GET is read-only",
    consentInsertion: { symbol: "GET", precedingStage: "GET parses bounded URL parameters and constructs the anon-only search client before database reads.", followingStage: "No mutation follows the read and in-memory ranking pipeline.", refactorRequired: false, smallestFutureRefactor: "No legal-consent guard applies because this method performs no consent-required mutation." },
    currentExecutionOrder: ["runtime-environment-validation", "url-query-type-circle-and-limit-parsing", "construct-anon-key-client-without-bearer-propagation", "matching-author-profile-id-read-first-database-operation", "parallel-post-circle-profile-and-local-device-searches", "active-canonical-parent-circle-runtime-filtering", "visible-post-circle-count-and-public-proxy-media-enrichment-reads", "in-memory-ranking-and-bounded-slicing", "response"],
    evidence: [
      { sourceFile: "src/pages/api/forum/search.ts", symbol: "GET,clampLimit", evidenceType: "handler-and-query-validation", conciseFinding: "GET accepts q, type, circle, and four numeric limit keys only. type is allowlisted to all/posts/circles/users/devices and each supplied limit is clamped to the helper maximum; offset, cursor, sort, table, column, RPC, actor, role, and staff controls are ignored." },
      { sourceFile: "src/lib/supabase-server.ts,src/pages/api/forum/search.ts", symbol: "createSSRClient,GET", evidenceType: "client-construction-and-authentication", conciseFinding: "GET creates only an anon-key client with sessions disabled. It does not inspect Authorization, derive a requester, construct a bearer-bound or service-role client, or expose authenticated-own/staff search branches; all results remain public-only." },
      { sourceFile: "src/lib/forum-search.ts", symbol: "sanitizeSearchInput,parseForumSearchParams", evidenceType: "query-safety", conciseFinding: "The query normalizer keeps only Unicode letters, numbers, whitespace, and hyphen before interpolation into fixed PostgREST ilike/or clauses. It rejects normalized values shorter than two or longer than 80 characters, blocks filter grammar/control characters, and leaves no input that can select another table, field, operator, sort, or RPC." },
      { sourceFile: "src/lib/forum-search.ts", symbol: "fetchMatchingAuthorIds,fetchPublishedPosts,isActivePublicSearchCircle", evidenceType: "post-visibility-and-ordering", conciseFinding: "When posts are enabled, the bounded profiles id lookup is first. Public post reads require published moderation-visible rows, load parent-circle id/slug/name/status, and discard rows whose exact parent is missing, inactive, deleted, or canonical-hidden before any title, excerpt, author, count, media, ranking, or response entry is computed." },
      { sourceFile: "src/lib/forum-search.ts", symbol: "resolveCircleIdForSearch,circlesPromise,fetchPublicProfiles", evidenceType: "circle-profile-and-metadata-visibility", conciseFinding: "Circle selection/results require active canonical visibility; profile rows contain only public display fields and must have visible derived post or circle activity. Derived counts apply the same parent-circle predicate, so hidden rows cannot leak through result totals, snippets, slugs, author/profile data, counts, ranking, or requester-specific flags." },
      { sourceFile: "src/lib/forum-media.ts,src/lib/profile-media.ts", symbol: "resolveSignedPostMedia,resolveProfileAvatarUrl", evidenceType: "read-only-media-proxying", conciseFinding: "Search uses same-origin public media/avatar proxies. Public-proxy post-media resolution skips storage signing, and profile proxying returns before signed URL creation; no storage request, R2 action, external search provider, external fetch, or persistent side effect occurs." },
      { sourceFile: "supabase/migrations/20260518_forum_phase1_schema.sql,supabase/migrations/20260713_comment_read_circle_visibility_authorization.sql", symbol: "profiles_select_public,circles_select_public,posts_select_published_public,can_access_public_circle", evidenceType: "current-and-forward-rls", conciseFinding: "Profile and circle base SELECT policies are public, so the route's runtime visibility filter is required today. The authored forward post policy adds published moderation-visible active canonical-circle enforcement; it is unexecuted and remains a deployment prerequisite for equivalent database-layer post filtering." },
      { sourceFile: "scripts/test-forum-search-visibility.mjs", symbol: "forum-search-visibility", evidenceType: "offline-visibility-regression", conciseFinding: "The fully offline fake client returns visible, inactive, canonical-hidden, and missing-ancestor rows as if permissive source RLS selected them. The helper emits only visible rows/counts/proxies, rejects excessive or grammar-shaped query text, and records zero writes, RPCs, storage signings, or real service calls." },
      { sourceFile: "src/pages/api/forum/search.ts,src/lib/forum-search.ts", symbol: "GET,runForumSearch", evidenceType: "error-sanitization-and-no-consent", conciseFinding: "The endpoint returns fixed SEARCH_FAILED or INVALID_QUERY codes rather than helper/database messages. It has no write, duplicate, idempotency, partial-write, or consent boundary because it is read-only; the only remaining requirement is deployment of existing forward visibility migrations." }
    ]
  }
};
export const batch6aTraces = {
  "src/pages/api/legal/consent.ts#GET": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    consentInsertionPoint: "not-applicable: GET is read-only",
    consentInsertion: { symbol: "GET", precedingStage: "GET obtains a verified bearer-derived actor and actor-bound read repository.", followingStage: "The active-bundle lookup is mapped to a fixed minimal JSON response; no mutation follows.", refactorRequired: false, smallestFutureRefactor: "No legal-consent guard applies because GET performs no consent-required mutation." },
    currentExecutionOrder: ["runtime-environment-validation", "bearer-token-parse", "construct-bearer-bound-anon-rls-client", "auth.getUser-token-verification", "derive-verified-actor-and-read-repository", "server-active-bundle-construction", "actor-and-active-bundle-consent-read-first-database-operation", "exact-current-bundle-comparison", "fixed-minimal-json-response"],
    evidence: [
      { sourceFile: "src/pages/api/legal/consent.ts,src/lib/server/admin-auth.ts", symbol: "GET,dependenciesFor,authenticate,getBearerToken,createUserClient,auth.getUser", evidenceType: "authentication-and-actor-derivation", conciseFinding: "GET requires an Authorization Bearer token. authenticate parses it, constructs only an anon-key bearer-bound client, verifies auth.getUser(token), and returns only data.user.id plus a read repository; missing or invalid credentials return fixed UNAUTHORIZED before any consent read." },
      { sourceFile: "src/pages/api/legal/consent.ts,src/lib/server/legal-consent-repository.server.ts", symbol: "authenticate,createLegalConsentReadRepository,findByUserAndBundle", evidenceType: "rls-client-and-cross-user-isolation", conciseFinding: "The read repository is created only after verified authentication from the bearer-bound client and its sole query binds user_id to the verified actor and bundle_version to the server-selected bundle. No request query, body, header, or route field can choose another user. Active RLS policy legal_policy_acceptances_select_own independently requires user_id = auth.uid()." },
      { sourceFile: "src/lib/server/legal-consent.server.ts,src/lib/legal-policy.ts", symbol: "getActiveLegalBundle,getCurrentConsentStatus,recordMatchesActiveBundle,LEGAL_POLICY", evidenceType: "server-authoritative-versioning", conciseFinding: "Terms, Privacy, Community Guidelines, minimum age, bundle version, and combined-current comparison come exclusively from LEGAL_POLICY server constants. GET reads only the current bundle and returns current true only when every required version and minimum age match; no client policy version or accepted timestamp is accepted." },
      { sourceFile: "src/lib/server/legal-consent.server.ts,src/lib/server/legal-consent-api.server.ts", symbol: "getCurrentConsentStatus,buildSafeConsentResponse,handleLegalConsentGet", evidenceType: "current-missing-outdated-response", conciseFinding: "A matching record returns current true. A missing active-bundle record and a record with a mismatched required version both return current false through the same minimal response, intentionally revealing neither history nor another user's state. Optional analytics and marketing are absent from the combined required bundle, request schema, persistence call, and response, so neither can satisfy or become required consent." },
      { sourceFile: "src/pages/api/legal/consent.ts,src/lib/server/legal-consent-api.server.ts,src/lib/server/legal-consent-repository.server.ts", symbol: "GET,handleLegalConsentGet,dependenciesFor,createLegalConsentServiceClient", evidenceType: "privileged-client-and-effect-absence", conciseFinding: "GET calls only authenticate then getCurrentConsentStatus. dependenciesFor keeps createLegalConsentServiceClient inside the lazy createWriteRepository closure used only by POST, so GET never constructs a service-role client and performs zero insert, update, delete, upsert, RPC, renewal, audit, notification, analytics, marketing, email, network, storage, or other irreversible effect." },
      { sourceFile: "src/lib/legal-consent-route-policy.ts,tests/fixtures/legal-consent-api-methods.mjs,src/lib/server/legal-consent-mutation.server.ts", symbol: "classifyLegalConsentRoute,EXEMPT_PREFIXES,AUTH_RECOVERY_EXEMPT,requireAuthenticatedLegalConsent", evidenceType: "bootstrap-exemption", conciseFinding: "The legal-consent page route is classified exempt, and consent.ts#POST is explicitly AUTH_RECOVERY_EXEMPT. The ordinary mutation guard is not called by this endpoint because requiring current consent before recording or renewing it would deadlock bootstrap; the exemption is limited to this endpoint's authenticated, fixed-schema consent function and grants no unrelated mutation authority." },
      { sourceFile: "src/lib/server/legal-consent-api.server.ts", symbol: "handleLegalConsentGet,legalConsentJson", evidenceType: "error-sanitization-and-response", conciseFinding: "Repository failures are caught and reduced to LEGAL_CONSENT_UNAVAILABLE. Successful responses expose only current, bundleVersion, minimumAge, and consentUrl with no-store caching; accepted versions, timestamps, sources, user id, SQL, service credentials, and raw error details are omitted." },
      { sourceFile: "scripts/test-legal-consent-api-get.mjs", symbol: "legal-consent-api-get", evidenceType: "offline-regression", conciseFinding: "Fully offline fakes cover current, missing, outdated, optional-choice-absent, anonymous, invalid-bearer, attempted user-id override, repository failure, lazy writer behavior, response minimization, and zero writes or external effects." }
    ]
  }
};
export const batch6bTraces = {
  "src/pages/api/media/circle/[circleId].ts#GET": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    consentInsertionPoint: "not-applicable: GET is read-only",
    consentInsertion: { symbol: "GET", precedingStage: "The route accepts only a route circle id and resolves the fixed public circle-cover target.", followingStage: "The authorized fixed storage object is signed and streamed; no mutation follows.", refactorRequired: false, smallestFutureRefactor: "No legal-consent guard applies because GET performs no consent-required mutation." },
    currentExecutionOrder: ["route-circle-id-presence-and-uuid-validation", "construct-anon-key-rls-client-without-bearer-propagation", "exact-circle-select-first-database-read", "active-canonical-public-circle-and-canonical-cover-path-authorization", "fixed-bucket-storage-signed-url-request", "trusted-storage-signed-url-fetch", "stream-response"],
    evidence: [
      { sourceFile: "src/pages/api/media/circle/[circleId].ts", symbol: "GET", evidenceType: "handler", conciseFinding: "GET receives only params.circleId and returns MEDIA_NOT_FOUND for missing, malformed, inaccessible, or invalid-cover targets before the proxy call. ALL returns 405 for all non-GET methods." },
      { sourceFile: "src/lib/circle-cover.ts", symbol: "resolvePublicCircleCoverTarget", evidenceType: "server-derived-circle-authorization", conciseFinding: "The helper validates the UUID, reads one exact circles row, requires exact id, active status, canonical visibility, and canonical image_path before returning the server-derived storage path." },
      { sourceFile: "src/lib/supabase-server.ts", symbol: "createSSRClient", evidenceType: "authentication-and-client-construction", conciseFinding: "The public route constructs only an anon-key client with sessions disabled. It does not parse or forward a bearer, derive an actor, query roles, construct a bearer-bound client, or expose owner/staff access, so ordinary authenticated callers cannot broaden public visibility." },
      { sourceFile: "src/lib/media-proxy.ts", symbol: "streamStorageObjectViaSignedUrl", evidenceType: "signing-and-outbound-effect", conciseFinding: "Only after cover authorization does the helper request a ten-minute signed URL for the fixed bucket/path, fetch the storage-returned URL, and stream it. The signed URL is not returned; no caller-controlled URL, host, bucket, or object key is fetched." },
      { sourceFile: "supabase/migrations/20260714_circle_cover_public_visibility_authorization.sql", symbol: "can_access_public_circle_cover_object,circles_select_public,circle_cover_objects_select_public", evidenceType: "forward-rls-alignment", conciseFinding: "The authored unexecuted migration enforces active canonical public circle access for public circle rows and exact canonical circles.image_path binding for public cover storage rows while preserving owner/staff management reads." },
      { sourceFile: "scripts/test-circle-cover-media-visibility.mjs", symbol: "circle-cover-media-visibility", evidenceType: "offline-regression", conciseFinding: "Offline fake data proves every denied circle/path stops before signing or fetch, with no writes or persistence. Static assertions prove no prefix-only public policy remains after migration execution and historical policy migrations are unchanged." }
    ]
  }
};
export const batch6cTraces = {
  "src/pages/api/media/post/[mediaId].ts#GET": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    historicalBlockerId: "POST_MEDIA_DELIVERY_ANCESTRY_AND_PROVENANCE_AUTHORIZATION",
    forwardMigration: "supabase/migrations/20260715_post_media_delivery_visibility_authorization.sql",
    migrationExecutionStatus: "authored-not-executed",
    runtimeRemediationStatus: "implemented-source-reaudited",
    consentInsertionPoint: "not-applicable: GET is read-only",
    consentInsertion: { symbol: "GET", precedingStage: "The route validates only a media UUID and resolves the exact public media-post-circle target.", followingStage: "The validated fixed object is signed or fetched from a configured trusted storage origin and streamed; no mutation follows.", refactorRequired: false, smallestFutureRefactor: "No legal-consent guard applies because GET performs no consent-required mutation." },
    currentExecutionOrder: ["route-media-id-uuid-validation", "construct-anon-key-rls-client-without-bearer-propagation", "exact-post-media-post-circle-select-first-database-read", "published-moderation-visible-active-canonical-public-ancestry-and-key-provenance-authorization", "fixed-storage-signed-url-or-server-configured-r2-url", "bounded-trusted-fetch", "sanitized-header-stream-response"],
    evidence: [
      { sourceFile: "src/pages/api/media/post/[mediaId].ts", symbol: "GET,createPostMediaGet", evidenceType: "handler", conciseFinding: "GET receives only route mediaId plus the display/thumb selector, rejects malformed ids before a database operation, constructs an anon-only SSR client, and never parses Authorization or derives an actor, post id, circle id, bucket, key, host, or redirect destination from the caller." },
      { sourceFile: "src/pages/api/media/post/[mediaId].ts", symbol: "resolvePublicPostMediaTarget,isMediaId,isCanonicalPostMediaObjectPath", evidenceType: "server-derived-ancestry-and-provenance", conciseFinding: "The first database read selects one exact media row with server-owned post and circle fields. The resolver requires exact id relations, published post and moderation states, active canonical public circle visibility, no stored URL, image/video kind, and exact owner/post-bound finalized or video-only temporary storage key before delivery." },
      { sourceFile: "src/lib/supabase-server.ts", symbol: "createSSRClient", evidenceType: "authentication-and-client-construction", conciseFinding: "The route uses only the anonymous key with persistent sessions disabled. A bearer, including malformed input, is intentionally ignored so ordinary authenticated callers receive no wider route branch than anonymous public visibility." },
      { sourceFile: "src/lib/media-proxy.ts", symbol: "streamStorageObjectViaSignedUrl,streamTrustedMediaUrl", evidenceType: "trusted-signing-fetch-and-header-boundary", conciseFinding: "Only an authorized final key is signed in the fixed post-media bucket; a validated tmp key is joined to the server-configured HTTPS R2 origin. The helper aborts fetch after fifteen seconds, rejects absent/invalid/oversized content length above 150 MiB, forwards only four safe representation headers, ignores Range, and returns no signed URL or storage credentials." },
      { sourceFile: "supabase/migrations/20260715_post_media_delivery_visibility_authorization.sql", symbol: "can_access_public_post_media_object,post_media_select_public_or_owner,post_media_objects_select_public_or_owner", evidenceType: "forward-rls-alignment", conciseFinding: "The authored unexecuted public SELECT remediation requires published moderation-visible post and active canonical circle ancestry plus exact canonical post_media storage/thumbnail binding. Missing relationships deny fail-closed; owner/staff RLS branches remain separate from this bearer-agnostic public route." },
      { sourceFile: "src/pages/api/media/post/[mediaId].ts", symbol: "GET", evidenceType: "read-only-effect-classification", conciseFinding: "The first external effect is only post-authorization signing or trusted object fetch, the first streamed byte follows that fetch, and no persistent effect exists. GET contains no insert, update, delete, upsert, counter, audit, analytics, cache, rate, notification, email, RPC, R2 mutation, or consent-required operation." },
      { sourceFile: "scripts/test-post-media-delivery-authorization.mjs", symbol: "post-media-delivery-authorization", evidenceType: "offline-regression", conciseFinding: "The deterministic fake-client matrix proves all denied media/post/circle/provenance/request cases cause zero signing, fetch, streaming, writes, audit, cache, and rate effects, while public final and temporary media follow their fixed trusted delivery paths without real Supabase, R2, auth, network, or migration execution." }
    ]
  }
};
export const batch6dTraces = {
  "src/pages/api/media/profile/[userId]/[kind].ts#GET": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    historicalBlockerId: "PROFILE_MEDIA_USER_KIND_OBJECT_DELIVERY_AUTHORIZATION",
    forwardMigration: "supabase/migrations/20260716_profile_media_delivery_authorization.sql",
    migrationExecutionStatus: "authored-not-executed",
    runtimeRemediationStatus: "implemented-source-reaudited",
    consentInsertionPoint: "not-applicable: GET is read-only",
    consentInsertion: { symbol: "GET", precedingStage: "The route validates only userId and the avatar/banner kind, then resolves one exact public profile field.", followingStage: "The validated fixed object is signed, fetched from the trusted storage URL, and streamed; no mutation follows.", refactorRequired: false, smallestFutureRefactor: "No legal-consent guard applies because GET performs no consent-required mutation." },
    currentExecutionOrder: ["route-user-id-uuid-and-exact-kind-validation", "construct-anon-key-rls-client-without-bearer-propagation", "exact-profiles-select-first-database-read", "exact-profile-id-kind-field-and-canonical-user-bound-key-authorization", "fixed-post-media-bucket-signed-url", "bounded-trusted-image-fetch", "sanitized-header-stream-response"],
    evidence: [
      { sourceFile: "src/pages/api/media/profile/[userId]/[kind].ts", symbol: "GET,createProfileMediaGet,isProfileMediaUserId,isProfileMediaKind", evidenceType: "handler", conciseFinding: "GET receives only the route userId and exact avatar/banner kind, rejects malformed values before client construction, and never parses Authorization or accepts query-derived actor, role, lifecycle, bucket, key, host, URL, or response destination." },
      { sourceFile: "src/pages/api/media/profile/[userId]/[kind].ts,src/lib/profile-media.ts", symbol: "resolvePublicProfileMediaTarget,isProfileMediaPathForUser", evidenceType: "server-derived-profile-kind-object-binding", conciseFinding: "The first database read selects the exact profile id with avatar_url and banner_url. The resolver chooses only the requested field and requires an exact canonical lower-case kind prefix and profile UUID segment before storage delivery; it has no default object or stored external URL branch." },
      { sourceFile: "supabase/migrations/20260518_forum_phase1_schema.sql,supabase/migrations/20260626_user_safety_states_and_bans.sql", symbol: "profiles_select_public,user_safety_states_select_self", evidenceType: "lifecycle-authorization", conciseFinding: "Public profile media follows the current public-profile contract: profiles are readable while their row exists, safety-state rows are not anonymous visibility input, and no hidden, soft-delete, or deactivation profile field exists. Suspended and banned profiles remain public; missing/deleted profiles or missing media deny before signing." },
      { sourceFile: "src/lib/supabase-server.ts,src/lib/media-proxy.ts", symbol: "createSSRClient,streamStorageObjectViaSignedUrl,streamTrustedMediaUrl", evidenceType: "trusted-signing-fetch-and-header-boundary", conciseFinding: "The anon-only route signs only the fixed post-media bucket after authorization, fetches only the storage-issued signed URL with fifteen-second timeout and 150 MiB content-length bound, accepts only JPEG/PNG/WebP/GIF for profile delivery, forwards four safe representation headers, and returns no signed URL, credential, Set-Cookie, authorization, tracing, or storage metadata." },
      { sourceFile: "supabase/migrations/20260716_profile_media_delivery_authorization.sql", symbol: "can_access_public_profile_media_object,profile_avatar_objects_select_public,profile_banner_objects_select_public", evidenceType: "forward-rls-alignment", conciseFinding: "The authored unexecuted public storage SELECT remediation requires an exact current profile avatar_url/banner_url value matching the canonical object and its UUID segment. Missing or mismatched profile relationships fail closed; public policies contain no INSERT, UPDATE, or DELETE authority." },
      { sourceFile: "src/pages/api/media/profile/[userId]/[kind].ts", symbol: "GET", evidenceType: "read-only-effect-classification", conciseFinding: "The first external effect is post-authorization storage signing, the first outbound request is the trusted signed-URL fetch, and the first streamed byte follows that fetch. GET contains no insert, update, delete, upsert, counter, audit, analytics, cache, rate, notification, email, RPC, R2 mutation, or consent-required operation." },
      { sourceFile: "scripts/test-profile-media-delivery-authorization.mjs", symbol: "profile-media-delivery-authorization", evidenceType: "offline-regression", conciseFinding: "The deterministic fake-client matrix proves supported avatar/banner delivery and zero-effect denial of malformed, missing, cross-kind, cross-user, foreign, traversal, encoded, and request-override cases. It statically pins historical RLS and the new exact-binding migration and mocks the only proxy fetch, so it performs no real auth, database, storage, network, or migration operation." }
    ]
  }
};
export const batch6eTraces = {
  "src/pages/api/news.ts#GET": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    historicalBlockerId: "PUBLIC_NEWS_API_QUERY_ERROR_AND_OUTPUT_SAFETY",
    runtimeRemediationStatus: "implemented-source-reaudited",
    consentInsertionPoint: "not-applicable: GET is public and read-only",
    consentInsertion: { symbol: "GET", precedingStage: "The route validates only public category/page/limit values before the anon client and published-news reads.", followingStage: "The normalized public result is serialized after bounded reads; no mutation follows.", refactorRequired: false, smallestFutureRefactor: "No legal-consent guard applies because this public GET performs no consent-required mutation or cache persistence." },
    currentExecutionOrder: ["runtime-binding-presence-validation", "exact-category-and-bounded-page-limit-validation", "construct-anon-key-client-without-bearer-propagation", "parallel-published-featured-latest-hot-database-reads", "public-row-url-text-status-category-normalization", "bounded-published-page-read", "normalized-json-response"],
    evidence: [
      { sourceFile: "src/pages/api/news.ts", symbol: "GET,createPublicNewsGet,parsePublicNewsQuery,parsePublicNewsPositiveInt", evidenceType: "handler-and-query-boundary", conciseFinding: "GET uses only category/page/limit query values. Category must be the exact allowlisted key; page is 1-1000 and limit 1-12 in canonical decimal form. Invalid values return a fixed 400 before a client or read; arbitrary URL, host, protocol, endpoint, header, cache key, environment key, parser, provider, cursor, search, sort, or date input is absent." },
      { sourceFile: "src/lib/supabase-server.ts,supabase/migrations/20260612_hot_news_mvp.sql", symbol: "createSSRClient,news_articles_select_published_public", evidenceType: "public-authentication-and-rls", conciseFinding: "The route constructs only the anon-key client with no bearer parsing, verified actor, service role, or requester-specific branch. The public RLS predicate is status = published, while the public normalizer also drops every nonpublished or invalid-category row." },
      { sourceFile: "src/lib/news.ts", symbol: "listPublicNewsFeed,MAX_PUBLIC_NEWS_PAGE", evidenceType: "database-read-order-and-bounds", conciseFinding: "After validation the helper makes parallel featured/latest/hot published-table SELECTs, then one status/category/pinned/published-order bounded range SELECT. Page is clamped to 1000 and limit to 12. A missing-table-only fallback uses fixed local articles; ordinary query, database, and parse failures do not create a cache entry because no cache exists." },
      { sourceFile: "src/lib/news.ts", symbol: "normalizePublicNewsArticle,normalizeNewsUrl,isPrivateOrLocalHostname", evidenceType: "response-sanitization-and-url-safety", conciseFinding: "Public rows bound title, summary, content, and source name; strip controls and literal HTML; omit author_id; reject unsafe status/category/slug; and retain only canonical news storage paths or safe HTTP(S) URLs. javascript, data, credentials/userinfo, localhost, loopback, private, link-local, and IPv6-loopback URLs fail closed. Image URLs are returned as values only and this route never server-fetches them." },
      { sourceFile: "src/lib/news-content.ts,src/pages/news/[slug].astro", symbol: "parseNewsContent,parseInlineSegments", evidenceType: "rendering-contract", conciseFinding: "The public page parses content into explicit text/link/image blocks and Astro emits text as escaped content. Its link parser only permits relative or HTTP(S) hrefs; route/helper normalization has already removed literal HTML and unsafe URL forms from returned rows." },
      { sourceFile: "src/pages/api/news.ts,src/lib/news.ts", symbol: "GET,listPublicNewsFeed", evidenceType: "external-fetch-cache-and-effect-absence", conciseFinding: "There is no RSS/provider/API adapter, fetch wrapper, outbound request, redirect, DNS resolution, request authorization header, cache lookup/write, KV, database write, filesystem write, audit, analytics, rate, notification, email, RPC, R2, or storage mutation. No client-selected outbound destination or SSRF-capable path exists." },
      { sourceFile: "scripts/test-public-news-api-safety.mjs", symbol: "public-news-api-safety", evidenceType: "offline-regression", conciseFinding: "The deterministic Vite fake matrix proves valid allowlisted reads, zero-read malformed-query denial, bearer-agnostic public behavior, generic errors without secret-like content, sanitized URLs/text/nonpublished rows, and zero provider/cache/write effects with no real service operation." }
    ]
  }
};
export const batch6fTraces = {
  "src/pages/api/news/[slug].ts#GET": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    historicalBlockerId: "PUBLIC_NEWS_DETAIL_SLUG_AND_OUTPUT_SAFETY",
    runtimeRemediationStatus: "implemented-source-reaudited",
    consentInsertionPoint: "not-applicable: GET is public and read-only",
    consentInsertion: { symbol: "GET", precedingStage: "The route validates runtime bindings and the single-decoded canonical public slug before its anon client and exact published read.", followingStage: "It serializes normalized article and bounded related rows without a mutation or operational cache write.", refactorRequired: false, smallestFutureRefactor: "No legal-consent guard applies because this public GET has no consent-required mutation, provider fetch, storage signing, or cache persistence." },
    currentExecutionOrder: ["runtime-binding-presence-validation", "single-decode-and-canonical-slug-validation", "construct-anon-key-client-without-bearer-propagation", "exact-published-news-row-database-read", "public-row-url-text-status-category-normalization", "bounded-related-published-news-database-read", "normalized-json-response"],
    evidence: [
      { sourceFile: "src/pages/api/news/[slug].ts", symbol: "GET,createPublicNewsDetailGet,parsePublicNewsSlug", evidenceType: "handler-and-slug-boundary", conciseFinding: "Slug input comes only from params.slug. One safe decode is followed by a 96-character bound, control/slash/backslash rejection, and the exact lower-case alphanumeric-hyphen grammar. Empty, malformed, encoded traversal, repeated-encoding, filter-grammar, whitespace, and noncanonical forms return NEWS_NOT_FOUND before a client or database read; no table, column, filter, sort, RPC, URL, provider, storage key, query, or header input is accepted." },
      { sourceFile: "src/lib/supabase-server.ts,supabase/migrations/20260612_hot_news_mvp.sql", symbol: "createSSRClient,news_articles_select_published_public,news_articles_select_staff_all", evidenceType: "public-authentication-and-rls", conciseFinding: "The route constructs only an anon-key client and never parses or propagates bearer input, so a caller cannot reach the authenticated staff SELECT branch. The public policy permits only status = published while staff access is database-role-derived; no public SELECT policy grants mutation authority." },
      { sourceFile: "src/lib/news.ts", symbol: "getPublicNewsArticleBySlug,listRelatedPublicNews", evidenceType: "database-read-order-and-visibility", conciseFinding: "After slug validation, the first and only target lookup is an exact news_articles slug equality plus status published maybeSingle. A normalized missing-table fallback is fixed local content only. Only a successful normalized article supplies its server-derived category and slug to the later related published-category query, whose limit is independently capped at four." },
      { sourceFile: "src/lib/news.ts", symbol: "normalizePublicNewsArticle,normalizeNewsUrl,isPrivateOrLocalHostname", evidenceType: "public-output-sanitization", conciseFinding: "Article and related output require published status, a valid slug and category, bounded title/summary/content/source text with controls and literal HTML removed, author_id removed, canonical storage references or safe HTTP(S) URLs only, and rejection of javascript, data, credentials, localhost, loopback, private, link-local, and IPv6-loopback URLs." },
      { sourceFile: "src/lib/news-content.ts,src/pages/news/[slug].astro", symbol: "parseNewsContent,parseInlineSegments", evidenceType: "rendering-contract", conciseFinding: "The public page parses returned text into explicit blocks and Astro escapes text. Links are represented as relative or HTTP(S) values rather than trusted HTML; API normalization has already removed literal tags and unsafe article-level URLs." },
      { sourceFile: "src/pages/api/news/[slug].ts,src/lib/news.ts", symbol: "GET,getPublicNewsArticleBySlug,listRelatedPublicNews", evidenceType: "effect-and-ssrf-absence", conciseFinding: "This GET has no provider or image fetch, URL preview, redirect, storage signing, cache/KV/filesystem access, view counter, database write, analytics, audit, notification, email, RPC, R2, or other persistent effect. It returns values only, so no client-selected outbound destination or SSRF-capable path exists." },
      { sourceFile: "scripts/test-public-news-detail-api-safety.mjs", symbol: "public-news-detail-api-safety", evidenceType: "offline-regression", conciseFinding: "The deterministic fake matrix proves canonical and safely decoded published slugs, zero-read denial of malformed/traversal/filter inputs, generic missing and error responses, bearer-agnostic visibility, public fallback/related normalization, and zero real network, database, cache, storage, or write effects." }
    ]
  }
};
export const batch6gTraces = {
  "src/pages/api/users/me/notifications.ts#GET": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    consentInsertionPoint: "not-applicable: GET is read-only",
    consentInsertion: { symbol: "GET", precedingStage: "GET verifies the bearer actor, then rejects noncanonical notification query keys and values before notification reads.", followingStage: "The recipient-bound notification and enrichment reads are normalized into a minimal response; no mutation follows.", refactorRequired: false, smallestFutureRefactor: "No legal-consent guard applies because GET has no consent-required mutation." },
    currentExecutionOrder: ["strict-bearer-parse", "construct-bearer-bound-anon-rls-client", "auth.getUser-token-verification", "derive-verified-recipient", "strict-query-validation", "parallel-recipient-bound-unread-and-notification-reads", "actor-and-post-circle-visibility-enrichment-reads", "comment-post-visibility-enrichment-read", "minimal-sorted-json-response"],
    evidence: [
      { sourceFile: "src/pages/api/users/me/notifications.ts", symbol: "GET,authenticate,getBearerToken,createUserClient", evidenceType: "authentication-and-recipient-binding", conciseFinding: "GET accepts one exact Bearer token, creates only an anon-key client bound to it, verifies auth.getUser(token), validates the resulting UUID actor, and derives the recipient solely from data.user.id. It accepts no query, body, header, or client field for recipient, actor, profile, role, or staff identity; absent or malformed bearer returns before a notification read and no service-role client exists." },
      { sourceFile: "src/pages/api/users/me/notifications.ts", symbol: "parseNotificationQuery,loadNotificationRows,getUnreadCount", evidenceType: "query-bounds-and-rls-scoped-reads", conciseFinding: "After authentication, GET permits only one optional limit and unread_only key. Limit is canonical decimal 1-50 with default 20; unread_only is exact true/false/1/0; duplicate, offset, cursor, type, before/after, sort, recipient, filter grammar, or unknown keys return INVALID_NOTIFICATION_QUERY before reads. Both count and rows bind recipient_id to the verified actor; rows use last_event_at, created_at, then id descending for deterministic output." },
      { sourceFile: "supabase/migrations/20260606_forum_notifications_mvp.sql", symbol: "forum_notifications_select_own,forum_notifications_update_own,forum_notifications_delete_own", evidenceType: "rls-alignment", conciseFinding: "The active notification policies grant authenticated SELECT, UPDATE, and DELETE only where recipient_id = auth.uid(); UPDATE additionally requires the same WITH CHECK. The route uses the bearer-bound RLS client and repeats recipient_id equality for every notification read or mutation, so a client cannot read or mark another recipient's row." },
      { sourceFile: "supabase/migrations/20260518_forum_phase1_schema.sql,supabase/migrations/20260611_forum_permission_lockdown.sql", symbol: "profiles_select_public,circles_select_public,posts_select_published_public,comments_select_public_or_staff", evidenceType: "enrichment-rls-and-runtime-lifecycle-boundary", conciseFinding: "Profile and circle SELECT are public, while current post/comment policies also retain verified-own or staff branches. The notifications route never exposes those broader rows directly: it selects only id, display fields, and target text, then independently requires published moderation-visible post/comment state plus an active canonical-visible exact parent circle before any target-derived title, body, href, or identifier can influence its minimal response." },
      { sourceFile: "src/pages/api/users/me/notifications.ts,src/lib/notifications.ts", symbol: "normalizeNotificationRow,resolveActors,resolvePosts,resolveComments,buildNotificationItem", evidenceType: "payload-allowlist-and-lifecycle-redaction", conciseFinding: "Malformed notification IDs, types, timestamps, and recipient mismatches are dropped. The response allowlists notification id/type/timestamps/read state, fixed message, safe actor display fields, same-origin avatar proxy, internal href, and bounded preview; it omits recipient_id, actor id, raw avatar_url, post_id, comment_id, raw metadata, profile role, safety state, moderation notes, report data, and database fields. Missing actors use the fixed someone fallback. Post and comment text or identifiers reach a link/preview only when exact published moderation-visible post, active canonical-visible circle, and where applicable exact published moderation-visible comment ancestry are read; hidden, deleted, missing, inactive, or mismatched targets reduce to /notifications/ with no preview." },
      { sourceFile: "src/lib/notifications.ts", symbol: "isForumNotificationType,isNotificationResourceId,buildNotificationHref,buildNotificationPreview", evidenceType: "link-and-text-safety", conciseFinding: "Notification types and target UUIDs are allowlisted before a route is built. Non-system links are fixed same-origin /posts/<uuid>/ paths with optional UUID fragment; absolute, protocol-relative, javascript, data, credentialed, traversal, backslash, encoded-separator, control, malformed, or cross-origin values cannot select a destination. Preview text strips tags and controls, collapses whitespace, and is capped at 48 characters; system notifications expose no preview or target link." },
      { sourceFile: "src/pages/api/users/me/notifications.ts", symbol: "GET", evidenceType: "read-only-effect-absence", conciseFinding: "GET first performs the recipient-bound unread count and notification-row reads, then actor/profile, post/circle, and comment reads. It contains no notification read-state update, mark-seen timestamp, view count, cache persistence, insert, delete, RPC mutation, notification generation, audit, analytics, email, R2/storage, network, or external effect. No future consent insertion point applies." },
      { sourceFile: "scripts/test-user-notifications-api-safety.mjs", symbol: "user-notifications-api-safety", evidenceType: "offline-regression", conciseFinding: "The Vite fake-client matrix proves exact bearer parsing, zero-read authentication/query denial, strict bounded query parsing, recipient-only output/mutation scoping, stable ordering, hidden-target redaction, actor and ownership-field omission, unsafe-link rejection, idempotent unread-only mark updates, active RLS policy predicates, and zero real auth/database/network/storage operations." }
    ]
  },
  "src/pages/api/users/me/notifications.ts#PATCH": {
    traceStatus: "complete",
    traceEvidenceComplete: true,
    traceCompleteness: "complete",
    consentInsertionPoint: "PATCH after authenticate and before Content-Type/body parsing or notification update",
    consentInsertion: { symbol: "PATCH", precedingStage: "authenticate verifies the bearer and derives the verified recipient from auth.getUser(token).", followingStage: "PATCH bounds Content-Length, accepts only a strict JSON action, and executes the recipient-scoped unread update.", refactorRequired: false, smallestFutureRefactor: "Await the consent guard directly after authenticate and pass only auth.userId before parsing or update construction." },
    currentExecutionOrder: ["strict-bearer-parse", "construct-bearer-bound-anon-rls-client", "auth.getUser-token-verification", "derive-verified-recipient", "content-length-and-content-type-validation", "strict-action-schema-validation", "recipient-bound-unread-only-notification-update", "response"],
    evidence: [
      { sourceFile: "src/pages/api/users/me/notifications.ts", symbol: "PATCH,authenticate,parseNotificationAction", evidenceType: "authentication-and-schema-validation", conciseFinding: "PATCH authenticates before reading JSON, accepts at most 1024 declared bytes and JSON media type, then permits exactly mark_all_read or mark_read plus a UUID notification_id. Recipient/user/actor/role/status fields, duplicate action fields, malformed JSON, unknown keys, and malformed identifiers return INVALID_ACTION before a write; no request field can replace auth.getUser-derived recipient identity." },
      { sourceFile: "src/pages/api/users/me/notifications.ts", symbol: "PATCH#forum_notifications.update", evidenceType: "mutation-order-and-idempotency", conciseFinding: "The first and only persistent effect is forum_notifications.update after verified authentication and strict payload validation. Both actions bind recipient_id to auth.userId and read_at IS NULL; mark_read then adds the supplied notification id. Cross-recipient or already-read rows match nothing, so repeats do not change timestamps or create records. The route has no target lookup, notification generation, audit, email, cache, RPC, storage, or external effect." },
      { sourceFile: "supabase/migrations/20260606_forum_notifications_mvp.sql", symbol: "forum_notifications_update_own,enforce_forum_notification_read_update", evidenceType: "rls-and-column-guard", conciseFinding: "The active UPDATE policy independently requires recipient_id = auth.uid in USING and WITH CHECK, while the trigger rejects changes other than read_at. Runtime scope and RLS therefore both deny another recipient or any client attempt to change notification ownership, type, target, actor, or metadata." },
      { sourceFile: "scripts/test-user-notifications-api-safety.mjs", symbol: "user-notifications-api-safety", evidenceType: "offline-regression", conciseFinding: "Offline PATCH fakes verify own mark_read and mark_all_read use recipient_id plus unread-only predicates, a payload-supplied other user is rejected with zero mutation calls, and repeat-safe predicates prevent rewriting read rows. No real auth, database, migration, email, storage, or network operation occurs." }
    ]
  }
};
export const batch3gTraces = {
  "src/pages/api/admin/users/[id]/warn.ts#POST": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "POST after requireModerator(request, env) and before request.json()", consentInsertion: { symbol: "POST", precedingStage: "requireModerator(request, env) resolves the verified moderator and bearer-bound RLS client", followingStage: "request.json() parses the reason before target processing", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireModerator and pass auth.user.id to it before request.json()." }, currentExecutionOrder: ["runtime-validation", "route-id-validation", "authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "schema-validation", "self-target-check", "actor-role-read", "target-role-read", "privilege-hierarchy-check", "postgres-read", "safety-state-mutation", "safety-event-write", "notification-rpc", "response"], evidence: [{ sourceFile: "src/pages/api/admin/users/[id]/warn.ts", symbol: "POST", evidenceType: "handler", conciseFinding: "Checks runtime env, non-empty params.id, verified moderator authentication, and a sanitized required reason; it passes only auth.user.id as actor and the route id as target to the helper." }, { sourceFile: "src/pages/api/admin/users/[id]/warn.ts", symbol: "POST", evidenceType: "validation", conciseFinding: "Malformed JSON becomes a missing reason; the route has no UUID, content-type, body-size, rate-limit, or warning-threshold input validation." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Verifies bearer auth, derives authData.user.id, creates the anon-key RLS client, and enforces moderator/admin role before JSON parsing." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "authorization", conciseFinding: "Rejects self-targeting, reads actor and target id,role through the RLS client, and completes the fail-closed hierarchy check before getUserSafetyState or any effect." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "authorizeUserSafetyAction", evidenceType: "privilege-hierarchy", conciseFinding: "Moderators may target only users; administrators may target users or moderators but never administrators; missing and unknown roles deny." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "computeUserSafetyTransition", evidenceType: "state-transition", conciseFinding: "Warning rejects effectively banned or suspended targets, increments warning_count exactly once, decrements reputation by one, leaves strike count unchanged, and has no automatic suspension or ban threshold." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "database-write", conciseFinding: "After authorization and state read, upsertUserSafetyState is the first irreversible effect; insertUserSafetyEvent records the verified actor with event type warning, followed by a user-warned notification RPC attempt." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "idempotency", conciseFinding: "No duplicate or already-warned guard exists; repeated or concurrent warning requests can add warnings, events, and notification attempts, which remains Phase 4B hardening." }, { sourceFile: "src/lib/server/moderation-notifications.server.ts", symbol: "createModerationNotification", evidenceType: "notification", conciseFinding: "Uses the RLS client RPC after the event and swallows notification failure; no email, auth-admin, service-role, R2, cache, or external-provider call exists." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "error-handling", conciseFinding: "Actor or target profile lookup errors return USER_SAFETY_AUTHORIZATION_UNAVAILABLE without raw details, while later safety helper errors can reach the route generic Error.message branch and remain Phase 4B hardening." }, { sourceFile: "src/pages/api/admin/users/[id]/warn.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than POST." }, { sourceFile: "src/pages/api/admin/users/[id]/warn.ts", symbol: "POST", evidenceType: "absence-confirmed", conciseFinding: "No client field can replace the verified actor id or role, and denied paths do not read safety state, mutate data, insert events, notify, email, or invoke external effects." }] },
};
export const batch3fTraces = {
  "src/pages/api/admin/users/[id]/unban.ts#POST": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "POST after requireModerator(request, env) and before request.json()", consentInsertion: { symbol: "POST", precedingStage: "requireModerator(request, env) resolves the verified moderator and bearer-bound RLS client", followingStage: "request.json() parses the optional reason before target processing", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireModerator and pass auth.user.id to it before request.json()." }, currentExecutionOrder: ["runtime-validation", "route-id-validation", "authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "self-target-check", "actor-role-read", "target-role-read", "privilege-hierarchy-check", "postgres-read", "restriction-state-validation", "safety-state-mutation", "safety-event-write", "response"], evidence: [{ sourceFile: "src/pages/api/admin/users/[id]/unban.ts", symbol: "POST", evidenceType: "handler", conciseFinding: "Checks runtime env and non-empty params.id, requires moderator, parses an optional sanitized reason, and passes only auth.user.id as actor plus the route target id to the shared helper." }, { sourceFile: "src/pages/api/admin/users/[id]/unban.ts", symbol: "POST", evidenceType: "validation", conciseFinding: "Malformed JSON becomes an optional null reason; the route has no UUID, content-type, body-size, rate-limit, or action validation." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Verifies bearer auth, derives authData.user.id, creates the anon-key RLS client, and enforces moderator/admin role before JSON parsing." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "authorization", conciseFinding: "Rejects self-targeting, reads actor and target id,role through the RLS client, and completes the fail-closed hierarchy check before getUserSafetyState or any effect." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "authorizeUserSafetyAction", evidenceType: "privilege-hierarchy", conciseFinding: "Moderators may target only users; administrators may target users or moderators but never administrators; missing and unknown roles deny." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "computeUserSafetyTransition", evidenceType: "state-transition", conciseFinding: "Unban rejects a target that is neither currently nor historically banned/suspended, but clears either banned or suspended state; it preserves warning count, strike count, and reputation while clearing restriction fields." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "database-write", conciseFinding: "After authorization and state read, upsertUserSafetyState is the first irreversible effect and insertUserSafetyEvent records the verified actor with event type unban; unban has no notification branch." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "computeUserSafetyTransition", evidenceType: "idempotency", conciseFinding: "Already unrestricted targets return USER_NOT_RESTRICTED before writes; no request idempotency key or transaction protects concurrent retry paths, which remains Phase 4B hardening." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "absence-confirmed", conciseFinding: "No notification, email, auth-admin, service-role, R2, cache, RPC, or external-provider operation occurs, and denied paths do not read safety state or write events." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "error-handling", conciseFinding: "Actor or target profile lookup errors return USER_SAFETY_AUTHORIZATION_UNAVAILABLE without raw details, while later safety helper errors can reach the route generic Error.message branch and remain Phase 4B hardening." }, { sourceFile: "src/pages/api/admin/users/[id]/unban.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than POST." }] },
};
export const batch3eTraces = {
  "src/pages/api/admin/users/[id]/suspend.ts#POST": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "POST after requireModerator(request, env) and before request.json()", consentInsertion: { symbol: "POST", precedingStage: "requireModerator(request, env) resolves the verified moderator and bearer-bound RLS client", followingStage: "request.json() parses reason and until before target processing", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireModerator and pass auth.user.id to it before request.json()." }, currentExecutionOrder: ["runtime-validation", "route-id-validation", "authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "schema-validation", "self-target-check", "actor-role-read", "target-role-read", "privilege-hierarchy-check", "postgres-read", "suspension-deadline-validation", "safety-state-mutation", "safety-event-write", "notification-rpc", "response"], evidence: [{ sourceFile: "src/pages/api/admin/users/[id]/suspend.ts", symbol: "POST", evidenceType: "handler", conciseFinding: "Checks runtime env, non-empty params.id, verified moderator authentication, and a sanitized required reason; it passes only auth.user.id as actor and the route id as target to the helper." }, { sourceFile: "src/pages/api/admin/users/[id]/suspend.ts", symbol: "POST", evidenceType: "validation", conciseFinding: "Malformed JSON becomes a missing reason, and until is forwarded for helper validation; the route has no UUID, content-type, body-size, or rate-limit validation." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Verifies the bearer session, derives authData.user.id, creates the anon-key RLS client, and enforces moderator/admin role before JSON parsing." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "authorization", conciseFinding: "Rejects self-targeting, reads actor and target id,role through the RLS client, and completes the fail-closed hierarchy check before getUserSafetyState or any effect." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "authorizeUserSafetyAction", evidenceType: "privilege-hierarchy", conciseFinding: "Moderators may target only users; administrators may target users or moderators but never administrators; missing and unknown roles deny." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "computeUserSafetyTransition", evidenceType: "validation", conciseFinding: "Rejects currently banned or effectively suspended users, requires a valid future until timestamp, then sets suspended state, records the reason, adds one strike, and decrements reputation." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "database-write", conciseFinding: "After authorization and state read, upsertUserSafetyState is the first irreversible effect; insertUserSafetyEvent records the verified actor next, followed by a user-restricted notification RPC attempt." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "computeUserSafetyTransition", evidenceType: "idempotency", conciseFinding: "A still-effective suspension returns USER_ALREADY_SUSPENDED before writes; no request idempotency key or transaction protects concurrent retry paths, which remains Phase 4B hardening." }, { sourceFile: "src/lib/server/moderation-notifications.server.ts", symbol: "createModerationNotification", evidenceType: "notification", conciseFinding: "Uses the RLS client RPC after the safety event and swallows notification failure; no email, auth-admin, service-role, R2, cache, or external-provider call exists." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "error-handling", conciseFinding: "Actor or target profile lookup errors return USER_SAFETY_AUTHORIZATION_UNAVAILABLE without raw details, while later safety helper errors can reach the route generic Error.message branch and remain Phase 4B hardening." }, { sourceFile: "src/pages/api/admin/users/[id]/suspend.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than POST." }, { sourceFile: "src/pages/api/admin/users/[id]/suspend.ts", symbol: "POST", evidenceType: "absence-confirmed", conciseFinding: "No client field can replace the verified actor id or role, and denied paths do not read safety state, mutate data, insert events, notify, email, or invoke external effects." }] },
};
export const batch3dTraces = {
  "src/pages/api/admin/users/[id]/safety.ts#GET": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", currentExecutionOrder: ["runtime-validation", "route-id-validation", "authenticate", "derive-verified-user", "construct-rls-client", "role-check", "target-profile-read", "parallel-safety-state-and-event-read", "event-actor-profile-read", "response"], evidence: [{ sourceFile: "src/pages/api/admin/users/[id]/safety.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Checks runtime env and a non-empty route id, requires moderator, then reads the target profile before loading safety state and history." }, { sourceFile: "src/pages/api/admin/users/[id]/safety.ts", symbol: "GET", evidenceType: "validation", conciseFinding: "Only rejects an empty id; it has no UUID validation, query filters, cursor, content-type, or body-size handling, and event history uses a fixed helper limit of 100." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Verifies bearer auth, derives authData.user.id, creates the anon-key RLS client, and reads the verified actor profile role before target reads." }, { sourceFile: "src/pages/api/admin/users/[id]/safety.ts", symbol: "GET", evidenceType: "database-read", conciseFinding: "Reads the target id, display fields, role, and created_at, then concurrently reads user_safety_states and user_safety_events ordered created_at descending; actor profiles are read only for distinct event actor ids." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "getUserSafetyState", evidenceType: "read-normalization", conciseFinding: "Returns the normalized state, including safety reason and updated_by fields, and the route falls back to an in-memory default for non-schema state-read errors." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "listUserSafetyEvents", evidenceType: "database-read", conciseFinding: "Bounds history to 100 and returns event actor_id, reason, metadata, and created_at; the route adds each actor profile to the staff response." }, { sourceFile: "src/pages/api/admin/users/[id]/safety.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "The route does not call applyUserSafetyAction or the privilege hierarchy helper because it is read-only; it performs no safety-state update, last-viewed marker, event insertion, notification, email, cache, RPC, external request, or privileged/service-role operation." }, { sourceFile: "src/pages/api/admin/users/[id]/safety.ts", symbol: "GET", evidenceType: "sensitive-response", conciseFinding: "Moderator/admin callers receive safety state including ban_reason and updated_by plus event actor_id, reason, metadata, and actor profiles; no email field is selected." }, { sourceFile: "src/pages/api/admin/users/[id]/safety.ts", symbol: "GET", evidenceType: "error-handling", conciseFinding: "Profile and actor lookup errors are returned as raw error.message, listUserSafetyEvents can throw a raw mapped database message, and the generic catch returns Error.message; these are Phase 4B hardening findings." }, { sourceFile: "src/pages/api/admin/users/[id]/safety.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET." }] },
};
export const batch3cTraces = {
  "src/pages/api/admin/users/[id]/clear-warning.ts#POST": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "POST after requireModerator(request, env) and before request.json()", consentInsertion: { symbol: "POST", precedingStage: "requireModerator(request, env) resolves the verified moderator and bearer-bound RLS client", followingStage: "request.json() parses the optional reason before target processing", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireModerator and pass auth.user.id to it before request.json()." }, currentExecutionOrder: ["runtime-validation", "route-id-validation", "authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "self-target-check", "actor-role-read", "target-role-read", "privilege-hierarchy-check", "postgres-read", "safety-state-mutation", "safety-event-write", "response"], evidence: [{ sourceFile: "src/pages/api/admin/users/[id]/clear-warning.ts", symbol: "POST", evidenceType: "handler", conciseFinding: "Checks runtime env and non-empty params.id, authenticates through requireModerator, parses an optional reason, and passes auth.user.id plus the route target id to applyUserSafetyAction; request data cannot provide actor id or role." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Verifies bearer authentication, derives authData.user.id, creates the anon-key RLS client, and enforces moderator or admin role before JSON parsing." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "authorization", conciseFinding: "Rejects self-targeting, reads actor and target id,role through the RLS client, and runs the fail-closed hierarchy check before getUserSafetyState or any effect." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "authorizeUserSafetyAction", evidenceType: "privilege-hierarchy", conciseFinding: "Moderators may target only users; administrators may target users or moderators but never administrators; missing and unknown roles deny." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "computeUserSafetyTransition", evidenceType: "idempotency", conciseFinding: "Clear warning rejects active suspensions and bans, decrements one warning, and returns changed false with no write when warning_count is zero and status is active; normal repeated clear requests are therefore no-op successes." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "database-write", conciseFinding: "After authorization and state read, upsertUserSafetyState is the first irreversible effect; a changed clear inserts a note user_safety_event with verified actor_id, and clear_warning has no notification branch." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "absence-confirmed", conciseFinding: "Clear warning does not remove separate warning metadata because none exists in user_safety_states, and it does not alter suspended or banned state because those states are rejected before transition." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "error-handling", conciseFinding: "Actor or target profile lookup errors return USER_SAFETY_AUTHORIZATION_UNAVAILABLE without raw details; later safety write errors can reach the route generic Error.message branch and remain Phase 4B hardening." }, { sourceFile: "src/pages/api/admin/users/[id]/clear-warning.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than POST." }, { sourceFile: "src/pages/api/admin/users/[id]/clear-warning.ts", symbol: "POST", evidenceType: "absence-confirmed", conciseFinding: "No service-role or auth-admin client, email, R2, cache, external provider, or notification call exists; authorization-denied and already-cleared paths perform no state or event write." }] },
};
export const batch3bTraces = {
  "src/pages/api/admin/users/[id]/ban.ts#POST": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "POST after requireModerator(request, env) and before request.json()", consentInsertion: { symbol: "POST", precedingStage: "requireModerator(request, env) resolves the verified moderator and bearer-bound RLS client", followingStage: "request.json() parses the reason before target processing", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireModerator and pass auth.user.id to it before request.json()." }, currentExecutionOrder: ["runtime-validation", "route-id-validation", "authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "schema-validation", "self-target-check", "actor-role-read", "target-role-read", "privilege-hierarchy-check", "postgres-read", "postgres-write", "safety-event-write", "notification-rpc", "response"], evidence: [{ sourceFile: "src/pages/api/admin/users/[id]/ban.ts", symbol: "POST", evidenceType: "handler", conciseFinding: "Checks runtime env and non-empty params.id, authenticates through requireModerator, parses only reason, and passes auth.user.id plus the route target id to the shared helper; no body, query, or header value supplies an actor id or role." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Verifies the bearer session with auth.getUser, derives authData.user.id, constructs the anon-key RLS client, and requires moderator or admin role before the route parses JSON." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "authorization", conciseFinding: "Rejects self-targeting, then reads actor and target profiles as id,role through the RLS client before getUserSafetyState or any mutation." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "authorizeUserSafetyAction", evidenceType: "privilege-hierarchy", conciseFinding: "Fails closed for missing or unknown roles; moderators may target only users, while administrators may target users or moderators but never administrators." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "database-write", conciseFinding: "After authorization, reads current safety state; upsertUserSafetyState is the first irreversible effect, followed by insertUserSafetyEvent and a notification RPC attempt for bans." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "computeUserSafetyTransition", evidenceType: "idempotency", conciseFinding: "A current effective banned state returns USER_ALREADY_BANNED before writes; there is no request idempotency key, so concurrent retry protection remains Phase 4B hardening." }, { sourceFile: "src/lib/server/moderation-notifications.server.ts", symbol: "createModerationNotification", evidenceType: "notification", conciseFinding: "The post-event notification uses the same RLS client RPC and swallows its own failure; no email, auth-admin, service-role, R2, cache, or external-provider operation exists." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "error-handling", conciseFinding: "Actor or target profile lookup failures return USER_SAFETY_AUTHORIZATION_UNAVAILABLE without database details, while later safety helper exceptions still reach the route's generic Error.message response and remain Phase 4B hardening." }, { sourceFile: "src/pages/api/admin/users/[id]/ban.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than POST." }, { sourceFile: "src/pages/api/admin/users/[id]/ban.ts", symbol: "POST", evidenceType: "absence-confirmed", conciseFinding: "No privileged client is constructed before authentication, and authorization-denied paths do not call getUserSafetyState, write safety state or events, invoke notifications, or perform email or external effects." }] },
};
export const batch3aTraces = {
  "src/pages/api/admin/users.ts#GET": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", currentExecutionOrder: ["runtime-validation", "authenticate", "derive-verified-user", "construct-rls-client", "role-check", "query-validation", "postgres-read", "postgres-read", "in-memory-normalization", "response"], evidence: [{ sourceFile: "src/pages/api/admin/users.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Checks runtime env, awaits requireModerator before parsing query parameters, lists profiles, then loads safety rows only for the returned profile ids." }, { sourceFile: "src/pages/api/admin/users.ts", symbol: "GET", evidenceType: "validation", conciseFinding: "Trims q and clamps parsed limit to 1 through 200 with a default of 60; it has no cursor or offset pagination, query-length bound, or escaping for q before embedding it in the PostgREST or filter string." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Parses bearer credentials, verifies auth.getUser, derives the actor solely from authData.user.id, constructs the anon-key RLS client, and requires moderator or admin role before user-list reads." }, { sourceFile: "src/pages/api/admin/users.ts", symbol: "GET", evidenceType: "database-read", conciseFinding: "Selects profile identity, public display fields, role, and created_at, then selects safety state by the listed user ids; it does not call Supabase auth-admin APIs, write profiles or safety state, or persist computed effective status." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "createDefaultUserSafetyState", evidenceType: "absence-confirmed", conciseFinding: "Creates a default safety object in memory when no row exists and performs no database, notification, email, cache, telemetry, or external-service operation." }, { sourceFile: "src/pages/api/admin/users.ts", symbol: "GET", evidenceType: "authorization", conciseFinding: "There is no target action, self-target path, role mutation, profile mutation, or privilege-escalation operation in this read-only endpoint; returned profile ids are response data only and cannot replace the verified administrator." }, { sourceFile: "src/pages/api/admin/users.ts", symbol: "GET", evidenceType: "error-handling", conciseFinding: "Returns profile and safety query error.message directly and also exposes caught Error.message through the generic 500 response; no auth-provider admin data, email address, or auth-admin result is read by this route." }, { sourceFile: "src/pages/api/admin/users.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET." }, { sourceFile: "src/pages/api/admin/users.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "No privileged or service-role client is constructed, and no audit, last-viewed, analytics, cache, safety, suspension, ban, notification, email, R2, RPC, or other side effect occurs after authorization." }] },
};
export const batch2hTraces = {
  "src/pages/api/admin/reports/[id]/action.ts#POST": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "POST after requireModerator(request, env) and before request.json()", consentInsertion: { symbol: "POST", precedingStage: "requireModerator(request, env) resolves the verified moderator and RLS client", followingStage: "request.json() parses the action payload before report lookup", refactorRequired: false, smallestFutureRefactor: "Await the consent guard immediately after requireModerator and pass auth.user.id to it before request.json()." }, currentExecutionOrder: ["runtime-validation", "route-id-validation", "authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "action-validation", "postgres-read", "target-read", "action-specific-authorization", "postgres-write", "report-event-write", "notification-rpc", "postgres-read", "response"], evidence: [{ sourceFile: "src/pages/api/admin/reports/[id]/action.ts", symbol: "POST", evidenceType: "handler", conciseFinding: "Checks runtime env and non-empty trimmed params.id, awaits requireModerator, parses JSON, allowlists the action, then passes auth.user.id rather than any payload actor field to applyAdminReportAction." }, { sourceFile: "src/pages/api/admin/reports/[id]/action.ts", symbol: "POST", evidenceType: "validation", conciseFinding: "The route rejects only an empty report id and an action outside ALLOWED_ACTIONS; it does not enforce report-id UUID format, content type, body-size limit, or rate limit, and malformed JSON becomes INVALID_REPORT_ACTION." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Parses a bearer token, verifies auth.getUser, derives authData.user.id, creates an anon-key RLS client, and checks profiles.role for moderator or admin before payload parsing or report reads." }, { sourceFile: "src/lib/server/reports.server.ts", symbol: "applyAdminReportAction", evidenceType: "authorization", conciseFinding: "Fetches the report and its target through the RLS client after moderator authentication; action branching permits all allowlisted actions to a moderator, rejects unsupported circle/user target-action pairs, and never substitutes an actor from request data." }, { sourceFile: "src/lib/server/reports.server.ts", symbol: "applyAdminReportAction", evidenceType: "absence-confirmed", conciseFinding: "It reads report status but contains no already-resolved status gate or report-action idempotency key, so repeated reviewing, dismiss, hide, reject, or warn requests can append report events after successful retries." }, { sourceFile: "src/lib/server/moderation-admin.ts", symbol: "applyModerationAdminAction", evidenceType: "database-write", conciseFinding: "For post/comment hide or reject, it reads target state, updates the target first, then inserts moderation_actions; action insertion failure leaves the target mutation committed before report status/event work." }, { sourceFile: "src/lib/server/reports.server.ts", symbol: "updateReportStatus", evidenceType: "database-write", conciseFinding: "Reviewing and dismiss update reports before inserting their event; successful target or safety actions are followed by the actioned report update and then insertReportEvent, so an event failure can follow a committed report state change." }, { sourceFile: "src/lib/server/user-safety.server.ts", symbol: "applyUserSafetyAction", evidenceType: "database-write", conciseFinding: "For warn, suspend, or ban, it looks up the target profile and safety state, upserts user_safety_states, inserts a user_safety_event, then attempts a notification before the report update and report event; transition conflicts prevent some duplicate suspend/ban writes but warn remains repeatable." }, { sourceFile: "src/lib/server/moderation-notifications.server.ts", symbol: "createModerationNotification", evidenceType: "notification", conciseFinding: "Post/comment target notifications are started with void only after the report event; user safety notifications call insert_forum_notification through the same RLS client and swallow their own RPC failures, with no email or external provider call in this path." }, { sourceFile: "src/pages/api/admin/reports/[id]/action.ts", symbol: "POST", evidenceType: "error-handling", conciseFinding: "Propagates helper error strings in non-ok JSON responses and returns caught Error.message in the generic 500 branch, so dependency or database details are not sanitized at this endpoint." }, { sourceFile: "src/pages/api/admin/reports/[id]/action.ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than POST." }, { sourceFile: "src/pages/api/admin/reports/[id]/action.ts", symbol: "POST", evidenceType: "absence-confirmed", conciseFinding: "No service-role or other privileged client is constructed, and no email, R2, cache, telemetry, or external-service call occurs; all database and notification RPC access uses the authenticated RLS client created after bearer verification." }] },
};
export const batch2gTraces = {
  "src/pages/api/admin/reports/[id].ts#GET": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", currentExecutionOrder: ["runtime-validation", "schema-validation", "authenticate", "derive-verified-user", "construct-rls-client", "role-check", "postgres-read", "response"], evidence: [{ sourceFile: "src/pages/api/admin/reports/[id].ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Checks runtime env, trims params.id, requires moderator, then delegates the response-only detail lookup to fetchAdminReportDetail." }, { sourceFile: "src/pages/api/admin/reports/[id].ts", symbol: "GET", evidenceType: "validation", conciseFinding: "Rejects an empty report id with REPORT_ID_REQUIRED; the source applies no UUID or other format constraint before the lookup." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Parses a bearer token, verifies auth.getUser, derives authData.user.id, constructs the anon-key RLS client, and checks profiles.role before detail queries." }, { sourceFile: "src/lib/server/reports.server.ts", symbol: "fetchAdminReportDetail", evidenceType: "database-read", conciseFinding: "Reads the report, target preview, reporter, open-count, report events, and event actors; its report-events reference is select-only and does not invoke the later action helper or its update/insert paths." }, { sourceFile: "src/pages/api/admin/reports/[id].ts", symbol: "GET", evidenceType: "error-handling", conciseFinding: "Rethrows intentional Response errors but returns Error.message in the generic 500 JSON response, so helper database messages are not sanitized by this endpoint." }, { sourceFile: "src/pages/api/admin/reports/[id].ts", symbol: "ALL", evidenceType: "method-guard", conciseFinding: "Returns 405 for methods other than GET." }, { sourceFile: "src/pages/api/admin/reports/[id].ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "The dynamic id is only a report resource identifier; no privileged client, report event, target, notification, email, cache, telemetry, R2, RPC, or external mutation occurs." }] },
};
export const batch2fTraces = {
  "src/pages/api/admin/reports.ts#GET": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "schema-validation", "postgres-read", "response"], evidence: [{ sourceFile: "src/pages/api/admin/reports.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Requires moderator, bounds limit and allowlists status, target type, reason, and priority filters before queue lookup." }, { sourceFile: "src/lib/server/reports.server.ts", symbol: "fetchAdminReportsQueue", evidenceType: "database-read", conciseFinding: "Builds the admin queue from report and target preview reads; the helper does not change report status or targets on list retrieval." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "verified-user", conciseFinding: "The RLS client and role check derive from the verified bearer session before report reads." }, { sourceFile: "src/pages/api/admin/reports.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "No report event, target mutation, notification, email, external service, cache persistence, or audit write occurs." }] },
};
export const batch2eTraces = {
  "src/pages/api/admin/news.ts#GET": { traceStatus: "complete", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "schema-validation", "postgres-read", "response"], evidence: [{ sourceFile: "src/pages/api/admin/news.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Requires moderator, parses query filters, then delegates to read-only news list/detail helpers." }, { sourceFile: "src/pages/api/admin/news.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "No cache refresh, external news synchronization, publication update, audit write, or other mutation occurs." }] },
  "src/pages/api/admin/news.ts#POST": { traceStatus: "complete", consentInsertionPoint: "POST after requireModerator before request.json", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "schema-validation", "duplicate-check", "postgres-write", "response"], evidence: [{ sourceFile: "src/pages/api/admin/news.ts", symbol: "POST", evidenceType: "validation", conciseFinding: "Validates title, category, status, content length, slug, URLs, and publication timestamp before slug lookup and insert." }, { sourceFile: "src/lib/news.ts", symbol: "buildUniqueNewsSlug", evidenceType: "duplicate-guard", conciseFinding: "Builds a conflict-free slug before inserting the article." }, { sourceFile: "src/pages/api/admin/news.ts", symbol: "POST", evidenceType: "database-write", conciseFinding: "Inserts one news_articles row with verified auth.user.id as author_id." }] },
  "src/pages/api/admin/news.ts#PATCH": { traceStatus: "complete", consentInsertionPoint: "PATCH after requireModerator before request.json", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "schema-validation", "duplicate-check", "postgres-write", "response"], evidence: [{ sourceFile: "src/pages/api/admin/news.ts", symbol: "PATCH", evidenceType: "handler", conciseFinding: "Requires article id and validated payload, derives slug with excludeId, then updates the target row." }, { sourceFile: "src/pages/api/admin/news.ts", symbol: "PATCH", evidenceType: "verified-user", conciseFinding: "author_id is assigned from verified auth.user.id; request payload cannot set the acting identity." }] },
  "src/pages/api/admin/news.ts#DELETE": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "DELETE after requireModerator before query parsing", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "schema-validation", "postgres-write", "response"], evidence: [{ sourceFile: "src/pages/api/admin/news.ts", symbol: "DELETE", evidenceType: "handler", conciseFinding: "Requires moderator, validates non-empty target id, then deletes the selected news_articles row." }, { sourceFile: "src/pages/api/admin/news.ts", symbol: "DELETE", evidenceType: "absence-confirmed", conciseFinding: "No R2 cleanup, external provider, notification, email, cache persistence, or second write exists." }] },
};
export const batch2dTraces = {
  "src/pages/api/admin/moderation/reject.ts#POST": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "POST after requireModerator before request.json", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "schema-validation", "read-resource", "postgres-read", "duplicate-check", "postgres-write", "response"], evidence: [{ sourceFile: "src/pages/api/admin/moderation/reject.ts", symbol: "POST", evidenceType: "handler", conciseFinding: "Requires moderator, validates the action payload, and passes only verified user.id as moderatorId to the shared reject action." }, { sourceFile: "src/lib/server/moderation-admin.ts", symbol: "parseModerationActionPayload", evidenceType: "validation", conciseFinding: "Restricts target type and UUID before resource lookup." }, { sourceFile: "src/lib/server/moderation-admin.ts", symbol: "applyModerationAdminAction", evidenceType: "duplicate-guard", conciseFinding: "Returns alreadyApplied without writes when target already has the desired rejected status." }, { sourceFile: "src/lib/server/moderation-admin.ts", symbol: "applyModerationAdminAction", evidenceType: "database-write", conciseFinding: "Updates the target rejected state first and inserts moderation_actions second; audit insertion can fail after target mutation, a Phase 4B transactional-hardening finding." }, { sourceFile: "src/pages/api/admin/moderation/reject.ts", symbol: "POST", evidenceType: "absence-confirmed", conciseFinding: "No privileged client, notification, email, or external moderation provider call occurs." }] },
};
export const batch2cTraces = {
  "src/pages/api/admin/moderation/queue.ts#GET": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "schema-validation", "postgres-read", "response"], evidence: [{ sourceFile: "src/pages/api/admin/moderation/queue.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Requires moderator, bounds limit to 1 through 120, selects pending moderation posts and comments, then sorts only in memory." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Bearer credentials are verified with auth.getUser and profile role is checked before queue reads." }, { sourceFile: "src/pages/api/admin/moderation/queue.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "Only select queries and in-memory mapping occur; no Postgres/R2 write, notification, email, external moderation call, or telemetry mutation exists." }] },
};
export const batch2bTraces = {
  "src/pages/api/admin/moderation/lexicon-health.ts#GET": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "r2-read", "response"], evidence: [{ sourceFile: "src/pages/api/admin/moderation/lexicon-health.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Requires moderator before loading lexicon health and returns a bounded health summary." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Bearer credentials are verified through auth.getUser and profile role is checked before health loading." }, { sourceFile: "src/lib/moderation/sensitive-lexicon-loader.server.ts", symbol: "getSensitiveLexiconHealth", evidenceType: "r2-read", conciseFinding: "Loads the lexicon through read-only R2 get or local generated file fallback, then calculates counts and hash prefix." }, { sourceFile: "src/lib/moderation/sensitive-lexicon-loader.server.ts", symbol: "loadSensitiveLexicon", evidenceType: "absence-confirmed", conciseFinding: "Caches lexicon data only in process memory; it performs no Postgres, R2 write/delete, RPC mutation, notification, email, or telemetry write." }] },
};
export const batch2aTraces = {
  "src/pages/api/admin/moderation/hide.ts#POST": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "POST after requireModerator before request.json", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "schema-validation", "read-resource", "postgres-read", "duplicate-check", "postgres-write", "response"], evidence: [{ sourceFile: "src/pages/api/admin/moderation/hide.ts", symbol: "POST", evidenceType: "handler", conciseFinding: "Requires moderator, parses and validates target payload, then calls the shared action helper with verified user.id as moderatorId." }, { sourceFile: "src/lib/server/moderation-admin.ts", symbol: "parseModerationActionPayload", evidenceType: "validation", conciseFinding: "Rejects unsupported target types and non-UUID target ids before database access." }, { sourceFile: "src/lib/server/moderation-admin.ts", symbol: "applyModerationAdminAction", evidenceType: "duplicate-guard", conciseFinding: "Reads target state and returns alreadyApplied without writes when hidden status is already present." }, { sourceFile: "src/lib/server/moderation-admin.ts", symbol: "applyModerationAdminAction", evidenceType: "database-write", conciseFinding: "Updates the target before inserting moderation_actions; action insertion failure can follow target mutation and is recorded as a future hardening concern." }, { sourceFile: "src/pages/api/admin/moderation/hide.ts", symbol: "POST", evidenceType: "absence-confirmed", conciseFinding: "No service-role client, email send, or external moderation provider call exists in this path." }] },
};
export const batch1fTraces = {
  "src/pages/api/admin/moderation/approve.ts#POST": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "POST after requireModerator before request.json", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "schema-validation", "read-resource", "postgres-read", "postgres-write", "response"], evidence: [{ sourceFile: "src/pages/api/admin/moderation/approve.ts", symbol: "POST", evidenceType: "handler", conciseFinding: "Requires moderator, parses the action payload, validates it, and passes verified user.id as moderatorId to the action helper." }, { sourceFile: "src/lib/server/moderation-admin.ts", symbol: "parseModerationActionPayload", evidenceType: "validation", conciseFinding: "Restricts target type to post/comment and target id to UUID before any database operation." }, { sourceFile: "src/lib/server/moderation-admin.ts", symbol: "applyModerationAdminAction", evidenceType: "duplicate-guard", conciseFinding: "Reads target state first and returns alreadyApplied when the desired moderation status is already present." }, { sourceFile: "src/lib/server/moderation-admin.ts", symbol: "applyModerationAdminAction", evidenceType: "database-write", conciseFinding: "Updates target moderation fields before inserting the moderation_actions audit row; no email or external service call exists." }] },
};
export const batch1eTraces = {
  "src/pages/api/admin/forum/reports.ts#GET": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "schema-validation", "postgres-read", "response"], evidence: [{ sourceFile: "src/pages/api/admin/forum/reports.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Requires moderator, bounds limit, then reads post reports and related target, circle, and profile records for a response-only report view." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Verified bearer session and moderator/admin role are required before report queries." }, { sourceFile: "src/pages/api/admin/forum/reports.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "Only select queries, maps, and response formatting occur; no report status, event, notification, email, or target mutation exists." }] },
};
export const batch1dTraces = {
  "src/pages/api/admin/forum/posts.ts#GET": { traceStatus: "complete", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "schema-validation", "postgres-read", "response"], evidence: [{ sourceFile: "src/pages/api/admin/forum/posts.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Requires moderator, bounds limit, validates optional focused UUID, then selects posts and report counts." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Verified bearer session and moderator/admin role are required before reads." }, { sourceFile: "src/pages/api/admin/forum/posts.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "No write, media cleanup, email, or external side effect occurs." }] },
  "src/pages/api/admin/forum/posts.ts#PATCH": { traceStatus: "complete", consentInsertionPoint: "PATCH after requireModerator before request.json", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "parse-body", "schema-validation", "postgres-write", "response"], evidence: [{ sourceFile: "src/pages/api/admin/forum/posts.ts", symbol: "PATCH", evidenceType: "handler", conciseFinding: "Requires moderator, validates post UUID and hide or restore action, then updates status by target id." }, { sourceFile: "src/pages/api/admin/forum/posts.ts", symbol: "PATCH", evidenceType: "database-write", conciseFinding: "The status update is the first irreversible side effect after role and payload validation." }] },
  "src/pages/api/admin/forum/posts.ts#DELETE": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "DELETE after requireModerator before post lookup", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "schema-validation", "read-resource", "postgres-read", "postgres-write", "r2-delete", "response"], evidence: [{ sourceFile: "src/pages/api/admin/forum/posts.ts", symbol: "DELETE", evidenceType: "handler", conciseFinding: "Requires moderator, validates target UUID, reads the post, soft-deletes it, then loads related media." }, { sourceFile: "src/lib/server/media-cleanup.ts", symbol: "deletePostMediaObjects", evidenceType: "r2-delete", conciseFinding: "Iterates post media cleanup and deletes managed storage before optionally deleting media rows." }, { sourceFile: "src/pages/api/admin/forum/posts.ts", symbol: "DELETE", evidenceType: "idempotency", conciseFinding: "Already-deleted posts skip the status update; cleanup reports warnings and errors without reversing the post deletion." }] },
};
export const batch1bTraces = {
  "src/pages/api/admin/forum/me.ts#GET": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "response"], evidence: [{ sourceFile: "src/pages/api/admin/forum/me.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Reads runtime env, awaits requireModerator, then returns verified actor and approved profile fields." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Reads bearer credentials and rejects absent or invalid auth.getUser results with 401." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "createUserClient", evidenceType: "verified-user", conciseFinding: "Constructs an RLS-preserving client with the verified bearer session." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "role-check", conciseFinding: "Reads profiles.role by verified authData.user.id and permits moderator or admin only." }, { sourceFile: "src/pages/api/admin/forum/me.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "No database write, R2 operation, email, privileged client, ownership mutation, or external service call occurs." }] },
};
export const batch1cTraces = {
  "src/pages/api/admin/forum/media.ts#GET": { traceStatus: "complete", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "postgres-read", "response"], evidence: [{ sourceFile: "src/pages/api/admin/forum/media.ts", symbol: "GET", evidenceType: "handler", conciseFinding: "Requires moderator before parsing optional list filters and selecting post_media with linked posts and profiles." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "authentication", conciseFinding: "Verifies bearer authentication and derives the acting user from auth.getUser." }, { sourceFile: "src/pages/api/admin/forum/media.ts", symbol: "GET", evidenceType: "validation", conciseFinding: "Bounds limit to 1 through 200 and restricts kind and recent filter values." }, { sourceFile: "src/pages/api/admin/forum/media.ts", symbol: "GET", evidenceType: "absence-confirmed", conciseFinding: "Only select queries and in-memory filtering occur; no storage, database, email, or external mutation is present." }] },
  "src/pages/api/admin/forum/media.ts#DELETE": { traceStatus: "complete", traceEvidenceComplete: true, traceCompleteness: "complete", consentInsertionPoint: "DELETE after requireModerator and before media lookup", currentExecutionOrder: ["authenticate", "derive-verified-user", "construct-rls-client", "role-check", "schema-validation", "read-resource", "postgres-read", "r2-delete", "postgres-write", "response"], evidence: [{ sourceFile: "src/pages/api/admin/forum/media.ts", symbol: "DELETE", evidenceType: "handler", conciseFinding: "Authenticates moderator, validates a UUID media id, reads the target media row, then calls cleanup before deleting the row." }, { sourceFile: "src/lib/server/admin-auth.ts", symbol: "requireModerator", evidenceType: "role-check", conciseFinding: "Verified actor must have moderator or admin role before the media lookup or deletion path." }, { sourceFile: "src/lib/server/media-cleanup.ts", symbol: "deleteMediaObject", evidenceType: "r2-delete", conciseFinding: "Resolves managed storage and may call deleteR2Object or authenticated Supabase storage.remove; already-missing storage is recorded as a warning." }, { sourceFile: "src/pages/api/admin/forum/media.ts", symbol: "DELETE", evidenceType: "database-write", conciseFinding: "Deletes the post_media row only after object cleanup returns ok." }] },
};
const REPRESENTATIVES = new Set(["src/pages/api/forum/comments.ts#POST", "src/pages/api/forum/posts.ts#POST", "src/pages/api/users/me/profile.ts#POST", "src/pages/api/admin/moderation/hide.ts#POST", "src/pages/api/forum/reports.ts#POST"]);
export function classifyApiMethod(sourceFile, method) {
  const id = `${sourceFile}#${method}`;
  const mutation = method !== "GET";
  const category = AUTH_RECOVERY_EXEMPT.has(id) ? "auth-or-recovery-exempt-mutation" : !mutation ? (AUTHENTICATED_READ_ONLY_PREFIXES.some((prefix) => sourceFile.startsWith(prefix)) ? "authenticated-read-only" : "public-read-only") : "authenticated-consent-required-mutation";
  const consentRequired = category === "authenticated-consent-required-mutation";
  return { id, route: sourceFile.replace("src/pages", "").replace(/\.ts$/, "").replaceAll("index", "").replaceAll("[id]", ":id"), category, stateChanging: mutation, authenticated: category !== "public-read-only", currentConsentRequired: consentRequired, phase4IntegrationStatus: consentRequired ? (REPRESENTATIVES.has(id) ? "phase4a2-representative" : "phase4b-pending") : (category.includes("exempt") ? "exempt" : "not-required"), authenticationMechanism: category === "public-read-only" ? "none" : "bearer-session", roleRequirement: sourceFile.includes("/admin/") ? "admin" : "authenticated-user", ownershipRequirement: sourceFile.includes("manage") ? "resource-owner-or-admin" : "route-specific", resourceAuthorizationRequirement: mutation ? "route-specific" : "none", clientUserIdAccepted: false, clientUserIdPurpose: "target-resource-only", verifiedUserSource: category === "public-read-only" ? "not-applicable" : "verified-session", authHelper: sourceFile.includes("admin") ? "requireModerator" : "route-local-or-requireForumUser", rlsUserClientUsed: category !== "public-read-only", privilegedClientUsed: sourceFile.includes("admin") || sourceFile.includes("media/") , privilegedClientType: sourceFile.includes("admin") ? "admin-helper" : "not-used", privilegedClientConstructionStage: sourceFile.includes("admin") ? "after-authentication-and-authorization" : "not-used", postgresRead: true, postgresWrite: mutation, r2Read: sourceFile.includes("media"), r2Write: sourceFile.includes("upload"), r2Delete: method === "DELETE" && sourceFile.includes("media"), emailSent: sourceFile.includes("resend"), moderationServiceCalled: sourceFile.includes("forum") || sourceFile.includes("profile"), otherExternalServiceCalled: false, externalServicesTouched: "repository-specific", externalSideEffectStage: mutation ? "after-authorization" : "not-applicable", contentTypeValidation: mutation ? "helper-enforced" : "not-applicable", bodySizeValidation: mutation ? "platform-limit-only" : "not-applicable", schemaValidation: mutation ? "route-specific" : "not-applicable", rateLimit: mutation ? "route-specific" : "not-applicable", rateLimitIdentity: mutation ? "verified-user-or-ip" : "not-applicable", idempotency: mutation ? "route-specific" : "read-only", duplicateHandling: mutation ? "route-specific" : "not-applicable", currentExecutionOrder: mutation ? "authenticate, authorize, mutate" : "read-only", requiredSecureExecutionOrder: "validate, authenticate, consent, authorize, mutate", consentInsertionPoint: consentRequired ? "after-authentication-before-authorization" : "not-applicable", orderingStatus: consentRequired ? "ready-for-guard" : "not-applicable", orderingFinding: "none", blocksPhase4A2: false, smallestRequiredRefactor: "none", exemptionReason: category.includes("exempt") ? "Consent recording or account-confirmation bootstrap must remain available before current consent." : "", exemptionValidationMechanism: category.includes("exempt") ? "authenticated-session-and-payload-validation" : "not-applicable", bootstrappingLoopReason: category.includes("exempt") ? "Existing consent cannot be required to record current consent or resend confirmation." : "not-applicable" };
}
