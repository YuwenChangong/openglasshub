{
  "migrationPath": "supabase/migrations/20260909120000_device_schema_v1_foundation.sql",
  "normalizedTables": [
    "device_spec_definitions",
    "device_specs",
    "device_sources",
    "device_source_links",
    "device_spec_evidence",
    "catalog_audit_events"
  ],
  "catalogAuthorityTables": [
    "device_spec_definitions",
    "device_specs",
    "device_sources",
    "device_source_links",
    "device_spec_evidence"
  ],
  "auditTable": "catalog_audit_events",
  "definitionSeedCases": [
    { "name": "eye brightness", "keyPattern": "display\\.eye_brightness", "contextPattern": "eye_brightness" },
    { "name": "panel/projector brightness", "keyPattern": "display\\.panel_or_projector_brightness", "contextPattern": "panel_or_projector_brightness" },
    { "name": "native 3dof", "keyPattern": "tracking\\.native_3dof", "contextPattern": "tracking" },
    { "name": "native 6dof", "keyPattern": "tracking\\.native_6dof", "contextPattern": "tracking" },
    { "name": "accessory 3dof", "keyPattern": "tracking\\.accessory_3dof", "contextPattern": "tracking" },
    { "name": "accessory 6dof", "keyPattern": "tracking\\.accessory_6dof", "contextPattern": "tracking" }
  ],
  "syntheticBaselineStatements": [
    "create type public.device_schema_type as enum ('display_ar', 'ai_hud');",
    "create type public.device_presentation_profile as enum ('display', 'ai_camera', 'hud', 'developer');",
    "create type public.device_spec_value_type as enum ('number', 'boolean', 'text', 'json');",
    "create type public.device_spec_state as enum ('KNOWN', 'NOT_DISCLOSED', 'NOT_APPLICABLE', 'CONFLICT', 'UNKNOWN_UNVERIFIED');",
    "create type public.device_spec_confidence as enum ('HIGH', 'MEDIUM_HIGH', 'MEDIUM', 'LOW');",
    "create type public.device_spec_comparison_mode as enum ('higher', 'lower', 'equal_only', 'none');",
    "create type public.device_source_type as enum ('current_official_product_page', 'official_manual', 'official_spec_sheet', 'official_developer_docs', 'official_faq', 'regulatory_document', 'archived_official', 'reputable_secondary');",
    "alter table public.devices add column if not exists generation text;",
    "alter table public.devices add column if not exists schema_type public.device_schema_type;",
    "alter table public.devices add column if not exists device_type text;",
    "alter table public.devices add column if not exists presentation_profile public.device_presentation_profile;",
    "alter table public.devices add column if not exists status text;",
    "alter table public.devices add column if not exists release_date date;",
    "alter table public.devices add column if not exists last_verified_at date;",
    "create or replace function public.is_catalog_admin() returns boolean language sql stable as $$ select public.current_user_role() = 'admin'; $$;",
    "create table if not exists public.device_spec_definitions (id uuid primary key, key text, context text);",
    "create table if not exists public.device_specs (id uuid primary key);",
    "create table if not exists public.device_sources (id uuid primary key);",
    "create table if not exists public.device_source_links (id uuid primary key);",
    "create table if not exists public.device_spec_evidence (id uuid primary key, is_primary boolean, is_conflicting boolean, check (not (is_primary and is_conflicting)));",
    "create table if not exists public.catalog_audit_events (id uuid primary key);",
    "alter table public.device_spec_definitions enable row level security;",
    "alter table public.device_specs enable row level security;",
    "alter table public.device_sources enable row level security;",
    "alter table public.device_source_links enable row level security;",
    "alter table public.device_spec_evidence enable row level security;",
    "alter table public.catalog_audit_events enable row level security;",
    "create policy definitions_admin on public.device_spec_definitions for all to authenticated using ((select public.is_catalog_admin())) with check ((select public.is_catalog_admin()));",
    "create policy specs_admin on public.device_specs for all to authenticated using ((select public.is_catalog_admin())) with check ((select public.is_catalog_admin()));",
    "create policy sources_admin on public.device_sources for all to authenticated using ((select public.is_catalog_admin())) with check ((select public.is_catalog_admin()));",
    "create policy links_admin on public.device_source_links for all to authenticated using ((select public.is_catalog_admin())) with check ((select public.is_catalog_admin()));",
    "create policy evidence_admin on public.device_spec_evidence for all to authenticated using ((select public.is_catalog_admin())) with check ((select public.is_catalog_admin()));",
    "create policy audit_insert on public.catalog_audit_events for insert to authenticated with check ((select public.is_catalog_admin()));",
    "create policy audit_select on public.catalog_audit_events for select to authenticated using ((select public.is_catalog_admin()));",
    "revoke update, delete on table public.catalog_audit_events from public, anon, authenticated;",
    "insert into public.device_spec_definitions (id, key, context) values (gen_random_uuid(), 'display.eye_brightness', 'eye_brightness'), (gen_random_uuid(), 'display.panel_or_projector_brightness', 'panel_or_projector_brightness'), (gen_random_uuid(), 'tracking.native_3dof', 'tracking'), (gen_random_uuid(), 'tracking.native_6dof', 'tracking'), (gen_random_uuid(), 'tracking.accessory_3dof', 'tracking'), (gen_random_uuid(), 'tracking.accessory_6dof', 'tracking');"
  ],
  "syntheticValidCases": [
    { "name": "explicit SELECT ignores nested FOR ALL literal", "appendSql": "create policy quoted_select on public.device_specs for select to authenticated using ('for all' = 'for all');" },
    { "name": "implicit ALL falls back to USING despite fake WITH CHECK literal", "appendSql": "create policy quoted_fallback on public.device_specs to authenticated using ((select public.is_catalog_admin()) and 'with check (true)' <> '');" },
    { "name": "audit followed by unrelated catalog UPDATE policy", "appendSql": "create policy extra_update on public.device_specs for update to authenticated using ((select public.is_catalog_admin())) with check ((select public.is_catalog_admin()));" },
    { "name": "ALTER preserves omitted catalog clauses", "appendSql": "alter policy specs_admin on public.device_specs to authenticated;" },
    { "name": "ALTER restores final guard", "appendSql": "alter policy specs_admin on public.device_specs using (true); alter policy specs_admin on public.device_specs using ((select public.is_catalog_admin()));" },
    { "name": "ALTER preserves audit INSERT guard", "appendSql": "alter policy audit_insert on public.catalog_audit_events to authenticated;" },
    { "name": "ALTER restores audit SELECT guard", "appendSql": "alter policy audit_select on public.catalog_audit_events using (true); alter policy audit_select on public.catalog_audit_events using ((select public.is_catalog_admin()));" },
    { "name": "DROP removes temporary audit mutation policy", "appendSql": "create policy temporary on public.catalog_audit_events for delete to authenticated using ((select public.is_catalog_admin())); drop policy temporary on public.catalog_audit_events;" },
    { "name": "RENAME retains policy expressions", "appendSql": "alter policy specs_admin on public.device_specs rename to renamed; alter policy renamed on public.device_specs to authenticated;" },
    { "name": "guarded catalog SELECT is not a mutation", "appendSql": "create policy staff_read on public.device_specs for select to authenticated using (true);" },
    { "name": "SQL text in literals and comments is inert", "appendSql": "select 'create policy p on public.catalog_audit_events for delete to public using (true);'; -- grant select on public.device_specs to anon;\n/* alter policy specs_admin on public.device_specs using (true); */" }
  ],
  "syntheticWeakCases": [
    { "name": "FOR SELECT literal cannot hide implicit catalog ALL", "appendSql": "create policy quoted_command on public.device_specs to authenticated using ('for select' = 'for select');", "expectedError": "Every all policy on device_specs must bind is_catalog_admin in USING", "exactError": true },
    { "name": "FOR SELECT dollar literal cannot hide implicit catalog ALL", "appendSql": "create policy dollar_command on public.device_specs to authenticated using ($tag$for select$tag$ = $tag$for select$tag$);", "expectedError": "Every all policy on device_specs must bind is_catalog_admin in USING", "exactError": true },
    { "name": "FOR SELECT literal cannot hide implicit audit ALL", "appendSql": "create policy quoted_audit on public.catalog_audit_events to authenticated using ((select public.is_catalog_admin()) and 'for select' <> '');", "expectedError": "Audit policy must not authorize update/delete: quoted_audit", "exactError": true },
    { "name": "WITH CHECK literal cannot hide permissive CREATE check", "appendSql": "create policy quoted_check on public.device_specs for update to authenticated using ((select public.is_catalog_admin()) and 'with check (is_catalog_admin())' <> '') with check (true);", "expectedError": "Every update policy on device_specs must bind is_catalog_admin in WITH CHECK", "exactError": true },
    { "name": "WITH CHECK literal cannot hide permissive ALTER check", "appendSql": "alter policy specs_admin on public.device_specs using ((select public.is_catalog_admin()) and 'with check (is_catalog_admin())' <> '') with check (true);", "expectedError": "Every all policy on device_specs must bind is_catalog_admin in WITH CHECK", "exactError": true },
    { "name": "WITH CHECK dollar literal cannot hide permissive check", "appendSql": "create policy dollar_check on public.device_specs for update to authenticated using ((select public.is_catalog_admin()) and $tag$with check (is_catalog_admin())$tag$ <> '') with check (true);", "expectedError": "Every update policy on device_specs must bind is_catalog_admin in WITH CHECK", "exactError": true },
    { "name": "ALTER removes catalog USING guard", "appendSql": "alter policy specs_admin on public.device_specs using (true);", "expectedError": "Every all policy on device_specs must bind is_catalog_admin in USING" },
    { "name": "ALTER removes catalog WITH CHECK guard", "appendSql": "alter policy specs_admin on public.device_specs with check (true);", "expectedError": "Every all policy on device_specs must bind is_catalog_admin in WITH CHECK" },
    { "name": "ALTER after RENAME removes guard", "appendSql": "alter policy specs_admin on public.device_specs rename to renamed; alter policy renamed on public.device_specs using (true);", "expectedError": "Every all policy on device_specs must bind is_catalog_admin in USING" },
    { "name": "DROP removes required catalog authority", "appendSql": "drop policy links_admin on public.device_source_links;", "expectedError": "Catalog authority requires a mutation policy on device_source_links" },
    { "name": "ALTER removes audit INSERT guard", "appendSql": "alter policy audit_insert on public.catalog_audit_events with check (true);", "expectedError": "Audit insert policy must bind is_catalog_admin in WITH CHECK" },
    { "name": "ALTER removes audit SELECT guard", "appendSql": "alter policy audit_select on public.catalog_audit_events using (true);", "expectedError": "Audit select policy must bind is_catalog_admin in USING" },
    { "name": "ALTER gives normalized SELECT to anon", "appendSql": "alter policy specs_admin on public.device_specs to authenticated, anon;", "expectedError": "Forbidden normalized anon/public all policy: device_specs" },
    { "name": "anon grant without TABLE", "appendSql": "grant select on public.device_specs to anon;", "expectedError": "Forbidden normalized anon/public grant: device_specs" },
    { "name": "public ALL grant", "appendSql": "grant all on device_specs to public;", "expectedError": "Forbidden normalized anon/public grant: device_specs" },
    { "name": "anon SELECT policy modifier", "appendSql": "create policy p on public.device_specs for select to authenticated, anon using (true);", "expectedError": "Forbidden normalized anon/public select policy: device_specs" },
    { "name": "anon ALL policy", "appendSql": "create policy p on public.device_specs for all to public using (true);", "expectedError": "Forbidden normalized anon/public all policy: device_specs" },
    { "name": "anon implicit ALL policy", "appendSql": "create policy p on public.device_specs to anon using (true);", "expectedError": "Forbidden normalized anon/public all policy: device_specs" },
    { "name": "omitted TO defaults to PUBLIC", "appendSql": "create policy p on public.device_specs for select using (true);", "expectedError": "Forbidden normalized anon/public select policy: device_specs" },
    { "name": "audit only UPDATE revoke", "replace": { "from": "revoke update, delete", "to": "revoke update" }, "expectedError": "Audit must explicitly deny delete" },
    { "name": "audit only DELETE revoke", "replace": { "from": "revoke update, delete", "to": "revoke delete" }, "expectedError": "Audit must explicitly deny update" },
    { "name": "audit UPDATE grant", "appendSql": "grant update on table public.catalog_audit_events to authenticated;", "expectedError": "Audit grants must not authorize update/delete" },
    { "name": "audit DELETE grant", "appendSql": "grant delete on public.catalog_audit_events to authenticated;", "expectedError": "Audit grants must not authorize update/delete" },
    { "name": "audit ALL policy", "appendSql": "create policy p on public.catalog_audit_events for all to authenticated using ((select public.is_catalog_admin()));", "expectedError": "Audit policy must not authorize update/delete" },
    { "name": "audit UPDATE policy", "appendSql": "create policy p on public.catalog_audit_events for update to authenticated using ((select public.is_catalog_admin()));", "expectedError": "Audit policy must not authorize update/delete" },
    { "name": "audit DELETE policy", "appendSql": "create policy p on public.catalog_audit_events for delete to authenticated using ((select public.is_catalog_admin()));", "expectedError": "Audit policy must not authorize update/delete" },
    { "name": "audit implicit ALL policy", "appendSql": "create policy p on public.catalog_audit_events to authenticated using ((select public.is_catalog_admin()));", "expectedError": "Audit policy must not authorize update/delete" },
    { "name": "authority in later policy/comment", "replace": { "from": "create policy links_admin on public.device_source_links for all to authenticated using ((select public.is_catalog_admin())) with check ((select public.is_catalog_admin()));", "to": "create policy links_admin on public.device_source_links for all to authenticated using (true); -- is_catalog_admin()\n" }, "expectedError": "Every all policy on device_source_links must bind is_catalog_admin in USING" },
    { "name": "guarded plus permissive mutation policies", "appendSql": "create policy permissive on public.device_specs for update to authenticated using (true);", "expectedError": "Every update policy on device_specs must bind is_catalog_admin in USING" },
    { "name": "missing evidence authority", "appendSql": "alter policy evidence_admin on public.device_spec_evidence using (true);", "expectedError": "Every all policy on device_spec_evidence must bind is_catalog_admin in USING" },
    { "name": "generic evidence check outside body", "replace": { "from": ", check (not (is_primary and is_conflicting))", "to": "" }, "appendSql": "create table public.other (is_primary boolean, is_conflicting boolean, check (not (is_primary and is_conflicting)));", "expectedError": "Evidence must locally prohibit primary and conflicting together" },
    { "name": "evidence columns in another table", "replace": { "from": "id uuid primary key, is_primary boolean, is_conflicting boolean, check (not (is_primary and is_conflicting))", "to": "id uuid primary key" }, "appendSql": "create table public.other (is_primary boolean, is_conflicting boolean, check (not (is_primary and is_conflicting)));", "expectedError": "Evidence must define is_primary in its table body" },
    { "name": "definition keys in comments/later SQL", "replace": { "from": "'display.eye_brightness'", "to": "'display.wrong'" }, "appendSql": "-- insert into public.device_spec_definitions display.eye_brightness eye_brightness\nselect 'display.eye_brightness';", "expectedError": "Definition seed missing key/context: eye brightness" },
    { "name": "definition name only in unrelated INSERT", "replace": { "from": "'display.eye_brightness'", "to": "'display.wrong'" }, "appendSql": "create table public.other (note text); insert into public.other (note) values ('device_spec_definitions display.eye_brightness eye_brightness');", "expectedError": "Definition seed missing key/context: eye brightness" }
  ],
  "required": [
    { "name": "schema type enum", "pattern": "create type public.device_schema_type as enum\\s*\\(\\s*'display_ar'\\s*,\\s*'ai_hud'\\s*\\)", "flags": "is" },
    { "name": "presentation profile enum", "pattern": "create type public.device_presentation_profile as enum\\s*\\(\\s*'display'\\s*,\\s*'ai_camera'\\s*,\\s*'hud'\\s*,\\s*'developer'\\s*\\)", "flags": "is" },
    { "name": "value type enum", "pattern": "create type public.device_spec_value_type as enum\\s*\\(\\s*'number'\\s*,\\s*'boolean'\\s*,\\s*'text'\\s*,\\s*'json'\\s*\\)", "flags": "is" },
    { "name": "state enum", "pattern": "create type public.device_spec_state as enum[\\s\\S]*'KNOWN'[\\s\\S]*'NOT_DISCLOSED'[\\s\\S]*'NOT_APPLICABLE'[\\s\\S]*'CONFLICT'[\\s\\S]*'UNKNOWN_UNVERIFIED'", "flags": "is" },
    { "name": "confidence enum with MEDIUM_HIGH", "pattern": "create type public.device_spec_confidence as enum[\\s\\S]*'HIGH'[\\s\\S]*'MEDIUM_HIGH'[\\s\\S]*'MEDIUM'[\\s\\S]*'LOW'", "flags": "is" },
    { "name": "comparison mode enum", "pattern": "create type public.device_spec_comparison_mode as enum[\\s\\S]*'higher'[\\s\\S]*'lower'[\\s\\S]*'equal_only'[\\s\\S]*'none'", "flags": "is" },
    { "name": "source type enum", "pattern": "create type public.device_source_type as enum[\\s\\S]*'current_official_product_page'[\\s\\S]*'official_manual'[\\s\\S]*'official_spec_sheet'[\\s\\S]*'official_developer_docs'[\\s\\S]*'official_faq'[\\s\\S]*'regulatory_document'[\\s\\S]*'archived_official'[\\s\\S]*'reputable_secondary'", "flags": "is" },
    { "name": "additive device generation", "pattern": "alter table public.devices add column if not exists generation text", "flags": "is" },
    { "name": "additive device schema type", "pattern": "alter table public.devices add column if not exists schema_type public.device_schema_type", "flags": "is" },
    { "name": "additive device type", "pattern": "alter table public.devices add column if not exists device_type text", "flags": "is" },
    { "name": "additive presentation profile", "pattern": "alter table public.devices add column if not exists presentation_profile public.device_presentation_profile", "flags": "is" },
    { "name": "additive status", "pattern": "alter table public.devices add column if not exists status text", "flags": "is" },
    { "name": "additive release date", "pattern": "alter table public.devices add column if not exists release_date date", "flags": "is" },
    { "name": "additive last verified date", "pattern": "alter table public.devices add column if not exists last_verified_at date", "flags": "is" },
    { "name": "definitions table", "pattern": "create table if not exists public.device_spec_definitions", "flags": "is" },
    { "name": "specs table", "pattern": "create table if not exists public.device_specs", "flags": "is" },
    { "name": "sources table", "pattern": "create table if not exists public.device_sources", "flags": "is" },
    { "name": "source links table", "pattern": "create table if not exists public.device_source_links", "flags": "is" },
    { "name": "evidence table", "pattern": "create table if not exists public.device_spec_evidence", "flags": "is" },
    { "name": "append-only audit table", "pattern": "create table if not exists public.catalog_audit_events", "flags": "is" },
    { "name": "catalog admin predicate", "pattern": "create or replace function public\\.is_catalog_admin[\\s\\S]*current_user_role\\(\\s*\\)[\\s\\S]*=\\s*'admin'", "flags": "is" },
    { "name": "normalized definitions RLS", "pattern": "alter table public\\.device_spec_definitions enable row level security", "flags": "is" },
    { "name": "normalized specs RLS", "pattern": "alter table public\\.device_specs enable row level security", "flags": "is" },
    { "name": "normalized sources RLS", "pattern": "alter table public\\.device_sources enable row level security", "flags": "is" },
    { "name": "normalized source links RLS", "pattern": "alter table public\\.device_source_links enable row level security", "flags": "is" },
    { "name": "normalized evidence RLS", "pattern": "alter table public\\.device_spec_evidence enable row level security", "flags": "is" },
    { "name": "audit RLS", "pattern": "alter table public\\.catalog_audit_events enable row level security", "flags": "is" }
  ],
  "forbidden": [
    { "name": "legacy key specs drop", "pattern": "drop\\s+(?:column|table)[^;]*(?:key_specs|full_specs)", "flags": "is" }
  ]
}
