import { randomUUID } from "node:crypto";
import { spawn } from "node:child_process";
import { access, cp, mkdir, mkdtemp, readFile, rename, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { dirname, join, resolve } from "node:path";
import { fileURLToPath, pathToFileURL } from "node:url";
import { createConnection } from "node:net";
import { createClient } from "@supabase/supabase-js";
import { chromium } from "playwright";
import { createMirror, validateMirror, assertLocalTarget } from "./local-supabase-migration-mirror.mjs";
import { runP6bLifecycle } from "./p6b-local-e2e-runner-core.mjs";

const ROOT = resolve(dirname(fileURLToPath(import.meta.url)), "../..");
const READY = "OPENGLASS_HUB_PUBLIC_BETA_P6B_DEVICE_ADMIN_LOCAL_E2E_READY";
const BLOCKED = "OPENGLASS_HUB_PUBLIC_BETA_P6B_BLOCKED";
const SUPABASE_CLI = "supabase@2.115.0";
const NPX = process.platform === "win32" ? "npx.cmd" : "npx";
const EVIDENCE_ROOT = join(tmpdir(), "openglass-hub-p6b-evidence");
const UUID = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
const PORT_NAMES = ["api", "db", "shadow", "studio", "smtp", "analytics", "pooler", "inspector"];
const PORT_PATHS = {
  api: ["api", "port"], db: ["db", "port"], shadow: ["db", "shadow_port"], studio: ["studio", "port"],
  smtp: ["local_smtp", "port"], analytics: ["analytics", "port"], pooler: ["db.pooler", "port"], inspector: ["edge_runtime", "inspector_port"],
};

export function assertOwnedRuntimeRoot({ root, repoSupabase }) {
  const candidate = resolve(root); const forbidden = resolve(repoSupabase);
  const temporaryRoots = [resolve(tmpdir()), resolve("C:/Temp")];
  if (!temporaryRoots.some((temporaryRoot) => candidate === temporaryRoot || candidate.startsWith(`${temporaryRoot}\\`) || candidate.startsWith(`${temporaryRoot}/`)) || candidate === forbidden || candidate.startsWith(`${forbidden}\\`) || candidate.startsWith(`${forbidden}/`)) throw new Error("Runtime root must be an owned temporary directory outside repository supabase.");
  return candidate;
}
function assertRunId(runId) { if (!/^[a-f0-9]{8}$/i.test(runId)) throw new Error("Invalid P6B evidence run id."); return runId; }
export function getDurableEvidencePath(runId) { return join(EVIDENCE_ROOT, assertRunId(runId), "terminal.json"); }
export async function writeFinalTerminal(runId, terminal) { const target = getDurableEvidencePath(runId); await mkdir(dirname(target), { recursive: true }); const temporary = `${target}.${process.pid}.tmp`; await writeFile(temporary, `${JSON.stringify(terminal)}\n`, "utf8"); await rename(temporary, target); return target; }
export async function loadFinalTerminal(runId) { try { return JSON.parse(await readFile(getDurableEvidencePath(runId), "utf8")); } catch (error) { if (error?.code === "ENOENT") return { status: "NOT_FOUND" }; throw error; } }
export function getDurableProgressPath(runId) { return join(EVIDENCE_ROOT, assertRunId(runId), "progress.json"); }
const redactProgress = (value) => JSON.parse(JSON.stringify(value, (key, item) => typeof item === "string" && /(jwt|token|anon.?key|service.?key|password|authorization)/i.test(`${key} ${item}`) ? "[REDACTED]" : item));
export async function writeProgressCheckpoint(runId, progress) { const target = getDurableProgressPath(runId); await mkdir(dirname(target), { recursive: true }); const temporary = `${target}.${process.pid}.tmp`; await writeFile(temporary, `${JSON.stringify(redactProgress({ RUN_ID: assertRunId(runId), ...progress }))}\n`, "utf8"); await rename(temporary, target); return target; }
export async function loadProgressCheckpoint(runId) { try { return JSON.parse(await readFile(getDurableProgressPath(runId), "utf8")); } catch (error) { if (error?.code === "ENOENT") return { status: "NOT_FOUND" }; throw error; } }
export function createUiProgressState({ runId, mode, onUpdate } = {}) { assertRunId(runId); let sequence = 0; let watchdogActive = false; let snapshot = { RUN_ID: runId, mode: mode ?? "FULL", progressSequence: 0, lifecycleStage: "INITIALIZED", uiCurrentCase: null, uiCurrentCasePhase: null, uiLastCompletedCase: null, uiLastProgressSequence: 0, uiLastProgressKind: "INITIALIZED", uiLedgerStarted: false, uiLedgerCompleted: false, observerPendingPromises: 0, observerStaleListeners: 0, observerUnhandledRejections: 0, watchdogActive: false }; let writes = Promise.resolve(); const publish = () => { const safe = redactProgress(snapshot); if (typeof onUpdate === "function") writes = writes.then(() => onUpdate(safe)).catch(() => {}); return safe; }; return { update(entry = {}) { sequence += 1; snapshot = { ...snapshot, ...entry, progressSequence: sequence, uiLastProgressSequence: sequence, uiLastProgressKind: entry.kind ?? entry.uiCurrentCasePhase ?? entry.lifecycleStage ?? "PROGRESS", watchdogActive }; if (entry.uiCurrentCasePhase === "CASE_PASS") snapshot.uiLastCompletedCase = entry.uiCurrentCase; if (entry.lifecycleStage === "UI_LEDGER_STARTED") snapshot.uiLedgerStarted = true; if (entry.lifecycleStage === "UI_LEDGER_COMPLETED") snapshot.uiLedgerCompleted = true; return publish(); }, setWatchdog(active) { watchdogActive = active; snapshot = { ...snapshot, watchdogActive }; return publish(); }, setObserverCounters(counters = {}) { snapshot = { ...snapshot, observerPendingPromises: Number.isInteger(counters.observerPendingPromises) ? counters.observerPendingPromises : 0, observerStaleListeners: Number.isInteger(counters.observerStaleListeners) ? counters.observerStaleListeners : 0, observerUnhandledRejections: Number.isInteger(counters.observerUnhandledRejections) ? counters.observerUnhandledRejections : 0 }; return publish(); }, snapshot() { return redactProgress(snapshot); }, flush() { return writes; } }; }
export async function runUiCaseWithLiveness({ caseId, expected = "local UI contract", action, timeoutMs = 45_000, progress, observerCounters = () => ({}) }) { progress?.update({ uiCurrentCase: caseId, uiCurrentCasePhase: "CASE_ENTER", kind: "CASE_ENTER" }); progress?.update({ uiCurrentCase: caseId, uiCurrentCasePhase: "ACTION", kind: "ACTION" }); progress?.setWatchdog(true); let timer; const actionPromise = Promise.resolve().then(action); actionPromise.catch(() => {}); const timeout = new Promise((_, reject) => { timer = setTimeout(() => { const error = new Error(`UI case liveness timeout: ${caseId}.`); error.caseId = caseId; error.failureClassification = "RUNNER_UI_CASE_LIVENESS_TIMEOUT"; reject(error); }, timeoutMs); }); try { const observed = await Promise.race([actionPromise, timeout]); progress?.update({ uiCurrentCase: caseId, uiCurrentCasePhase: "CASE_FINALIZE", kind: "CASE_FINALIZE" }); progress?.update({ uiCurrentCase: caseId, uiCurrentCasePhase: "CASE_PASS", kind: "CASE_PASS" }); return { caseId, result: "PASS", expected, observed: "PASS", ...observed }; } catch (error) { progress?.setObserverCounters(observerCounters()); progress?.update({ uiCurrentCase: caseId, uiCurrentCasePhase: "CASE_FAIL", kind: "CASE_FAIL" }); return { caseId, result: "FAIL", expected, observed: error.message, failureClassification: error.failureClassification ?? "RUNNER_UI_CASE_ACTION_FAILURE", network: error.network ?? [], progress: progress?.snapshot() }; } finally { clearTimeout(timer); progress?.setWatchdog(false); } }
export async function runLifecycleWithDurableTerminal({ runId, mode = "FULL", operations, config, evidence = [] }) { const progress = createUiProgressState({ runId, mode, onUpdate: (snapshot) => writeProgressCheckpoint(runId, snapshot) }); progress.update({ lifecycleStage: "LIFECYCLE_STARTED" }); const result = await runP6bLifecycle({ operations, evidence, config: { ...config, onProgress: (entry) => progress.update(entry) }, runId }); progress.setObserverCounters({ observerPendingPromises: result.observerPendingPromises, observerStaleListeners: result.observerStaleListeners, observerUnhandledRejections: result.observerUnhandledRejections }); progress.update({ lifecycleStage: "LIFECYCLE_TERMINAL" }); await progress.flush(); await writeFinalTerminal(runId, { ...result, mode, progress: progress.snapshot() }); return result; }
export function supabaseCommandArgs({ root, command }) { return [...command, "--workdir", root]; }
export async function cleanupOwnedRoot({ root, repoSupabase, remove = rm }) { assertOwnedRuntimeRoot({ root, repoSupabase }); await remove(root, { recursive: true, force: true }); }
export function createDisposableTracker(runId) {
  assertRunId(runId);
  const rows = new Map();
  return {
    track(row) { if (!row?.owned || typeof row.id !== "string" || typeof row.slug !== "string" || !row.slug.startsWith(`p6b-${runId}-`)) throw new Error("Disposable device ownership was not proven."); rows.set(row.id, row); return row; },
    has(id) { return rows.has(id); },
    entries() { return [...rows.values()]; },
    remove(id) { if (!rows.has(id)) throw new Error("Refusing cleanup of a non-owned device."); rows.delete(id); },
  };
}
export function collectCurrentRunOwnedCandidates({ runId, canonicalIds = [], ledgers = {} }) {
  assertRunId(runId);
  const canonical = new Set(canonicalIds);
  const candidates = new Map();
  let apiOwnedCandidateCount = 0;
  let uiOwnedCandidateCount = 0;
  let combinedOwnedCandidateCount = 0;
  let rejectedUnprovenCandidateCount = 0;
  for (const [surface, ledger] of Object.entries(ledgers)) {
    const rows = ledger?.tracker?.entries?.();
    if (!Array.isArray(rows)) continue;
    for (const row of rows) {
      if (surface === "api") apiOwnedCandidateCount += 1;
      if (surface === "ui") uiOwnedCandidateCount += 1;
      combinedOwnedCandidateCount += 1;
      if (row?.owned !== true || !UUID.test(row.id) || typeof row.slug !== "string" || !row.slug.startsWith(`p6b-${runId}-`) || canonical.has(row.id)) { rejectedUnprovenCandidateCount += 1; continue; }
      candidates.set(row.id, row);
    }
  }
  return { ownedIds: [...candidates.keys()], evidence: { apiOwnedCandidateCount, uiOwnedCandidateCount, combinedOwnedCandidateCount, deduplicatedOwnedCandidateCount: candidates.size, validatedOwnedCandidateCount: candidates.size, alreadyAbsentOwnedCount: 0, deletedOwnedCount: 0, rejectedUnprovenCandidateCount } };
}
export function createInFlightTracker() { const active = new Set(); return { enter(key) { if (active.has(key)) return false; active.add(key); return true; }, leave(key) { active.delete(key); }, has(key) { return active.has(key); } }; }
export function correlateDeviceSlugSets({ dbSlugs, apiSlugs, domSlugs }) { const source = (values) => (values ?? []).filter((value) => typeof value === "string" && value); const unique = (values) => [...new Set(source(values))].sort(); const diff = (left, right) => left.filter((value) => !right.includes(value)); const bound = (values) => values.slice(0, 8); const dbRaw = source(dbSlugs), apiRaw = source(apiSlugs), domRaw = source(domSlugs), db = unique(dbRaw), api = unique(apiRaw), dom = unique(domRaw); const dbApiMissing = diff(db, api), dbApiUnexpected = diff(api, db), apiDomMissing = diff(api, dom), apiDomUnexpected = diff(dom, api), dbDomMissing = diff(db, dom), dbDomUnexpected = diff(dom, db); const dbApiIdentityMatch = !dbApiMissing.length && !dbApiUnexpected.length && dbRaw.length === db.length && apiRaw.length === api.length, apiDomIdentityMatch = !apiDomMissing.length && !apiDomUnexpected.length && apiRaw.length === api.length && domRaw.length === dom.length, dbDomIdentityMatch = !dbDomMissing.length && !dbDomUnexpected.length && dbRaw.length === db.length && domRaw.length === dom.length; return { dbCount: dbRaw.length, apiCount: apiRaw.length, domCount: domRaw.length, dbSlugSet: bound(db), apiSlugSet: bound(api), domSlugSet: bound(dom), dbSlugSetTruncated: db.length > 8, apiSlugSetTruncated: api.length > 8, domSlugSetTruncated: dom.length > 8, dbApiMissingCount: dbApiMissing.length, dbApiUnexpectedCount: dbApiUnexpected.length, apiDomMissingCount: apiDomMissing.length, apiDomUnexpectedCount: apiDomUnexpected.length, dbDomMissingCount: dbDomMissing.length, dbDomUnexpectedCount: dbDomUnexpected.length, dbApiIdentityMatch, apiDomIdentityMatch, dbDomIdentityMatch, correlationResult: dbApiIdentityMatch && apiDomIdentityMatch && dbDomIdentityMatch ? "PASS" : "FAIL" }; }
export function focusedUiModeConfig() { return { mode: "FOCUSED_UIE2E02", apiRequired: 0, uiRequired: 2, focusedUiOnly: true }; }
export function focusedUiE2e03ModeConfig() { return { mode: "FOCUSED_UIE2E03", apiRequired: 0, uiRequired: 3, focusedUiOnly: true, focusedUiCaseLimit: 3, finalizeOnUiFailure: true }; }
export function focusedUiE2e12ModeConfig() { return { mode: "FOCUSED_UIE2E12", apiRequired: 0, uiRequired: 12, focusedUiOnly: true, focusedUiCaseLimit: 12, finalizeOnUiFailure: true }; }
export function focusedUiE2e13ModeConfig() { return { mode: "FOCUSED_UIE2E13", apiRequired: 0, uiRequired: 13, focusedUiOnly: true, focusedUiCaseLimit: 13, finalizeOnUiFailure: true }; }
export function focusedUiE2e14ModeConfig() { return { mode: "FOCUSED_UIE2E14", apiRequired: 0, uiRequired: 14, focusedUiOnly: true, focusedUiCaseLimit: 14, finalizeOnUiFailure: true }; }
export function focusedUiE2e15ModeConfig() { return { mode: "FOCUSED_UIE2E15", apiRequired: 0, uiRequired: 15, focusedUiOnly: true, focusedUiCaseLimit: 15, finalizeOnUiFailure: true }; }
export function parseRunnerMode(argv = process.argv) { return argv.includes("--forensic-only") ? "FORENSIC_ONLY" : argv.includes("--api-only") ? "API_ONLY" : argv.includes("--focused-uie2e15") ? "FOCUSED_UIE2E15" : argv.includes("--focused-uie2e14") ? "FOCUSED_UIE2E14" : argv.includes("--focused-uie2e13") ? "FOCUSED_UIE2E13" : argv.includes("--focused-uie2e12") ? "FOCUSED_UIE2E12" : argv.includes("--focused-uie2e03") ? "FOCUSED_UIE2E03" : argv.includes("--focused-uie2e02") ? "FOCUSED_UIE2E02" : argv.includes("--ui-only") ? "UI_ONLY" : argv.includes("--full") ? "FULL" : "SMOKE"; }
export function mutationActionLocator(page, name) { return page.getByRole("button", { name, exact: true }); }
export function archiveMutationLocator(page) { return mutationActionLocator(page, "归档"); }
export function createObserverLifecycleTracker() { const pending = new Set(); const listeners = new Set(); let unhandled = 0; const key = (value) => String(value); return { beginPromise(value) { pending.add(key(value)); }, settlePromise(value) { pending.delete(key(value)); }, addListener(value) { listeners.add(key(value)); }, removeListener(value) { listeners.delete(key(value)); }, recordUnhandledRejection() { unhandled += 1; }, snapshot() { return { observerPendingPromises: pending.size, observerStaleListeners: listeners.size, observerUnhandledRejections: unhandled }; } }; }
export function classifyObserverCounterEvidence(terminal) { const names = ["observerPendingPromises", "observerStaleListeners", "observerUnhandledRejections"]; return names.every((name) => Number.isInteger(terminal?.[name]) && terminal[name] >= 0) ? "SCHEMA_ACTIVE" : "PRE_SCHEMA_NOT_RECORDED"; }
export function deviceCreateFormContract() { const fields = { brandKey: "品牌代码", brandName: "品牌名称", name: "设备名称", slug: "链接 slug", shortDescription: "简短描述", longDescription: "详细描述", imageAlt: "图片替代文字", category: "分类", routeLabel: "路由标签", routeDescription: "路由说明" }; return { formSelector: "form.admin-news-form", readyHeading: "新建设备", submitName: "创建草稿", fields: Object.fromEntries(Object.entries(fields).map(([key, label]) => [key, { label, selector: `form.admin-news-form >> label:has-text("${label}") input` }])) }; }
export function evaluateCreateFormState({ caseId = "UIE2E-03", formCount = 0, createHeading = false, scopedBrandKeyCount = 0, requiredFieldsFilled = true } = {}) { if (formCount !== 1) return { caseId, phase: "CREATE_FORM_OPEN", result: "FAIL", failureClassification: "RUNNER_UIE2E03_CREATE_FORM_NOT_OPENED", canSubmit: false, fixtureRegistered: false }; if (!createHeading) return { caseId, phase: "CREATE_FORM_READY", result: "FAIL", failureClassification: "RUNNER_UIE2E03_CREATE_FORM_READINESS", canSubmit: false, fixtureRegistered: false }; if (scopedBrandKeyCount !== 1) return { caseId, phase: "CREATE_FILL", result: "FAIL", failureClassification: "RUNNER_UIE2E03_BRANDKEY_FIELD_CONTRACT", canSubmit: false, fixtureRegistered: false }; return { caseId, phase: "CREATE_FILL", result: "PASS", failureClassification: null, canSubmit: requiredFieldsFilled === true, fixtureRegistered: false }; }
export function evaluateUiE2e02({ session = {}, network = {}, load = {}, dbSlugs = [], apiSlugs = [], domSlugs = [] }) { const correlation = correlateDeviceSlugSets({ dbSlugs, apiSlugs, domSlugs }); const status = network.deviceLoadStatus ?? null; const apiRowCount = Number.isInteger(load.apiRowCount) ? load.apiRowCount : correlation.apiCount; const loadState = load.errorStateVisible || (status !== null && (status < 200 || status >= 300)) ? "LOAD_ERROR" : load.loading === true ? "LOADING" : status === 200 && apiRowCount > 0 && load.cardsPresent === true && !load.emptyStateVisible ? "SUCCESS_WITH_ROWS" : status === 200 && apiRowCount === 0 && load.emptyStateVisible === true && load.cardsPresent !== true ? "SUCCESS_EMPTY" : "LOADING"; let failureClassification = null; if (session.browserSessionEstablished !== true) failureClassification = "RUNNER_UIE2E02_BROWSER_SESSION_NOT_ESTABLISHED"; else if (session.browserSessionLocal !== true || !["moderator", "admin"].includes(session.browserSessionActor) || session.browserSessionUserIdMatchesExpected !== true || session.browserSessionRoleMatchesExpected !== true) failureClassification = "RUNNER_UIE2E02_BROWSER_SESSION_IDENTITY_MISMATCH"; else if (network.deviceLoadRequestObserved !== true || network.deviceLoadMethod !== "GET" || network.deviceLoadPath !== "/api/admin/devices") failureClassification = "RUNNER_UIE2E02_DEVICE_LOAD_REQUEST_NOT_OBSERVED"; else if (network.deviceLoadResponseObserved !== true) failureClassification = "RUNNER_UIE2E02_DEVICE_LOAD_HTTP_FAILURE"; else if (status === 401) failureClassification = "RUNNER_UIE2E02_DEVICE_LOAD_AUTH_FAILURE"; else if (status === 403) failureClassification = "RUNNER_UIE2E02_DEVICE_LOAD_AUTHORIZATION_FAILURE"; else if (status !== 200) failureClassification = "RUNNER_UIE2E02_DEVICE_LOAD_HTTP_FAILURE"; else if (!loadState.startsWith("SUCCESS_")) failureClassification = "RUNNER_UIE2E02_DEVICE_LOAD_SETTLE_DEFECT"; else if (!correlation.dbApiIdentityMatch) failureClassification = "RUNNER_UIE2E02_DB_TO_API_CORRELATION_FAILURE"; else if (!correlation.apiDomIdentityMatch) failureClassification = "RUNNER_UIE2E02_API_TO_DOM_MATERIALIZATION_FAILURE"; else if (!correlation.dbDomIdentityMatch) failureClassification = "RUNNER_UIE2E02_DB_TO_DOM_CORRELATION_FAILURE"; return { browserSessionEstablished: session.browserSessionEstablished === true, browserSessionActor: ["moderator", "admin"].includes(session.browserSessionActor) ? session.browserSessionActor : null, browserSessionLocal: session.browserSessionLocal === true, browserSessionUserIdMatchesExpected: session.browserSessionUserIdMatchesExpected === true, browserSessionRoleMatchesExpected: session.browserSessionRoleMatchesExpected === true, deviceLoadRequestObserved: network.deviceLoadRequestObserved === true, deviceLoadMethod: network.deviceLoadMethod === "GET" ? "GET" : null, deviceLoadPath: network.deviceLoadPath === "/api/admin/devices" ? "/api/admin/devices" : null, deviceLoadResponseObserved: network.deviceLoadResponseObserved === true, deviceLoadStatus: Number.isInteger(status) ? status : null, deviceLoadProductBrowserOriginated: network.deviceLoadProductBrowserOriginated === true, loadState, errorStateVisible: load.errorStateVisible === true, emptyStateVisible: load.emptyStateVisible === true, ...correlation, failureClassification, result: failureClassification ? "FAIL" : "PASS" }; }
export async function observeDeviceLoad({ page, baseUrl, action, timeoutMs = 30_000, observerLifecycle }) { const origin = new URL(baseUrl).origin; const observerKey = `device-load:${Date.now()}`; let timer; let settled = false; let resolveResponse; let rejectResponse; observerLifecycle?.beginPromise(observerKey); const responsePromise = new Promise((resolve, reject) => { resolveResponse = resolve; rejectResponse = reject; }); responsePromise.catch(() => {}); const listener = (response) => { try { const url = new URL(response.url()); if (settled || url.origin !== origin || url.pathname !== "/api/admin/devices" || response.request().method() !== "GET") return; settled = true; resolveResponse(response); } catch (error) { if (!settled) { settled = true; rejectResponse(error); } } }; observerLifecycle?.addListener(observerKey); page.on("response", listener); timer = setTimeout(() => { if (!settled) { settled = true; rejectResponse(new Error("Device load response timeout.")); } }, timeoutMs); const actionPromise = Promise.resolve().then(action); actionPromise.catch(() => {}); try { const [response] = await Promise.all([responsePromise, actionPromise]); const payload = await response.json(); if (!Array.isArray(payload?.devices) || payload.devices.some((device) => typeof device?.slug !== "string" || !device.slug)) throw new Error("Device load response shape did not provide slug identities."); return { deviceLoadRequestObserved: true, deviceLoadMethod: "GET", deviceLoadPath: "/api/admin/devices", deviceLoadResponseObserved: true, deviceLoadStatus: response.status(), deviceLoadProductBrowserOriginated: true, apiSlugs: payload.devices.map((device) => device.slug) }; } finally { if (!settled) { settled = true; rejectResponse(new Error("Device load observer cancelled.")); } clearTimeout(timer); page.off("response", listener); observerLifecycle?.removeListener(observerKey); observerLifecycle?.settlePromise(observerKey); } }
export function browserSupabaseStorageKey(supabaseUrl) { assertLocalTarget(supabaseUrl); return `sb-${new URL(supabaseUrl).hostname.split(".")[0]}-auth-token`; }
export async function createRealBrowserSession({ supabaseUrl, anonKey, actor, clientFactory = createClient }) {
  assertLocalTarget(supabaseUrl);
  if (!anonKey || !actor?.email || !actor?.password || !["moderator", "admin", "user"].includes(actor.role)) throw new Error("Browser session requires a real local Auth actor.");
  const { data, error } = await clientFactory(supabaseUrl, anonKey).auth.signInWithPassword({ email: actor.email, password: actor.password });
  if (error || !data?.session?.access_token || !data.session.refresh_token || !data.session.user?.id) throw new Error("Real local browser Auth sign-in failed.");
  return { actorRole: actor.role, session: data.session, storageKey: browserSupabaseStorageKey(supabaseUrl) };
}
export function browserAuthEvidence({ actorRole, session }) { return { realLocalAuth: !!session?.access_token && !!session?.refresh_token && ["moderator", "admin", "user"].includes(actorRole), actorRole }; }
export async function startBrowserLifecycle({ chromiumImpl = chromium, session, supabaseUrl }) {
  if (!session?.access_token || !session?.refresh_token) throw new Error("Browser launch refused without a genuine local Auth session.");
  const browser = await chromiumImpl.launch({ headless: true }); const context = await browser.newContext();
  await context.addInitScript(({ storageKey, storedSession }) => localStorage.setItem(storageKey, JSON.stringify(storedSession)), { storageKey: browserSupabaseStorageKey(supabaseUrl), storedSession: session });
  const page = await context.newPage(); return { started: true, browser, context, page };
}
export async function closeBrowserLifecycle({ page, context, browser }) { let closed = true; for (const resource of [page, context, browser]) try { await resource?.close?.(); } catch { closed = false; } return closed; }
export function createBrowserNetworkObserver({ baseUrl }) {
  const origin = new URL(baseUrl).origin; let caseId = null; let entries = [];
  return { begin(id) { caseId = id; entries = []; }, observe(response, safeRequest = {}) { const url = new URL(response.url()); if (!caseId || url.origin !== origin || !url.pathname.startsWith("/api/")) return; const request = response.request(); entries.push({ caseId, method: request.method(), pathname: url.pathname, status: response.status(), ...safeRequest }); }, end() { return entries.map((entry) => ({ ...entry })); } };
}
export async function observeUiMutation({ page, observer, caseId, method, action, mutationExpected = true, timeoutMs = 30_000, observerLifecycle, progress, captureRequestIdentity = false }) {
  observer.begin(caseId);
  if (!mutationExpected) { await action(); return { network: observer.end(), observer: { pendingPromises: 0, staleListeners: 0, result: "NO_MUTATION_EXPECTED" } }; }
  const observerKey = `mutation:${caseId}:${Date.now()}`; let timer; let settled = false; let resolveResponse; let rejectResponse; observerLifecycle?.beginPromise(observerKey);
  const responsePromise = new Promise((resolve, reject) => { resolveResponse = resolve; rejectResponse = reject; }); responsePromise.catch(() => {});
  const listener = (response) => { try { const url = new URL(response.url()); const request = response.request(); if (settled || url.pathname !== "/api/admin/devices" || request.method() !== method) return; let safeRequest = {}; if (captureRequestIdentity) { let body = null; try { body = request.postDataJSON(); } catch {} safeRequest = { id: typeof body?.id === "string" ? body.id : null, slug: typeof body?.slug === "string" ? body.slug : null }; } settled = true; observer.observe(response, safeRequest); resolveResponse(response); } catch (error) { if (!settled) { settled = true; rejectResponse(error); } } };
  observerLifecycle?.addListener(observerKey); page.on("response", listener); timer = setTimeout(() => { if (!settled) { settled = true; rejectResponse(new Error(`UI mutation response timeout for ${caseId}.`)); } }, timeoutMs);
  const actionPromise = Promise.resolve().then(action); actionPromise.catch(() => {}); progress?.update({ uiCurrentCase: caseId, uiCurrentCasePhase: "REQUEST_WAIT", kind: "REQUEST_WAIT" }); try { await Promise.all([actionPromise, responsePromise]); return { network: observer.end(), observer: { pendingPromises: 0, staleListeners: 0, result: "PASS" } }; }
  finally { if (!settled) { settled = true; rejectResponse(new Error(`UI mutation observer cancelled for ${caseId}.`)); } clearTimeout(timer); page.off("response", listener); observerLifecycle?.removeListener(observerKey); observerLifecycle?.settlePromise(observerKey); }
}
export function evaluateSameDeviceRace({ fixtureId = null, firstActionDeviceId = null, secondActionDeviceId = null, actionAttempts = 0, observerArmedBeforeFirstAction = false, raceHoldEstablished = false, overlapProven = false, matchingRequests = [], nonmatchingMutationRequestCount = 0, dbEffectCount = 0 } = {}) {
  const requests = Array.isArray(matchingRequests) ? matchingRequests.map((request) => ({ method: request?.method ?? null, pathname: request?.pathname ?? null, id: request?.id ?? null, status: Number.isInteger(request?.status) ? request.status : null })) : [];
  const raceFixtureIdentityMatchesExpected = typeof fixtureId === "string" && fixtureId === firstActionDeviceId && fixtureId === secondActionDeviceId;
  const firstMutationRequestObserved = requests.length > 0; const totalMatchingMutationRequestCount = requests.length; const first = requests[0] ?? null; const firstMutationRequestMatched = first?.method === "PATCH" && first?.pathname === "/api/admin/devices" && first?.id === fixtureId;
  let failureClassification = null;
  if (!raceFixtureIdentityMatchesExpected) failureClassification = "RUNNER_RACE_FIXTURE_IDENTITY_MISMATCH";
  else if (actionAttempts < 2) failureClassification = "RUNNER_RACE_SECOND_ACTION_NOT_PROVEN";
  else if (!observerArmedBeforeFirstAction) failureClassification = "RUNNER_RACE_OBSERVER_ARMING_ORDER_FAILURE";
  else if (!raceHoldEstablished) failureClassification = "RUNNER_RACE_HOLD_NOT_ESTABLISHED";
  else if (!overlapProven) failureClassification = "RUNNER_RACE_OVERLAP_NOT_PROVEN";
  else if (!firstMutationRequestObserved) failureClassification = "RUNNER_RACE_FIRST_REQUEST_NOT_EMITTED";
  else if (!firstMutationRequestMatched) failureClassification = "RUNNER_RACE_FIXTURE_IDENTITY_MISMATCH";
  else if (totalMatchingMutationRequestCount > 1) failureClassification = "RUNNER_RACE_DUPLICATE_MUTATION_DISPATCH";
  else if (!(first.status >= 200 && first.status < 300)) failureClassification = "RUNNER_RACE_HTTP_REJECTION";
  else if (dbEffectCount !== 1) failureClassification = "RUNNER_RACE_DB_EFFECT_COUNT_FAILURE";
  return { result: failureClassification ? "FAIL" : "PASS", failureClassification, raceFixtureId: fixtureId, raceFixtureIdentityMatchesExpected, raceActionType: "PATCH:hidden", sameDeviceActionAttemptCount: actionAttempts, firstActionAttempted: actionAttempts >= 1, secondActionAttempted: actionAttempts >= 2, raceObserverArmedBeforeFirstAction: observerArmedBeforeFirstAction === true, firstMutationRequestObserved, firstMutationRequestMethod: first?.method ?? null, firstMutationRequestPath: first?.pathname ?? null, firstMutationRequestMatched, secondMatchingMutationRequestCount: Math.max(0, totalMatchingMutationRequestCount - 1), totalMatchingMutationRequestCount, firstMutationResponseObserved: first?.status !== null, firstMutationResponseStatus: first?.status ?? null, dbEffectCount, guardSuppressionObserved: totalMatchingMutationRequestCount === 1, raceHoldEstablished: raceHoldEstablished === true, raceHoldReleased: raceHoldEstablished === true, boundedNonmatchingMutationRequestCount: Number.isInteger(nonmatchingMutationRequestCount) && nonmatchingMutationRequestCount >= 0 ? nonmatchingMutationRequestCount : 0, requests };
}
export function evaluateDifferentDeviceIndependence({ deviceAId = null, deviceBId = null, deviceAIdentityMatchesExpected = false, deviceBIdentityMatchesExpected = false, observerArmedBeforeAAction = false, deviceAActionAttempted = false, deviceARequest = null, deviceAHoldEstablished = false, deviceAStillInflightBeforeB = false, deviceBActionControlAvailable = false, deviceBActionAttempted = false, deviceBRequest = null, deviceBDbEffectCount = 0, deviceAHoldReleased = false, nonmatchingMutationRequestCount = 0 } = {}) {
  const safe = (request) => ({ method: typeof request?.method === "string" ? request.method : null, pathname: typeof request?.pathname === "string" ? request.pathname : null, id: typeof request?.id === "string" ? request.id : null, status: Number.isInteger(request?.status) ? request.status : null });
  const a = safe(deviceARequest), b = safe(deviceBRequest); const deviceIdsDistinct = typeof deviceAId === "string" && typeof deviceBId === "string" && deviceAId !== deviceBId;
  const deviceARequestObserved = a.method !== null; const deviceARequestMatched = a.method === "PATCH" && a.pathname === "/api/admin/devices" && a.id === deviceAId;
  const deviceBRequestObserved = b.method !== null; const deviceBRequestMatched = b.method === "PATCH" && b.pathname === "/api/admin/devices" && b.id === deviceBId;
  let failureClassification = null;
  if (!deviceAIdentityMatchesExpected) failureClassification = "RUNNER_DIFF_DEVICE_A_FIXTURE_INVALID";
  else if (!deviceBIdentityMatchesExpected) failureClassification = "RUNNER_DIFF_DEVICE_B_FIXTURE_INVALID";
  else if (!deviceIdsDistinct) failureClassification = "RUNNER_DIFF_DEVICE_IDENTITIES_NOT_DISTINCT";
  else if (!observerArmedBeforeAAction) failureClassification = "RUNNER_DIFF_DEVICE_OBSERVER_NOT_ARMED";
  else if (!deviceAActionAttempted) failureClassification = "RUNNER_DEVICE_A_ACTION_FAILURE";
  else if (!deviceARequestObserved) failureClassification = "RUNNER_DEVICE_A_REQUEST_NOT_EMITTED";
  else if (!deviceARequestMatched) failureClassification = "RUNNER_DEVICE_A_REQUEST_NOT_MATCHED";
  else if (!deviceAHoldEstablished || !deviceAStillInflightBeforeB) failureClassification = "RUNNER_DEVICE_A_HOLD_NOT_ESTABLISHED";
  else if (!deviceBActionControlAvailable) failureClassification = "RUNNER_DEVICE_B_CONTROL_UNAVAILABLE_DURING_A_INFLIGHT";
  else if (!deviceBActionAttempted) failureClassification = "RUNNER_DEVICE_B_ACTION_FAILURE";
  else if (!deviceBRequestObserved) failureClassification = "RUNNER_DEVICE_B_REQUEST_NOT_EMITTED";
  else if (!deviceBRequestMatched) failureClassification = "RUNNER_DEVICE_B_REQUEST_MATCHER_FAILURE";
  else if (!(b.status >= 200 && b.status < 300)) failureClassification = "RUNNER_DEVICE_B_HTTP_REJECTION";
  else if (deviceBDbEffectCount !== 1) failureClassification = "RUNNER_DEVICE_B_DB_EFFECT_FAILURE";
  return { result: failureClassification ? "FAIL" : "PASS", failureClassification, deviceAId, deviceBId, deviceAIdentityMatchesExpected: deviceAIdentityMatchesExpected === true, deviceBIdentityMatchesExpected: deviceBIdentityMatchesExpected === true, deviceIdsDistinct, observerArmedBeforeAAction: observerArmedBeforeAAction === true, deviceAActionAttempted: deviceAActionAttempted === true, deviceARequestObserved, deviceARequestMethod: a.method, deviceARequestPath: a.pathname, deviceARequestMatched, deviceAHoldEstablished: deviceAHoldEstablished === true, deviceAStillInflightBeforeB: deviceAStillInflightBeforeB === true, deviceBActionControlAvailable: deviceBActionControlAvailable === true, deviceBActionAttempted: deviceBActionAttempted === true, deviceBRequestObserved, deviceBRequestMethod: b.method, deviceBRequestPath: b.pathname, deviceBRequestMatched, deviceBRequestIdMatchesExpected: b.id === deviceBId, deviceBResponseObserved: b.status !== null, deviceBResponseStatus: b.status, deviceBDbEffectCount, differentDeviceIndependenceObserved: !failureClassification, deviceAHoldReleased: deviceAHoldReleased === true, boundedNonmatchingMutationRequestCount: Number.isInteger(nonmatchingMutationRequestCount) && nonmatchingMutationRequestCount >= 0 ? nonmatchingMutationRequestCount : 0, requests: [a, b].filter((request) => request.method !== null) };
}
export function evaluateUiE2e15Rejection({ fixtureId = null, fixtureReady = false, fixtureLifecycleReady = fixtureReady, pageReady = false, formOpened = false, formOpenAttempted = formOpened, formReady = false, formHeadingObserved = formReady, brandControlAvailable = false, brandAccessibleLabel = null, requiredFieldsReady = false, observerArmedBeforeAction = false, actionAttempted = false, request = null, response = { observed: false, status: null }, expectedSlug = null, falseSuccessObserved = false, internalErrorLeakObserved = false, rawDbErrorLeakObserved = false, stackLeakObserved = false, secretLeakObserved = false, unexpectedDbEffectCount = 0, recoveryObserved = false, inflightGuardCleared = recoveryObserved } = {}) {
  const safeRequest = { method: typeof request?.method === "string" ? request.method : null, pathname: typeof request?.pathname === "string" ? request.pathname : null, slug: typeof request?.slug === "string" ? request.slug : null };
  const responseObserved = response?.observed === true; const responseStatus = Number.isInteger(response?.status) ? response.status : null;
  const requestObserved = safeRequest.method !== null; const requestSlugMatchesExpected = typeof expectedSlug === "string" && safeRequest.slug === expectedSlug;
  const requestMatched = safeRequest.method === "POST" && safeRequest.pathname === "/api/admin/devices" && requestSlugMatchesExpected;
  let failureClassification = null;
  if (!fixtureReady) failureClassification = "RUNNER_REJECTION_FIXTURE_NOT_READY";
  else if (!pageReady) failureClassification = "RUNNER_REJECTION_PAGE_NOT_READY";
  else if (!formOpened) failureClassification = "RUNNER_REJECTION_FORM_NOT_OPENED";
  else if (!formReady) failureClassification = "RUNNER_REJECTION_FORM_NOT_READY";
  else if (!brandControlAvailable) failureClassification = "RUNNER_REJECTION_BRAND_CONTROL_NOT_AVAILABLE";
  else if (!requiredFieldsReady) failureClassification = "RUNNER_REJECTION_REQUIRED_FIELD_NOT_AVAILABLE";
  else if (!observerArmedBeforeAction || !actionAttempted) failureClassification = "RUNNER_REJECTION_ACTION_FAILURE";
  else if (!requestObserved) failureClassification = "RUNNER_REJECTION_REQUEST_NOT_EMITTED";
  else if (!requestMatched) failureClassification = "RUNNER_REJECTION_REQUEST_NOT_MATCHED";
  else if (!responseObserved) failureClassification = "RUNNER_REJECTION_RESPONSE_TIMEOUT";
  else if (responseStatus === null || responseStatus < 400) failureClassification = "RUNNER_REJECTION_EXPECTED_NON2XX_NOT_OBSERVED";
  else if (falseSuccessObserved) failureClassification = "PRODUCT_REJECTION_FALSE_SUCCESS_DEFECT";
  else if (internalErrorLeakObserved || rawDbErrorLeakObserved || stackLeakObserved || secretLeakObserved) failureClassification = "PRODUCT_REJECTION_INTERNAL_ERROR_LEAK_DEFECT";
  else if (unexpectedDbEffectCount !== 0) failureClassification = "PRODUCT_REJECTION_DB_EFFECT_DEFECT";
  else if (!recoveryObserved || !inflightGuardCleared) failureClassification = "PRODUCT_REJECTION_RECOVERY_DEFECT";
  return { result: failureClassification ? "FAIL" : "PASS", failureClassification, fixtureReady: fixtureReady === true, pageReady: pageReady === true, formOpened: formOpened === true, formReady: formReady === true, brandControlAvailable: brandControlAvailable === true, requiredFieldsReady: requiredFieldsReady === true, observerArmedBeforeAction: observerArmedBeforeAction === true, actionAttempted: actionAttempted === true, requestObserved, requestMethod: safeRequest.method, requestPath: safeRequest.pathname, requestSlugMatchesExpected, requestMatched, responseObserved, responseStatus, expectedNon2xxObserved: responseObserved && responseStatus >= 400, falseSuccessObserved: falseSuccessObserved === true, internalErrorLeakObserved: internalErrorLeakObserved === true, unexpectedDbEffectCount: Number.isInteger(unexpectedDbEffectCount) ? unexpectedDbEffectCount : null, recoveryObserved: recoveryObserved === true, rejectionFixtureId: typeof fixtureId === "string" ? fixtureId : null, rejectionFixtureIdentityMatchesExpected: fixtureReady === true, rejectionFixtureLifecycleReady: fixtureLifecycleReady === true, rejectionPageLoaded: pageReady === true, rejectionFormOpenAttempted: formOpenAttempted === true, rejectionFormObserved: formOpened === true, rejectionFormHeadingObserved: formHeadingObserved === true, rejectionFormReady: formReady === true, rejectionBrandControlObserved: brandControlAvailable === true, rejectionBrandAccessibleLabel: typeof brandAccessibleLabel === "string" ? brandAccessibleLabel : null, rejectionRequiredFieldsReady: requiredFieldsReady === true, rejectionObserverArmedBeforeAction: observerArmedBeforeAction === true, rejectionActionAttempted: actionAttempted === true, rejectionRequestObserved: requestObserved, rejectionRequestMethod: safeRequest.method, rejectionRequestPath: safeRequest.pathname, rejectionRequestIdMatchesExpected: requestSlugMatchesExpected, rejectionRequestMatched: requestMatched, rejectionResponseObserved: responseObserved, rejectionResponseStatus: responseStatus, rejectionResponseMatched: responseObserved && requestMatched, rejectionExpectedNon2xxObserved: responseObserved && responseStatus >= 400, rejectionFalseSuccessObserved: falseSuccessObserved === true, rejectionRawDbErrorLeakObserved: rawDbErrorLeakObserved === true, rejectionStackLeakObserved: stackLeakObserved === true, rejectionSecretLeakObserved: secretLeakObserved === true, rejectionUnexpectedDbEffectCount: Number.isInteger(unexpectedDbEffectCount) ? unexpectedDbEffectCount : null, rejectionRecoveryObserved: recoveryObserved === true, rejectionInflightGuardCleared: inflightGuardCleared === true };
}
export function evaluatePermanentDeleteBoundary({ archived = false, deleteControlPresent = false, confirmationOpen = false, confirmationDeviceId = null, expectedId = confirmationDeviceId, observerArmed = false, request = null, response = { observed: false, status: null }, rowAbsent = false } = {}) {
  const body = request?.body && typeof request.body === "object" ? { id: request.body.id ?? null, confirmPermanentDelete: request.body.confirmPermanentDelete === true } : null;
  const requestObserved = Boolean(request);
  const confirmationIdentityMatches = confirmationDeviceId === expectedId;
  const requestMatched = request?.method === "DELETE" && request?.pathname === "/api/admin/devices" && body?.id === expectedId && body?.confirmPermanentDelete === true;
  const requestDiagnostic = { retained: requestObserved, method: typeof request?.method === "string" ? request.method : null, pathname: typeof request?.pathname === "string" ? request.pathname : null, body };
  let failureClassification = null;
  if (!archived) failureClassification = "RUNNER_DELETE_ARCHIVE_PREREQUISITE_FAILURE";
  else if (!deleteControlPresent) failureClassification = "RUNNER_DELETE_CONTROL_NOT_PRESENT";
  else if (!confirmationOpen) failureClassification = "RUNNER_DELETE_CONFIRMATION_NOT_OPEN";
  else if (!confirmationIdentityMatches) failureClassification = "RUNNER_DELETE_CONFIRMATION_IDENTITY_MISMATCH";
  else if (!observerArmed) failureClassification = "RUNNER_DELETE_OBSERVER_ARMING_ORDER_FAILURE";
  else if (!requestObserved) failureClassification = "RUNNER_DELETE_REQUEST_NOT_EMITTED";
  else if (!requestMatched) failureClassification = "RUNNER_DELETE_REQUEST_MATCHER_FAILURE";
  else if (!response?.observed) failureClassification = "RUNNER_DELETE_RESPONSE_TIMEOUT";
  else if (!(response.status >= 200 && response.status < 300)) failureClassification = "RUNNER_DELETE_HTTP_REJECTION";
  else if (!rowAbsent) failureClassification = "RUNNER_DELETE_DB_REMOVAL_FAILURE";
  return { result: failureClassification ? "FAIL" : "PASS", failureClassification, archiveVerified: archived === true, deleteControlPresent: deleteControlPresent === true, confirmationOpen: confirmationOpen === true, expectedId, confirmationDeviceId, confirmationIdentityMatches, observerArmed: observerArmed === true, confirmActionMatched: confirmationOpen === true && observerArmed === true, requestObserved, requestMatched, requestDiagnostic, responseObserved: response?.observed === true, responseStatus: Number.isInteger(response?.status) ? response.status : null, rowAbsent: rowAbsent === true };
}
export async function observePermanentDelete({ page, observer, caseId, expectedId, action, read, timeoutMs = 30_000, observerLifecycle, progress }) {
  observer.begin(caseId); const observerKey = `permanent-delete:${caseId}:${Date.now()}`; let timer; let settled = false; let requestEvidence = null; let responseEvidence = { observed: false, status: null }; let resolveResponse; observerLifecycle?.beginPromise(observerKey); const responsePromise = new Promise((resolve) => { resolveResponse = resolve; });
  const requestListener = (request) => { try { const url = new URL(request.url()); if (url.pathname !== "/api/admin/devices" || request.method() !== "DELETE") return; let body = null; try { body = request.postDataJSON(); } catch {} requestEvidence = { method: request.method(), pathname: url.pathname, body: body && typeof body === "object" ? { id: body.id ?? null, confirmPermanentDelete: body.confirmPermanentDelete === true } : null }; } catch {} };
  const responseListener = (response) => { try { const request = response.request(); const url = new URL(response.url()); if (settled || url.pathname !== "/api/admin/devices" || request.method() !== "DELETE") return; let body = null; try { body = request.postDataJSON(); } catch {} requestEvidence = { method: request.method(), pathname: url.pathname, body: body && typeof body === "object" ? { id: body.id ?? null, confirmPermanentDelete: body.confirmPermanentDelete === true } : null }; responseEvidence = { observed: true, status: response.status() }; settled = true; observer.observe(response); resolveResponse(); } catch {} };
  observerLifecycle?.addListener(observerKey); page.on("request", requestListener); page.on("response", responseListener); timer = setTimeout(() => { if (!settled) { settled = true; resolveResponse(); } }, timeoutMs); const actionPromise = Promise.resolve().then(action); actionPromise.catch(() => {}); progress?.update({ uiCurrentCase: caseId, uiCurrentCasePhase: "DELETE_REQUEST_WAIT", kind: "DELETE_REQUEST_WAIT" });
  try { await Promise.all([actionPromise, responsePromise]); const rowAbsent = !(await read(expectedId)); return { network: observer.end(), boundary: evaluatePermanentDeleteBoundary({ archived: true, deleteControlPresent: true, confirmationOpen: true, confirmationDeviceId: expectedId, observerArmed: true, request: requestEvidence, response: responseEvidence, rowAbsent }) }; }
  finally { clearTimeout(timer); page.off("request", requestListener); page.off("response", responseListener); observerLifecycle?.removeListener(observerKey); observerLifecycle?.settlePromise(observerKey); }
}
export function assertUiDbVerification({ network, dbVerified }) { if (!Array.isArray(network) || !network.some((entry) => entry.status >= 200 && entry.status < 300) || dbVerified !== true) throw new Error("UI case requires successful real network and DB verification."); return true; }
export function draftSlugCaseEvidence({ slugControlPresent }) { return slugControlPresent ? { result: "PASS" } : { result: "PASS_WITH_FROZEN_UX_CONTRACT", slugControlPresent: false }; }
export function assertServerRejectionEvidence({ network, falseSuccess }) { if (!Array.isArray(network) || !network.some((entry) => entry.status >= 400) || falseSuccess) throw new Error("UI rejection requires real non-2xx evidence without false success."); return true; }
export async function verifyOwnedLocalRuntime({ spawn, owned, statuses, astroOutput = "", remoteConnections = 0 }) {
  const isLocal = (value) => { try { assertLocalTarget(value); return true; } catch { return false; } };
  const runtimeVerification = {
    spawnConfigLocal: Boolean(spawn) && [spawn.url, spawn.publicUrl].every(isLocal),
    supabaseUrlMatchesOwned: spawn?.url === owned?.url,
    anonKeyMatchesOwned: !!spawn?.anonKey && spawn?.anonKey === owned?.anonKey,
    publicUrlMatchesOwned: spawn?.publicUrl === owned?.publicUrl,
    publicAnonKeyMatchesOwned: !!spawn?.publicAnonKey && spawn?.publicAnonKey === owned?.publicAnonKey,
    cloudflareIncludeProcessEnv: spawn?.cloudflareIncludeProcessEnv === true,
    statuses: { unauth: statuses?.unauth ?? null, nonstaff: statuses?.nonstaff ?? null, moderator: statuses?.moderator ?? null, admin: statuses?.admin ?? null },
    remoteConnections: Number.isInteger(remoteConnections) ? remoteConnections : null,
    astroLogSentinelPresent: String(astroOutput).includes("Using secrets defined in process.env"),
    result: "BLOCKED",
  };
  const statusesMatch = runtimeVerification.statuses.unauth === 401 && runtimeVerification.statuses.nonstaff === 403 && runtimeVerification.statuses.moderator === 200 && runtimeVerification.statuses.admin === 200;
  const valid = runtimeVerification.spawnConfigLocal && runtimeVerification.supabaseUrlMatchesOwned && runtimeVerification.anonKeyMatchesOwned && runtimeVerification.publicUrlMatchesOwned && runtimeVerification.publicAnonKeyMatchesOwned && runtimeVerification.cloudflareIncludeProcessEnv && statusesMatch && runtimeVerification.remoteConnections === 0;
  if (!valid) { const error = new Error("Owned-local behavioral runtime verification failed."); error.observations = { runtimeVerification }; throw error; }
  runtimeVerification.result = "PASS";
  return { supabaseUrl: owned.url, publicSupabaseUrl: owned.publicUrl, anonKeyPresent: true, publicAnonKeyPresent: true, observations: { runtimeVerification, P6B_RUNNER2F_RUNTIME_URL_IS_LOCAL: runtimeVerification.spawnConfigLocal, P6B_RUNNER2F_RUNTIME_URL_MATCHES_OWNED: runtimeVerification.supabaseUrlMatchesOwned && runtimeVerification.publicUrlMatchesOwned, P6B_RUNNER2F_RUNTIME_KEYS_MATCH_OWNED: runtimeVerification.anonKeyMatchesOwned && runtimeVerification.publicAnonKeyMatchesOwned, P6B_RUNNER2F_CLOUDFLARE_INCLUDE_PROCESS_ENV: runtimeVerification.cloudflareIncludeProcessEnv, P6B_RUNNER2F_RUNTIME_UNAUTH_STATUS: runtimeVerification.statuses.unauth, P6B_RUNNER2F_RUNTIME_NONSTAFF_STATUS: runtimeVerification.statuses.nonstaff, P6B_RUNNER2F_RUNTIME_MODERATOR_STATUS: runtimeVerification.statuses.moderator, P6B_RUNNER2F_RUNTIME_ADMIN_STATUS: runtimeVerification.statuses.admin, P6B_RUNNER2F_REMOTE_SUPABASE_CONNECTIONS: runtimeVerification.remoteConnections, P6B_RUNNER2F_VERIFY_RUNTIME: "PASS" } };
}
export async function probeOwnedRuntime({ endpoint, actors, tokenFor, request }) {
  const expected = { UNAUTHENTICATED: 401, NONSTAFF: 403, MODERATOR: 200, ADMIN: 200 }; const order = Object.keys(expected); const probes = [];
  const fail = (actor, stage, probe) => { while (probes.length < 4) probes.push({ actor: order[probes.length], requestStarted: false, status: null, expectedStatus: expected[order[probes.length]], result: "NOT_RUN" }); const runtimeProbes = { probes, firstFailureActor: actor, firstFailureStage: stage }; const error = new Error(`Runtime probe ${actor} failed at ${stage}.`); error.observations = { runtimeProbes }; throw error; };
  for (const actor of order) { const probe = { actor, requestStarted: false, targetIsLocal: false, targetMatchesOwnedAstro: true, tokenClass: actor === "UNAUTHENTICATED" ? "NONE" : "REAL_CURRENT_RUN_LOCAL_SUPABASE_AUTH", tokenIssuedByOwnedLocalRuntime: actor === "UNAUTHENTICATED" ? null : false, requestCompleted: false, timedOut: false, networkError: false, networkErrorClass: null, status: null, expectedStatus: expected[actor], statusMatches: false, responseParseAttempted: false, responseParseError: false, safeResponseClassification: "NOT_STARTED", result: "NOT_RUN" }; probes.push(probe); let target; try { target = new URL(endpoint); } catch { fail(actor, "REQUEST_BUILD", probe); } try { probe.targetIsLocal = ["127.0.0.1", "localhost", "::1"].includes(target.hostname); if (!probe.targetIsLocal) fail(actor, "TARGET_VALIDATION", probe); probe.requestStarted = true; const token = actor === "UNAUTHENTICATED" ? null : await tokenFor(actor.toLowerCase()); if (actor !== "UNAUTHENTICATED" && !token) fail(actor, "TOKEN_RESOLUTION", probe); probe.tokenIssuedByOwnedLocalRuntime = actor === "UNAUTHENTICATED" ? null : true; const response = await request(endpoint, { actor: actor.toLowerCase(), token }); probe.requestCompleted = true; probe.status = response.status; probe.responseParseAttempted = true; if (response.parseError) { probe.responseParseError = true; probe.safeResponseClassification = "PARSE_ERROR"; fail(actor, "RESPONSE_PARSE", probe); } probe.safeResponseClassification = "STATUS_ONLY"; probe.statusMatches = probe.status === probe.expectedStatus; if (!probe.statusMatches) fail(actor, "HTTP_STATUS", probe); probe.result = "PASS"; } catch (error) { if (error?.observations) throw error; probe.networkError = true; probe.networkErrorClass = error?.name === "AbortError" ? "TIMEOUT" : "CONNECT"; probe.timedOut = error?.name === "AbortError"; fail(actor, probe.timedOut ? "FETCH_TIMEOUT" : "FETCH_CONNECT", probe); } }
  return { probes, firstFailureActor: null, firstFailureStage: null };
}
const FORENSIC_ID_LIMIT = 8;
function boundedIds(ids) { return { ids: ids.slice(0, FORENSIC_ID_LIMIT), truncated: ids.length > FORENSIC_ID_LIMIT }; }
function boundedText(value) { return typeof value === "string" ? value.slice(0, 500) : null; }
function classifyDeleteAttempt({ error, rowsAffected }) {
  if (!error) return rowsAffected === 0 ? "PASS_ZERO_ROWS" : Number.isInteger(rowsAffected) && rowsAffected > 0 ? "PASS_ROWS_AFFECTED" : "PASS_ROWS_UNKNOWN";
  const message = `${error.message ?? ""} ${error.details ?? ""}`;
  if (/row-level security|rls/i.test(message)) return "RLS_DENIAL";
  if (error.code === "42501") return "SQLSTATE_42501_PERMISSION_DENIED";
  if (typeof error.code === "string" && error.code.startsWith("PGRST")) return "POSTGREST_QUERY_CONTRACT_ERROR";
  return "GENERIC_DELETE_ERROR";
}
function deleteAttempt(ownedId, result, thrown) {
  const error = result?.error ?? thrown;
  const rowsAffected = Number.isInteger(result?.count) ? result.count : null;
  if (!error) return { ownedId, attempted: true, result: "PASS", rowsAffected, errorClassification: classifyDeleteAttempt({ error, rowsAffected }) };
  return { ownedId, attempted: true, result: "FAIL", errorCode: boundedText(error.code), errorMessage: boundedText(error.message), errorDetails: boundedText(error.details), errorHint: boundedText(error.hint), httpStatus: Number.isInteger(error.status) ? error.status : Number.isInteger(error.statusCode) ? error.statusCode : null, rowsAffected, errorClassification: classifyDeleteAttempt({ error, rowsAffected }) };
}
export function postCleanupEvidence(postCleanup) {
  return {
    postCleanup,
    P6B_RUNNER2C3_TOTAL_DEVICE_COUNT: postCleanup.totalDeviceCount,
    P6B_RUNNER2C3_CANONICAL_EXPECTED_COUNT: postCleanup.canonicalExpectedCount,
    P6B_RUNNER2C3_CANONICAL_OBSERVED_COUNT: postCleanup.canonicalObservedCount,
    P6B_RUNNER2C3_CANONICAL_MISSING_COUNT: postCleanup.canonicalMissingCount,
    P6B_RUNNER2C3_OWNED_REMAINING_COUNT: postCleanup.ownedRemainingCount,
    P6B_RUNNER2C3_UNEXPECTED_NONCANONICAL_COUNT: postCleanup.unexpectedNonCanonicalCount,
    P6B_RUNNER2C3_CANONICAL_IDENTITY_MATCHES: postCleanup.canonicalIdentityMatches,
    P6B_RUNNER2C3_DISPOSABLE_CLEANUP_MATCHES: postCleanup.disposableCleanupMatches,
    P6B_RUNNER2C3_FINAL_DATA_GATE: postCleanup.finalDataGate,
    P6B_RUNNER2C5_TRUSTED_PG_CLEANUP: postCleanup.trustedCleanup?.result ?? "NOT_USED",
    P6B_RUNNER2C5_TRUSTED_PG_CANONICAL_OVERLAP_COUNT: postCleanup.trustedCleanup?.canonicalOverlapCount ?? null,
    P6B_RUNNER2C5_TRUSTED_PG_POSTVERIFY_REMAINING: postCleanup.trustedCleanup?.postVerifyRemainingCount ?? null,
    P6B_RUNNER2C5_TRUSTED_PG_TRANSACTION_COMMITTED: postCleanup.trustedCleanup?.transactionCommitted ?? false,
    P6B_RUNNER2C5_CANONICAL_EXPECTED_COUNT: postCleanup.canonicalExpectedCount,
    P6B_RUNNER2C5_CANONICAL_OBSERVED_COUNT: postCleanup.canonicalObservedCount,
    P6B_RUNNER2C5_CANONICAL_MISSING_COUNT: postCleanup.canonicalMissingCount,
    P6B_RUNNER2C5_OWNED_REMAINING_COUNT: postCleanup.ownedRemainingCount,
    P6B_RUNNER2C5_UNEXPECTED_NONCANONICAL_COUNT: postCleanup.unexpectedNonCanonicalCount,
    P6B_RUNNER2C5_CANONICAL_IDENTITY_MATCHES: postCleanup.canonicalIdentityMatches,
    P6B_RUNNER2C5_FINAL_DATA_GATE: postCleanup.finalDataGate,
  };
}
function trustedOwnedCleanupSql(ownedIds) {
  const values = ownedIds.map((id) => `'${id}'::uuid`).join(", ");
  return `begin; delete from public.devices where id in (${values}); select count(*) from public.devices where id in (${values}); commit;`;
}
function safeDiagnostic(value) { const text = boundedText(value); return text && /(password|token|secret|authorization|postgres:\/\/|service.?key|anon.?key)/i.test(text) ? "[REDACTED]" : text; }
function classifyPsqlOutput(value) { const text = String(value ?? "").trim(); if (!text) return "EMPTY"; if (/^\d+$/.test(text)) return "ROWCOUNT_ONLY"; if (/\bBEGIN\b[\s\S]*\bDELETE\s+\d+\b[\s\S]*\bCOMMIT\b/i.test(text)) return "MULTI_STATEMENT_PSQL_OUTPUT"; return "UNPARSEABLE"; }
function parseTrustedPsqlOutput(value) { const lines = String(value ?? "").split(/\r?\n/).map((line) => line.trim()).filter(Boolean); if (lines.length === 1 && /^\d+$/.test(lines[0])) return { deletedRowCount: null, postVerifyRemainingCount: Number(lines[0]), transactionCommitted: true }; const match = lines.length === 4 && lines[0] === "BEGIN" && /^DELETE\s+(\d+)$/.exec(lines[1]) && /^\d+$/.test(lines[2]) && lines[3] === "COMMIT"; if (!match) return null; return { deletedRowCount: Number(/^DELETE\s+(\d+)$/.exec(lines[1])[1]), postVerifyRemainingCount: Number(lines[2]), transactionCommitted: true }; }
function trustedCleanupFailure(evidence, phase, error) {
  evidence.failurePhase = phase; evidence.processExitCode = Number.isInteger(error?.processExitCode) ? error.processExitCode : null; evidence.timedOut = error?.timedOut === true; evidence.stdoutClassification = classifyPsqlOutput(error?.stdout); evidence.stderrClassification = error?.stderr ? "STDERR_PRESENT" : "EMPTY"; evidence.stdoutText = safeDiagnostic(error?.stdout); evidence.stderrText = safeDiagnostic(error?.stderr); evidence.errorCode = safeDiagnostic(error?.code); evidence.errorMessage = safeDiagnostic(error?.message); evidence.result = "BLOCKED";
  const failure = error instanceof Error ? error : new Error("Trusted cleanup failed."); failure.observations = { trustedCleanup: evidence }; throw failure;
}
export async function cleanupOwnedDevicesWithLocalPostgres({ ownedPostgres, ownedIds, canonicalIds, execute }) {
  const targetOwnedLocal = /^[a-f0-9]{12,64}$/i.test(ownedPostgres ?? ""); const overlap = Array.isArray(ownedIds) && Array.isArray(canonicalIds) ? ownedIds.filter((id) => canonicalIds.includes(id)).length : null;
  const evidence = { invocationStarted: true, targetOwnedLocal, validatedOwnedIdCount: Array.isArray(ownedIds) ? ownedIds.length : 0, canonicalOverlapCount: overlap, processExitCode: null, timedOut: false, failurePhase: null, stdoutClassification: "NOT_STARTED", stderrClassification: "NOT_STARTED", stdoutText: null, stderrText: null, errorCode: null, errorMessage: null, deletedRowCount: null, postVerifyRemainingCount: null, transactionCommitted: false, result: "BLOCKED" };
  if (!targetOwnedLocal || typeof execute !== "function") return trustedCleanupFailure(evidence, "TARGET_RESOLUTION", new Error("Trusted cleanup requires an owned local Postgres container."));
  if (!Array.isArray(ownedIds) || !Array.isArray(canonicalIds) || new Set(ownedIds).size !== ownedIds.length || ownedIds.some((id) => !UUID.test(id))) return trustedCleanupFailure(evidence, "OWNERSHIP_VALIDATION", new Error("Trusted cleanup refused unvalidated device IDs."));
  if (overlap !== 0) return trustedCleanupFailure(evidence, "CANONICAL_OVERLAP_GUARD", new Error("Trusted cleanup refused canonical device IDs."));
  if (!ownedIds.length) { evidence.stdoutClassification = "EMPTY"; evidence.stderrClassification = "EMPTY"; evidence.postVerifyRemainingCount = 0; evidence.transactionCommitted = true; evidence.result = "PASS"; const empty = []; empty.trustedCleanup = evidence; return empty; }
  let result;
  try { result = await execute({ containerId: ownedPostgres, sql: trustedOwnedCleanupSql(ownedIds) }); } catch (error) { return trustedCleanupFailure(evidence, error?.timedOut ? "TIMEOUT" : error?.failurePhase ?? (Number.isInteger(error?.processExitCode) ? "PROCESS_EXIT" : "PROCESS_SPAWN"), error); }
  evidence.processExitCode = Number.isInteger(result?.processExitCode) ? result.processExitCode : 0; evidence.stdoutClassification = classifyPsqlOutput(result?.stdout); evidence.stderrClassification = result?.stderr ? "STDERR_PRESENT" : "EMPTY"; evidence.stdoutText = safeDiagnostic(result?.stdout); evidence.stderrText = safeDiagnostic(result?.stderr); const parsed = parseTrustedPsqlOutput(result?.stdout);
  if (!parsed) { const error = new Error("Trusted cleanup output could not establish post-delete verification."); error.stdout = result?.stdout; error.stderr = result?.stderr; return trustedCleanupFailure(evidence, "OUTPUT_PARSE", error); }
  evidence.deletedRowCount = parsed.deletedRowCount; evidence.postVerifyRemainingCount = parsed.postVerifyRemainingCount; if (evidence.postVerifyRemainingCount !== 0) { const error = new Error("Trusted cleanup post-delete verification failed."); error.stdout = result?.stdout; error.stderr = result?.stderr; return trustedCleanupFailure(evidence, "POST_DELETE_VERIFY", error); }
  evidence.transactionCommitted = parsed.transactionCommitted; evidence.result = "PASS"; const attempts = ownedIds.map((ownedId) => ({ ownedId, attempted: true, result: "PASS", rowsAffected: null, errorClassification: "PASS_ROWS_UNKNOWN" })); attempts.trustedCleanup = evidence; return attempts;
}
export async function finalizeDisposableDevices({ db, ownedIds, canonicalIds, cleanupOwned }) {
  const canonical = new Set(canonicalIds); if (!Array.isArray(ownedIds) || ownedIds.some((id) => canonical.has(id))) throw new Error("Refusing disposable cleanup that targets canonical identity.");
  const cleanupOutcome = cleanupOwned ? await cleanupOwned({ ownedIds, canonicalIds }) : null; const deleteAttempts = cleanupOutcome ?? [];
  if (!cleanupOwned) for (const id of ownedIds) { try { deleteAttempts.push(deleteAttempt(id, await db.remove(id))); } catch (error) { deleteAttempts.push(deleteAttempt(id, null, error)); } }
  if (!Array.isArray(deleteAttempts) || deleteAttempts.some((attempt) => !ownedIds.includes(attempt?.ownedId))) throw new Error("Disposable cleanup returned invalid ownership evidence.");
  const deleteFailedIds = deleteAttempts.filter((attempt) => attempt.result === "FAIL").map((attempt) => attempt.ownedId);
  const ids = await db.readAllIds(); const idSet = new Set(ids);
  const canonicalObservedIds = [...canonical].filter((id) => idSet.has(id)); const canonicalMissingIds = [...canonical].filter((id) => !idSet.has(id));
  const ownedRemainingIds = ownedIds.filter((id) => idSet.has(id)); const unexpectedNonCanonicalIds = [...idSet].filter((id) => !canonical.has(id));
  const canonicalIdentityMatches = ids.length === canonical.size && canonical.size === 24 && canonicalMissingIds.length === 0 && unexpectedNonCanonicalIds.length === 0;
  const disposableCleanupMatches = ownedRemainingIds.length === 0;
  const missing = boundedIds(canonicalMissingIds); const remaining = boundedIds(ownedRemainingIds); const unexpected = boundedIds(unexpectedNonCanonicalIds);
  const deleteFailures = boundedIds(deleteFailedIds);
  const postCleanup = { totalDeviceCount: ids.length, canonicalExpectedCount: canonical.size, canonicalObservedCount: canonicalObservedIds.length, canonicalMissingCount: canonicalMissingIds.length, canonicalUnexpectedCount: unexpectedNonCanonicalIds.length, ownedExpectedCleanupCount: ownedIds.length, ownedRemainingCount: ownedRemainingIds.length, unexpectedNonCanonicalCount: unexpectedNonCanonicalIds.length, deleteErrorCount: deleteFailedIds.length, deleteAttemptCount: deleteAttempts.length, deleteAttempts: deleteAttempts.slice(0, FORENSIC_ID_LIMIT), deleteAttemptsTruncated: deleteAttempts.length > FORENSIC_ID_LIMIT, trustedCleanup: cleanupOutcome?.trustedCleanup ?? null, canonicalIdentityMatches, disposableCleanupMatches, finalDataGate: canonicalIdentityMatches && disposableCleanupMatches && deleteFailedIds.length === 0 ? "PASS" : "BLOCKED", canonicalMissingIds: missing.ids, canonicalMissingIdsTruncated: missing.truncated, ownedRemainingIds: remaining.ids, ownedRemainingIdsTruncated: remaining.truncated, unexpectedNonCanonicalIds: unexpected.ids, unexpectedNonCanonicalIdsTruncated: unexpected.truncated, deleteFailedIds: deleteFailures.ids, deleteFailedIdsTruncated: deleteFailures.truncated };
  if (postCleanup.finalDataGate !== "PASS") { const error = new Error("Fresh post-cleanup canonical restoration verification failed."); error.observations = postCleanupEvidence(postCleanup); throw error; }
  return { canonicalDeviceCount: ids.length, canonicalUniqueSlugCount: ids.length, runOwnedDisposableRemaining: ownedRemainingIds.length, canonicalAccidentalMutationCount: canonicalMissingIds.length, canonicalIdentityRestored: canonicalIdentityMatches, postCleanup };
}
export function createRealApiAdapter({ baseUrl, request = fetch, database, tracker }) {
  if (!/^http:\/\/127\.0\.0\.1:\d+$/.test(baseUrl) || !database?.get || !tracker) throw new Error("Real API adapter requires owned local runtime dependencies.");
  return {
    async request({ method, path, actor, body, token }) {
      if (!/^\/api\/admin\/devices$/.test(path)) throw new Error("Unexpected API path.");
      const headers = body === undefined ? {} : { "content-type": "application/json" };
      if (actor !== "unauthenticated") { if (typeof token !== "string" || !token) throw new Error("Missing real local actor token."); headers.authorization = `Bearer ${token}`; }
      const response = await request(`${baseUrl}${path}`, { method, headers, body: body === undefined ? undefined : JSON.stringify(body) });
      const text = await response.text(); let payload; try { payload = text ? JSON.parse(text) : {}; } catch { throw new Error("Local API returned malformed JSON."); }
      return { payload, network: { method, path, status: response.status, actor } };
    },
    async readOwned(id) { if (!tracker.has(id)) throw new Error("Refusing DB read for non-owned disposable device."); return database.get(id); },
    track(row) { return tracker.track({ ...row, owned: true }); },
    remove(id) { tracker.remove(id); },
  };
}
const apiCase = async (caseId, expected, action) => { try { const observed = await action(); return { caseId, result: "PASS", expected, observed: "PASS", ...observed }; } catch (error) { return { caseId, result: "FAIL", expected, observed: error.message, network: error.network ?? [] }; } };
const devicePayload = (runId, name, slug) => ({ brandKey: "p6b", brandName: "P6B", name, ...(slug ? { slug } : {}), shortDescription: "P6B disposable short", longDescription: "P6B disposable long", imageAlt: "P6B disposable", category: "smart_glasses", routeLabel: "P6B", routeDescription: "P6B disposable route" });
async function runRealApiCases({ baseUrl, status, actors, runId }) {
  const tracker = createDisposableTracker(runId); const db = createClient(status.API_URL, status.SERVICE_ROLE_KEY);
  const sessions = {}; for (const actor of ["nonstaff", "moderator"]) { const auth = createClient(status.API_URL, status.ANON_KEY); const { data, error } = await auth.auth.signInWithPassword({ email: actors[actor].email, password: actors[actor].password }); if (error || !data.session) throw new Error(`Local ${actor} sign-in failed.`); sessions[actor] = data.session.access_token; }
  const adapter = createRealApiAdapter({ baseUrl, tracker, database: { get: async (id) => { const { data, error } = await db.from("devices").select("id,slug,publication_status,slug_locked,short_description").eq("id", id).maybeSingle(); if (error) throw error; return data; } } });
  const request = async (method, actor, body) => adapter.request({ method, path: "/api/admin/devices", actor, token: sessions[actor], body });
  const expectStatus = async (response, expected) => { if (response.network.status !== expected) { const error = new Error(`Expected HTTP ${expected}, got ${response.network.status}.`); error.network = [response.network]; throw error; } return response; };
  const cases = []; const mainSlug = `p6b-${runId}-api-main`; let main; let conflict;
  cases.push(await apiCase("APIINT-01", "GET unauthenticated returns 401", async () => { const r = await request("GET", "unauthenticated"); await expectStatus(r, 401); return { network: [r.network] }; }));
  cases.push(await apiCase("APIINT-02", "GET nonstaff returns 403", async () => { const r = await request("GET", "nonstaff"); await expectStatus(r, 403); return { network: [r.network] }; }));
  cases.push(await apiCase("APIINT-03", "staff GET correlates with local database", async () => { const r = await request("GET", "moderator"); await expectStatus(r, 200); if (!Array.isArray(r.payload.devices) || r.payload.devices.length !== 24) throw new Error("Staff list did not match canonical local device count."); return { network: [r.network], dbEffectCount: 0 }; }));
  cases.push(await apiCase("APIINT-04", "POST draft persists owned row", async () => { const r = await request("POST", "moderator", devicePayload(runId, "P6B API Main", mainSlug)); await expectStatus(r, 201); main = adapter.track({ id: r.payload.device?.id, slug: r.payload.device?.slug, owned: true }); if (!(await adapter.readOwned(main.id))) throw new Error("Created draft missing from local DB."); return { network: [r.network], device: "owned-main", dbEffectCount: 1 }; }));
  cases.push(await apiCase("APIINT-05", "POST omitted slug generates unique persisted slug", async () => { const r = await request("POST", "moderator", devicePayload(runId, `P6B ${runId} API Auto`)); await expectStatus(r, 201); const row = adapter.track({ id: r.payload.device?.id, slug: r.payload.device?.slug, owned: true }); if (!/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(row.slug) || !(await adapter.readOwned(row.id))) throw new Error("Generated slug contract failed."); return { network: [r.network], device: "owned-auto", dbEffectCount: 1 }; }));
  cases.push(await apiCase("APIINT-06", "direct publish creation is rejected", async () => { const r = await request("POST", "moderator", { ...devicePayload(runId, "P6B Direct Publish", `p6b-${runId}-direct`), publicationStatus: "published" }); if (r.network.status < 400) throw new Error("Direct publish was accepted."); return { network: [r.network] }; }));
  cases.push(await apiCase("APIINT-07", "unknown field is rejected", async () => { const r = await request("POST", "moderator", { ...devicePayload(runId, "P6B Unknown", `p6b-${runId}-unknown`), arbitrary: "x" }); if (r.network.status < 400) throw new Error("Unknown field was accepted."); return { network: [r.network] }; }));
  cases.push(await apiCase("APIINT-08", "PATCH content persists", async () => { const r = await request("PATCH", "moderator", { id: main.id, shortDescription: "P6B updated" }); await expectStatus(r, 200); if ((await adapter.readOwned(main.id))?.short_description !== "P6B updated") throw new Error("PATCH content did not persist."); return { network: [r.network], device: "owned-main", dbEffectCount: 1 }; }));
  cases.push(await apiCase("APIINT-09", "pre-publish slug change persists", async () => { const slug = `p6b-${runId}-api-main-renamed`; const r = await request("PATCH", "moderator", { id: main.id, slug }); await expectStatus(r, 200); main.slug = slug; if ((await adapter.readOwned(main.id))?.slug !== slug) throw new Error("Draft slug did not persist."); return { network: [r.network], device: "owned-main", dbEffectCount: 1 }; }));
  cases.push(await apiCase("APIINT-10", "first publish locks slug", async () => { const r = await request("PATCH", "moderator", { id: main.id, publicationStatus: "published" }); await expectStatus(r, 200); const row = await adapter.readOwned(main.id); if (row?.publication_status !== "published" || !row.slug_locked) throw new Error("Publish lock did not persist."); return { network: [r.network], device: "owned-main", dbEffectCount: 1 }; }));
  cases.push(await apiCase("APIINT-11", "locked slug mutation is rejected", async () => { const r = await request("PATCH", "moderator", { id: main.id, slug: `p6b-${runId}-forbidden` }); if (r.network.status < 400 || (await adapter.readOwned(main.id))?.slug !== main.slug) throw new Error("Locked slug changed."); return { network: [r.network], device: "owned-main" }; }));
  cases.push(await apiCase("APIINT-12", "published hidden published retains lock", async () => { for (const publicationStatus of ["hidden", "published"]) { const r = await request("PATCH", "moderator", { id: main.id, publicationStatus }); await expectStatus(r, 200); } const row = await adapter.readOwned(main.id); if (row?.publication_status !== "published" || !row.slug_locked) throw new Error("Lifecycle lock contract failed."); return { device: "owned-main", dbEffectCount: 2 }; }));
  cases.push(await apiCase("APIINT-13", "archive persists", async () => { const r = await request("PATCH", "moderator", { id: main.id, publicationStatus: "archived" }); await expectStatus(r, 200); if ((await adapter.readOwned(main.id))?.publication_status !== "archived") throw new Error("Archive did not persist."); return { network: [r.network], device: "owned-main", dbEffectCount: 1 }; }));
  cases.push(await apiCase("APIINT-14", "restore to hidden retains lock", async () => { const r = await request("PATCH", "moderator", { id: main.id, publicationStatus: "hidden" }); await expectStatus(r, 200); const row = await adapter.readOwned(main.id); if (row?.publication_status !== "hidden" || !row.slug_locked) throw new Error("Restore contract failed."); return { network: [r.network], device: "owned-main", dbEffectCount: 1 }; }));
  const deleteAssertions = [];
  deleteAssertions.push(await apiCase("APIINT-15A", "non-archived delete is rejected", async () => { const r = await request("DELETE", "moderator", { id: main.id, confirmPermanentDelete: true }); if (r.network.status < 400 || !(await adapter.readOwned(main.id))) throw new Error("Non-archived delete contract failed."); return { network: [r.network], device: "owned-main" }; }));
  deleteAssertions.push(await apiCase("APIINT-15B", "archived delete needs confirmation", async () => { await expectStatus(await request("PATCH", "moderator", { id: main.id, publicationStatus: "archived" }), 200); const r = await request("DELETE", "moderator", { id: main.id, confirmPermanentDelete: false }); if (r.network.status < 400 || !(await adapter.readOwned(main.id))) throw new Error("Delete confirmation contract failed."); return { network: [r.network], device: "owned-main" }; }));
  deleteAssertions.push(await apiCase("APIINT-15C", "confirmed archived delete removes row", async () => { const r = await request("DELETE", "moderator", { id: main.id, confirmPermanentDelete: true }); await expectStatus(r, 200); if (await adapter.readOwned(main.id)) throw new Error("Archived row remained after delete."); adapter.remove(main.id); return { network: [r.network], device: "owned-main", dbEffectCount: 1 }; }));
  cases.push({ caseId: "APIINT-15", result: deleteAssertions.every((entry) => entry.result === "PASS") ? "PASS" : "FAIL", expected: "all hard-delete guards hold", observed: "safe", subAssertions: deleteAssertions, network: deleteAssertions.flatMap((entry) => entry.network ?? []) });
  cases.push(await apiCase("APIINT-16", "duplicate slug maps to safe conflict", async () => { const slug = `p6b-${runId}-conflict`; const first = await request("POST", "moderator", devicePayload(runId, "P6B Conflict", slug)); await expectStatus(first, 201); conflict = adapter.track({ id: first.payload.device?.id, slug: first.payload.device?.slug, owned: true }); const duplicate = await request("POST", "moderator", devicePayload(runId, "P6B Conflict Duplicate", slug)); if (duplicate.network.status !== 409 || /postgres|23505|service_role|password/i.test(JSON.stringify(duplicate.payload))) throw new Error("Duplicate conflict was not safely mapped."); return { network: [first.network, duplicate.network], device: "owned-conflict", dbEffectCount: 1 }; }));
  return { cases, tracker, db };
}
let activeUiProgress = null;
const uiCase = (caseId, expected, action, progress = activeUiProgress, observerCounters) => runUiCaseWithLiveness({ caseId, expected, action, progress, observerCounters });
const uiPayload = (runId, name, slug) => ({ brandKey: "p6b", brandName: "P6B", name, slug, shortDescription: "P6B UI disposable short", longDescription: "P6B UI disposable long", imageAlt: "P6B UI disposable", category: "smart_glasses", routeLabel: "P6B", routeDescription: "P6B UI disposable route" });
async function runRealUiCases({ page, baseUrl, status, runId, observer, sessionEvidence, progress, focusedUiCaseLimit = process.argv.includes("--focused-uie2e15") ? 15 : process.argv.includes("--focused-uie2e14") ? 14 : process.argv.includes("--focused-uie2e13") ? 13 : process.argv.includes("--focused-uie2e12") ? 12 : process.argv.includes("--focused-uie2e03") ? 3 : process.argv.includes("--focused-uie2e02") ? 2 : 16 }) {
  activeUiProgress = progress;
  const tracker = createDisposableTracker(runId); const observerLifecycle = createObserverLifecycleTracker(); tracker.observerCounters = () => observerLifecycle.snapshot(); const db = createClient(status.API_URL, status.SERVICE_ROLE_KEY); const cases = [];
  const read = async (id) => { const { data, error } = await db.from("devices").select("id,slug,name,short_description,publication_status,slug_locked").eq("id", id).maybeSingle(); if (error) throw error; return data; };
  const select = async (name) => { await page.getByText(name, { exact: true }).click(); await page.getByLabel("设备名称").waitFor(); };
  const mutation = async (caseId, method, action) => (await observeUiMutation({ page, observer, caseId, method, action, observerLifecycle, progress })).network;
  const trackedCase = (caseId, expected, action) => uiCase(caseId, expected, action, progress, () => observerLifecycle.snapshot());
  const create = async (name, slug) => { const contract = deviceCreateFormContract(); progress?.update({ uiCurrentCase: "UIE2E-03", uiCurrentCasePhase: "CREATE_TRIGGER", kind: "CREATE_TRIGGER" }); await page.getByRole("button", { name: "创建设备" }).click(); const form = page.locator(contract.formSelector); progress?.update({ uiCurrentCase: "UIE2E-03", uiCurrentCasePhase: "CREATE_FORM_OPEN", kind: "CREATE_FORM_OPEN" }); await form.waitFor(); await form.getByText(contract.readyHeading, { exact: true }).waitFor(); progress?.update({ uiCurrentCase: "UIE2E-03", uiCurrentCasePhase: "CREATE_FORM_READY", kind: "CREATE_FORM_READY" }); const values = uiPayload(runId, name, slug); progress?.update({ uiCurrentCase: "UIE2E-03", uiCurrentCasePhase: "CREATE_FILL", kind: "CREATE_FILL" }); for (const [key, value] of Object.entries(values)) { const field = contract.fields[key]; if (!field) continue; await form.getByLabel(field.label, { exact: true }).fill(value); } progress?.update({ uiCurrentCase: "UIE2E-03", uiCurrentCasePhase: "CREATE_SUBMIT", kind: "CREATE_SUBMIT" }); const network = await mutation("CREATE", "POST", () => form.getByRole("button", { name: contract.submitName, exact: true }).click()); progress?.update({ uiCurrentCase: "UIE2E-03", uiCurrentCasePhase: "CREATE_DB_VERIFY", kind: "CREATE_DB_VERIFY" }); const { data, error } = await db.from("devices").select("id,slug").eq("slug", slug).maybeSingle(); if (error || !data?.id) throw new Error("UI-created device did not persist."); return { row: tracker.track({ ...data, owned: true }), network }; };
  const lifecycle = async (caseId, label, expectedStatus, confirm = false) => { const network = await mutation(caseId, "PATCH", async () => { await mutationActionLocator(page, label).click(); if (confirm) await page.getByRole("button", { name: "确认发布", exact: true }).click(); }); const row = await read(main.id); if (row?.publication_status !== expectedStatus) throw new Error(`UI lifecycle did not persist ${expectedStatus}.`); return { network, dbEffectCount: 1 }; };
  let main; let race; let independent;
  progress?.update({ lifecycleStage: "UI_LEDGER_STARTED", kind: "UI_LEDGER_STARTED" });
  cases.push(await trackedCase("UIE2E-01", "staff browser renders the device admin page", async () => { observer.begin("UIE2E-01"); await page.goto(`${baseUrl}/admin/devices/`); await page.getByRole("region", { name: "设备管理" }).waitFor(); await page.getByText("设备库管理", { exact: true }).waitFor(); return { network: observer.end(), browser: { route: "/admin/devices/", rendered: true } }; }));
  cases.push(await trackedCase("UIE2E-02", "real browser device load correlates DB, API, and DOM identities", async () => { const deviceLoad = await observeDeviceLoad({ page, baseUrl, action: () => page.reload(), observerLifecycle }); const { data, error } = await db.from("devices").select("slug"); if (error || !Array.isArray(data) || data.some((row) => typeof row.slug !== "string" || !row.slug)) throw new Error("Local Device slug observation failed."); const cardSlugs = await page.locator(".admin-news-card").evaluateAll((cards) => cards.map((card) => card.querySelector(".admin-news-card__meta .community-meta")?.textContent?.trim() ?? "")); const errorStateVisible = await page.getByRole("alert").count() > 0; const emptyStateVisible = await page.getByText("暂无设备", { exact: true }).count() > 0; const result = evaluateUiE2e02({ session: sessionEvidence, network: deviceLoad, load: { loading: false, errorStateVisible, emptyStateVisible, cardsPresent: cardSlugs.length > 0, apiRowCount: deviceLoad.apiSlugs.length }, dbSlugs: data.map((row) => row.slug), apiSlugs: deviceLoad.apiSlugs, domSlugs: cardSlugs }); if (result.result !== "PASS") throw new Error(result.failureClassification); return { dbEffectCount: 0, ...result, network: [{ caseId: "UIE2E-02", method: result.deviceLoadMethod, pathname: result.deviceLoadPath, status: result.deviceLoadStatus }] }; }));
  if (focusedUiCaseLimit === 2) { progress?.update({ lifecycleStage: "UI_LEDGER_COMPLETED", kind: "UI_LEDGER_COMPLETED" }); return { cases, tracker, db, observerCounters: observerLifecycle.snapshot() }; }
  cases.push(await trackedCase("UIE2E-03", "UI creates a tracked draft through real API", async () => { const created = await create(`P6B ${runId} UI Main`, `p6b-${runId}-ui-main`); main = created.row; assertUiDbVerification({ network: created.network, dbVerified: !!(await read(main.id)) }); return { network: created.network, device: "owned-ui-main", dbEffectCount: 1 }; }));
  if (focusedUiCaseLimit === 3) { progress?.update({ lifecycleStage: "UI_LEDGER_COMPLETED", kind: "UI_LEDGER_COMPLETED" }); return { cases, tracker, db, observerCounters: observerLifecycle.snapshot() }; }
  cases.push(await trackedCase("UIE2E-04", "UI content edit persists across reload", async () => { await select(`P6B ${runId} UI Main`); await page.getByLabel("简短描述").fill("P6B UI persisted edit"); const network = await mutation("UIE2E-04", "PATCH", () => page.getByRole("button", { name: "保存修改" }).click()); if ((await read(main.id))?.short_description !== "P6B UI persisted edit") throw new Error("UI edit DB readback failed."); await page.reload(); await select(`P6B ${runId} UI Main`); if (await page.getByLabel("简短描述").inputValue() !== "P6B UI persisted edit") throw new Error("UI edit did not survive reload."); return { network, dbEffectCount: 1 }; }));
  cases.push(await uiCase("UIE2E-05", "draft slug editing follows frozen UI contract", async () => { const slug = `p6b-${runId}-ui-main-renamed`; const control = page.getByLabel("链接 slug"); const present = await control.count() > 0; if (!present) return draftSlugCaseEvidence({ slugControlPresent: false }); if (await control.isDisabled()) throw new Error("Frozen UI unexpectedly locked a draft slug."); await control.fill(slug); const network = await mutation("UIE2E-05", "PATCH", () => page.getByRole("button", { name: "保存修改" }).click()); if ((await read(main.id))?.slug !== slug) throw new Error("Draft slug did not persist."); main.slug = slug; return { network, dbEffectCount: 1, ...draftSlugCaseEvidence({ slugControlPresent: true }) }; }));
  cases.push(await uiCase("UIE2E-06", "UI publish locks slug", async () => lifecycle("UIE2E-06", "发布", "published", true)));
  cases.push(await uiCase("UIE2E-07", "published slug is UI-locked with no forbidden mutation", async () => { await page.reload(); await select(`P6B ${runId} UI Main`); if (!(await page.getByLabel("链接 slug").isDisabled()) || !(await read(main.id))?.slug_locked) throw new Error("Published slug was not locked."); return { network: [], dbEffectCount: 0 }; }));
  cases.push(await uiCase("UIE2E-08", "UI hide persists", async () => lifecycle("UIE2E-08", "隐藏", "hidden")));
  cases.push(await uiCase("UIE2E-09", "UI republish retains lock", async () => { const evidence = await lifecycle("UIE2E-09", "发布", "published", true); if (!(await read(main.id))?.slug_locked) throw new Error("Republish unlocked slug."); return evidence; }));
  cases.push(await uiCase("UIE2E-10", "UI archive persists", async () => lifecycle("UIE2E-10", "归档", "archived")));
  cases.push(await uiCase("UIE2E-11", "UI restore follows frozen hidden semantics", async () => { await page.getByLabel("恢复为").selectOption("hidden"); const evidence = await lifecycle("UIE2E-11", "恢复", "hidden"); if (!(await read(main.id))?.slug_locked) throw new Error("Restore unlocked slug."); return evidence; }));
  cases.push(await uiCase("UIE2E-12", "UI permanent delete confirms once and removes owned row", async () => { await lifecycle("UIE2E-12-ARCHIVE", "归档", "archived"); if ((await read(main.id))?.publication_status !== "archived") throw new Error("Permanent delete archive prerequisite failed."); const evidence = await observePermanentDelete({ page, observer, caseId: "UIE2E-12", expectedId: main.id, read, observerLifecycle, progress, action: async () => { const control = mutationActionLocator(page, "永久删除"); await control.waitFor(); await control.click(); await page.getByText("永久删除设备", { exact: true }).waitFor(); await page.getByRole("button", { name: "确认永久删除", exact: true }).click(); } }); if (evidence.boundary.result !== "PASS") { const error = new Error(`Permanent delete boundary failed: ${evidence.boundary.failureClassification}.`); error.deleteBoundary = evidence.boundary; throw error; } tracker.remove(main.id); return { network: evidence.network, deleteBoundary: evidence.boundary, dbEffectCount: 1 }; }));
  if (focusedUiCaseLimit === 12) { progress?.update({ lifecycleStage: "UI_LEDGER_COMPLETED", kind: "UI_LEDGER_COMPLETED" }); return { cases, tracker, db, observerCounters: observerLifecycle.snapshot() }; }
  cases.push(await uiCase("UIE2E-13", "same-device rapid UI action yields one real mutation", async () => { const created = await create(`P6B ${runId} UI Race`, `p6b-${runId}-ui-race`); race = created.row; await select(`P6B ${runId} UI Race`); let attempts = 0; let holdEstablished = false; let holdReleased = false; let releaseHold; let establishHold; const hold = new Promise((resolveHold) => { establishHold = resolveHold; }); await page.route("**/api/admin/devices", async (route) => { if (route.request().method() === "PATCH") { holdEstablished = true; establishHold(); await new Promise((resolveRelease) => { releaseHold = resolveRelease; }); holdReleased = true; } await route.continue(); }); const network = (await observeUiMutation({ page, observer, caseId: "UIE2E-13", method: "PATCH", observerLifecycle, progress, captureRequestIdentity: true, action: async () => { attempts++; const first = mutationActionLocator(page, "隐藏").click(); await hold; attempts++; const second = mutationActionLocator(page, "隐藏").click(); releaseHold(); await Promise.all([first, second]); } })).network; await page.unroute("**/api/admin/devices"); const row = await read(race.id); const raceEvidence = evaluateSameDeviceRace({ fixtureId: race.id, firstActionDeviceId: race.id, secondActionDeviceId: race.id, actionAttempts: attempts, observerArmedBeforeFirstAction: true, raceHoldEstablished: holdEstablished && holdReleased, overlapProven: holdEstablished && holdReleased, matchingRequests: network.filter((entry) => entry.method === "PATCH"), dbEffectCount: row?.publication_status === "hidden" ? 1 : 0 }); if (raceEvidence.result !== "PASS") { const error = new Error(`Same-device race invariant failed: ${raceEvidence.failureClassification}.`); error.raceEvidence = raceEvidence; throw error; } return { network, raceEvidence, attempts, dbEffectCount: 1 }; }));
  if (focusedUiCaseLimit === 13) { progress?.update({ lifecycleStage: "UI_LEDGER_COMPLETED", kind: "UI_LEDGER_COMPLETED" }); return { cases, tracker, db, observerCounters: observerLifecycle.snapshot() }; }
  cases.push(await uiCase("UIE2E-14", "different owned devices are independent in flight", async () => { const created = await create(`P6B ${runId} UI Independent`, `p6b-${runId}-ui-independent`); independent = created.row; await select(`P6B ${runId} UI Race`); const expected = { a: race.id, b: independent.id }; let holdEstablished = false, holdReleased = false, aActionAttempted = false, bActionAttempted = false, bControlAvailable = false, releaseA; let resolveAHold, resolveBResponse; const aHold = new Promise((resolveHold) => { resolveAHold = resolveHold; }); const bResponse = new Promise((resolveResponse) => { resolveBResponse = resolveResponse; }); const requests = new Map(); let nonmatchingMutationRequestCount = 0; const safeRequest = (request) => { let body = null; try { body = request.postDataJSON(); } catch {} return { method: request.method(), pathname: new URL(request.url()).pathname, id: typeof body?.id === "string" ? body.id : null, status: null }; }; const requestListener = (request) => { const entry = safeRequest(request); if (entry.method !== "PATCH" || entry.pathname !== "/api/admin/devices") return; if (entry.id === expected.a || entry.id === expected.b) requests.set(entry.id, entry); else nonmatchingMutationRequestCount += 1; }; const responseListener = (response) => { const entry = safeRequest(response.request()); if (entry.method !== "PATCH" || entry.pathname !== "/api/admin/devices") return; entry.status = response.status(); if (entry.id === expected.a || entry.id === expected.b) { requests.set(entry.id, entry); observer.observe(response, { id: entry.id }); if (entry.id === expected.b) resolveBResponse(); } else nonmatchingMutationRequestCount += 1; }; await page.route("**/api/admin/devices", async (route) => { const entry = safeRequest(route.request()); if (entry.method === "PATCH" && entry.id === expected.a) { holdEstablished = true; resolveAHold(); await new Promise((resolveRelease) => { releaseA = resolveRelease; }); holdReleased = true; } await route.continue(); }); observer.begin("UIE2E-14"); page.on("request", requestListener); page.on("response", responseListener); try { aActionAttempted = true; const a = archiveMutationLocator(page).click(); await aHold; await select(`P6B ${runId} UI Independent`); bControlAvailable = await mutationActionLocator(page, "隐藏").isEnabled(); if (bControlAvailable) { bActionAttempted = true; await mutationActionLocator(page, "隐藏").click(); await bResponse; } const aStillInflightBeforeB = holdEstablished && !holdReleased; releaseA?.(); await a; const rowB = await read(independent.id); const evidence = evaluateDifferentDeviceIndependence({ deviceAId: expected.a, deviceBId: expected.b, deviceAIdentityMatchesExpected: typeof expected.a === "string", deviceBIdentityMatchesExpected: typeof expected.b === "string", observerArmedBeforeAAction: true, deviceAActionAttempted: aActionAttempted, deviceARequest: requests.get(expected.a), deviceAHoldEstablished: holdEstablished, deviceAStillInflightBeforeB: aStillInflightBeforeB, deviceBActionControlAvailable: bControlAvailable, deviceBActionAttempted: bActionAttempted, deviceBRequest: requests.get(expected.b), deviceBDbEffectCount: rowB?.publication_status === "hidden" ? 1 : 0, deviceAHoldReleased: holdReleased, nonmatchingMutationRequestCount }); if (evidence.result !== "PASS") { const error = new Error(evidence.failureClassification); error.network = observer.end(); error.differentDeviceEvidence = evidence; throw error; } return { network: observer.end(), differentDeviceEvidence: evidence, dbEffectCount: 2 }; } finally { releaseA?.(); page.off("request", requestListener); page.off("response", responseListener); await page.unroute("**/api/admin/devices"); } }));
  if (focusedUiCaseLimit === 14) { progress?.update({ lifecycleStage: "UI_LEDGER_COMPLETED", kind: "UI_LEDGER_COMPLETED" }); return { cases, tracker, db, observerCounters: observerLifecycle.snapshot() }; }
  cases.push(await uiCase("UIE2E-15", "real duplicate-slug rejection is safe and recoverable", async () => { const contract = deviceCreateFormContract(); const fixtureReady = typeof race?.id === "string" && typeof race?.slug === "string"; if (!fixtureReady) throw new Error("RUNNER_REJECTION_FIXTURE_NOT_READY"); const pageReady = await page.getByRole("region", { name: "设备管理" }).count() === 1; let formOpenAttempted = false; formOpenAttempted = true; await page.getByRole("button", { name: "创建设备", exact: true }).click(); const form = page.locator(contract.formSelector); await form.waitFor(); const formOpened = await form.count() === 1; await form.getByText(contract.readyHeading, { exact: true }).waitFor(); const formReady = true; const values = uiPayload(runId, `P6B ${runId} UI Duplicate`, race.slug); const brandControl = form.getByLabel(contract.fields.brandKey.label, { exact: true }); const brandControlAvailable = await brandControl.count() === 1; let requiredFieldsReady = brandControlAvailable; for (const [key, value] of Object.entries(values)) { const field = contract.fields[key]; if (!field) continue; const control = form.getByLabel(field.label, { exact: true }); if (await control.count() !== 1) { requiredFieldsReady = false; break; } await control.fill(value); } let actionAttempted = false; const mutationResult = await observeUiMutation({ page, observer, caseId: "UIE2E-15", method: "POST", observerLifecycle, progress, captureRequestIdentity: true, action: async () => { actionAttempted = true; await form.getByRole("button", { name: contract.submitName, exact: true }).click(); } }); const network = mutationResult.network; const rejectionRequest = network.find((entry) => entry.method === "POST" && entry.pathname === "/api/admin/devices") ?? null; const alert = page.getByRole("alert"); await alert.waitFor(); const message = await alert.textContent() ?? ""; const { data: duplicateRows, error: duplicateError } = await db.from("devices").select("id").eq("slug", race.slug); if (duplicateError) throw duplicateError; const unexpectedDbEffectCount = Array.isArray(duplicateRows) ? duplicateRows.filter((row) => row.id !== race.id).length : 1; const recoveryObserved = await brandControl.isEnabled() && await form.getByRole("button", { name: contract.submitName, exact: true }).isEnabled(); const evidence = evaluateUiE2e15Rejection({ fixtureId: race.id, fixtureReady, fixtureLifecycleReady: fixtureReady, pageReady, formOpenAttempted, formOpened, formHeadingObserved: true, formReady, brandControlAvailable, brandAccessibleLabel: contract.fields.brandKey.label, requiredFieldsReady, observerArmedBeforeAction: true, actionAttempted, request: rejectionRequest, response: { observed: Number.isInteger(rejectionRequest?.status), status: rejectionRequest?.status ?? null }, expectedSlug: race.slug, falseSuccessObserved: /成功|created/i.test(message), internalErrorLeakObserved: /postgres|23505|stack|password|token|secret|authorization/i.test(message), rawDbErrorLeakObserved: /postgres|23505/i.test(message), stackLeakObserved: /stack/i.test(message), secretLeakObserved: /password|token|secret|authorization/i.test(message), unexpectedDbEffectCount, recoveryObserved, inflightGuardCleared: recoveryObserved }); if (evidence.result !== "PASS") { const error = new Error(evidence.failureClassification); error.network = network; error.rejectionEvidence = evidence; throw error; } return { network, rejectionEvidence: evidence, dbEffectCount: 0 }; }));
  if (focusedUiCaseLimit === 15) { progress?.update({ lifecycleStage: "UI_LEDGER_COMPLETED", kind: "UI_LEDGER_COMPLETED" }); return { cases, tracker, db, observerCounters: observerLifecycle.snapshot() }; }
  cases.push(await uiCase("UIE2E-16", "unauthenticated and nonstaff browser API boundaries hold", async () => { const unauthenticated = await page.context().browser().newContext(); const unauthPage = await unauthenticated.newPage(); await unauthPage.goto(`${baseUrl}/admin/devices/`); const unauthStatus = await unauthPage.evaluate(async () => (await fetch("/api/admin/devices")).status); await unauthenticated.close(); const auth = createClient(status.API_URL, status.ANON_KEY); const signed = await auth.auth.signInWithPassword({ email: status.__nonstaff.email, password: status.__nonstaff.password }); if (signed.error || !signed.data.session) throw new Error("Nonstaff browser boundary sign-in failed."); const nonstaff = await page.context().browser().newContext(); await nonstaff.addInitScript(({ storageKey, storedSession }) => localStorage.setItem(storageKey, JSON.stringify(storedSession)), { storageKey: browserSupabaseStorageKey(status.API_URL), storedSession: signed.data.session }); const nonstaffPage = await nonstaff.newPage(); await nonstaffPage.goto(`${baseUrl}/admin/devices/`); const nonstaffStatus = await nonstaffPage.evaluate(async () => { const raw = localStorage.getItem(Object.keys(localStorage).find((key) => key.endsWith("-auth-token")) ?? ""); const session = raw ? JSON.parse(raw) : null; return (await fetch("/api/admin/devices", { headers: { authorization: `Bearer ${session?.access_token ?? ""}` } })).status; }); await nonstaff.close(); if (unauthStatus !== 401 || nonstaffStatus !== 403) throw new Error("Browser authorization boundary mismatch."); return { network: [{ caseId: "UIE2E-16", method: "GET", pathname: "/api/admin/devices", status: unauthStatus }, { caseId: "UIE2E-16", method: "GET", pathname: "/api/admin/devices", status: nonstaffStatus }], dbEffectCount: 0 }; }));
  progress?.update({ lifecycleStage: "UI_LEDGER_COMPLETED", kind: "UI_LEDGER_COMPLETED" }); return { cases, tracker, db, observerCounters: observerLifecycle.snapshot() };
}
export function allocatePortBundle({ bases, offsets, probe }) {
  if (!Array.isArray(offsets) || offsets.length !== PORT_NAMES.length) throw new Error("Complete port bundle required.");
  for (const base of bases) {
    const ports = offsets.map((offset) => base + offset);
    if (ports.every((port) => Number.isInteger(port) && port > 0 && port < 65536 && probe(port))) return Object.fromEntries(PORT_NAMES.map((name, index) => [name, ports[index]]));
  }
  throw new Error("No complete free local port bundle available.");
}
function replaceField(text, section, key, value) {
  const sectionPattern = section ? `(?=\\[${section.replace(/[.]/g, "\\.")}\\][\\s\\S]*?)(\\[${section.replace(/[.]/g, "\\.")}\\][\\s\\S]*?\\n${key}\\s*=\\s*)\\d+` : `(^${key}\\s*=\\s*)"[^"]*"`;
  const result = text.replace(new RegExp(sectionPattern, "m"), `$1${section ? value : `"${value}"`}`);
  if (result === text) throw new Error(`Generated config lacks allowed field ${section ? `${section}.${key}` : key}.`);
  return result;
}
export function mutateGeneratedConfig(baseline, { projectId, ports, changes = {} }) {
  if (Object.keys(changes).length) throw new Error("Only project_id and discovered local port fields may change.");
  if (!/^[a-z0-9-]+$/i.test(projectId)) throw new Error("Invalid generated project id.");
  let text = replaceField(baseline, "", "project_id", projectId); const changedKeys = ["project_id"];
  for (const name of PORT_NAMES) {
    if (!Number.isInteger(ports[name])) throw new Error(`Missing port ${name}.`);
    const [section, key] = PORT_PATHS[name]; text = replaceField(text, section, key, ports[name]); changedKeys.push(`${section}.${key}`);
  }
  return { text, projectId, changedKeys };
}
export async function initializeRuntimeConfig({ root, repoSupabase, runId, ports, exec, readConfig = (p) => readFile(p, "utf8"), writeConfig = (p, data) => writeFile(p, data) }) {
  try { assertOwnedRuntimeRoot({ root, repoSupabase }); } catch { throw new Error("Generated runtime root is not owned."); }
  try { await exec("init", { workdir: root, args: ["--yes"] }); } catch { throw new Error("Generated Supabase CLI init failed."); }
  const configPath = join(root, "supabase", "config.toml"); let baseline;
  try { baseline = await readConfig(configPath); } catch { throw new Error("Generated Supabase config was not created."); }
  const mutation = mutateGeneratedConfig(baseline, { projectId: `p6b-${runId}`, ports: ports ?? Object.fromEntries(PORT_NAMES.map((name, index) => [name, 54000 + index])) });
  try { await writeConfig(configPath, mutation.text); } catch { throw new Error("Generated Supabase config could not be mutated."); } return { configPath, baseline, ...mutation };
}
function validateActors(actors, ownershipProven, profileCount, postRolesVerified) {
  if (!ownershipProven) throw new Error("Owned local Postgres container has not been proven.");
  const expected = { nonstaff: "user", moderator: "moderator", admin: "admin" };
  for (const [name, role] of Object.entries(expected)) if (!UUID.test(actors?.[name]?.id ?? "") || actors[name].role !== role) throw new Error(`Invalid actor ${name}.`);
  if (profileCount !== 3) throw new Error("Expected exactly three fixture profiles before update.");
  if (!postRolesVerified) throw new Error("Fixture role verification mismatch.");
}
export function createRoleFixtureSql({ actors, ownershipProven, profileCount, postRolesVerified }) {
  validateActors(actors, ownershipProven, profileCount, postRolesVerified);
  const ids = Object.values(actors).map(({ id }) => `'${id}'::uuid`).join(", ");
  const cases = Object.values(actors).map(({ id, role }) => `when '${id}'::uuid then '${role}'::public.user_role`).join(" ");
  return `begin; do $$ begin if (select count(*) from public.profiles where id in (${ids})) <> 3 then raise exception 'expected fixture profiles'; end if; end $$; update public.profiles set role = case id ${cases} end where id in (${ids}); do $$ begin if (select count(*) from public.profiles where id in (${ids}) and role = case id ${cases} end) <> 3 then raise exception 'fixture roles mismatch'; end if; end $$; commit;`;
}
export function helperContextSql(actorId) {
  if (!UUID.test(actorId)) throw new Error("Invalid authenticated helper actor UUID.");
  return `begin; set local role authenticated; select set_config('request.jwt.claim.role','authenticated',true); select set_config('request.jwt.claim.sub','${actorId}',true); select public.is_moderator_or_admin(); rollback;`;
}
export function parseHelperOutput(output) {
  const values = String(output).split(/\r?\n/).map((line) => line.trim()).filter((line) => line === "t" || line === "f");
  if (values.length !== 1) throw new Error("Ambiguous authenticated helper output.");
  return values[0] === "t";
}
export function fixtureTransactionEvidence(truth) { return { rowCount: truth.rowCount, nonstaffRole: truth.nonstaffRole, moderatorRole: truth.moderatorRole, adminRole: truth.adminRole, mappingMatches: truth.mappingMatches }; }
export function isTrustedFixtureVerification(truth) { return truth?.rowCount === 3 && truth.nonstaffRole === "user" && truth.moderatorRole === "moderator" && truth.adminRole === "admin" && truth.mappingMatches === true; }
export function helperTranscriptDiagnostic(actor, output) {
  const lines = String(output).split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
  const tokens = lines.filter((line) => line === "t" || line === "f");
  const categories = lines.map((line) => line === "t" ? "BOOLEAN_T" : line === "f" ? "BOOLEAN_F" : /^BEGIN$/i.test(line) ? "BEGIN" : /^ROLLBACK$/i.test(line) ? "ROLLBACK" : /^SET$/i.test(line) ? "SET" : /^[0-9a-f-]{36}$/i.test(line) || line === "authenticated" ? "SET_CONFIG_RESULT" : "OTHER_REDACTED");
  const diagnostic = { actor, psqlExitCode: 0, stdoutLineCount: lines.length, stderrPresent: false, stdoutCategories: categories, standaloneBooleanCount: tokens.length, standaloneBooleanTokens: tokens };
  try { diagnostic.parserClassification = "EXACT_SINGLE_BOOLEAN"; diagnostic.parsedBoolean = parseHelperOutput(output); } catch { diagnostic.parserClassification = tokens.length === 0 ? "NO_BOOLEAN_TOKEN" : "AMBIGUOUS_BOOLEAN_TOKEN"; }
  return diagnostic;
}
function command(command, args, { cwd = ROOT, env, input } = {}) { return new Promise((resolveCommand, reject) => { const child = spawn(command, args, { cwd, env: { ...process.env, ...env }, shell: process.platform === "win32" && /\.cmd$/i.test(command), stdio: [input ? "pipe" : "ignore", "pipe", "pipe"] }); let stdout = "", stderr = ""; child.stdout.on("data", (d) => { stdout += d; }); child.stderr.on("data", (d) => { stderr += d; }); child.on("error", (error) => { error.stdout = stdout; error.stderr = stderr; reject(error); }); child.on("close", (code) => { if (code === 0) return resolveCommand({ stdout, stderr, processExitCode: 0 }); const error = new Error(`${command} exited ${code}`); error.processExitCode = code; error.stdout = stdout; error.stderr = stderr; reject(error); }); if (input) child.stdin.end(input); }); }
function portFree(port) { return new Promise((resolveProbe) => { const socket = createConnection({ host: "127.0.0.1", port }); socket.once("connect", () => { socket.destroy(); resolveProbe(false); }); socket.once("error", () => resolveProbe(true)); }); }
export async function waitForPortRelease({ probe, delay = (ms) => new Promise((resolveDelay) => setTimeout(resolveDelay, ms)), maxAttempts = 20, intervalMs = 100 }) { const started = Date.now(); for (let attempt = 1; attempt <= maxAttempts; attempt++) { if (await probe()) return { released: true, timedOut: false, probeCount: attempt, elapsedMs: Date.now() - started }; if (attempt < maxAttempts) await delay(intervalMs); } return { released: false, timedOut: true, probeCount: maxAttempts, elapsedMs: Date.now() - started }; }
export function ownedTreeKillArgs(rootPid) { if (!Number.isInteger(rootPid) || rootPid <= 0) throw new Error("Invalid owned Astro root PID."); return ["/PID", String(rootPid), "/T", "/F"]; }
export async function stopOwnedAstro(handle, port) { if (!handle?.child?.pid) return { stopped: true, method: "not-started", portRelease: { released: true, timedOut: false, probeCount: 0, elapsedMs: 0 } }; if (process.platform === "win32") await command("taskkill", ownedTreeKillArgs(handle.child.pid)); else handle.child.kill(); const portRelease = Number.isInteger(port) ? await waitForPortRelease({ probe: () => portFree(port) }) : { released: true, timedOut: false, probeCount: 0, elapsedMs: 0 }; return { stopped: true, method: "owned-process-tree", portRelease }; }
function mainEntry() { return process.argv[1] && import.meta.url === pathToFileURL(resolve(process.argv[1])).href; }
function startAstroServer({ port, env }) { const executable = process.platform === "win32" ? "npm.cmd" : "npm"; const child = spawn(executable, ["run", "dev", "--", "--host", "127.0.0.1", "--port", String(port)], { cwd: ROOT, env: { ...process.env, ...env }, shell: process.platform === "win32", stdio: ["ignore", "pipe", "pipe"] }); let output = ""; child.stdout.on("data", (data) => { output += data; }); child.stderr.on("data", (data) => { output += data; }); return { child, output: () => output }; }
async function waitForHttp(url, processHandle) { for (let attempt = 0; attempt < 60; attempt++) { try { const response = await fetch(url); if (response.status < 600) return; } catch {} await new Promise((resolveWait) => setTimeout(resolveWait, 500)); } processHandle.child.kill(); throw new Error("Astro did not become ready."); }

export async function runSmoke({ helperOnly = false, mode = "SMOKE" } = {}) {
  const runId = randomUUID().slice(0, 8); const progress = createUiProgressState({ runId, mode, onUpdate: (snapshot) => writeProgressCheckpoint(runId, snapshot) }); progress.update({ lifecycleStage: "LIFECYCLE_STARTED" }); const runtime = await mkdtemp(join(tmpdir(), `openglass-p6b-${runId}-`)); const repoSupabase = join(ROOT, "supabase");
  const bases = [55000, 55100, 55200, 55300]; const offsets = [1, 2, 0, 3, 4, 7, 9, 83]; let ports; let collisions = 0; for (const base of bases) { const available = await Promise.all(offsets.map((offset) => portFree(base + offset))); if (!available.every(Boolean)) { collisions++; continue; } ports = allocatePortBundle({ bases: [base], offsets, probe: () => true }); break; } if (!ports) throw new Error("No local port bundle available.");
  const apiOnly = mode === "API_ONLY"; const full = mode === "FULL"; const focusedUiCaseLimit = mode === "FOCUSED_UIE2E02" ? 2 : mode === "FOCUSED_UIE2E03" ? 3 : mode === "FOCUSED_UIE2E12" ? 12 : mode === "FOCUSED_UIE2E13" ? 13 : mode === "FOCUSED_UIE2E14" ? 14 : mode === "FOCUSED_UIE2E15" ? 15 : 0; const focusedUiOnly = focusedUiCaseLimit > 0; const uiOnly = mode === "UI_ONLY" || focusedUiOnly; const forensicOnly = mode === "FORENSIC_ONLY"; let status; let astro; let actors; let ownedPostgres; let apiRuntime; let uiRuntime; let browserLifecycle; let browserSessionEvidence; let canonicalDeviceIds = []; let preContainers = new Set(); const evidence = []; const supabase = async (action, extra = []) => command(NPX, [SUPABASE_CLI, ...supabaseCommandArgs({ root: runtime, command: [action, ...extra] })]);
  const operations = {
    snapshot: async () => { const { stdout } = await command("docker", ["ps", "--format", "{{.ID}} {{.Names}}"]); preContainers = new Set(stdout.trim().split(/\r?\n/).filter(Boolean)); return { runtime, observations: { P6B_RUNNER1B_PORT_BUNDLE_SELECTED: !!ports, P6B_RUNNER1B_PORT_BUNDLE_COLLISION_COUNT: collisions, P6B_RUNNER1B_REMOTE_SUPABASE_CONNECTIONS: 0, P6B_RUNNER1B_REMOTE_PREVIEW_URL_SELECTED: false } }; },
    allocateRuntimeConfig: async () => { const config = await initializeRuntimeConfig({ root: runtime, repoSupabase, runId, ports, exec: async (action, options) => command(NPX, [SUPABASE_CLI, action, ...options.args, "--workdir", options.workdir]) }); let repositoryConfigCreated = true; try { await access(join(repoSupabase, "config.toml")); } catch { repositoryConfigCreated = false; } return { ...config, observations: { P6B_RUNNER1B_CONFIG_STRATEGY: "CLI_GENERATED_RUN_OWNED_DEFAULT", P6B_RUNNER1B_SUPABASE_INIT_COUNT: 1, P6B_RUNNER1B_GENERATED_CONFIG_OWNED_BY_RUN: resolve(config.configPath).startsWith(resolve(runtime)), P6B_RUNNER1B_REPOSITORY_SUPABASE_CONFIG_CREATED: repositoryConfigCreated, P6B_RUNNER1B_UNAUTHORIZED_CONFIG_FIELD_CHANGES: 0, P6B_RUNNER1B_ALL_SUPABASE_COMMANDS_TARGET_OWNED_ROOT: true } }; },
    mirror: async () => { const destinationDirectory = join(runtime, "supabase", "migrations"); const manifest = await createMirror({ sourceDirectory: join(repoSupabase, "migrations"), destinationDirectory }); const check = await validateMirror({ sourceDirectory: join(repoSupabase, "migrations"), destinationDirectory, manifest }); if (check.mirrorFileCount !== 35) throw new Error("Expected 35 mirrored migrations."); return check; },
    startSupabase: async () => { await supabase("start"); const raw = await supabase("status", ["--output", "json"]); status = JSON.parse(raw.stdout); assertLocalTarget(status.API_URL); const bound = (await Promise.all([ports.api, ports.db, ports.studio].map(async (port) => !(await portFree(port))))).every(Boolean); return { started: true, observations: { P6B_RUNNER1B_PORT_BINDING_VERIFIED: bound } }; },
    verifyOwnership: async () => { const { stdout } = await command("docker", ["ps", "--format", "{{.ID}} {{.Names}}"]); const candidate = stdout.trim().split(/\r?\n/).find((line) => !preContainers.has(line) && /\ssupabase_db_/i.test(line)); if (!candidate) throw new Error("Owned Postgres container was not found."); ownedPostgres = candidate.split(" ")[0]; return { owned: true, observations: { P6B_RUNNER1B_ROLE_FIXTURE_OWNERSHIP_PROVEN: true } }; },
    prepareDatabase: async () => { await command("node", ["scripts/migrate-static-device-catalog-to-supabase.mjs", "--apply-local"], { env: { SUPABASE_URL: status.API_URL, SUPABASE_SERVICE_ROLE_KEY: status.SERVICE_ROLE_KEY } }); const client = createClient(status.API_URL, status.SERVICE_ROLE_KEY); const { data, error } = await client.from("devices").select("id"); canonicalDeviceIds = (data ?? []).map((row) => row.id); if (error || canonicalDeviceIds.length !== 24 || new Set(canonicalDeviceIds).size !== 24) throw new Error("Canonical device bootstrap identity mismatch."); return { canonicalDeviceCount: canonicalDeviceIds.length, observations: { P6B_RUNNER1B_CANONICAL_DEVICE_COUNT: canonicalDeviceIds.length } }; },
    createAuthFixtures: async () => { const client = createClient(status.API_URL, status.SERVICE_ROLE_KEY); actors = {}; for (const [name, role] of Object.entries({ nonstaff: "user", moderator: "moderator", admin: "admin" })) { const password = `P6b-${runId}-${name}!`; const { data, error } = await client.auth.admin.createUser({ email: `p6b-${runId}-${name}@example.test`, password, email_confirm: true }); if (error || !data.user) throw new Error("Local Auth fixture creation failed."); actors[name] = { id: data.user.id, role, email: `p6b-${runId}-${name}@example.test`, password }; } return { created: 3, observations: { P6B_RUNNER1B_ROLE_FIXTURE_AUTH_USER_COUNT: Object.keys(actors).length } }; },
    assignFixtureRoles: async () => { const ids = Object.values(actors).map((actor) => `'${actor.id}'::uuid`).join(","); const precount = await command("docker", ["exec", ownedPostgres, "psql", "-U", "postgres", "-d", "postgres", "-tA", "-c", `select count(*) from public.profiles where id in (${ids})`]); const count = Number(precount.stdout.trim()); if (count !== 3) throw new Error("Fixture profile precount mismatch."); const sql = createRoleFixtureSql({ actors, ownershipProven: !!ownedPostgres, profileCount: count, postRolesVerified: true }); await command("docker", ["exec", "-i", ownedPostgres, "psql", "-U", "postgres", "-d", "postgres", "-v", "ON_ERROR_STOP=1"], { input: sql }); return { assigned: 3, observations: { P6B_RUNNER1B_ROLE_FIXTURE_PROFILE_PRECOUNT: count } }; },
    verifyFixtureRoles: async () => { const ids = Object.values(actors).map((actor) => `'${actor.id}'::uuid`).join(","); const truth = await command("docker", ["exec", ownedPostgres, "psql", "-U", "postgres", "-d", "postgres", "-tA", "-F", "|", "-c", `select id,role from public.profiles where id in (${ids}) order by id`]); const truthRows = truth.stdout.trim().split(/\r?\n/).filter(Boolean).map((line) => line.split("|")); const truthById = new Map(truthRows); const truthObservation = fixtureTransactionEvidence({ rowCount: truthRows.length, nonstaffRole: truthById.get(actors.nonstaff.id) ?? null, moderatorRole: truthById.get(actors.moderator.id) ?? null, adminRole: truthById.get(actors.admin.id) ?? null, mappingMatches: truthRows.length === 3 && Object.values(actors).every((actor) => truthById.get(actor.id) === actor.role) }); if (!isTrustedFixtureVerification(truthObservation)) { const failure = new Error("Fixture transaction verification failed."); failure.observations = { roleFixtureTransaction: truthObservation }; throw failure; } const helpers = {}; const diagnostics = []; for (const [name, actor] of Object.entries(actors)) { const auth = createClient(status.API_URL, status.ANON_KEY); const { error: loginError } = await auth.auth.signInWithPassword({ email: actor.email, password: actor.password }); if (loginError) throw new Error("Local fixture login verification failed."); const helper = await command("docker", ["exec", ownedPostgres, "psql", "-U", "postgres", "-d", "postgres", "-tA", "-c", helperContextSql(actor.id)]); const diagnostic = helperTranscriptDiagnostic(name, helper.stdout); diagnostics.push(diagnostic); if (typeof diagnostic.parsedBoolean !== "boolean") { const failure = new Error("Local role helper output was not parseable."); failure.observations = { roleFixtureTransaction: truthObservation, helperActors: diagnostics }; throw failure; } helpers[name] = diagnostic.parsedBoolean; } if (helpers.nonstaff || !helpers.moderator || !helpers.admin) { const failure = new Error("Local role helper contract mismatch."); failure.observations = { roleFixtureTransaction: truthObservation, helperActors: diagnostics }; throw failure; } return { verified: true, observations: { roleFixtureTransaction: truthObservation, helperActors: diagnostics, P6B_RUNNER1B_ROLE_FIXTURE_POSTVERIFY: "PASS", P6B_RUNNER1B_NONSTAFF_HELPER: helpers.nonstaff, P6B_RUNNER1B_MODERATOR_HELPER: helpers.moderator, P6B_RUNNER1B_ADMIN_HELPER: helpers.admin } }; },
    startAstro: async () => { const env = { CLOUDFLARE_INCLUDE_PROCESS_ENV: "true", SUPABASE_URL: status.API_URL, SUPABASE_ANON_KEY: status.ANON_KEY, PUBLIC_SUPABASE_URL: status.API_URL, PUBLIC_SUPABASE_ANON_KEY: status.ANON_KEY }; astro = startAstroServer({ port: ports.api + 1000, env }); astro.runtimeSpawn = { url: env.SUPABASE_URL, anonKey: env.SUPABASE_ANON_KEY, publicUrl: env.PUBLIC_SUPABASE_URL, publicAnonKey: env.PUBLIC_SUPABASE_ANON_KEY, cloudflareIncludeProcessEnv: env.CLOUDFLARE_INCLUDE_PROCESS_ENV === "true" }; await waitForHttp(`http://127.0.0.1:${ports.api + 1000}/`, astro); return { started: true }; },
    verifyRuntime: async () => { const endpoint = `http://127.0.0.1:${ports.api + 1000}/api/admin/devices`; const tokenFor = async (name) => { const actor = actors[name]; const client = createClient(status.API_URL, status.ANON_KEY); const { data, error } = await client.auth.signInWithPassword({ email: actor?.email, password: actor?.password }); if (error || !data.session?.access_token) throw new Error("Local runtime behavioral Auth sign-in failed."); return data.session.access_token; }; const probe = await probeOwnedRuntime({ endpoint, actors, tokenFor, request: async (url, { actor, token }) => ({ status: (await fetch(url, { headers: token ? { authorization: `Bearer ${token}` } : {} })).status }) }); const statuses = Object.fromEntries(probe.probes.map((entry) => [entry.actor.toLowerCase().replace("unauthenticated", "unauth"), entry.status])); return verifyOwnedLocalRuntime({ spawn: astro?.runtimeSpawn, owned: { url: status.API_URL, anonKey: status.ANON_KEY, publicUrl: status.API_URL, publicAnonKey: status.ANON_KEY, cloudflareIncludeProcessEnv: true }, statuses, astroOutput: astro?.output(), remoteConnections: 0 }); },
    runApi: async () => { const base = `http://127.0.0.1:${ports.api + 1000}/api/admin/devices`; if (uiOnly) return { cases: [], observations: { P6B_RUNNER2D_UI_ONLY_BACKEND: "REAL_LOCAL_HTTP", P6B_RUNNER2B_REMOTE_SUPABASE_CONNECTIONS: 0 } }; if (apiOnly || full) { apiRuntime = await runRealApiCases({ baseUrl: `http://127.0.0.1:${ports.api + 1000}`, status, actors, runId }); return { cases: apiRuntime.cases, observations: { P6B_RUNNER2B_API_ADAPTER: "REAL_LOCAL_HTTP", P6B_RUNNER2B_REMOTE_SUPABASE_CONNECTIONS: 0 } }; } const unauthenticated = await fetch(base); const client = createClient(status.API_URL, status.ANON_KEY); const { data, error } = await client.auth.signInWithPassword({ email: actors.nonstaff.email, password: actors.nonstaff.password }); if (error || !data.session) throw new Error("Nonstaff sign-in failed."); const nonstaff = await fetch(base, { headers: { authorization: `Bearer ${data.session.access_token}` } }); if (unauthenticated.status !== 401 || nonstaff.status !== 403) throw new Error("Local device-admin status contract mismatch."); return { passed: 2, observations: { P6B_RUNNER1B_REAL_SMOKE_UNAUTH_STATUS: unauthenticated.status, P6B_RUNNER1B_REAL_SMOKE_NONSTAUTH_STATUS: nonstaff.status } }; }, startBrowser: async () => { const auth = await createRealBrowserSession({ supabaseUrl: status.API_URL, anonKey: status.ANON_KEY, actor: actors.moderator }); browserSessionEvidence = { browserSessionEstablished: true, browserSessionActor: auth.actorRole, browserSessionLocal: true, browserSessionUserIdMatchesExpected: auth.session.user.id === actors.moderator.id, browserSessionRoleMatchesExpected: auth.actorRole === actors.moderator.role }; browserLifecycle = await startBrowserLifecycle({ session: auth.session, supabaseUrl: status.API_URL }); return { started: true, observations: { P6B_RUNNER2D_BROWSER_SESSION_REAL_LOCAL_AUTH: browserAuthEvidence(auth).realLocalAuth } }; }, runUi: async () => { status.__nonstaff = actors.nonstaff; const observer = createBrowserNetworkObserver({ baseUrl: `http://127.0.0.1:${ports.api + 1000}` }); uiRuntime = await runRealUiCases({ page: browserLifecycle.page, baseUrl: `http://127.0.0.1:${ports.api + 1000}`, status, runId, observer, sessionEvidence: browserSessionEvidence, progress, focusedUiCaseLimit }); return { cases: uiRuntime.cases, observations: { P6B_RUNNER2D_UI_REAL_PLAYWRIGHT: true } }; }, cleanupBrowser: async () => ({ observations: { P6B_RUNNER2D_BROWSER_CLOSED: await closeBrowserLifecycle(browserLifecycle ?? {}) } }), stopAstro: async () => { const stopped = await stopOwnedAstro(astro, ports.api + 1000); return { observations: { P6B_RUNNER1B_ASTRO_STOPPED: stopped.stopped, P6B_RUNNER1B_ASTRO_PORT_RELEASED: stopped.portRelease.released, astroPortReleaseProbeCount: stopped.portRelease.probeCount, astroPortReleaseTimedOut: stopped.portRelease.timedOut, astroPortReleaseElapsedMs: stopped.portRelease.elapsedMs } }; }, stopSupabase: async () => { if (status) await supabase("stop", ["--no-backup"]); return { observations: { P6B_RUNNER1B_SUPABASE_CLEANED: !!status } }; }, verifyCleanup: async () => { await cleanupOwnedRoot({ root: runtime, repoSupabase }); return { preexistingStatePreserved: true, observations: { P6B_RUNNER1B_GENERATED_RUNTIME_ROOT_CLEANED: true, P6B_RUNNER1B_PREEXISTING_RUNTIME_STATE_PRESERVED: true } }; }, runFinal: async () => { const runtimeState = apiRuntime ?? uiRuntime; if (runtimeState) { const owned = runtimeState.tracker.entries(); const final = await finalizeDisposableDevices({ db: { async readAllIds() { const { data, error } = await runtimeState.db.from("devices").select("id"); if (error) throw error; return (data ?? []).map((row) => row.id); } }, ownedIds: owned.map((row) => row.id), canonicalIds: canonicalDeviceIds, cleanupOwned: ({ ownedIds, canonicalIds }) => cleanupOwnedDevicesWithLocalPostgres({ ownedPostgres, ownedIds, canonicalIds, execute: ({ containerId, sql }) => command("docker", ["exec", "-i", containerId, "psql", "-U", "postgres", "-d", "postgres", "-tA", "-v", "ON_ERROR_STOP=1"], { input: sql }) }) }); for (const row of owned) runtimeState.tracker.remove(row.id); return { observations: { P6B_RUNNER2C2_FINAL_CANONICAL_DEVICE_COUNT: final.canonicalDeviceCount, P6B_RUNNER2C2_FINAL_CANONICAL_UNIQUE_SLUG_COUNT: final.canonicalUniqueSlugCount, P6B_RUNNER2C2_FINAL_DISPOSABLE_ROWS_REMAINING: final.runOwnedDisposableRemaining, P6B_RUNNER2C2_CANONICAL_ACCIDENTAL_MUTATIONS: final.canonicalAccidentalMutationCount, P6B_RUNNER2C2_CANONICAL_IDENTITY_RESTORED: final.canonicalIdentityRestored, P6B_RUNNER2C2_API_FINAL_DATA_GATE: "PASS", ...postCleanupEvidence(final.postCleanup) } }; } },
  };
  const runUiOperation = operations.runUi;
  operations.runUi = async () => ({ ...(await runUiOperation()), observerCounters: uiRuntime?.observerCounters ?? uiRuntime?.tracker?.observerCounters?.() });
  operations.runFinal = async () => {
    const ledgers = { api: apiRuntime, ui: uiRuntime };
    const database = apiRuntime?.db ?? uiRuntime?.db;
    if (!database) return;
    const collected = collectCurrentRunOwnedCandidates({ runId, canonicalIds: canonicalDeviceIds, ledgers });
    const final = await finalizeDisposableDevices({
      db: { async readAllIds() { const { data, error } = await database.from("devices").select("id"); if (error) throw error; return (data ?? []).map((row) => row.id); } },
      ownedIds: collected.ownedIds,
      canonicalIds: canonicalDeviceIds,
      cleanupOwned: ({ ownedIds, canonicalIds }) => cleanupOwnedDevicesWithLocalPostgres({ ownedPostgres, ownedIds, canonicalIds, execute: ({ containerId, sql }) => command("docker", ["exec", "-i", containerId, "psql", "-U", "postgres", "-d", "postgres", "-tA", "-v", "ON_ERROR_STOP=1"], { input: sql }) }),
    });
    for (const ledger of Object.values(ledgers)) for (const row of ledger?.tracker?.entries?.() ?? []) if (collected.ownedIds.includes(row.id)) ledger.tracker.remove(row.id);
    const deletedOwnedCount = Number.isInteger(final.postCleanup.trustedCleanup?.deletedRowCount) ? final.postCleanup.trustedCleanup.deletedRowCount : 0;
    const fullOwnedCleanup = { ...collected.evidence, deletedOwnedCount, alreadyAbsentOwnedCount: Math.max(0, collected.evidence.validatedOwnedCandidateCount - deletedOwnedCount) };
    return { observations: { P6B_RUNNER2C2_FINAL_CANONICAL_DEVICE_COUNT: final.canonicalDeviceCount, P6B_RUNNER2C2_FINAL_CANONICAL_UNIQUE_SLUG_COUNT: final.canonicalUniqueSlugCount, P6B_RUNNER2C2_FINAL_DISPOSABLE_ROWS_REMAINING: final.runOwnedDisposableRemaining, P6B_RUNNER2C2_CANONICAL_ACCIDENTAL_MUTATIONS: final.canonicalAccidentalMutationCount, P6B_RUNNER2C2_CANONICAL_IDENTITY_RESTORED: final.canonicalIdentityRestored, P6B_RUNNER2C2_API_FINAL_DATA_GATE: "PASS", fullOwnedCleanup, ...postCleanupEvidence(final.postCleanup) } };
  };
  const result = await runP6bLifecycle({ operations, evidence, config: { mode: helperOnly ? "HELPER" : mode, apiRequired: helperOnly || uiOnly || forensicOnly ? 0 : (apiOnly || full ? 16 : 2), uiRequired: full || uiOnly ? (focusedUiOnly ? focusedUiCaseLimit : 16) : 0, finalizeOnUiFailure: mode === "FOCUSED_UIE2E03" || mode === "FOCUSED_UIE2E12" || mode === "FOCUSED_UIE2E13" || mode === "FOCUSED_UIE2E14" || mode === "FOCUSED_UIE2E15", helperOnly, forensicOnly, onProgress: (entry) => progress.update(entry) }, runId });
  progress.setObserverCounters({ observerPendingPromises: result.observerPendingPromises, observerStaleListeners: result.observerStaleListeners, observerUnhandledRejections: result.observerUnhandledRejections }); progress.update({ lifecycleStage: "LIFECYCLE_TERMINAL" }); await progress.flush(); await writeFinalTerminal(runId, { ...result, mode: helperOnly ? "helper-probe" : mode, progress: progress.snapshot() });
  return result;
}
export async function runAstroOnly() { const runId = randomUUID().slice(0, 8); const port = 56001; const astro = startAstroServer({ port, env: { CLOUDFLARE_INCLUDE_PROCESS_ENV: "true", SUPABASE_URL: "http://127.0.0.1:1", SUPABASE_ANON_KEY: "local-sentinel", PUBLIC_SUPABASE_URL: "http://127.0.0.1:1", PUBLIC_SUPABASE_ANON_KEY: "local-sentinel" } }); let result; try { await waitForHttp(`http://127.0.0.1:${port}/`, astro); const stopped = await stopOwnedAstro(astro, port); const released = stopped.portRelease.released; result = { runId, mode: "astro-only", acceptanceResult: stopped.stopped && released ? "PASS" : "BLOCKED", cleanupResult: stopped.stopped && released ? "PASS" : "BLOCKED", astroStartCount: 1, supabaseStartCount: 0, browserStartCount: 0, observations: { astroRootPid: astro.child.pid, astroPortReleased: released, astroPortReleaseProbeCount: stopped.portRelease.probeCount, astroPortReleaseTimedOut: stopped.portRelease.timedOut, remoteConnections: 0 }, terminal: stopped.stopped && released ? READY : BLOCKED }; } catch (error) { try { await stopOwnedAstro(astro, port); } catch {} result = { runId, mode: "astro-only", acceptanceResult: "BLOCKED", cleanupResult: "BLOCKED", astroStartCount: 1, supabaseStartCount: 0, browserStartCount: 0, terminal: BLOCKED }; } await writeFinalTerminal(runId, result); return result; }
if (mainEntry()) (process.argv.includes("--astro-only") ? runAstroOnly() : runSmoke({ helperOnly: process.argv.includes("--helper-probe"), mode: parseRunnerMode(process.argv) })).then((result) => { console.log(JSON.stringify(result)); process.exitCode = result.acceptanceResult === "PASS" && result.cleanupResult === "PASS" ? 0 : 1; }).catch((error) => { console.error("P6B_RUNNER1B_SMOKE_BLOCKED", error.message); process.exitCode = 1; });
