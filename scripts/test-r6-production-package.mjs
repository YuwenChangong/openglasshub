import assert from "node:assert/strict";
import { createHash } from "node:crypto";
import { execFileSync, spawnSync } from "node:child_process";
import { mkdir, mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { validateProductionAuthorization, validateProductionManifest, validateProductionPackageReviewEligibility } from "./qa/r6-production-package-contract.mjs";
import { validateProductionLauncherTerminal } from "./qa/validate-r6-production-launcher-terminal.mjs";
import { validateProductionLauncherBreadcrumb } from "./qa/validate-r6-production-launcher-breadcrumb.mjs";
import { createCanonicalCanaryTargetBinding } from "./qa/canonical-canary-target-binding.mjs";
import { FINAL_AUTHORIZATION_VERSION, getMinimalCanaryMutationPlan } from "./qa/r6-final-canary-execution-contract.mjs";
import { FINAL_EXECUTION_BINDING_VERSION } from "./qa/r6-final-execution-binding.mjs";
import { createDryRunSourceManifest } from "./qa/r6-dryrun-source-chain-contract.mjs";
import { createOAuthReadinessAttestation, writeOAuthReadinessAttestation } from "./qa/r6-oauth-readiness-attestation.mjs";

const root = await mkdtemp(path.join(os.tmpdir(), "r6-production-package-"));
const hash = (value) => createHash("sha256").update(value).digest("hex");
try {
  const operatorRoot = path.join(root, "operator"); const evidenceRoot = path.join(root, "evidence"); const launcher = path.join(root, "launch-production.ps1"); const manifest = path.join(operatorRoot, "production-binding-manifest.json"); const authorization = path.join(operatorRoot, "production-authorization.json");
  const executionCommit = "a".repeat(40); const sourceRunId = "qa-canary-bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb"; const sourceRoot = path.join(root, "source-evidence"); const sourceOperator = path.join(root, "source-operator"); const sourceRegistryRoot = path.join(root, "registry"); const sourceManifestPath = path.join(sourceOperator, "dryrun-binding-manifest.json"); const receiptPath = path.join(sourceRegistryRoot, "consumed-run-receipts-v1", sourceRunId, "aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa.json"); const authenticatedResultPath = path.join(sourceRoot, "auth.json"); const dryRunTerminalPath = path.join(sourceRoot, "dry-run.json"); const orchestrationTerminalPath = path.join(sourceRoot, "orchestration.json"); const targetBindingPath = path.join(sourceRoot, "target-binding.json");
  const plan = getMinimalCanaryMutationPlan(); const targetBinding = createCanonicalCanaryTargetBinding({ resolvedAtUtc: "2099-01-01T00:00:00.000Z", canonicalCircleId: "11111111-1111-4111-8111-111111111111", canonicalCircleSlug: "synthetic-target", baseMutationPlanSchema: plan.schemaVersion, baseMutationPlanHash: plan.planSha256, executionCommit, toolingCommit: executionCommit });
  await Promise.all([mkdir(sourceRoot, { recursive: true }), mkdir(sourceOperator, { recursive: true }), mkdir(path.dirname(receiptPath), { recursive: true })]);
  await Promise.all([
    writeFile(receiptPath, JSON.stringify({ state: "CONSUMED", runId: sourceRunId, runnerCommit: executionCommit })),
    writeFile(authenticatedResultPath, JSON.stringify({ success: true })),
    writeFile(dryRunTerminalPath, JSON.stringify({ success: true, runId: sourceRunId, actualMutationCount: 0, supabaseWriteCount: 0, productionMutationCount: 0, retryCount: 0 })),
    writeFile(orchestrationTerminalPath, JSON.stringify({ success: true, runId: sourceRunId })),
    writeFile(targetBindingPath, JSON.stringify(targetBinding)),
  ]);
  const nonce = "a".repeat(64); const session = "11111111-1111-4111-8111-111111111111"; const sourceAttestationPath = path.join(sourceOperator, "oauth-readiness-attestation.json");
  const sourceAttestation = createOAuthReadinessAttestation({ profile: { profilePath: path.join(root, "profile.toml"), profileFileSha256: "f".repeat(64), token: "safe", hasRefreshCapability: true }, executionCommit, branch: "feature/r6-current-canonical-production-identity-v1", wrapperSha256: "b".repeat(64), wranglerVersion: "4.106.0", wranglerEntrySha256: "c".repeat(64), atomicSessionId: session, parentPowerShellPid: 42, parentPowerShellStartTime: "2099-01-01T00:00:00.000Z", handoffNonce: nonce, now: () => new Date("2099-01-01T00:00:00.000Z") });
  const sourceEnvelope = await writeOAuthReadinessAttestation({ attestation: sourceAttestation, attestationPath: sourceAttestationPath });
  const verifiedOauthReadiness = { attestation: sourceAttestation, attestationPath: sourceAttestationPath, attestationSha256: sourceEnvelope.attestationSha256, issuerStartedAt: "2099-01-01T00:00:30.000Z", ageSeconds: 30, remainingSeconds: 870 };
  const sourceManifest = createDryRunSourceManifest({ runId: sourceRunId, executionCommit, branch: "feature/r6-current-canonical-production-identity-v1", executionWorktree: path.join(root, "detached"), wrapperPath: path.join(root, "wrapper.ps1"), wrapperSha256: "b".repeat(64), wranglerVersion: "4.106.0", wranglerEntrySha256: "c".repeat(64), evidenceRoot: sourceRoot, operatorRoot: sourceOperator, launcherPath: path.join(root, "source-launcher.ps1"), launcherTerminalPath: path.join(sourceOperator, "launcher-terminal-result.json"), launcherBreadcrumbPath: path.join(sourceOperator, "launcher-stage-breadcrumb.json"), wrapperEntryMarkerPath: path.join(sourceOperator, "wrapper-entry-marker.json"), captureTerminalPath: path.join(sourceRoot, "capture-terminal.json"), authCheckTerminalPath: authenticatedResultPath, targetBindingPath, receiptPath, dryRunTerminalPath, orchestrationTerminalPath, registryRoot: sourceRegistryRoot, registryPath: path.join(sourceRegistryRoot, "consumed-run-registry-v1.json"), confirmationSha256: "d".repeat(64), oauthReadinessAttestationPath: sourceAttestationPath }, { now: () => new Date("2099-01-01T00:00:30.000Z"), atomicOAuth: true, verifiedOauthReadiness });
  await writeFile(sourceManifestPath, JSON.stringify(sourceManifest));
  const finalAuthorizationPath = path.join(sourceOperator, "final-dryrun-authorization.json");
  const finalAuthorization = { schemaVersion: FINAL_AUTHORIZATION_VERSION, dryRunRunId: sourceRunId, dryRunReceiptPath: receiptPath, dryRunReceiptSha256: hash(await readFile(receiptPath)), dryRunTerminalPath, dryRunTerminalSha256: hash(await readFile(dryRunTerminalPath)), dryRunOrchestrationTerminalPath: orchestrationTerminalPath, dryRunOrchestrationTerminalSha256: hash(await readFile(orchestrationTerminalPath)), executionCommit, toolingCommit: executionCommit, plan, targetBinding, plannedMutationCount: 2, actualMutationCount: 0, supabaseWriteCount: 0, productionMutationCount: 0, retryCount: 0, successClassification: "R6_CURRENT_CANONICAL_V3_DRY_RUN_ONLY_READY" };
  await writeFile(finalAuthorizationPath, JSON.stringify(finalAuthorization));
  const finalAuthorizationSha256 = hash(await readFile(finalAuthorizationPath));
  const finalBindingPath = path.join(sourceOperator, "final-execution-binding.json");
  const finalBinding = { schemaVersion: FINAL_EXECUTION_BINDING_VERSION, executionWorktree: path.join(root, "detached"), executionCommit, runnerCommit: executionCommit, toolingCommit: executionCommit, wrapperPath: path.join(root, "wrapper.ps1"), wrapperSha256: "b".repeat(64), finalContractGitBlob: "1".repeat(40), executeRunnerGitBlob: "2".repeat(40), postflightRunnerGitBlob: "3".repeat(40), bindingValidatorGitBlob: "4".repeat(40), bindingLibraryGitBlob: "5".repeat(40), parentAuthorizationPath: finalAuthorizationPath, parentAuthorizationSha256: finalAuthorizationSha256, parentReceiptPath: receiptPath, parentReceiptSha256: hash(await readFile(receiptPath)), parentDryRunRunId: sourceRunId, planSchema: plan.schemaVersion, planSha256: plan.planSha256, targetBinding, approvedOperationIds: ["CREATE_POST", "CREATE_COMMENT"], plannedMutationCount: 2, parentActualMutationCount: 0 };
  await writeFile(finalBindingPath, JSON.stringify(finalBinding));
  const source = { manifestSchema: sourceManifest.schemaVersion, manifestPath: sourceManifestPath, manifestSha256: hash(await readFile(sourceManifestPath)), sourceManifest, oauthReadinessAttestation: sourceAttestation, oauthReadinessAttestationPath: sourceAttestationPath, oauthReadinessAttestationSha256: sourceEnvelope.attestationSha256, runId: sourceRunId, executionCommit, evidenceRoot: sourceRoot, receiptPath, receiptSha256: hash(await readFile(receiptPath)), authenticatedResultPath, authenticatedResultSha256: hash(await readFile(authenticatedResultPath)), dryRunTerminalPath, dryRunTerminalSha256: hash(await readFile(dryRunTerminalPath)), orchestrationTerminalPath, orchestrationTerminalSha256: hash(await readFile(orchestrationTerminalPath)), targetBindingPath, targetBindingSha256: hash(await readFile(targetBindingPath)), finalAuthorizationPath, finalAuthorizationSha256, sourcePlanSchema: plan.schemaVersion, sourcePlanSha256: plan.planSha256, sameCommitBinding: true };
  const config = { runId: "qa-canary-aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa", executionCommit, branch: "feature/r6-current-canonical-production-identity-v1", executionWorktree: path.join(root, "detached"), wrapperPath: path.join(root, "wrapper.ps1"), wrapperSha256: "b".repeat(64), wranglerVersion: "4.106.0", wranglerEntrySha256: "c".repeat(64), evidenceRoot, operatorRoot, launcherTerminalPath: path.join(operatorRoot, "launcher-terminal-result.json"), launcherBreadcrumbPath: path.join(operatorRoot, "launcher-stage-breadcrumb.json"), executionTerminalPath: path.join(evidenceRoot, "final-canary-execution-terminal-result.json"), orchestrationTerminalPath: path.join(evidenceRoot, "final-canary-execute-and-postflight-orchestration-terminal-result.json"), postflightTerminalPath: path.join(evidenceRoot, "final-canary-read-only-postflight-terminal-result.json"), receiptPath: path.join(root, "receipts", "receipt.json"), journalPath: path.join(root, "journals", "journal.json"), confirmationSha256: hash("confirmation"), source };
  const configPath = path.join(root, "config.json"); await writeFile(configPath, JSON.stringify(config));
  const output = execFileSync(process.execPath, ["scripts/qa/issue-r6-production-package.mjs", "--config", configPath, "--launcher", launcher, "--manifest", manifest, "--authorization", authorization], { cwd: process.cwd(), encoding: "utf8" });
  assert.ok(JSON.parse(output).launcherSha256);
  const [manifestValue, authorizationValue] = await Promise.all([readFile(manifest, "utf8").then(JSON.parse), readFile(authorization, "utf8").then(JSON.parse)]);
  validateProductionManifest(manifestValue); validateProductionAuthorization(authorizationValue);
  const parser = spawnSync("powershell.exe", ["-NoProfile", "-ExecutionPolicy", "Bypass", "-File", launcher, "-ValidateOnly"], { encoding: "utf8" }); assert.equal(parser.status, 0); assert.match(parser.stdout, /R6_PRODUCTION_LAUNCH_VALIDATE_ONLY_READY/);
  const terminal = validateProductionLauncherTerminal(JSON.parse(await readFile(config.launcherTerminalPath, "utf8"))); assert.equal(terminal.success, true); assert.equal(terminal.wrapperStarted, false);
  const breadcrumb = validateProductionLauncherBreadcrumb(JSON.parse(await readFile(config.launcherBreadcrumbPath, "utf8"))); assert.equal(breadcrumb.runId, config.runId);
  const invalidPrimary = { ...finalBinding, bindingSha256: hash(JSON.stringify(finalBinding)) };
  await writeFile(finalBindingPath, JSON.stringify(invalidPrimary));
  const replacementPath = path.join(sourceOperator, "final-execution-binding-reissue-v1.json");
  await writeFile(replacementPath, JSON.stringify(finalBinding));
  const replacementSha256 = hash(await readFile(replacementPath));
  await writeFile(path.join(sourceOperator, "final-execution-binding-reissue-terminal.json"), JSON.stringify({ schemaVersion: "r6-final-execution-binding-reissue-terminal-v1", issuedAt: "2099-01-01T00:00:00.000Z", primaryInvalidBindingPath: finalBindingPath, primaryInvalidBindingSha256: hash(await readFile(finalBindingPath)), primaryFailureClassification: "R6_FINAL_EXECUTION_BINDING_INVALID", primaryFailureReasonCode: "unexpected_field_bindingSha256", replacementBindingPath: replacementPath, replacementBindingSha256: replacementSha256 }));
  const reissueReview = await validateProductionPackageReviewEligibility({ source, executionCommit });
  assert.equal(reissueReview.bindingSelection, "reissue");
  assert.equal((await Promise.all([evidenceRoot, config.receiptPath, config.journalPath].map(async (item) => await import("node:fs/promises").then(({ stat }) => stat(item).then(() => true).catch(() => false))))).some(Boolean), false);
  console.log("R6_PRODUCTION_PACKAGE_PIPELINE_TEST_OK rendered launcher, manifest, authorization, and validate-only fixture passed with zero live operations");
} finally { await rm(root, { recursive: true, force: true }); }
