# Legal Consent API Ordering Audit

This audit traces authenticated mutation handlers before Phase 4A2 integration. The authoritative method inventory is `tests/fixtures/legal-consent-api-methods.mjs`; deterministic batch construction is `tests/fixtures/legal-consent-api-trace-batches.mjs`; validation runs with `npm run test:legal-consent-trace-batches`.

Methods are sorted by source file then `GET`, `POST`, `PUT`, `PATCH`, `DELETE` and divided into six pending batches. A completed trace must identify handler and helper chain, authenticated actor, role/ownership checks, privileged-client first use, database/R2/external effects, validation/rate/idempotency, response, and the future consent insertion point.

Checkpoint progress: 0/66 fully traced; 66 pending. Phase 4A1 remains NO_GO. No production endpoint is protected by this checkpoint, and direct API bypass remains possible for all 37 normal consent-required mutations.

## Phase 4A1 Batch 1F - admin/moderation/approve.ts

POST requires `requireModerator`, validates a post/comment UUID action payload, reads the target for already-applied protection, updates the target moderation fields, then records a moderation action. The verified actor is passed as `moderatorId`; no client identity replaces it. Future consent belongs after `requireModerator`, before payload parsing and target lookup.

## Parent Batch 1 closure

All 11 Batch 1 method records are traced. Parent Batch 1 is complete; batches 2 through 6 remain pending. Cumulative progress is 11/66 traced and 55 pending. No endpoint enforcement is active.

## Phase 4A1 Batch 2A - admin/moderation/hide.ts POST

POST verifies moderator identity, validates target type and UUID, reads the target for already-hidden protection, updates its moderation state, and then inserts the moderation action record. Future consent belongs after `requireModerator`, before payload parsing and target lookup. The action record may fail after target update, so this is a documented Phase 4B transactional-hardening concern, not a pre-authorization side effect.

## Phase 4A1 Batch 2B - admin/moderation/lexicon-health.ts GET

GET requires moderator authentication and then calls `getSensitiveLexiconHealth`. The loader reads the configured R2 object when available, otherwise reads the generated local file or uses the emergency in-memory lexicon. Caching is process-memory only; no persistent write, RPC mutation, audit event, notification, email, or telemetry side effect exists.

## Phase 4A1 Batch 2C - admin/moderation/queue.ts GET

GET requires moderator authentication, bounds the queue limit, selects matching post and comment moderation rows, and sorts mapped results in memory. No persistent or external side effect occurs.

## Phase 4A1 Batch 2D - admin/moderation/reject.ts POST

POST requires moderator identity, validates the post/comment target payload, reads the target for already-rejected protection, updates target moderation state, then inserts the moderation action record. The verified actor supplies `moderatorId`; target fields cannot replace it. The audit record can fail after target mutation, so transactional compensation is Phase 4B hardening. Future consent belongs immediately after `requireModerator`.

## Phase 4A1 Batch 2E - admin/news.ts

GET delegates only to read-only admin news queries and does not synchronize external news or mutate a cache. POST/PATCH validate all article fields, build a unique slug, and write `news_articles` with the verified moderator as author. DELETE validates the target id and deletes the article. Future consent belongs immediately after `requireModerator` for each mutation; no R2, email, notification, provider, or cache-persistence side effect exists.

## Phase 4A1 Batch 2F - admin/reports.ts GET

GET requires moderator authentication, bounds and allowlists filters, and calls `fetchAdminReportsQueue` for report and target preview reads. It does not update report state, targets, events, notifications, cache, email, or external services.

## Phase 4A1 Batch 2G - admin/reports/[id].ts GET

GET validates a non-empty dynamic report id, requires moderator authentication, then calls `fetchAdminReportDetail` for report, target preview, reporter, count, event, and actor reads. The dynamic id never supplies the acting moderator and no status/event/target/notification/cache mutation occurs. The route only checks that the id is non-empty, and its generic 500 branch returns the caught `Error.message` rather than sanitizing helper errors.

## Phase 4A1 Batch 2H - admin/reports/[id]/action.ts POST

POST validates runtime env and a non-empty report id, authenticates and derives the moderator from the bearer token through `requireModerator`, then parses JSON and allowlists `dismiss`, `reviewing`, `hide_target`, `reject_target`, `warn_user`, `suspend_user`, and `ban_user`. It does not check the id format, content type, request size, or rate limit. A future consent guard can be awaited in `POST` immediately after `requireModerator(request, env)` and before `request.json()`; this needs no refactor beyond passing the verified `auth.user.id` to the guard.

`applyAdminReportAction` reads the report and target after authentication. Reviewing and dismiss update `reports` before inserting the report event. Post/comment hide or reject reads the target again, updates the target, inserts `moderation_actions`, updates the report, inserts the report event, then starts a notification RPC without awaiting it. Circle hide updates the circle, then follows the report/event sequence; circle reject and user hide are rejected. User warn/suspend/ban read user safety state, upsert it, insert a user-safety event, attempt a notification, optionally attempt and swallow a supplemental note failure, then update the report and insert the report event.

Findings: raw helper `Error.message` values are returned to clients through both non-ok results and the generic 500 branch (Phase 4B hardening). The target/state, moderation-action, report-state, report-event, and notification writes are not transactional; each later failure can leave earlier writes committed (Phase 4B hardening). There is no report-resolved gate or action idempotency key, so repeated requests can duplicate report events and some target/user-safety actions; target post/comment moderation suppresses only an already-desired target update, not the later report event (Phase 4B hardening). Missing request-size, content-type, rate-limit, and UUID controls are Phase 4B hardening. These findings do not block this Phase 4A1 source trace.

## Phase 4A1 Batch 3A - admin/users.ts GET

GET requires a verified moderator or administrator before parsing `q` and `limit`. It clamps `limit` to 1 through 200, lists profile identity/display/role fields, then reads safety rows for only those returned profile ids. Missing safety rows are represented by `createDefaultUserSafetyState`, and expired suspensions are normalized in memory; neither operation writes state. There is no auth-admin call, target action, self-action path, profile or role mutation, notification, email, audit, analytics, cache, or other side effect. The acting administrator comes only from `requireModerator`; response profile ids are not action inputs.

Findings: profile and safety query errors, plus generic caught errors, return raw `Error.message` values (Phase 4B hardening). `q` is embedded directly in the PostgREST `or` filter without a length bound or filter-grammar escaping, and the endpoint has no cursor pagination or read-rate limit (Phase 4B hardening). This is a read-only method, so no legal-consent insertion point applies and no mutation-specific partial-failure, idempotency, self-target, or privilege-escalation path exists.

## Phase 4A1 Batch 3B re-audit - admin/users/[id]/ban.ts POST

`POST` validates runtime env and a non-empty `[id]`, authenticates with `requireModerator`, then parses and bounds the reason. The route passes `auth.user.id` as actor and `[id]` only as the target resource; body, query, and headers cannot supply an actor id or role. The verified bearer produces an anon-key RLS client and the route rejects non-moderator/admin actors before JSON parsing.

Historical blocker: the former helper read only target `id`, allowing a moderator to target staff. Remediation commit `56af1cf6b7c4e0aa5df8f35539d5e4cceea80217` adds server-side actor and target `id,role` reads. Self-targeting is rejected first; missing or unknown roles deny; moderators may target only users; administrators may target users or moderators but never administrators. All authorization failures occur before `getUserSafetyState`, the first safety-state read, and before every write or notification.

After authorization, the method reads current safety state, upserts the state as its first irreversible effect, inserts the safety event, and attempts the notification RPC. An already-banned state returns `USER_ALREADY_BANNED` before writes, but no request idempotency key or transaction spans state/event/notification, and later helper errors can still reach the route's generic `Error.message` branch. Those are Phase 4B hardening findings. Future consent belongs immediately after `requireModerator` and before `request.json()`. The privilege blocker is cleared by this re-audit; Batch 3 remains pending at 24/66 traced and 42 pending, and Phase 4A1 remains NO_GO.

## Phase 4A1 Batch 3C - admin/users/[id]/clear-warning.ts POST

POST validates runtime env and a non-empty route target id, verifies the bearer moderator/admin before parsing its optional reason, then calls the shared safety helper with only `auth.user.id` as actor. The helper rejects self-targeting, reads both server-side profile roles, and applies the same fail-closed hierarchy before any safety-state read: moderators may target only users; administrators may target users or moderators but never administrators.

Clear warning rejects suspended or banned states. It decrements one warning and emits a `note` event with the verified actor when a warning exists; an active zero-warning state returns a no-op success without state write, event, or notification. There is no separate warning metadata to erase, and no clear-warning notification/email/external branch. The state upsert precedes the event, so event failure can leave committed state; concurrent requests have no idempotency key beyond the normal no-op path. Content type, body-size, rate limit, route UUID validation, and later raw helper-error exposure remain Phase 4B hardening. Future consent belongs after `requireModerator` and before JSON parsing. Batch 3 remains pending at 25/66 traced and 41 pending.

## Phase 4A1 Batch 3D - admin/users/[id]/safety.ts GET

GET validates runtime env and only a non-empty target id, then requires a verified moderator/admin before reading the target profile. It concurrently loads normalized safety state and the 100 most recent safety events ordered newest first, then reads profiles for distinct event actors. It does not invoke the user-safety action or privilege hierarchy helper because no mutation is attempted; there is no self-target or target-hierarchy action authorization path to apply.

The staff-only response includes the target role, full normalized state including `ban_reason` and `updated_by`, plus each event's actor id, reason, metadata, and actor profile. It does not select email. No safety-state, last-viewed, audit, event, notification, cache, RPC, email, external-service, or privileged-client mutation occurs. Raw database/helper `Error.message` values can reach 500 responses, and the endpoint has no UUID validation, cursor pagination, or read-rate limit; these are Phase 4B hardening findings. No consent insertion point applies to this read-only route. Batch 3 remains pending at 26/66 traced and 40 pending.

## Phase 4A1 Batch 3E - admin/users/[id]/suspend.ts POST

POST validates runtime env and a non-empty route target id, authenticates the bearer moderator/admin, parses a required sanitized reason, and passes `auth.user.id` plus the target id to the shared helper. The helper rejects self-targeting, re-reads actor and target roles through the RLS client, and completes the fail-closed hierarchy before safety-state access: moderators may target only users; administrators may target users or moderators but never administrators.

After a safety-state read, suspension rejects banned or effectively suspended targets and requires a valid future deadline. The first irreversible effect is the safety-state upsert, followed by the verified-actor safety event and a notification RPC attempt. A notification failure is swallowed; event insertion failure can follow a committed state update. An effective existing suspension returns `USER_ALREADY_SUSPENDED` before writes, but there is no request idempotency key or transaction for concurrent retries. Route UUID/content-type/body-size/rate validation and later raw helper-error exposure remain Phase 4B hardening. Future consent belongs after `requireModerator` and before JSON parsing. Batch 3 remains pending at 27/66 traced and 39 pending.

## Phase 4A1 Batch 3F - admin/users/[id]/unban.ts POST

POST validates runtime env and a non-empty route target id, verifies moderator/admin bearer identity, parses an optional reason, and passes only `auth.user.id` plus the target resource id to the shared helper. Self-target and the server-side actor/target role hierarchy complete before safety-state access: moderators may target only users; administrators may target users or moderators but never administrators.

The `unban` transition is not limited to a banned state: it also clears suspended state, including an expired suspension whose stored status remains suspended. It preserves warning count, strike count, and reputation while resetting status from the warning count and clearing `suspended_until`, `banned_at`, and `ban_reason`. An unrestricted target returns `USER_NOT_RESTRICTED` before writes. The state upsert precedes the verified-actor unban event; there is no unban notification/email/external branch. Event failure can follow committed state, and concurrent retries have no idempotency key. Route UUID/content-type/body-size/rate validation and later raw helper-error exposure remain Phase 4B hardening. Future consent belongs after `requireModerator` and before JSON parsing. Batch 3 remains pending at 28/66 traced and 38 pending.

## Phase 4A1 Batch 3G - admin/users/[id]/warn.ts POST

POST validates runtime env and a non-empty route target id, verifies moderator/admin bearer identity, requires a sanitized reason, and passes only `auth.user.id` and the target resource id to the shared helper. Self-target and server-side actor/target role hierarchy checks complete before safety-state access: moderators may target only users; administrators may target users or moderators but never administrators.

Warning rejects effectively banned or suspended targets. Each successful call increments warning count by one and decreases reputation by one, leaves strike count unchanged, and does not automatically suspend or ban at any threshold. State upsert is the first irreversible effect, followed by a verified-actor warning event and notification RPC attempt; notification failure is swallowed, while event failure can follow committed state. No already-warned/idempotency guard exists, so repeated or concurrent requests can create duplicate warnings/events/notification attempts. Route UUID/content-type/body-size/rate validation and later raw helper-error exposure remain Phase 4B hardening. Future consent belongs after `requireModerator` and before JSON parsing. Batch 3 remains pending at 29/66 traced and 37 pending.

## Phase 4A1 Batch 3 blocker - auth/resend-confirmation.ts POST

`POST /api/auth/resend-confirmation` is expected to remain an unauthenticated auth/recovery exemption: it accepts a target email and a client-controlled JSON `next`, so requiring legal consent before confirmation would prevent an unconfirmed account from reaching the authenticated callback and consent flow. That exemption does not validate redirects.

The route passes `payload.next` to `getSafeNext`. In `src/lib/auth-redirect.ts`, `getSafeNext` rejects a leading `//` but immediately returns every value satisfying `candidate.startsWith("/")`; it neither rejects backslashes nor resolves that candidate against the trusted origin. The unsafe input `/\\evil.example` therefore survives the sanitizer. `buildAuthCallbackRedirect` then serializes it into the same-origin `/auth/callback/` URL. `src/pages/auth/callback.astro` passes callback `next` to `AuthCallback`, which calls `getSafeNext` again and passes the result to `browserNavigationAdapter.replace`. That adapter invokes `window.location.replace`, whose special-URL normalization interprets `/\\evil.example` as `https://evil.example/`.

This is arbitrary external redirect injection after confirmation/callback processing and is release-blocking. Consent enforcement cannot repair it because the unsafe value is already propagated through the callback metadata and is also used after consent. Batch 3 tracing is paused: `resend-confirmation.ts#POST` remains pending, no later Batch 3 method may be completed, and progress remains 29/66 traced and 37 pending. All `getSafeNext` callers require centralized remediation review before this method receives a separate re-audit.

Centralized remediation is now implemented in `getSafeNext`: it accepts only a canonical application-relative path/query/hash that remains at the trusted origin after URL resolution, and rejects raw or repeatedly decoded backslashes, controls, boundary whitespace, protocol-relative authority, malformed encodings, absolute URLs, and cross-origin resolution. `buildAuthCallbackRedirect` and `buildResetPasswordRedirect` now share trusted-origin parsing, and browser navigation plus the legal-consent gate sanitize again at the final sink. Login, register/signup, resend confirmation, callback, consent, header/menu, CTA, and recovery callers were reviewed.

Re-audit at remediation commit `eb92faf98dd0dd60c10fb6d770781b3a99c1e604` clears the active redirect blocker. `POST` performs runtime-env handling, JSON parsing, email normalization, `getSafeNext`, basic email validation, anon auth-client construction, salted request-IP hashing, the five-per-24-hour verification rate RPC, trusted callback URL construction, and `supabase.auth.resend` in that order. The rate RPC records the first persistent effect before the provider call; generic success covers both provider-return and provider-throw paths, while outer failures return a fixed code. `/\\evil.example` now yields `/`; the test matrix also proves the same fallback behavior for authority, encoded authority/backslash, absolute URL, scheme, malformed-percent, control, whitespace, and userinfo variants. The callback component, consent gate, and browser adapter all sanitize again before navigation, so the browser sink receives only canonical internal output.

This remains an auth/recovery exemption: it intentionally has no authenticated actor, and the request email identifies only the account to confirm. Legal consent cannot be required before the confirmation session can exist, so no future consent insertion applies. The trace preserves non-blocking Phase 4B observations: no content-type or body-size check, an IP-only server rate limit that fails open when its backend is unavailable, no source-proven request idempotency or per-email limit, and no explicit RPC serialization. Progress is 30/66 traced and 36 pending; parent Batch 3 remains pending.

## Phase 4A1 Batch 3I - forum/circles.ts

GET is intentionally public and uses the anon-key RLS client to select all circles ordered by name, with no caller query, pagination, limit, or sort parameter. The server then excludes deleted rows and known QA-artifact slugs/names through `isPublicVisibleCircle`; the migrated status constraint permits only active or deleted values. No view counter, last-viewed marker, membership, audit, analytics, cache, notification, RPC, email, storage, or moderation-provider operation occurs. The public RLS select policy is `using (true)`, so this handler's active/deleted filter is the source-level visibility control. Raw query/caught errors and the unpaginated list remain Phase 4B hardening findings.

POST first verifies the bearer through `requireForumUser`, derives the actor solely from `auth.getUser`, constructs the bearer-bound anon-key RLS client, loads the verified profile, and checks the actor safety state. A future consent guard belongs immediately after `requireForumUser` and before that safety-state read or JSON parse. Body validation then bounds name, description, type, image path, and prevents `owner_id` from differing from the verified actor; the requested slug is ignored. The server hashes the request IP and `enforceUserRateLimit` inserts a verified-user attempt row before duplicate checks, moderation, or the circle write, making rate accounting the first persistent effect. Name duplicate checking, slug probing, local sensitive-lexicon checks, optional text/provider moderation, optional signed cover reads/provider moderation, and their reject paths all complete before insertion. The only circle domain write is the `circles` row with `owner_id: auth.user.id`; no member or owner-membership relation exists in this endpoint or the current schema. RLS independently requires owner id to equal `auth.uid()` unless the verified session is moderator/admin.

PATCH follows the same bearer/profile/safety order and takes future consent at the same point. It requires an id or slug, reads the target through the bearer-bound RLS client, then allows only the matching owner or verified moderator/admin profile role before field validation, duplicate checks, moderation, and a single `circles` update. No request field supplies actor, owner, role, type, slug, or status. The RLS update policy repeats owner-or-staff checks. The moderation reject path can remove a supplied managed cover before the domain update, while successful updates have no companion membership/audit/notification/email/RPC/cache/R2/service-role write.

Phase 4B: POST rate accounting persists even if later create stages fail; neither mutation has a request idempotency key or transaction; concurrent name races rely on the lower(name) unique index and the create path makes one slug-conflict retry. Missing content-type/body-size controls, PATCH rate limiting and UUID validation, GET pagination/read rate limiting, and raw database/caught error details remain to harden. Progress is 33/66 traced and 33 pending; parent Batch 3 remains pending.

## Phase 4A1 Batch 4A - forum/circles/[slug]/comments.ts

This source contains authenticated circle-management methods only: GET, PATCH, and DELETE. It does not create comments or replies, so it has no comment body/parent validation, rate-attempt persistence, comment insertion, insert-triggered notification, or comment/reply count update. Those behaviors are in the separate `forum/comments.ts` source and are not completed by this trace.

Every method trims/lowercases `[slug]` and rejects only an empty value. `requireManagedCircleBySlug` then verifies the bearer through `auth.getUser`, creates the bearer-bound anon-key RLS client, reads the verified profile and exact circle, and permits only the circle owner or moderator/admin. It has no service-role client, request-supplied actor, membership model, public path, or private-circle setting. It does not reject a deleted circle, so the owner/staff management surface remains available for a deleted circle. GET reads all RLS-visible posts in that circle, all their comments newest-first, and the returned author profiles; it has no pagination/filter/cursor and no write, cache, audit, notification, count, RPC, email, storage, or external effect.

PATCH parses a UUID comment id and allowlisted `published`/`deleted` status after managed-circle authorization. DELETE reads only a UUID id query parameter after the same authorization. Both read the target comment, then prove its post belongs to the resolved circle before allowing only the verified author or staff; a cross-circle id returns 404 before update. Non-staff paths include a second `author_id = auth.user.id` predicate. Same-status PATCH and already-deleted DELETE return no-op success before the single comment status update, which is each mutation's first and only persistent domain effect. There is no companion notification/count/audit write, so no multi-write partial-failure transaction applies.

Phase 4B: neither mutation calls `assertUserCanWrite`; PATCH also permits an author to restore an owned deleted comment to published without re-running moderation. Neither method has content-type/body-size/rate/idempotency-key controls, route slug syntax is only non-empty, and database/helper errors are returned raw. Progress is 36/66 traced and 30 pending; Batch 3 is complete and parent Batch 4 remains pending.

## Phase 4A1 Batch 4B - forum/circles/[slug]/manage.ts

This authenticated management source exports GET, PATCH, and DELETE only. `requireManagedCircleBySlug` verifies the bearer with `auth.getUser`, derives the actor from that verified result, constructs the bearer-bound anon-key RLS client, reads the verified profile and exact circle, then allows only its owner or moderator/admin. The request has no actor, owner, role, member, invite, transfer, or target-circle field. There is no membership model or operation in this endpoint, so it cannot create an ownerless circle, change membership, or cross circles through a supplied identifier. No service-role client is constructed.

GET counts only posts with `circle_id = auth.circle.id`, conditionally counts comments on those post ids, and creates a short-lived cover signed URL when the image path has the managed prefix. It has no mutation, cache write, audit, notification, email, telemetry, RPC, R2 write, or provider call. PATCH completes the same authentication and ownership/staff check, then `assertUserCanWrite`, before JSON parsing. It allowlists name, description, image path, and status; reads name duplicates; moderates text and an optional cover; then makes one RLS-scoped `circles.update` fixed to the resolved circle id. Rejected cover cleanup can remove only a proposed managed `circle-covers/` object after authorization and before the circle update, so it is a separate storage effect rather than a pre-authorization write. DELETE completes the same checks and performs one status=`deleted` update; it neither hard-deletes nor changes memberships, posts, comments, roles, ownership, or any external state.

Both mutations can insert future consent immediately after `requireManagedCircleBySlug` and before `assertUserCanWrite` or payload handling; no refactor is required. There is no privilege bypass, client-controlled actor, ownerless state, cross-circle membership manipulation, or mutation before authentication/management authorization. Phase 4B: the management helper permits deleted circles, and PATCH accepts status=`active`, so owner/staff can reactivate a deleted circle without a separate moderation-deletion/history policy. PATCH has no request idempotency key or transaction; DELETE repeats the soft-delete update rather than no-oping; raw database/caught error details, limited slug validation, absent request-size/content-type/rate controls, and unbounded GET post-id counting remain hardening work. Progress is 39/66 traced and 27 pending; Batch 3 remains complete and parent Batch 4 remains pending.

## Phase 4A1 Batch 4C - forum/circles/[slug]/posts.ts

This source is an authenticated circle-management list/status surface only: GET, PATCH, and DELETE. Each method trims and lowercases `[slug]`, rejects only an empty value, then `requireManagedCircleBySlug` verifies the bearer, derives `auth.user.id`, builds the bearer-bound anon-key RLS client, reads the profile and exact circle, and permits the owner or moderator/admin. The public/anon client constructor is not used and no service-role client exists. The circles schema has type and soft-delete status but no private visibility or membership relation; this source has no member, invite, ownership transfer, or role input or mutation.

GET reads every RLS-visible post for the resolved circle newest first, then author profiles plus post-media and report rows for those post ids. Final permission-lockdown RLS means a non-staff owner does not receive another author's hidden/deleted post merely through circle ownership. The method has no query filters, pagination, cursor, limit, cache, view/read marker, audit, analytics, notification, search-index, RPC, R2/storage, email, or external side effect.

PATCH parses only a UUID post id and `published`/`hidden`/`deleted` status after managed-circle authorization. It reads the post and requires its `circle_id` to equal the resolved circle; non-staff additionally must be the verified author and receive a second `author_id` update predicate. Final RLS independently allows only author or moderator/admin, and limits ordinary authors to pending/published/deleted status. Same-status PATCH is a no-op; otherwise its one status update is the first and only persistent effect. DELETE follows the same target/author/staff proof and returns a no-op for already-deleted posts before its single soft-delete update. Neither mutation creates a post, attaches or removes media, persists rate attempts, notifies, audits, indexes, emails, calls an external provider, or has a multi-write transaction.

Future consent can be inserted after `requireManagedCircleBySlug` and before staff derivation/body or query parsing without refactor. There is no client-controlled actor, private-circle bypass, cross-circle mutation, author/owner bypass, or effect before authentication/authorization. Phase 4B: PATCH and DELETE omit `assertUserCanWrite`; an author can restore an owned deleted post to published without a safety gate or fresh moderation, and deleted circles remain manageable through the shared helper. Missing request content-type/size/rate/idempotency controls, raw errors, weak slug validation, unbounded GET pagination, and lifecycle policy remain hardening. Progress is 42/66 traced and 24 pending; Batch 3 remains complete and parent Batch 4 remains pending.

## Resolved source finding - forum/comments.ts#PUT reaction visibility

The historical mismatch was that `PUT` authenticated the bearer, derived `authData.user.id`, and validated `comment_id`, but then read only the comment before its first reaction mutation. `GET` in the same source resolves the parent post and returns 404 unless both post status and moderation status are published. A hidden or soft-deleted post can retain its published comments because the circle post delete route changes only `posts.status`.

The re-audit clears the source authorization blocker. `PUT` verifies the bearer before parsing, validates only `comment_id`, derives comment → post → circle from server reads, and completes that full visibility chain before its own-like lookup and either reaction DELETE or INSERT. The forward migration `20260713_comment_reaction_visibility_authorization.sql` mirrors it in `comment_reactions_select_accessible`, `comment_reactions_insert_self`, `comment_reactions_update_self`, and `comment_reactions_delete_self`; INSERT requires `user_id = auth.uid()`, and UPDATE applies ownership plus visibility in both `USING` and `WITH CHECK`. There is no private-circle membership relation in the current schema, so no membership branch exists to authorize. Remediation commit `d57aae680fb81ed4133af73aae955473218d9c09` is source-complete. The migration is authored but not executed, which is a deployment prerequisite rather than an unresolved source bypass. `PUT` is now complete; progress is 43/66 traced and 23 pending. Batches 1-3 remain complete and Batch 4 remains pending.

## Resolved source finding - forum/comments.ts POST creation visibility

`POST` now verifies the bearer, derives `authData.user.id`, checks the actor safety state, validates `post_id`, body, and optional `parent_id`, reads the verified profile, then calls `resolveAccessibleCommentCreationTarget` before moderation or rate persistence. The resolver server-derives `posts.circle_id`, requires the exact post to be published with `moderation_status = published`, requires its exact circle to be active and canonical-public-visible, and requires an optional parent to be published and bound to that exact post. The insert uses only the resolver's post and parent ids plus the verified actor. There is no private-circle or membership relation in the current schema; the public visibility rule is therefore the complete current access contract.

The historical `comments_insert_self` policy in `20260616_community_moderation_mvp.sql` required only `author_id = auth.uid()` plus comment status. The forward migration `20260713_comment_creation_circle_authorization.sql` replaces that INSERT policy with actor ownership, valid published or pending-review comment state, and `can_create_comment_target(post_id, parent_id)`. Its security-definer predicate joins the post to an active canonical-visible circle and, for replies, requires a published parent on that same post; missing ancestors fail closed. The re-audit confirms runtime/RLS alignment for deleted, hidden, inactive, missing, and mismatched ancestors, including cross-post and non-published parents. `enforceUserRateLimit` is the first persistent effect after target authorization and moderation; `comments.insert` is the first domain mutation, and the existing after-insert notification trigger can run only after that insert succeeds. The route has no direct count/RPC, audit, email, cache, R2, or external effect. Source remediation commit `485215b2c311a0347258c5618db4db5326f84a58` clears the active source blocker. The migration remains authored but unexecuted, so deployment application is still required. GET is now complete, Batch 4 is complete, and progress is 45/66 traced and 21 pending.

## Resolved source finding - forum/comments.ts GET public read visibility

`GET /api/forum/comments?post_id=<uuid>` is publicly reachable: absent or invalid bearer credentials use the anon-key RLS client, while a verified bearer is used only for own pending-comment visibility, `liked_by_me`, and `can_delete`. The re-audit confirms this exact order: runtime check; `post_id` parsing and UUID validation; optional bearer parsing; anon RLS client creation; optional bearer-client authentication and verified viewer derivation; first database read of the exact post; published/moderation-state validation; exact server-derived circle read; active canonical-visibility validation; comment read/filtering; profile and reaction enrichment; conditional late bearer re-verification only when the earlier verification produced no viewer; response mapping. No client field supplies an actor, post, or circle relationship.

The historical source SELECT policies do not close that route gap: `posts_select_published_public` in `20260611_forum_permission_lockdown.sql` permits `status = 'published'` without a circle join, and `comments_select_public_or_staff` permits `status = 'published'` without a public comment-to-post-to-circle predicate. The new forward migration `20260713_comment_read_circle_visibility_authorization.sql` replaces their public branches with matching active canonical-circle and comment-post-circle predicates, and replaces reaction SELECT with the same full chain while preserving authenticated-own and staff branches. Published top-level comments and replies, author-profile enrichment (`username`, `display_name`, `avatar_url`, `role`), reaction/count enrichment, and verified-requester `liked_by_me` occur only after runtime ancestry authorization. Moderation-hidden comments are excluded from the public branch; the existing authenticated-own pending-review branch and body-empty deleted structural placeholder with a published child are intentional, non-public/structural response behavior rather than public ancestry bypasses.

The GET re-audit is complete under remediation commit `aa2cf01be3a21eed9d3a0de111bc249c0ead046a`, clearing the active source blocker. Route-only remediation leaves direct public SELECT paths under-protected; RLS-only remediation leaves GET without an explicit shared ancestor-visibility contract for its enrichment. Runtime and SELECT-RLS are source-aligned, while legal-consent enforcement still cannot verify public resource ancestry. The three forward comment migrations remain authored, committed, and unexecuted: `20260713_comment_creation_circle_authorization.sql` (POST), `20260713_comment_reaction_visibility_authorization.sql` (PUT), and `20260713_comment_read_circle_visibility_authorization.sql` (GET). All are deployment prerequisites. Batch 4 is complete; current progress is 49/66 traced and 17 pending, the next pending Batch 5 source is `src/pages/api/forum/posts.ts#GET`, Batches 5-6 remain pending, and production remains NO_GO.

## Phase 4A1 Batch 5A - forum/comments.ts DELETE

`DELETE /api/forum/comments?id=<uuid>` checks runtime and bearer presence, reads URL `id` or optional JSON `comment_id`, validates a UUID-shaped target, constructs a bearer-bound anon-key RLS client, verifies `auth.getUser(token)`, and derives the acting user only from that verified session. It then reads `profiles.role` by that user id; only `moderator` and `admin` are staff. Request data has no author, user, role, post, circle, status, reaction, or descendant field, so it cannot replace either the actor or the target relationship.

The route reads one server-selected comment row (`id`, `author_id`, `status`, `post_id`). Already-deleted rows return `already_deleted` before a write. Otherwise the verified author may delete only that own comment, while server-derived staff may delete any RLS-visible comment. The final `comments_update_self_or_staff` RLS policy independently applies the same author-or-staff control. DELETE accepts no post or circle id and performs no ancestor lookup, so an author or staff member can soft-delete an RLS-visible comment after its parent post/circle becomes hidden, deleted, or inaccessible. This is existing cleanup lifecycle behavior, not a cross-post/circle mutation; it remains a Phase 4B visibility-policy question.

The first irreversible and only persistent effect is `comments.update({ status: 'deleted', updated_at })`. It is a soft delete that preserves the comment body in storage, replies, reactions, profiles, and media relationships; this source does not clean reactions, create or remove notifications, update comment/reply counts, audit, call RPC, send email, touch cache/R2, invoke moderation, or call external services. There is no multi-write transaction or downstream partial-failure chain. Sequential retries are no-op success after the status read; concurrent retries have no idempotency key, lock, or status predicate and can issue equivalent updates. Missing content-type/body-size/rate controls, no `assertUserCanWrite` safety check, raw helper/database errors, and ancestor-lifecycle semantics remain Phase 4B hardening.

Future consent can be inserted in `DELETE` immediately after verified `auth.getUser(token)` and before the verified profile-role read, comment lookup, or mutation, with no refactor. The three forward comment migrations above remain authored and unexecuted deployment prerequisites. DELETE is complete; progress is 49/66 traced and 17 pending, Batch 5 remains pending, and the next pending Batch 5 source is `src/pages/api/forum/posts.ts#GET`.

## Phase 4A1 Batch 5B - forum/external-video-upload.ts POST

`POST /api/forum/external-video-upload` verifies a bearer with `auth.getUser(token)` and derives the actor only from `authData.user.id`; it then calls `assertUserCanWrite` for global banned/suspended safety state. It accepts JSON `post_id`, `file_name`, `mime_type`, declared `size_bytes`, and optional `turnstile_token`. It has no client actor, owner, author, role, staff, storage-key, video URL, provider URL, or target-circle field. The endpoint does not fetch a client-selected video URL and contains no SSRF-capable destination path.

The separate re-audit confirms the exact order: runtime environment validation; bearer presence; bearer-bound anon-key client construction; `auth.getUser(token)` verification and actor derivation from `authData.user.id`; `assertUserCanWrite`; JSON parse and `post_id`/file/mime/declared-size validation; server-side `posts(id, author_id)` lookup; `post.author_id === authData.user.id` comparison; conditional Turnstile validation; daily IP-hash and quota reads; local R2 PUT URL signing; rate-attempt read/insert; signed-upload response. Request fields provide only target and file metadata, never actor, author, owner, role, staff, storage-key, or relationship data. Missing targets return 404 and wrong owners return 403 before every later helper.

`validateTurnstileToken` is the first external effect, immediately after target authorization and immediately before rate processing. Its `fetch` has exactly one server-coded Cloudflare siteverify destination and accepts no client URL, host, protocol, port, redirect, DNS, download, or video metadata value, so no SSRF-capable path exists. A failed Turnstile result returns only `TURNSTILE_REQUIRED` or `TURNSTILE_INVALID` before rate-attempt insertion. The disabled/test path still reaches post lookup and ownership comparison before later processing. The fully offline ordering test proves all missing, wrong-owner, malformed, unauthenticated, and safety-denied paths make zero Turnstile/Cloudflare-fetch, daily-rate, R2-signing, rate-attempt, or direct-mutation calls.

The bearer-bound client has no service-role authority. The deployed-source `posts_select_published_public` policy in `20260611_forum_permission_lockdown.sql` permits only published rows, `author_id = auth.uid()`, or server-derived moderator/admin access; the route independently rejects any selected row not owned by the verified actor. The authored but unexecuted `20260713_comment_read_circle_visibility_authorization.sql` further narrows public post reads to a moderation-visible post in an accessible public circle while retaining own/staff branches; that migration remains a comment-read deployment prerequisite, not an external-video blocker. `signR2PutUrl` builds a `PutObjectCommand` and signs it but performs no R2 upload; browser-side PUT occurs only after this response. The first persistent effect is the operational `enforceUploadRateLimit` insertion into `forum_upload_attempts`, after all authorization and enabled Turnstile checks. POST itself has no `post_media` or other domain mutation, notification, count, audit, email, cache, RPC, or telemetry write.

There is no idempotency key or transaction: retries can create duplicate operational attempt rows, and an attempt can remain if a later response path fails. Daily attempt-read errors are treated as zero usage; post-media read errors fail the request; rate-limit helper backend errors fail open. POST does not require a Content-Type header or transport body-size cap, and formatted database/caught errors remain Phase 4B hardening work. The Turnstile helper retains error text internally, although this endpoint exposes only fixed failure codes. A future consent guard belongs immediately after verified `auth.getUser(token)` and before `assertUserCanWrite`, JSON parsing, target lookup, Turnstile, rate reads, signing, and persistence; no refactor is required, and it cannot replace target ownership authorization. Re-audit under remediation commit `30ece7ec89e7de477b3b74d94e36ee51709419d5` clears the external-video ordering blocker. POST is complete at 49/66 traced and 17 pending; Batch 5 remains pending and the next source is `src/pages/api/forum/posts.ts#GET`. Production remains NO_GO.

## Phase 4A1 Batch 5C - forum/media-upload-guard.ts POST

`POST /api/forum/media-upload-guard` is a verified-user and upload-purpose guard, not a target-resource authorization or upload-credential route. It validates the runtime environment and bearer presence, constructs an anon-key client carrying that bearer, calls `auth.getUser(token)`, and derives the only actor from `authData.user.id`. The body accepts only `upload_kind`, `size_bytes`, and optional `turnstile_token`; it accepts no post, circle, comment, media, actor, owner, author, role, storage-key, upload-URL, or target relationship identifier. It returns only `{ ok: true }`, performs no R2/storage operation, and does not issue a signed URL, object key, upload token, or other reusable credential. There is therefore no route-local target to bind, no client-controlled actor path, and no signed-upload credential that could be cross-user, broad, long-lived, or predictable.

The exact order is runtime validation; bearer extraction; bearer-bound client construction; `auth.getUser(token)` verification; JSON parsing; `upload_kind` allow-list validation; `size_bytes` numeric coercion; `assertUserCanWrite` safety-state read; conditional `validateTurnstileToken`; rate-limit salt/IP hashing; `enforceUploadRateLimit`; and the success response. The safety helper reads only the verified actor's safety state and denies banned, suspended, or unavailable users before Turnstile or rate persistence. Conditional Turnstile verification is the first external effect and fetches only the server-coded Cloudflare siteverify endpoint; no client field controls a destination, host, protocol, port, redirect, DNS lookup, or download, so the route has no SSRF-capable path. When Turnstile is not required, no external effect occurs.

The first persistent effect is operational only: `enforceUploadRateLimit` counts the IP window and inserts a `forum_upload_attempts` row under the verified actor when below the limit. The bearer-bound RLS policy `forum_upload_attempts_insert_authenticated` requires `user_id = auth.uid()` or null, while this route supplies the verified non-null actor. The endpoint does not mutate `post_media`, storage objects, posts, circles, notifications, audit rows, email, caches, RPC state, or telemetry. Later target-specific media insertion is separately constrained by `post_media_insert_self` (verified owner plus own-post relationship) and `post_media_objects_insert_self` (authenticated owner and user-prefixed object path); those policies are not reached by this route.

There is no request idempotency key or transaction, so retries can duplicate operational rate-attempt rows and one can remain if a later response path fails. Rate backend errors fail open, and `size_bytes` has no route-local positive/max bound, Content-Type/body-size requirement, MIME/extension validation, or quota lookup. Caught error messages can be returned. These are Phase 4B hardening items, not a target-authorization blocker. A future consent check belongs immediately after `auth.getUser(token)` and before JSON parsing, safety-state access, Turnstile, and rate persistence; no refactor is required, and consent cannot replace later target authorization. POST is complete at 49/66 traced and 17 pending; Batch 5 remains pending and the next source is `src/pages/api/forum/posts.ts#GET`. Production remains NO_GO.

## Resolved source finding - forum/post-media.ts POST cross-post media provenance

The blocker checkpoint `7fae49fc9e0d13928bbd9ab196051fbbdacc4c0a` is resolved in source by the smallest deterministic design: `buildTmpVideoKey` now derives exactly `tmp/<verified-actor-id>/<authorized-post-id>/<random-object-name>` after the issuer verifies the bearer actor, applies `assertUserCanWrite`, validates `post_id`, reads the server post, and proves server-derived ownership. Only then does the route create the post-bound key, call Turnstile, process rate limits, sign R2, persist an operational rate attempt, and respond. Missing or wrong-owner targets perform none of the later key-generation, Turnstile, signing, or persistence effects.

`post-media.ts#POST` independently verifies the bearer actor, applies user-safety authorization, parses and validates the payload, reads and authorizes the selected target post, then runs `validateMediaArray`. It accepts video storage only when the complete canonical object path exactly binds the verified actor and selected post, either `<actor>/<post>/...` or `tmp/<actor>/<post>/...`; actor-only legacy tmp paths, post-A-to-post-B replay, cross-user replay, missing post segments, encoded or duplicate separators, backslashes, queries, fragments, and foreign video URLs fail before `resetCover` or `post_media` insertion. `resetCover` is the first persistent domain mutation, followed by insertion and an optional moderation update. The route has no transaction or idempotency key, so retry/partial-failure handling remains Phase 4B work rather than an authorization bypass.

The forward-only migration `20260713_post_bound_media_provenance.sql` supplies matching RLS. Its anchored helper and replacement `post_media_insert_self` plus ordinary-user `post_media_update_self_or_staff` branches require `auth.uid()` row ownership, an `auth.uid()`-authored target post, and the canonical actor/post/object relation; missing or malformed provenance fails closed. Historical migrations remain unchanged. The new migration has been authored but not executed, so source re-audit is complete while deployment execution remains a production prerequisite. Legal consent cannot establish object provenance and remains a separate future insertion immediately after verified authentication. The fully offline test proves the allowed post-A flow and denied cross-post/cross-user/malformed paths without real R2, Supabase, auth, or network operations. `post-media.ts#POST` is complete; progress is 49/66 traced and 17 pending, Batch 5 remains pending, and the next Batch 5 source is `src/pages/api/forum/posts.ts#GET`. The three earlier comment migrations remain authored and unexecuted prerequisites. Production remains NO_GO.

## Phase 4A1 Batch 1E - admin/forum/reports.ts

GET is a moderator-only report read. It bounds the limit, selects post reports, and reads linked posts, circles, and profiles for response formatting. No report status/event, target, notification, email, external-service, or other state mutation occurs. Parent Batch 1 remains pending.

## Phase 4A1 Batch 1D - admin/forum/posts.ts

GET is a moderator-only read. PATCH validates post id and action before status update. DELETE validates and loads the target post, soft-deletes it, then invokes `deletePostMediaObjects` for storage and media-row cleanup. Future consent belongs immediately after `requireModerator`, before resource lookup and any mutation. Parent Batch 1 remains pending.

## Phase 4A1 Batch 1A - admin/forum/circles.ts

`GET`, `POST`, and `PATCH` were inspected through `requireModerator`. The helper derives the actor from `auth.getUser`, creates an RLS client from that verified bearer session, and checks moderator/admin role before returning. GET is read-only. POST and PATCH can receive future consent enforcement immediately after `requireModerator`, before payload work and before their first Postgres update/insert. POST has duplicate-name checking and moderation before insertion; PATCH validates the target id and update fields before moderation and update. No service-role client or email path was found in this endpoint. Parent Batch 1 remains pending because its other source files are not traced.

## Phase 4A1 Batch 1B - admin/forum/me.ts

GET is an authenticated admin read. `requireModerator` obtains the bearer credential, creates the RLS user client, verifies `auth.getUser`, and checks the profile role before the handler returns selected actor/profile fields. No mutation, R2 access, email, privileged client, ownership lookup, or external side effect exists. Parent Batch 1 remains pending.

## Phase 4A1 Batch 1C - admin/forum/media.ts

GET is a moderator-only read with bounded list filters and no mutation. DELETE verifies moderator role, validates the target media UUID, reads the media row, invokes `deleteMediaObject` for managed R2 or Supabase storage cleanup, then deletes the Postgres row. Future consent belongs after `requireModerator` and before the media lookup, so it precedes the first irreversible storage deletion. Parent Batch 1 remains pending.
